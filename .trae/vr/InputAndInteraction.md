# 输入与交互

- Enhanced Input 为输入事实源；动作资产使用 `IA_`，映射上下文使用 `IMC_`。
- 左右手动作语义一致，设备按键映射与玩法动作解耦。
- 抓取接口统一支持单手、双手、主副手切换、释放、掉落恢复与禁用状态。
- 武器碰撞结算不得直接依赖控制器瞬时位姿；应使用连续轨迹、速度阈值和命中去重。
- 追踪丢失、控制器断连、系统菜单返回后必须进入安全状态，禁止保留持续攻击或移动输入。
- 玩家和 AI 对角色能力的请求进入同一规则层，设备输入仅是玩家控制源适配器。
