# 技术债登记

本登记册记录项目中有意接受的临时实现、占位方案和已知缺陷，追踪其影响范围与偿还计划。技术债不等同于风险（风险是"可能发生的问题"），而是"已经存在的妥协"。

每次审核任务交付时，审核模型应检查是否引入新债务并登记。债务偿还后标记为 `resolved`，保留记录供审计。

## 当前技术债

| ID | 描述 | 位置 | 引入原因 | 影响 | 偿还计划 | 优先级 | 状态 |
|---|---|---|---|---|---|---|---|
| TD-001 | UE5.8 与 PICO Neo3 不兼容，项目需降级到 UE5.6 | 引擎版本 / 全项目 | M00-T004 真机验证发现 Neo3 运行时 3.0.1 与 UE5.8 xrCreateSession 不兼容 | 阻塞 PICO 真机验证；需重建项目 | **已偿还（2026-08-11）**：项目已重建为 UE5.6（EngineAssociation=5.6）+ PICO OpenXR Plugin OS5，Source/ 与 Content/ 迁移完成，Neo3 真机场景可见；遗留链路债务见 TD-005（swapchain 补丁） | 高 | resolved |
| TD-002 | systems/ 指引缺少接口契约定义 | systems/01-07 | M00 规划期侧重职责边界，未到接口定义阶段 | M01+ 实现时接口依赖隐式约定 | 已补充规划级接口契约（2026-08-11） | 中 | resolved |
| TD-003 | 决策登记缺少 ADR 上下文 | registers/01-decision-register.md | 表格格式侧重快速浏览，未记录备选方案和后果 | 关键决策的"为什么"不可追溯 | 已补充关键决策 ADR 上下文（2026-08-11） | 中 | resolved |
| TD-004 | 无代码-文档漂移检测机制 | governance/ReviewProtocol.md | M00 阶段无实际代码，漂移问题尚未显现 | 实现推进后 systems/ 指引可能静默失效 | 已在 ReviewProtocol 增加文档同步验证（2026-08-11） | 中 | resolved |
| TD-005 | PICO 插件源码级 swapchain 补丁（拦截 `xrCreateSwapchain` 剥离 format list） | Plugins/PICOOpen174f9f81d266V8/Source/PICOOpenXRHMD/Private/PICO_HMD.cpp | Neo3 系统 OpenXR 运行时（R2.1.12.0）Vulkan swapchain format list 触发 ION ENOTTY 崩溃 | 渲染格式受限（固定 B8G8R8A8_SRGB）；PICO 插件升级可能覆盖补丁；换新设备后补丁可能不再需要 | 补丁已编译进 libUnreal.so 并真机验证场景可见（V-008）；升级插件后须重打补丁；新设备接入时评估是否保留（见 DeviceConfigurationMatrix） | 高 | open |
| TD-006 | PICO 插件从引擎 Marketplace 迁移至项目 Plugins（含 Build.cs `PrecompileForTargets`、uplugin `Installed=false`） | Plugins/PICOOpen174f9f81d266V8 | 安装版引擎对 Marketplace 插件强制 precompiled 处理、增量构建检测不到引擎目录源码变化 | 插件不再随引擎自动更新；Fab 后续升级需手工迁移；引擎原插件已改名 `.disabled` | Fab 发布新版本时评估：迁移回引擎目录并重打补丁，或维持项目内插件并手工更新 | 中 | open |
| TD-007 | 输入映射为 Quest 风格，未按 PICO Touch 绑定；OpenXR Input 缺 PlayerMappableInputConfig | Content/VRTemplate/Input/*.uasset、Config/DefaultInput.ini | 基于 UE5.6 官方 VR 模板（Quest 默认按键）；PICO 官方文档要求绑定 PICO Touch 按键 | 真机手柄按键不可用、手柄模型不显示 | **已偿还（2026-08-12）**：5 个 IMC 已按 PICO 官方文档绑定 PICO Neo3 键（IMC_Default/Hands/Menu/Weapon×2，二进制核验通过）；遗留项（PlayerMappableInputConfig、手柄模型挂载、真机复测）转 M00-T004 跟踪 | 高 | resolved |
| TD-008 | `DefaultInput.ini` 曾指向不存在的 `/Game/XRFramework/Input/`（5.8 迁移残留） | Config/DefaultInput.ini | UE5.8→5.6 迁移时 IMC 路径未同步 | 曾导致 5 个 IMC 全部加载失败、输入不可用 | 已修复为 `/Game/VRTemplate/Input/`（2026-08-11，v3 日志确认 IMC 加载成功） | 高 | resolved |
| TD-009 | 安装版引擎 `Engine\Intermediate\Build\BuildRules\` 被误删（UE5Rules.dll） | 引擎目录（项目外） | 排障时误将引擎 BuildRules 当缓存删除 | UE5Rules.dll 本机无法从源码完整重建（引擎裁剪工具源码），曾阻塞全部构建 | 已用 Epic Launcher「验证/修复」恢复（2026-08-11）；经验已写入 PicoNeo3BuildGuide「已知构建坑」 | 高 | resolved |
| TD-010 | 编辑器自动化工具链变更：安装 UEBridgeMCP（GPL-3.0），移除未编译通过的 UnrealMCP（MIT） | Plugins/UEBridgeMCP（已退役→彻底删除）、VRSanguoYanWuchang.uproject | UE5.6 无官方 MCP（官方仅 5.8）；UnrealMCP 在 5.6 编译失败，UEBridgeMCP 5.6 编译零改动通过 | 编辑器可通过 HTTP 8080 被 AI 自动化操作（407 工具）；GPL-3.0 许可证对商业分发有传染风险 | **已偿还（2026-08-31，M02-PREP-002）**：按用户批准方案 A + manager 重试授权，迁移至 db-lyon/ue-mcp **v1.0.87**（MIT）完成——npm v1.3.0 在 UE5.6 系统性不兼容（61 编译错误，静态探针证实 5.7+ API 未门控），横向探测 6 个历史版本后选定 v1.0.87（13 个问题符号仅 2 处且已门控），1 行版本门控补丁（AssetHandlers_Import.cpp `SetSourceString` 3参→2参，5.7+ 才用 3 参）后 UBT 编译 `Result: Succeeded`；TC-02 蓝图测试副本写读验证读回一致；UEBridgeMCP 退役删除（`.uproject` 移除条目；gitlink 随 296ebed 移除；本地副本按用户 2026-09-03 裁决**彻底删除、不保留回退**——原「改名 .disabled 保留审计」方案因未入库目录丢失作废，回退路径=重新克隆上游源码并重打编译修复），重启无缺失模块错误，L_SkeletonTest 冒烟正常。**GPL-3.0 传染风险随退役消除**。维护注：桥插件含本地 1 行补丁，未来执行 `ue-mcp deploy/update` 会覆盖插件源，需重打补丁或向上游提交。证据：`Saved/Evidence/M02-PREP-002/`。历史尝试记录：v1.3.0 首轮失败（blocked）→ v1.3.1-beta.2 静态探针判定仍不兼容（未浪费编译）→ v1.0.87 成功 | 中 | resolved |
| TD-011 | PICO Neo3 真机运行时卡顿 | 全项目运行时 | 2026-08-13 真机验收时用户报告"有些卡顿"（隐藏骨骼手部动画后仍存在） | 影响 VR 体验流畅度，可能触发晕动 | **已偿还（2026-08-19）**：M00-T006 性能基线建立（stat unit/fps/scenerendering 可用 + LogVRSanguoPerf 注册），1v1 灰盒关卡（M01-T005）已完成 NavMesh/光照/掩体布局，PIE 帧率采集的旧阻塞（TD-012）已解除，**但真机帧率复验至今未执行**——留待 M01-T005 真机验收阶段执行。 | 中 | resolved |
| TD-012 | PICO Connect 10.6.6 未自动切换 VR 串流模式（停留在桌面串流） | PICO Connect PC 端 + 头显端串流应用 | PICO Connect 桌面串流默认优先；VR 串流需头显端手动操作且无明确 UI 入口 | 编辑器 VR Preview 渲染正常（XR Session FOCUSED + PICO Runtime）但头显无法接收 VR 帧流 | **已解决（2026-09-10）**：① **用户裁定**——「PICO 串流在 m01t001 已经解决了」（2026-09-10 明示），并据此在 `CR-20260908-001` §B 撤回「TD-012 阻塞 M01 链」的旧结论。② 串流侧实证：M01-T001 完成握持方向校准并**经用户实机确认**（"刀方向不错"，`execution/sessions/session-20260830-001.md:633`），位姿追踪与串流画面均正常。③ **原「三条替代路径」均未被采用**——真因不在串流模式切换：S1 探针坐实**串流运行时增强输入的按钮值未注入**（五张 IMC 全部挂载、Verbose 日志零条 action 值，同记录 :600-606），根因落在 PICO Connect 自带 SteamVR 驱动对 pico_neo3 控制器的绑定侧，**非项目代码**（同记录 :612-613）。④ **连带影响**：M01-T007「Direct Preview 验证（TD-012）」的前提随之消失，其去留转用户裁定（见 `07-task-register.md`）。⑤ 原「复审时机：M01 结束前必须有一条路径可用」已满足。 | 中 | resolved |
| TD-013 | M01-T001 使用 VRE 内置 GripObject 做抓取功能验证，未按 systems/02 IGrabbable 接口封装 | Source/VRSanguoYanWuchang/Combat/VRSanguoGripLibrary.h/.cpp（已实现，M01-T001 路线C）；Content/VRSanguo/VR/BP_VRCharacter | M01-T001 决策：功能验证优先，Route B（自研 IGrabbable 接口）投入产出比不合理且蓝图调用具体类可能触犯 03-blueprint-cpp-boundary-standard | GripObject 是 VRE 内部实现，不经过项目 IGrabbable 接口（TryGrab/Release/SwitchHand/GetGrabPoints）；武器抓取逻辑与项目交互系统脱钩。**注**：VRSanguoGripLibrary（检测→GripObject→DropObject）是未来 IGrabbable 桥接层的雏形，接口演进时直接基于此封装 | M01 后续任务（T002/T003）或 M02 按 `systems/02-interaction-and-weapon-system.md` 接口标准重构，将 GripObject 调用升级为 IGrabbable 桥接层 | 中 | open |
| TD-014 | ue-mcp 桥 `blueprint.add_node` 触发编辑器崩溃 → **2026-09-08 触发条件精化：崩溃相关性 = 节点编辑触发的自动保存撞只读资产，非节点编辑本身** | ue-mcp 桥工具链（编辑器内 game-thread executor，调用栈 GameThreadExecutor.cpp:106）；受影响操作：只读态下对 BP 事件图 add_node | 2026-08-31 BP_VRCharacter 接线时首次暴露（STATUS 备注曾误标 TD-010，登记册补正）；2026-09-06 S1 探针补测两次复现（只读态）；2026-09-08 18:2x 只读态再崩一次；**同日 22:30 目标资产解锁后连续 5 次 add_node + 5 次 connect_pins 零崩溃并成功落盘**（msg-20260908-224655 实证） | 只读态下节点级编辑自动化不可用（保存撞只读即致命）；解锁态已实证可用 | 规避（精化）：节点编辑前确认目标资产处于解锁态，只读态禁用节点编辑类桥操作；**本期不修理由**：对保存失败的崩溃响应仍是桥上游缺陷（钉死 1.0.87，升级被 UE5.6 兼容性阻塞，见 TD-010/TD-015）；**复审时机**：M01-T002 开工前 / 上游 5.6 兼容修复发布时 | 中 | deferred |
| TD-015 | ue-mcp 桥 `editor.save_dirty` handler 崩溃编辑器（EditorHandlers.cpp:1221，桥 1.0.87 bug） | ue-mcp 桥工具链；受影响操作：editor.save_dirty（脏包批量落盘） | 2026-09-07 CR-20260907-001 实施中复现（executor 回执 msg-20260907-223820 记录；当时仅记入 CR 未入登记册，2026-09-08 指挥台补正立账） | 脏包批量落盘路径不可用，改动有因崩溃/退出丢失的风险（09-06 已实际丢失一次改动） | 规避：落盘统一走 `asset.save_all_dirty`（已验证可用）或用户编辑器内手动保存；每次修改后立即落盘；**本期不修理由**：桥上游缺陷（钉死 1.0.87，升级被 UE5.6 兼容性阻塞，同 TD-014）；**复审时机**：与 TD-014 一并（M01-T002 开工前 / 上游 5.6 兼容修复发布时） | 中 | deferred |
| TD-016 | ue-mcp 1.0.87 C++ 桥侧缺口：SceneCapture2D show_flags 非 UPROPERTY 不可达（离屏捕获类验证受限，离屏天空黑成因之一） | ue-mcp 桥工具链 UE 侧 C++（UE_MCP_Bridge 桥插件，需 UBT 重编译）；Node 侧缺口 ①③④（截图最小化不落盘 / place_actor 嵌套参数 / 组件属性无 native 读写）另立 1.0.87-patch 分支偿还（2026-09-09 指挥台已批准，M01-T001 验收后立项） | 2026-09-09 M01-T005 收尾评估：该会话 execute_python 顶替官方工具 64 次 | 离屏捕获类验证受 show_flags 不可达限制；组件属性读写靠 execute_python 顶替 | 规避：execute_python 顶替（M01-T005 实证 64 次可行）；**本期不修理由**：桥 C++ 是自动化生命线（TD-014/015 前车之鉴），UBT 重编译风险中等；Node 侧 patch（零重编译、低风险）先偿还验证改造收益；**复审时机**：Node 侧 1.0.87-patch 交付验证后评估是否动 C++，或上游发布 5.6 兼容版时（TD-010 链一并复审） | 中 | deferred |
| TD-017 | 被 `.gitignore` 忽略的可再生目录，其**删除既不留痕、也检测不出** | 门禁与验收口径：`dashboard/check-integrity.py`、`execution/reports/audit/GIT-BASELINE.md` §11#6、`governance/ExecutionModel.md` 的删除类验收条款 | 2026-09-11 实测：`.vs/`（3,902,485,459 B / 3.63 GB，`AUDIT-repo.md:62` 记 47 文件）**已完全不存在**，而 `.trae/CHANGELOG.md`、`GIT-BASELINE.md`、`t12-inputs-t5-corrections.md` 三处**均无任何删除记录**；用户 2026-09-10 裁定 ④ 明写「8 GB 编译缓存 → 不动」。**删除者与时点未核实——只能确证现在不存在，不猜。** | ① `GIT-BASELINE.md` §11#6「未删除任何项目内容 = passed」的判据是「` D` 行数 = 0」，而 `Intermediate/`、`Binaries/`、`ArchivedBuilds/`、`DerivedDataCache/`、`Saved/`、`.vs/` **全部被 `.gitignore` 忽略**——删除它们**不产生任何 ` D` 行**，故**该验收条款对全部可再生目录无效**；② 同类风险覆盖 `Saved/Autosaves/Game/VRSanguo/Dev/L_Prototype_1v1_v3_Auto6.umap`（v3 关卡**最后一份**）与 `Saved/Evidence/`（M01-T005 43 文件 + M02-PREP-002 4 文件，**唯一副本、不在任何备份**） | 规避：**删除类验收改为目录级快照比对**（文件数 + 总字节的前后对照），**不得使用 `git status`**；盘点「废弃与冗余」时，可再生目录一律**列清单待用户逐项批准、不自行删除**（`Saved/` 在 v3 唯一副本与证据去留裁定前**禁止清理**）；**本期不修理由**：属判据设计缺陷，修复需动验收口径并新增门禁检查项（须用户批准），且当前 `open` 恰为 3、无余量，故登记为 `deferred`；**复审时机**：用户裁定 `.vs/` 事件处置时 / 门禁加固批次二立项时 | 中 | deferred |

| TD-018 | `git lfs push --all` 的「无待上传」输出**不足以证明远程 LFS 完整**（疑为假阴性） | 门禁与推送核验口径：`governance/SessionCommands.md` §推送与核验、`execution/reports/audit/GIT-BASELINE.md` | 2026-09-10/11 共四次 `git lfs push --all origin`：**前三次（t10/t13/t15）均输出「无待上传」**，而 `cb7ab79` 推送当晚实测**真的上传了 496 对象 / 272 MB**；复跑同命令后无上传，本地 LFS 库 501 对象 / 717.9 MB。**那三次「无待上传」极可能是假阴性；原因未查明，不推测。** | ①「**仓库 + origin 是唯一副本**」这句话的强度**依赖远程 LFS 完整性** —— 若远程 LFS 曾长期不完整，则期间的「已备份」属**过强表述**；② 另一个仪器陷阱：`git lfs push --all --dry-run` **不联系服务器**，只列出本地引用到的全部对象，**不能**用作缺口检查（t13 一度误用后自行纠正） | 规避：判据改为**三者同时成立**——`git lfs fsck` OK **且** 实际 `push --all` 无上传 **且** `ls-remote` 同 SHA；**不得单凭 push 输出为空**下结论。恢复演练必须**实测 LFS 对象可拉取**（而非只看 ref）。**复现（2026-09-11 实测原话，经代理执行）**：① 推送时 git-lfs 打印 `Uploading LFS objects: 100% (496/496), 272 MB`；② 紧接执行 `git -c http.proxy=<代理> lfs push --all origin` → `exit=0` 且**无任何输出行**（与 ① 矛盾）；③ 此前 t10/t13/t15 三次同命令同样无输出。对照量：本地 LFS 库 501 对象 / 717.9 MB、`git lfs ls-files` 369 行 —— 两者不等属正常（store 含历史/其他 ref 的对象），**仅作对照、不构成判据**。**本期不修理由**：原因未查明、需专门复现实验，且缺口当前**已闭合**、不影响现状；当前 `open` 恰为 3 无余量，故登记 `deferred` | 中 | deferred |

## 债务状态定义

| 状态 | 含义 |
|------|------|
| `open` | 已记录，需在下一个相关里程碑开始前偿还或有明确计划 |
| `deferred` | 已记录完整偿还计划但当前里程碑不解决（外部依赖/工具限制/低优先级），需用户决策时机 |
| `resolved` | 已偿还，保留记录供审计 |

> `open` 数量作为质量门禁（dashboard/check-integrity.py 阈值 ≤3）。`deferred` 不计入 open，但要求每条 deferred 都有明确的"为什么本期不解决"理由和"何时复审"承诺。

## 债务管理规则

- **引入**：执行模型在实现中做出妥协时，在任务报告中声明并登记。
- **追踪**：每次审核检查新增债务；里程碑验收时审查未偿还债务清单。
- **偿还**：偿还后在登记册标记 `resolved`，保留记录；在 CHANGELOG 记录偿还事件。
- **升级**：高优先级 `open` 债务在下一个里程碑开始前必须偿还或有明确计划。
- **关联**：技术债可能关联风险（RSK-ID）或决策（DEC-ID），在描述中注明关联。
