# 配置规范

- 默认工程配置进入 `Config/Default*.ini`，本机路径、密钥、设备序列号和用户偏好不得提交。
- 插件启用状态以 `.uproject` 为事实源；平台配置必须同时记录支持平台和验证状态。
- 修改配置前读取现有文件并做最小差异，不整文件覆盖模板配置。
- Android/PICO 签名、商店凭据和 keystore 仅保存在项目外安全位置。
- 配置变更必须关联任务编号，并在验证登记册记录编辑器启动、目标构建或真机结果。
