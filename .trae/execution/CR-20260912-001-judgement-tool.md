# CR-20260912-001 · 判据工具「核对执行器」立项 + 唯一 Skill 挂钩

> **状态**：**已批准（用户 2026-09-12）· 待实现**
> **关联**：`AGENTS.md` 规则 15 / 19 / 20 / 24；`policy.md` §4.1（锁定）、§4.2（受控）
> **权威声明**：本文件是「判据工具」的**唯一权威规格正文**。其他地方（`AGENTS.md`、唯一 Skill、会话记录）**只链接，不复制**（规则 19：一个主题只有一个权威正文）。
> **沿革**：本规格由 `governance/SessionCommands.md` §判据补充 的 **17 例**同族立法反推而成 —— **规则不是被"再写一遍"，而是被"固化进一件工具"**。

---

## 1. 批准依据（用户原话，逐字）

| # | 场景 | 用户原话 | 日期 |
|---|---|---|---|
| 1 | 决策侧提出"要不要把这套检查做成小工具" | 「**可以啊，写的话后面记得规则库、skill是否能记录这个工具让其他决策模型使用呢，后面可能换人的，我怕到别人那里就忘了**」 | 2026-09-12 |
| 2 | 决策侧请求专项批准修改**锁定事实源「唯一 Skill」** | 「**允许改唯一 Skill，把工具挂进去**」 | 2026-09-12 |

**⇒ 批准覆盖范围（三项一并获批）**：
1. **新建工具**（`.trae/tools/` 内新增，规则 20 的新建文件须用户批准）；
2. **修改锁定事实源「唯一 Skill」**（`policy.md` §4.1：除用户明确批准的治理任务外，只能读取）；
3. **挂钩到规则库**（`AGENTS.md`，受控；用户第 1 句明确要求"规则库、skill 是否能记录"）。

**⇒ 未覆盖（须另行批准）**：门禁接入（改 `dashboard/check-integrity.py`）。该脚本处于**决策侧自设冻结**中，本 CR **不含**此项。

---

## 2. 问题陈述（为什么必须做）

**同日（2026-09-12）同族——"判据自身不可信"——立法达 17 例**，同日实测失误 **9 起**（执行侧 5、决策侧 4）。四类形态：

| 类别 | 形态 | 实例 |
|---|---|---|
| **过松** | 假阳性（把没有判成有） | 第 3 例（CRLF 上 `.` / `\s` 假阳性） |
| **过严** | 假阴性（把有判成没有） | 第 10、11、14 例 |
| **假绿** | 没跑却像跑过 | 第 12 例（变量名大小写撞车）；固定汇总文本 |
| **假红** | 不可满足却像没过 | 第 13 例（自造字面串）；第 17 例（分支落错） |

**共同点：与"内容对不对"无关，全是判据自身不可信。**

**⇒ 根因诊断（本 CR 的核心判断）**：
> **当同一族立法超过约 10 条，失效的根因不是「执行者不知道规矩」，而是「判据每次都靠人手临时写脚本」。**
> **规则确实能防错（第 21 提交零事故且可归因已证明），但代价是"每一步动手前都要由人逐条回忆并应用"。**
> **把"靠自觉"换成"靠机械"，就是本工具的唯一目的。**

**⇒ 用户关切的直接回应**：
> 用户原话「**后面可能换人的，我怕到别人那里就忘了**」。
> **"忘不忘"不取决于文档写得多好，取决于两件事**：**① 新模型开工时它是否自动摆在眼前；② 机器是否不放行。**
> `policy.md` §5 规定**开工流程第 1 步 = 读取根索引、治理政策、唯一 Skill、当前任务** ⇒ **唯一 Skill 是本项目唯一"换人也躲不开"的位置** ⇒ **工具必须挂在那里（§5.1）。**

---

## 3. 目标与非目标

**目标**
1. 让"核对"从**手写脚本**变成**声明式判据 + 固定执行器**。
2. 让 17 例同族失效**在机制上不可能发生**（不是靠人记得）。
3. 让新决策模型**无需读完全部 17 例**即可正确核对 —— **规则由工具承载，不由记忆承载**。

**非目标**
1. **不替代人做判断**：工具只回答"这条判据成不成立 / 过没过 / 有没有取到数"，**不回答"内容该不该这样写"**。
2. **不修改门禁**（`check-integrity.py` 冻结中）。
3. **不新建第二个 Skill**（`policy.md` §3 明令禁止平行 Skill）。
4. **不引入新依赖**：只允许 Python 标准库 + `pyyaml`（本机已验证可用）。

---

## 4. 工具规格（正式）

### 4.1 形态与位置
- **位置**：`.trae/tools/verify-claims.py`（项目工具；索引同步 `.trae/tools/README.md`）。
- **宿主语言**：**Python 3**（与既有 `check-integrity.py` 一致）。
  **理由（实测）**：PowerShell 格式串与 `-f` 的优先级冲突**反复**出现（`{m,n}`、`{0}` 两轮均致文本不渲染或数值错位）⇒ **不得把判据结论建立在 PowerShell 格式化输出上**。
- **调用**：绝对路径解释器（本机 Python 不在 PATH，命令见 `governance/SessionCommands.md` §校验）。
- **输出**：**文本 + 机器可读 JSON**（`--json <path>`），两者结论必须一致。

### 4.2 三态输出（**硬要求，最高优先级**）
每条判据的输出**必须**是三态之一，且**可区分**：

| 态 | 含义 | 何时产生 |
|---|---|---|
| **通过** | 判据成立且满足 | 测量值 == 期望值 |
| **未通过** | 判据成立但未满足 | 测量值 ≠ 期望值 |
| **未取到数** | **无法判定** | 文件不存在、命令无输出、读数为空、外部服务未索引 |

**⇒ 「未取到数」不得塌缩进任何一态。** 只有两态、且"未取到数"会塌缩的判据 = **缺陷判据**（三态判据原则）。
**⇒ fail-closed（默认）**：存在「未取到数」时**默认不判通过**（退出码非 0）；调用方须**显式**加 `--allow-unknown` 才可放行，且报告中必须保留该态的计数。

### 4.3 内置能力（**由 17 例反推，逐条对应**）
| # | 能力 | 来源例 |
|---|---|---|
| 1 | 三态输出 + fail-closed 默认 | 三态判据原则 |
| 2 | **可满足性前置**：`contains` 类判据先扫候选行；**0 候选 ⇒ 判「判据不成立」，不判「未通过」** | 可满足性前置（第 13 例） |
| 3 | **匹配方式显式声明**：`exact` / `prefix` / `substring`；**未声明 ⇒ 拒绝执行该判据（不算通过也不算未通过）** | 第 14 例 |
| 4 | **三口径**：原样 / 去反引号 / 去行内标记，三者分别出具并给合并结论 | 第 11 例 |
| 5 | **双算法交叉**：计数类判据必须两种独立算法各算一遍，不一致 ⇒ 报「不一致」而**不得取其一** | 第 11 / 15 例 |
| 6 | **字节导出法**：读提交产物一律"导出到临时文件后按字节读"，**禁止 `git show` + join 计行** | 第 15 例 |
| 7 | **行数权威 = `ReadAllLines` 语义**（Python 侧为 `read_text().splitlines()`）；**报告须标注所用口径** | 第 15 例 |
| 8 | **非空断言**：关键读数或变量为空 ⇒ 判「未取到数」，**不下结论** | 四项要求 ③ |
| 9 | **显式退出码与 stderr 检查**：不得只看 stdout | 四项要求 ① |
| 10 | **结果变量每轮显式置空**（禁止跨轮复用） | 四项要求 ②（第 12 例根因） |
| 11 | **汇总段必须"测量驱动"，禁止固定文本** | 工具层新要求 ① |
| 12 | **归属标签用严格枚举 + 前置断言**（枚举外取值 ⇒ 立刻抛错，不得落 default 分支） | 第 17 例 |
| 13 | **标识符统一规范化**：文件路径 / 工作流名 / 归属标签 / 判据 `id` 一律走**同一份规范化表**，禁止各处自造字符串；**不在表中的标识符 ⇒ 拒绝执行该判据** | 第 17 例 **及其复发**（CI 汇总脚本用正则抓到键名 `治理校验`，查询却用 `治理校验 CI 门禁` ⇒ **误报「未取到」**；而**同一段输出上一行已逐字打印了正确数据**） |
| 14 | **量具定义显式声明**：凡**数字 / 结构类**判据（行数、围栏数、计数）必须声明**量具定义**（例：「围栏数 = 行首允许前导空白的 ``` 行数，含缩进围栏」）；**未声明 ⇒ 拒绝执行该判据** | 第 15 例**第三次**复发（同一文件围栏数 **8**（允许前导空白）与 **2**（第 0 列即反引号）**都能自称"围栏数"**；根因是缩进围栏被后一量具漏掉。**偶数性在两口径下都成立 ⇒ 判定未受影响，但"数不同"即量具定义未声明**） |
| 15 | **报告义务不可被短路（禁止静默）**：**报告项的产出路径必须与任何 `continue` / `skip` / `return` / 异常分支结构性解耦** —— 报告段**对每一条应报项都要产出**，**只能被它自身的条件跳过**，**不得因其它判据的跳过逻辑而整体消失**。**并且：条目计数正确 ≠ 报告完整** ⇒ 必须**另有一条独立的「报告段非空（当存在应报项时）」断言** | **第 18 例**（报告项被控制流短路 ⇒ 输出为空；而**条目计数仍正确** ⇒ **计数判据结构上无法发现它**） |
| 16 | **取数必须 fail-closed（禁止伪读数）**：任何**量具 / 取数函数返回 `null`、空串或空集合 ⇒ 必须抛错**；**严禁把「取数失败」表示成 `0` / 空串 / 空集合当作读数**。**并配套断言：报告段与结构段的每一个数值，都必须可追溯至一次「非空取数」。** | **第 19 例**（执行侧 `$GLOB:` 作用域笔误 ⇒ **取数失败被显示成一个数值 `0`**、围栏数被算成行数 ⇒ **假读数**；**已由其自建断言拦住 ⇒ 未成假绿、零副作用**） |

### 4.4 禁止事项
1. **只读**：工具不得修改任何被检查文件（前后 SHA256 必须一致，TC-11）。
2. **不得在任何输出里写死结论文字**（TC-07）。
3. **不得把"脚本没报错"当作"判据跑过"的证据**（第 12 例）。
4. **不得使用 `git show` + join 计行**（第 15 例）。
5. **不得依赖 PowerShell 格式化**（第 15 例第三次复发）。
6. **不写入项目外目录以外的任何临时位置**；临时文件统一写项目外临时目录且用完即删。
7. **禁止用「截断文本」做归属判定**（如列表页被截断的标题、被 `head -c` 截断的字符串）—— **归属判定必须以结构化字段为准**（如 CI 的 `head_sha`）；**无结构化字段时应判「未取到数」，不得用模糊/前缀比对替代**。
   **依据（2026-09-12 实测）**：执行侧用 `commit subject` 与**列表页被截断的标题**做前缀比对 ⇒ 归属判定 = `False`，**而真实归属由 API `head_sha == 97a15e9` 直接证明**。**内容是对的，判据方法是错的**（同第 17 例家族：判据自身不可信）。

### 4.5 输入契约（声明式判据文件，YAML）
```yaml
- id: ledger-rows                      # 必填，唯一
  kind: count_regex                    # count_regex | contains | shape | sha256 | exec
  file: .trae/CHANGELOG.md             # 必填
  pattern: '^\s*\|\s*\d+\s*\|\s*`[0-9a-f]{7,}`'
  expect: 22
  algorithm: dual                      # 计数类必填：dual（两种独立算法）
- id: rule17-present
  kind: contains
  file: .trae/CHANGELOG.md
  text: 立法同族第 17 例
  match: substring                     # 必填（第 14 例）
  cross_check_file: .trae/governance/SessionCommands.md   # 第 16 例：须同时报该文件的候选数
- id: shape-changelog
  kind: shape
  file: .trae/CHANGELOG.md
  lines: 1130
  measure: readalllines                 # 量具定义（第 15 例口径）：行数 = ReadAllLines 语义
  h2: 93
  fences: 0
  fence_measure: indent_aware           # 量具定义：围栏 = 行首允许前导空白的 ``` 行数（含缩进围栏）
```
**⇒ `measure` / `fence_measure` 缺失时，工具必须拒绝执行该判据**（第 15 例第三次复发的直接对策）。
**⇒ 所有 `file` / 工作流名 / 归属标签取自同一份规范化表（§4.3 第 13 条）**；表外取值一律拒绝。
**⇒ 判据文件本身不合法**（缺 `match`、缺 `algorithm`、`id` 重复、枚举外取值）⇒ **退出码 3，且不得执行任何判据**。

### 4.6 输出与退出码
| 退出码 | 含义 |
|---|---|
| `0` | 全部「通过」（且无「未取到数」；或已显式 `--allow-unknown`） |
| `1` | 至少一项「未通过」 |
| `2` | 无「未通过」，但存在「未取到数」且**未**加 `--allow-unknown` |
| `3` | 判据文件本身不合法（**未执行任何判据**） |

**⇒ 汇总段格式（内容必须由测量结果生成）**：
```
判据 N 项 · 通过 A · 未通过 B · 未取到数 C · 不成立 D      ← 全部由测量填充
⇒ 结论：<由 A/B/C/D 机械推出，禁止写死>
```

---

## 5. 三个挂钩（**用户第 1 句直指此项**）

### 5.1 唯一 Skill（**锁定级；已获用户专项批准**）
**改什么**：`.trae/skills/three-kingdoms-vr-arena/SKILL.md`
1. **「必读顺序」新增一项**（紧随四份治理契约之后，属"开工第一步"序列）：
   > **`.trae/tools/verify-claims.py` 的规格正文 `execution/CR-20260912-001-judgement-tool.md`** —— **凡作出"通过 / 未通过 / 已核验"类结论前，必须经该工具执行判据；不得以手写临时脚本的结论替代。**
2. **「交付门禁」新增一条**：
   > **「已核验」类声明必须附带工具输出（文本或 `--json`）**；**手写脚本结论不构成"已核验"**。**「未取到数」不得写成"通过"或"未通过"。**
**为什么挂这里**：`policy.md` §5 的**开工流程第 1 步**即读唯一 Skill ⇒ **换人也躲不开**。

### 5.2 `AGENTS.md`（受控；决策侧立法职责）
**改什么**：新增一条规则（编号顺延，不回溯改旧编号）：
> **核对必须走工具。** 作出「通过 / 未通过 / 已核验」类结论前，必须以判据工具执行判据并附输出；**工具报「未取到数」时不得写入任一确定结论**。规格正文见 `execution/CR-20260912-001-judgement-tool.md`。

并在「必读」清单中指向该规格正文（**只链接，不复制**）。

### 5.3 门禁接入（**可选，本 CR 不做，须另行批准**）
将工具作为 `check-integrity.py` 的一项检查（例如"登记册/台账的强断言必须由工具产出"）。**冻结未解除前不实施。**

---

## 6. 影响与回滚

| 项 | 内容 |
|---|---|
| **影响面** | 新增 1 个工具文件 + 1 个 CR 文件；修改锁定 Skill 1 份、`AGENTS.md` 1 份、`.trae/tools/README.md` 索引 1 份。**不触碰 `Content/`、`Source/`、`Config/`、`Plugins/`，不触碰 `check-integrity.py`。** |
| **对既有流程的影响** | 「核对」环节由手写脚本改为调用工具；**门禁不受影响**（不改门禁脚本）。 |
| **回滚** | ① 工具文件删除即可（无其他文件依赖其存在）；② Skill 与 `AGENTS.md` 的挂钩段落**按 CR 记录原地撤回**（保留 CR 与 CHANGELOG，**不回溯改写历史**，规则 3）；③ **回滚不需要改动任何工程资产**。 |
| **风险** | 低。工具只读；挂钩段落为"必须调用"，**若工具不稳定会阻塞核对流程** ⇒ 故 §7 的 TC 必须先全绿才可挂钩。 |

---

## 7. 测试用例（**决策侧设计，执行侧执行** —— `AGENTS.md` 规则 15）

| # | 用例 | 期望 |
|---|---|---|
| **TC-01** | 对**不存在的文件**下一条 `contains` 判据 | 输出「**未取到数**」；退出码 `2`；**不得**输出"通过"或"未通过" |
| **TC-02** | `contains` 一条**必然 0 候选**的串（可满足性前置） | 判「**判据不成立**」而非「未通过」；退出码 ≠ 1；报告中给出候选数 = 0 |
| **TC-03** | 判据**缺 `match` 字段** | 退出码 `3`；**其余判据一条都不执行** |
| **TC-04** | 目标串被**反引号 / 加粗**包裹 | 判**命中**；三口径（原样 / 去反引号 / 去行内标记）**分别打印** |
| **TC-05** | 构造一条**两种算法结果不同**的计数判据 | 报「**不一致**」；**不得取其一**；退出码 ≠ 0 |
| **TC-06** | 对**以 LF 结尾**的文件计行 | 采用 `ReadAllLines` 语义值（比 `-split` 少 1）；报告标注所用口径 |
| **TC-07** | 注入一条「未通过」 | **汇总段随之变化**；**不得出现写死的"全部通过"文本** |
| **TC-08** | 归属标签取**枚举外**的值 | **立刻抛错**（不得落 default 分支、不得判未通过） |
| **TC-09** | 三种情形各跑一次 | 退出码分别为 `0` / `1` / `2`，与 §4.6 一致 |
| **TC-10** | 「未取到数」存在但**不加** `--allow-unknown`；再加一次 | 不加 ⇒ 退出码非 0（fail-closed）；加了 ⇒ `0`，**且报告仍保留该态计数** |
| **TC-11** | 跑完工具后对被检查文件算 SHA256 | **与跑前逐字节相同**（工具只读） |
| **TC-12** | 在**项目外干净浅克隆**内跑同一条判据 | 结论与本机一致（**"只在开发机跑绿即宣称通过"= 无效声明**） |
| **TC-13** | 构造"某条目被 skip，且该 skip 分支位于报告段之前"的场景 | **报告段仍必须非空**；**工具在每条退出路径上都必须输出报告段**；**不得出现"应报项存在但报告为空"**（第 18 例） |
| **TC-14** | 构造一个「取数函数返回 `null` / 空串 / 空集合」的场景 | **必须抛错**（fail-closed）；**不得输出 `0` / 空串 / 空集合当作读数**；**报告段与结构段的每个数值须可追溯至一次非空取数**（第 19 例） |

**⇒ 上线判据**：**TC-01 ~ TC-12 全绿**方可执行 §5.1 / §5.2 挂钩。**任一不绿 ⇒ 挂钩不做。**

---

## 8. 分工、门禁与交付物

| 角色 | 职责 |
|---|---|
| **决策侧** | 本规格（已完成）；TC 设计（已完成）；挂钩正文；验收裁定。**不实现、不验收自己的实现。** |
| **实现方** | 按 §4 实现工具 + 按 §7 自跑 TC 并附原始输出 |
| **独立复核方** | **与实现方不同的一方**，对照 §4/§7 复核，**重点复核 §4.2 三态与 §4.4 禁止事项** |

**交付物**
```
.trae/tools/verify-claims.py                  ← 工具本体
.trae/tools/README.md                         ← 索引同步（新增一行）
.trae/skills/three-kingdoms-vr-arena/SKILL.md ← 锁定文件：挂钩①（获用户专项批准）
AGENTS.md                                     ← 受控：挂钩②
.trae/CHANGELOG.md                            ← 变更记录
```

**门禁**：每步提交前后各跑一次 `dashboard/check-integrity.py`，**`EXIT=0`**；**且"绿"必须在本机项目根与项目外干净浅克隆两处都成立**。

---

## 9. 记录与沿革（守规则 3）

- 本 CR **不回写、不改写**任何历史记录；`governance/SessionCommands.md` §判据补充 的 17 例**原样保留**（它们是本规格的来源与证据）。
- **规则与工具并存**：17 例立法继续有效，**工具是其机械化实现**；**二者冲突时以工具行为为准并回填立法**（避免出现第二权威）。
- 本 CR 从决议到落地全程逐条入 `.trae/CHANGELOG.md`。

---

*—— 决策侧立，2026-09-12。用户已于同日批准（依据见 §1）。*
