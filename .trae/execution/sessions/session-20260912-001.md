# 会话记录 · session-20260912-001（决策侧 / 指挥台）

- **会话身份**：`decision`（指挥台）—— 用户明示的角色边界：**只裁定**（方案批准 / 范围变更 / TC 设计 / 任务包设计 / 登记册维护 / 规则立法）；**立法正文由决策侧自写，不外派执行侧**。
- **时间跨度**：2026-09-11 夜 → 2026-09-12（跨零点连续会话）。
- **协作方**：`git-operator`（AgentTeams `vrsanguo-audit-backup`，负责提交/推送/核验）、`session-20260908-002`（演武场执行会话）、`exec-m01-t001`（M01-T001 执行会话）。
- **本记录性质**：**规则 26 要求的会话记录**。**本会话未认领任何 MNN-TNNN 任务** —— 属决策侧治理会话，故**不写 `STATUS.json`、不改任何任务状态**；本文件只作"执行进度 / 沟通 / 关键决策 / 阻塞 / 恢复上下文"用途。

---

## 一、本会话要解决的问题（用户原话）

| 用户原话 | 落地动作 |
|---|---|
| 「**我不是让你核对完就推，而是要核对完，看哪些清理，哪些改，本地都梳理清楚了，改完了再推**」 | 确立**盘点 → 分清废弃与冗余 → 用户确认 → 本地清理/修改 → 用户确认 → 才推送 Git** 的标准流程 |
| 「**删   回信处理**」 | 删除冗余脚本 `flip_topdown.ps1`；处理信箱回信 |
| 「**放行，能推，其实 git 推送，我觉得可以你自己来定，对于我来说，只要做的东西不会丢失，有版本管理就 OK 了**」 | **推送授权下放决策侧**；**但"能推"不等于"能删"** —— 删除 / 范围变更 / 破坏性操作**仍需逐项明确批准** |

## 二、关键裁定（本会话产出）

1. **AFS 密钥：不轮换，仅移出版本控制**。性质是「**停止继续泄露**」，**不是消除已发生的暴露**（自 `98b2eb7` 2026-08-09 起存在于公开仓库全部历史）。`bCompileAFSProject=False` + 插件缺失 ⇒ **当前休眠、无可利用面**；**启用 AFS 前必须先轮换**。
2. **`M01-T007` 撤销**，编号保留给后续任务（不得复用）。
3. **演武场尺寸口径**：用户「**120*120 我是满意的，但尺寸还可能调整，不是最终决策**」⇒ 唯一权威表述落 `CR-20260911-001` §9.3：**现行 120×120（确认满意）· 尺寸待定，非最终**。
4. **`Saved/` 清理**：`Cooked` / `StagedBuilds` / `Screenshots` **保留**（用户「1.不删」）；`Saved/Shaders` 是着色器缓存、**非脏数据**。
5. **`Docs/Scene/` 上 git**（用户裁定）⇒ 5 个文件已入库，**冻结不得重写**。
6. **演练（备份恢复演练）没必要**（用户裁定）。
7. **项目外材料归 `D:\AWork\TraeAdmin\`**；**`E:` 不新增文件/文件夹**。
8. **记录自指立法**（`CR-20260911-001` §10.1–§10.6）：提交无法记录自身 SHA ⇒ **台账滞后合法**；记录**不得自称「终态」**，只能写「**截至 `<SHA>` 已核验**」。
9. **判据出错时执行侧怎么走**（本会话立法）：假阳性/假阴性 ⇒ 允许「**停手（零副作用）→ 诊断（含对照实验）→ 改用严格不弱于原判据的口径 → 权威对象复核 → 回报显式列出**」；**修正后若放宽则必须停手等裁决**；**等价性必须可证**。
10. **同族陷阱立法**（截至本会话共 9 例）：① 工具自报聚合数字不可作判据；② 命令失败后不得使用其输出变量；③ CRLF 使 `.`/`\s` 假阳性；④ CI 步骤级证据需配额无关路径；⑤ 只读数据行不读表头 ⇒ 列含义误判；⑥ **仪器取数失败 ≠ 阴性结论**；⑦ **「编辑成功」≠「文件结构完好」**（锚点即内容）等。

## 三、本会话执行动作

- **【文件清理·用户裁定执行，2026-09-12】** 用户裁定「**1、2 删**」⇒ 已删 `E:\AWork\Temp\` 2 件（删后回读 False）+ 项目外 `TraeAdmin\…\tmp\` **18 件**。**据实更正**：此前把 tmp 描述为「25 个一次性临时文件」，**盘点后确认不准确，实为 18 删 / 7 留**。**保留 7 件**（项目内无副本）：`hall_proposal*.png/.ps1`（**用户「按图动工」所依据的方案图**）与 `STATUS.json.broken-20260910` / `STATUS.repaired.json` / `repair-report.txt`（**STATUS.json 损坏事故原始物证**）。**判定依据：删除以「项目内是否存在等价副本」为准。** 另：`tmp\` 内 2 个 `b22-*.bin` 系执行侧字节导出法中间产物，**未动**。详见 `CHANGELOG.md` 同日条目。
- **【第 3 项·已问执行侧】** `L_Prototype_1v1_v4.umap` 可否删除 —— 用户指示「你问问执行模型可以删吗，我觉得可以」⇒ 已发问询至 `inboxes/session-20260908-002/`（三问：现存用途 / 不可恢复损失 / 是否先归档；并声明**收到答复前不删**）。**待回信。**


| 动作 | 结果 |
|---|---|
| **证据分类清理（选项 A）** | `Saved/Evidence/M01-T005/` 删除 **27 件 / 27.4 MB**（v3/v4/v4b/v53/v54/v55 系列），保留 15 件；`Saved/Evidence/` 47 件 → **23 件** |
| **v5.8 系列证据归档** | 执行侧自曝的欠账：8 件**仅存在于项目外临时目录**（「用完即删」目录）⇒ 只读复制归档（**源未删、逐件字节一致**）⇒ `M01-T005/` **23 件 / 32.5 MB** |
| **冗余脚本删除** | `flip_topdown.ps1` 项目外副本（935 B，与项目内 git 版哈希一致）删除 ⇒ `TraeAdmin` 剩 **6 个文件** |
| **`rebuild_v5.py` 归位** | 项目外 57,059 B 版覆盖项目内 50,614 B 版（哈希核验一致），项目外副本删除 ⇒ **单一事实源** |
| **git 提交与推送** | **14 个提交**（截至本记录撰写时 13 已推送，见 `CHANGELOG.md` 台账）；**每个提交**均通过：门禁提交前后各一次 `EXIT=0`、干净浅克隆复验、CI 任务级 + 步骤级两层、三方同 SHA、`lfs fsck` |
| **文档回写** | `M01-T005/TASK.md` + `CHECKS.md` 不再以 **20m×20m** 作验收依据；`TechnicalDecisions.md` 补记握持姿态条目；`SessionCommands.md` 新增 4 条判据 |

## 四、决策侧自身失误（据实登记，不掩盖）

| # | 失误 | 发现方式 |
|---|---|---|
| 1 | AFS 密钥判据「`SecurityToken=.` 非空即停」在 CRLF 文件上 **100% 假阳性** | **执行侧**抛出并做对照实验；裁定"处置得当，错在决策侧" |
| 2 | 提交八**静默删掉** `CHANGELOG.md` 的 `## 2026-09-11 晚（…）` 标题 | **纯属偶然**（下次写入找锚点时）—— **门禁 30 项全绿、CI success、六项核验全过，无任何检查发现** |
| 3 | 只读数据行、未读表头 ⇒ 把 `LevelDesignSpec.md` 列含义反推错，**错话进了已推送提交 `38cd714` 的提交信息** | **决策侧自行**在报告前多看一眼表头；文件内容正确，**已用追加更正提交 `1b0e34f` 补正**（不重写历史） |

**⇒ 三条共同点：失误 2、3 都不是被检查发现的。** 已据此登记两项门禁缺口（`CHANGELOG.md` 章节结构校验；表格列含义无校验）。

## 五、未决事项（**待用户拍板，禁止自行假设**）

0. **【已决 · 2026-09-12】演武场尺寸 —— 用户选 A**：原话「**A 把 120×120 写进规格书当标准，场地太小施展不开**」。
   - **⇒ 1v1 演武场可活动区域标准 = `120m × 120m`**（统一尺寸）。已写入 `knowledge/Production/LevelDesignSpec.md`（新增小节）+ `registers/01-decision-register.md` **DEC-014**；权威裁定正文 = `CR-20260911-001` **§9.6**。
   - **⇒ 该裁定改变了原 §9.3「现行 120×120 · 尺寸待定、非最终」的状态** —— 「非最终」已被用户拍板取代。
   - **⚠ 派生未决项 → 已决（同日）**：用户在 **①概念分离 / ②三档整体上调 / ③整表作废** 中选择 **①**。⇒ **「具体场地尺寸」（演武场 = 120×120）与「按人数计的通用活动区规格」判为两个概念，不得混于一表**；**「核心指标」表移除「可活动区域」一行**，旧三档**带删除线移入新小节「活动区规格（按人数计）—— 待定」留档（不抹记录）**；**该概念现无任何有效数值，AI 不得自行推算，也不得由 1v1 外推**（规则 10）。权威正文 `CR-20260911-001` **§9.7**。
1. **`E:\AWork\Temp\` 两个文件是否删除**（`vrsanguo_pie_t0.png` 339 KB、`vrsanguo_sword_view5.png` 2.7 MB）。
2. **`tmp\` 24 个文件是否清理**（`AGENTS.md` 原则「用完即删」，但删除需逐项批准）。
3. **v4/v5 umap**：用户曾说「v4 umap 可删」，但 v4 是**已跟踪的上一版布局** ⇒ **决策侧暂不删**，待澄清意图。
4. **「定时清理」规则化**：场地换代后上一代证据在下一轮清理窗口删除（只留最近一代 + 被登记册引用的）。
5. **6 张「其他」证据图**（5.4 MB，被 `session-20260908-002.md` 引用）去留。

## 六、阻塞与风险

- **`Content/VRSanguo/VR/BP_VRCharacter.uasset`**：用户正在编辑器内微调握持/手部（11:24:38 → 11:30:51 → 11:33:44 三次保存）⇒ **裁定不逐次快照**；工作区存在 **1 条用户未提交改动属预期，不作异常**。定稿后由 M01-T001 执行侧在其正式流程内提交。
- **`secret_patterns` 门禁项不覆盖 AFS 格式令牌** —— 已实测证伪（注入假令牌仍 `EXIT=0`）；`check-integrity.py` 封版中，**唯一剩余保护 = 人的纪律 + CR**。
- **CI API 403**：根因 = **代理共享出口 IP 的未认证配额耗尽**，**CI API 可用性不受本项目控制** ⇒ 步骤级改走 HTML `<check-step>` 配额无关路径；**受 403 阻碍时不得停写「受限于限流」**。
- **冻结令仍有效**：① 不得运行 `rebuild_v5.py` ② 不得重写 `Docs/Scene/` ③ 编辑器内不动手。

## 七、恢复上下文（下次会话怎么接）

1. **先读**：`.trae/CHANGELOG.md` 顶部 `## 2026-09-12` 节（本会话全部补记在此）+ `.trae/governance/SessionCommands.md` 判据补充节（9 例陷阱）。
2. **看任务状态**：`.trae/execution/active/STATUS.json`（唯一权威）+ `registers/07-task-register.md`。
3. **看技术债**：`registers/11-tech-debt-register.md` —— `open` **恰为 3**（TD-005/006/013），新增一律 `deferred`（本会话新增 **TD-017/018/019**）。
4. **看信箱**：`D:\App\trae\AgentHub\inboxes\decision\` + `channels/registry-changes`。
5. **接手注意**：① 用户**已授权决策侧自主推送**常规治理/文档提交，但**删除与范围变更仍须逐项批准**；② 两个执行会话**原地待命**、`rebuild` 冻结；③ 决策侧当日已有 3 次自造失误 —— **写入前后必须回读结构**（标题清单 / 行数 / 表格列数），**读表格必须先读表头**。

---

## 八、M00-T007 执行记录（判据工具 `verify-claims.py`）

> **本节由 M00-T007 执行方追加**（2026-09-12）；**追加而非改写**（规则 3：不回溯改写历史）。执行身份 = 决策侧会话 `session-20260912-001` 派出的**执行子代理**，认领标识 `session-20260912-001`（规则 16）。

- **认领（规则 16，先于任何文件修改）**：根 `.trae/execution/active/STATUS.json` 的 `M00-T007` → `status: in_progress` / `claimedBy: session-20260912-001` / `updatedAt: 2026-09-12`；包内镜像 `.trae/execution/active/M00-T007/STATUS.json` 同步一致。
- **任务范围**：新建 `.trae/tools/verify-claims.py` + `.trae/tools/README.md` 新增一行索引 + 报告 `.trae/execution/reports/tasks/M00-T007.md`；规格正文 = `execution/CR-20260912-001-judgement-tool.md`（只读引用）。
- **验收判据**：规格 §7 的 **TC-01 ~ TC-14**（决策侧设计，执行侧执行 —— 规则 15）。
- **执行进度**：
  1. ✅ 认领（根 + 镜像 + 会话记录同一轮完成）
  2. ⏳ 认领后门禁复跑
  3. ⏳ 实现 `verify-claims.py`
  4. ⏳ 逐条跑 TC-01 ~ TC-14 并留原始输出
  5. ⏳ 报告 + `.trae/tools/README.md` 索引
- **关键决策 / 发现**：见下（实现完成后回填；**未验证项一律显式标记**）。
  - **[发现 D-1]** 认领把 `M00-T007` 置为 `in_progress` 后，`dashboard/check-integrity.py` 第 10 项「STATUS.json 与任务登记册一致」比对根 STATUS.json ↔ `registers/07-task-register.md` 的归一化状态；该登记册行仍为 `**ready**`，而 **`07-task-register.md` 不在本任务白名单内** ⇒ 门禁读数与决策侧预期（29/1/0）存在偏差。**未自行扩权修改**，已在认领后立即回读门禁实测并上报决策侧。

### 8.1 执行进度（全部完成 · 追加于 2026-09-12 15:5x）

1. ✅ 认领（根 + 镜像 + 会话记录同一轮完成）
2. ✅ 认领后门禁复跑并上报（读数：`30 项 通过 27 失败 3` · EXIT=1；3 条失败逐条定位到「两条白名单路径尚未存在」+「`07-task-register.md` 行仍为 ready」）⇒ 决策侧裁定 **裁 B**：由其自行修正登记册；**明令不得为让门禁变绿而写空文件/占位文件**
3. ✅ 实现 `.trae/tools/verify-claims.py`（新建）
4. ✅ 逐条跑 **TC-01 ~ TC-15** 并留原始输出（规格正文 §7 为准；**TC-15 系决策侧在本次执行期间新增**）
5. ✅ 报告 + `.trae/tools/README.md` 索引

### 8.2 交付物（字节 / SHA256）

| 路径 | 字节 | SHA256 / 指纹 | mtime |
|---|---|---|---|
| `.trae/tools/verify-claims.py` | 72,206 | `6F67F3903BCD23F1B5201C08168032DD1804DE529C9DB493CE22AEA60FC75D70` | 2026-09-12 15:52:32 |
| `.trae/tools/README.md` | 见报告 | 新增「`verify-claims.py` — 判据执行器」一节（**纯追加，既有行零改动**） | — |
| `.trae/execution/reports/tasks/M00-T007.md` | 见报告 | 含 §七 附录（28 份原始日志逐字 + 每份的字节/SHA256[:16] 指纹表） | — |

### 8.3 判据结论（TC-01 ~ TC-15）

- **TC-01 ~ TC-15 全部通过**（逐条实测见报告 §三；原始输出见报告 §七）。跨用例机械检查：21 份判据日志全部含 `【报告段】` 与「判据门自断言」；全部用例 stderr = 0 字节；全日志 grep 写死文本「全部通过」= 0 命中；伪读数「行数 = 0」= 0 命中。
- `--self-test`：**9 项 通过 9 · 未通过 0 ⇒ EXIT=0**。
- **TC-12 偏差已获指挥台批准**：工具按纪律不提交 git ⇒ 干净克隆内没有它，故**逐字节复制**进克隆后再跑，并按要求把「克隆内工具 SHA256 == 工作区 SHA256」**单列一行**作为证据。被检查文件改用 `.trae/governance/ReviewProtocol.md`（工作区与克隆 SHA256 逐字节相同）。

### 8.4 关键发现（追加）

- **[发现 D-2 · 并发写]** 首选 TC-12 目标 `.trae/governance/SessionCommands.md` 在本次执行期间被**决策侧并发改写**：15:35 实测 244 行 / 8 个 h2 ⇒ 15:48 实测 267 行 / 9 个 h2。**未取到数不是缺陷，过期读数才是缺陷** ⇒ 已改用「已提交且未修改」的目标文件，并在跑前用 SHA256 双向证明可比性。
- **[发现 D-3 · 假阳性自查]** 自检初版用「整文件原文」口径扫禁词，把**说明性文字**（docstring / 注释 / 报告标题）里的词判成违规 ⇒ 工具**自己拦住自己**（9 项里 1 项假阳性）。改用 `ast.parse` 去 docstring + `ast.unparse` 去注释的「代码口径」后 0 命中，并**同时打印两个口径的命中数**留证。
- **[发现 D-4 · 假告警自查]** 执行脚手架（项目外，临时）在 PS 5.1 下出现三类系统性假象，均已定位并修正，全部记入报告 §6.2：`.ps1` 无 BOM 被按 GBK 解码；`Get-Content -Raw` 对无 BOM UTF-8 按 ANSI 解码（导致「报告段缺失 21/21」假告警）；`git clone` 把进度写 stderr 而被 `ErrorActionPreference='Stop'` 判成终止性错误（**同族第 12 例配套**）。
- **[发现 D-5 · 输出缺陷]** 结论串里的 `spec_errors` 计数恒显示 0（取值自空列表）⇒ 与真实错误数（1）矛盾，已修为取真实列表；`shape` 判据在 `measure=split_lf` 时「对照口径」与所用口径同值 ⇒ 已改为真正的另一口径。

### 8.5 门禁读数（**四次**，全部 EXIT=0）

| 读数 | 时间 | 总计 | 通过 | 警告 | 失败 | 退出码 | 取读时点 |
|---|---|---|---|---|---|---|---|
| 1 | 2026-09-12 15:54:47 | 30 | 29 | 1 | 0 | **0** | 交付物 + 报告均落地后 |
| 2 | 2026-09-12 15:55:01 | 30 | 29 | 1 | 0 | **0** | 与读数 1 逐字相同 |
| 3 | 2026-09-12 15:56:32 | 30 | 29 | 1 | 0 | **0** | 报告正文（§一~§七）写入之后 |
| 4 | 2026-09-12 15:56:58 | 30 | 29 | 1 | 0 | **0** | **收尾读数**：报告 §4.4 写入之后（其后仅追加本节） |

- 唯一警告 = `五件套结构`：缺失清单只剩 **M00-T005 / M00-T006（均 approved）的 6 条历史路径**，**已不含任何 M00-T007 路径** ⇒ 本任务两条白名单路径真实存在。**未以占位文件换绿。**
- 与交付物落地前对比：`30 项 通过 27 失败 3` · **EXIT=1** ⇒ 三条失败（五件套结构缺两条白名单路径、STATUS↔登记册、五方状态）全部清除。

### 8.6 未验证 / 未做（显式标记，不包装）

- **未验证**：真实 CI / 门禁接入（规格 §5.3 明令本 CR 不做）；非 Windows / 非 Python 3.10 环境；`--quiet` 与 `exec` 判据无专项 TC（**已实现未验证**）。
- **未做（白名单外，禁止）**：`.trae/skills/three-kingdoms-vr-arena/SKILL.md`、`AGENTS.md`、五件套回填、验证登记册 / CHANGELOG 登记（后者属审核/决策侧职责；且任务未 approved 时登记「已验证/通过」会触发 `task_verif_consistency` 失败）。
- **未做（纪律）**：任何 `git add` / `commit` / `push`。
- **待决策侧裁定**：门自断言失败的退出码取 3；`README.md` 索引段非字面「一行」；`TASK.md`/`CHECKS.md` 的 TC 数量是否回填（详见报告 §6.1）。

### 8.7 追加轮（规格 §4.3 第 18 条 + TC-16）· 2026-09-12 16:0x~16:1x

**规格侧追加**：§4.3 增第 18 条（① 数字/结构类判据必须声明**测量时点**，未声明 ⇒ 读数一律不采信；② 外部命令输出必须先**结构化**：剥离 ANSI + 显式类型转换，严禁把集合/多值交给格式化或比较）+ §7 增 **TC-16**。三条现实原型：(a) LFS 断言跑在 `git add` 之前（时点错）(b) 门禁项数正则被 ANSI 切断 ⇒「项数 =（空）」(c) `@(git rev-list --count …)` ⇒ `System.Object[]`。

**交付物刷新（同一个文件，版本号 1.0.0 → 1.1.0）**：

| 交付物 | v1.0.0 | **v1.1.0（现行）** |
|---|---|---|
| `.trae/tools/verify-claims.py` | 1,500 行 / 72,206 字节 / `6F67F390…` / 15:52:32 | **1,848 行 / 93,546 字节 / `B975FAFD250F0BF83E3CE1607BBAB6038ED88FDF7D766C6DFBC46BCF4A93A9F0` / 16:03:07** |
| `.trae/tools/README.md` | 4,528 字节 / `091598CD45B07A2D` | **5,323 字节 / 58 行 / `F88B09EEF75FC97EC0861F44D09E36E2773123A88135F31B77CE936C722CAAEE` / 16:09:02**（索引行按新指纹刷新 + 追加「必填声明（第 18 条）」一段） |
| `.trae/execution/reports/tasks/M00-T007.md` | 1,168 行 / 80,569 字节 / `CD71F33113AF6CAE` | **2,243 行 / 156,205 字节 / `F1C4C229A286C281`（`[:16]`）/ 16:11:04**（§八 追加轮分析 + §九 附录二逐字日志 + §一 指纹刷新；§一~§七 结论未改写） |

**为什么全量复跑**：第 18 条 ① 让「测量时点」成为必填 ⇒ 上一轮 TC-01~TC-15 的夹具 yaml 在 v1.1.0 下**一律被拒**（旧的 15 条读数无法再复现）。只补 TC-16 会让交付字节**没有**对应的 TC 证据（规则 11/22 的实质违规）⇒ 以 v1.1.0 的最终字节把 **TC-01~TC-16 全量重跑**，`--self-test` 由 9 项扩到 **14 项**。

**复跑结论（工具 v1.1.0）**：TC-01~TC-16 **全部符合期望**（逐条表见报告 §8.3）；`--self-test` **14 项 · 通过 14 · 未通过 0 ⇒ EXIT=0**；跨用例机械检查：**21 份判据日志 + TC-12 两份**全部含【报告段】/ 判据门自断言 / 测量时点，**全部 stderr = 0 字节**；TC-11 两阶段快照 **30 个文件 0 处变化**；TC-12 `git clone --depth 1 file://…` rc=0、两处 HEAD 相同、**克隆内工具 SHA256 == 工作区 SHA256（逐字节相同）**、两处 EXIT=0 且结论逐字相同。**临时目录与脚手架均在项目外，用完即删（残留检查 = False）。**

**本轮自查出的自身缺陷（全部已在报告 §8.4 留痕，不包装）**：① 自检第 14 项断言方向写反（自己红了 1 项）；② TC-12 二次跑 `git clone` rc=128（`rmtree(ignore_errors)` 删不掉 git 只读 pack ⇒ 目录非空），且首跑**没记 clone rc** 导致二轮排查；③ 「浅克隆」其实不浅（本地路径克隆 git **忽略 `--depth`**）⇒ 改 `file://` 后警告消失；④ 复跑期间 HEAD 被推进 `0a32773…`→`8965b97…`（含 LFS）⇒ 夹具显式 `GIT_LFS_SKIP_SMUDGE=1`（非 git 默认行为，已标注）；⑤ 四处断言假阳性（猜格式/猜措辞/把说明文字当违规/子串撞上「报告段完整性断言 ⇒ 通过」）⇒ 全部改为**结构位置断言**；⑥ **报告 §一 曾写入一个「没测过的全量 SHA256」（按 `[:16]` 前缀凭字形补全）**，同轮改为实测值 —— 与本节要防的「读数与证据脱钩」同族，属自查自纠。

### 8.8 门禁读数（**第五次**）

命令：`& <python> -B -X utf8 dashboard/check-integrity.py`（在项目根执行；`dashboard/check-integrity.py` 只读，本会话未改动它）

| 读数 | 时间 | 总计 | 通过 | 警告 | 失败 | 退出码 | 取读时点 |
|---|---|---|---|---|---|---|---|
| 5 | 2026-09-12 16:12:16 | 30 | 29 | 1 | 0 | **0** | 报告 §八/§九 与 §一 指纹刷新**之后**、本节 §8.8 写入之前 |

- 唯一警告与读数 1~4 **同一条**：`五件套结构` 的 **6 条历史路径**（M00-T005 / M00-T006，均 approved），**不含任何 M00-T007 路径**。
- 关键项均绿：`active 任务五件套完整`、`STATUS.json 与任务登记册一致`、`任务登记与验证登记状态一致`、`in_progress 任务会话记录完整`、`五方任务状态一致`、`链接完整性 163/0`、`无 >100MB 大文件`。
- 本轮**未**新增任何验证登记册行 / CHANGELOG 行（属审核/决策侧职责；且任务仍 `in_progress` 时登记「已验证」会触发 `check_task_verif_consistency` 失败）—— **未以占位文件或提前登记换绿**。

### 8.9 未验证 / 未做（本轮增量，显式标记）

- **本轮消除**：`exec` 判据「无专项 TC」⇒ 由 **TC-16②a / ②b** 取得专项证据（ANSI 剥离取数、多值抛错）。
- **仍未验证**：`--quiet`（无专项 TC，已实现未验证）；`--json` 的 `at` 字段未经逐字段 TC 核对；非 Windows / 非 Python 3.10 环境；真实 CI / 门禁接入（规格 §5.3 明令本 CR 不做）。
- **未做（纪律）**：任何 `git add` / `commit` / `push`；未改 `dashboard/check-integrity.py`；未改 `AGENTS.md` / Skill / 五件套回填（均在本任务白名单外）。

### 8.10 本轮新增待裁定口径（3 项，详见报告 §8.5）

1. 缺 `at` ⇒ **整份判据文件不合法**（退出码 3、不执行任何判据），而非「该条不出结论、其余照跑」——比规格文字**更严**（fail-closed）。
2. `at` 的适用范围 = **五类判据全部必填**（规格文字限「数字 / 结构类」）——理由：原型 (a) 恰是**存在性**断言，只卡数字类会漏掉原型本身。
3. `exec` 新增声明键 `stdout_int_regex` + `expect_int`（**恰好 1 个**捕获组）——第 18 条 ②「显式类型转换」的可执行抓手，键名属实现自定。

### 8.11 收尾读数（**第六次**）

| 读数 | 时间 | 总计 | 通过 | 警告 | 失败 | 退出码 | 取读时点 |
|---|---|---|---|---|---|---|---|
| 6 | 2026-09-12 16:12:53 | 30 | 29 | 1 | 0 | **0** | §8.8/§8.9/§8.10 写入**之后**（报告 §八/§九 已在位）⇒ 交付物、报告、会话记录三者同为最终字节 |

- **本行（§8.11）是读数 6 之后唯一的写盘**；随后在同一静止态上再复跑一次（**读数 7**）作收尾确认，其读数只在本次汇报中给出、**不写回文件**——写回会把该读数所测的那个状态本身改掉（这正是「量具改了被测对象」的经典陷阱，也是第 18 条 ①要声明时点的原因）。
- ⇒ 执行会话**在此停止写盘**，等待决策侧/审核侧在同一静止态上核对。
