# 会话记录 session-20260906-001

- 日期：2026-09-06
- 模式：执行模式（M01-T001 执行会话，接管 `exec-m01-t001` 信箱角色；运行环境 DSH）
- 关联任务：M01-T001（武器抓取与 VRE 集成）
- 前置会话：session-20260830-001（信箱 exec-m01-t001，交接记录见其会话记录末节「2026-09-06 交接/当前状态」）

## 认领轮（2026-09-06）

- 已更新根 `.trae/execution/active/STATUS.json`：M01-T001 claimedBy=**session-20260906-001**、updatedAt=2026-09-06、note 追加接管与 B 路线进展摘要。
- 已同步任务目录 `.trae/execution/active/M01-T001/STATUS.json`（claimedBy/updatedAt/info）。
- 本会话记录与认领同轮创建（规则 26）。

## 上下文恢复（读盘核对，不凭记忆）

1. **任务三件套**：TASK.md / CHECKS.md / STATUS.json 已完整阅读。
2. **前会话记录** session-20260830-001.md（628 行）已读，关键状态：
   - 抓取链路全部建成并桌面验证：BP_VRCharacter（VRE AVRCharacter）+ BP_VRGameMode + C++ VRSanguoGripLibrary（TryGrip/ReleaseGrip + [TryGrip诊断] 日志）+ 12 节点接线 + 环首剑 GrippableStaticMeshActor（socket VRGripP__0/VRGripS__0 完好）+ Manny XR 手部（MotionSource LeftGrip/RightGrip 已修正）。
   - **唯一阻塞**：PICO 串流（PICO Connect 10.6.6 / PicoStreamingXR 1.1.46）下按钮值完全不注入（S1-c 终判坐实：五 IMC 全挂 + LogEnhancedInput Verbose 正常 + 双独立实验零事件）；位姿追踪正常（FOCUSED、非零位姿）。
   - CR-20260905-001（PICO 插件 battery 门控 + 关手部 EXT）：①②⑤ 已过，③④（抓真剑实测）待按键问题解决后回补；回归调查 C-0 已结案（无回归）。
3. **决策链**：session-20260904-002.md 决策轮 1~7 已读；最后指令 msg-20260906-135627-decision-001 = c-1 帮助中心全文 fetch 升为最高优先、a 检并行（**已完成**，msg-20260906-135702 已报）、d 切运行时试验暂缓、c-2 更新日志继续。
4. **信箱**：exec-m01-t001 未读 0（最新归档 msg-20260906-135627 已读）；decision 未读 3（为前会话发往决策的回执，非本会话职责）；dispatch 板空闲。

## CHECKS.md 执行前清单

| 检查项 | 状态 | 依据 |
|---|---|---|
| 认领任务（根+任务 STATUS.json） | ✅ 本轮完成 | 规则 16/26 |
| VRE 插件编译通过 | ✅ | 2026-08-31 Rebuild Succeeded；CR-20260905-001 UBT Succeeded（2026-09-05） |
| 与 in_progress 任务无白名单冲突 | ✅ | M01-T005 范围=L_Prototype_1v1_v2 校场（无交集）；CR-20260905-001 白名单扩展已批准落账 |
| UE 5.6 编辑器可正常打开项目 | ✅（历史验证） | 2026-09-04/05 桥 start_editor 多次成功；本轮未重复启动（当前步骤无需编辑器） |
| PICO Neo3 输入映射可用 | ⚠️ 即当前调查项 | 串流下按钮值不注入（S1-c），B 路线查证中；真机 standalone 未复测 |

## 本轮计划（延续决策指令，B 路线 c 项收尾）

1. **c-1**：PICO 官方帮助中心《手柄按键异常/SteamVR 按键绑定》全文 fetch，整理原文要点 + 对本项目的适用步骤（用户侧 UI 操作，零代码零注册表）。
2. **c-2**：PICO Connect 更新日志查证（10.6.6 之后是否有输入/action 修复版本；SteamVR 控制器调节功能自 10.1.5 引入的佐证）。
3. **c-3**：Streaming APK ≥v4.3.5 适用范围（消费版 Neo3 是否适用）。
4. **汇总**：B 路线证据汇总 → 修复方案呈报用户批准（用户侧操作不出 CR，涉项目文件才立 CR）→ ③④ 回补 → CR-20260905-001 关闭 → TC-01~06 真机验证。

## 执行进度

### 2026-09-06 B 路线 c 项查证收尾 + 本机 SteamVR 层实锤（本轮核心）

1. **信箱与认领**：接管通报已投 decision（msg-20260906-162502-exec-m01-t001-001）；registry-changes 已广播认领变更（msg-20260906-162502-exec-m01-t001-002）。
2. **c-1 帮助中心全文（浏览器引擎渲染成功）**：《SteamVR 异常问题》（pico-connect-guide.bytedance.com/docs/help_center/3bwaisms）。关键章节：
   - 《手柄按键异常怎么办？》**官方明示**：按键不生效/抢占/映射差异 → 经 SteamVR 控制器按键绑定功能调节：① 进入 SteamVR 场景按左手柄菜单键 → ② 控制器 → ③ 管理控制器绑定 → ④ 可选"自定义"或采用高时长玩家模板；游玩时长少者建议用高分社区模板。
   - 《游戏手柄偏移》：10.0 版本暂不支持自定义手柄调节（邮箱反馈口径，较旧）。
   - 《SteamVR 报错 108》：`%LOCALAPPDATA%\openvr\openvrpaths.vrpath` 的 `external_drivers` **为空或多行会导致 SteamVR 异常；多行应仅保留 PICO 互联安装位置条目**。
3. **c-2 版本查证**：官方下载页（picoxr.com/cn/software/pico-link）确认 **10.6.6 = 当前最新正式版**（2025-09-02 发布，更新日志无输入/绑定修复项）；Beta/历史版本汇总页（bbs.picoxr.com/post/7174295725314932772）为 React SPA 无法抓取正文（记录为通道限制，不阻塞——正式版已确认无升级路径）。
4. **c-3 Streaming APK 4.3.5 适用范围**：开发者平台「安装串流服务」页仅返回导航壳，正文未获取；决策已标注不阻塞，挂起。
5. **本机只读自检（新增实锤，官方文档路径）**：
   - `C:\Users\PC\AppData\Local\openvr\openvrpaths.vrpath`：`external_drivers` **两条** = `D:/App/PicoConnected/PICO Connect/openvr_driver/`（现役）+ `D:/AGame/Pico_Neo_3/Streaming Assistant/driver`（**已卸载软件的悬空残留**，目录不存在）→ 命中官方"多行"故障条件。
   - `D:\AGame\Steam\logs\vrserver.txt`（SteamVR 09-05/09-06 有运行，覆盖 S1 实验窗口 20:48-20:49）：
     - 悬空驱动行每次被优雅跳过（`Skipping external driver ... not a directory`）→ F1 清理为官方卫生项，根治概率下调。
     - **`[Remapping] ParseRemappingData warning. No remappings or autoremappings specified in layout from controller: pico_controller to controller: oculus_touch.`（双向）** —— pico_controller 布局已加载但 SteamVR 重映射/绑定层按键映射为空，与 S1-c「按钮值完全不注入」精确吻合 → **F2 绑定层修复升为主路径**。
     - 另有 `Cannot use 'compatibility_mode_controller_type' and 'remapping' in the same input profile. 'compatibility_mode_controller_type' will be ignored.`（PICO 驱动自带 input profile 配置矛盾，佐证其绑定层缺陷）。
6. **c-1 佐证（社区）**：贴吧 VRChat 吧用户对同类 PICO 手柄映射 bug 的实测解法 = 游戏内 SteamVR 菜单 → 管理控制器绑定 → 编辑此绑定（与官方路径一致）。

### 修复方案阶梯（呈报用户批准；均用户侧/环境侧，零项目文件改动，零代码）

| 级 | 内容 | 性质 | 预期 |
|---|---|---|---|
| F1 | 备份 openvrpaths.vrpath → 删除悬空 Streaming Assistant driver 行（保留 PICO Connect 行） | 官方建议卫生项，1 行可回退 | 根治概率低（SteamVR 已优雅跳过），但消除文档化故障条件 |
| F2 | SteamVR → 控制器 → 管理控制器绑定 → 检查当前应用（UnrealEditor/通用）+ PICO 手柄绑定；空/缺 Grip/Trigger 则应用默认或高分社区模板 | **主路径**（官方文档 + SteamVR Remapping 空映射实锤 + 社区佐证） | 直接针对"按钮值不注入" |
| F3 | 切 OpenXR ActiveRuntime → SteamVR 对照试验（完整步骤 + 回切清单，用户同意后） | 隔离试验，F2 无效时 | 定位 PICO runtime vs 驱动/绑定层 |
| F4 | 方案 2 IMC 双绑定（涉 /Game/VRTemplate/Input/*，需 CR） | 兜底，维持暂停 | — |

**验证口径**：F1/F2 后重跑 S1 式零触实验（LogEnhancedInput Verbose）+ `[TryGrip诊断]` 日志；打通后回补 CR-20260905-001 ③④ → 用户 PICO 真机 standalone 复测 TC-01~06。

## 关键决策 / 阻塞待办

- 修复涉用户环境文件（openvrpaths.vrpath 在 `C:\Users\PC\AppData\Local\`）与用户侧 UI 操作 → 按决策轮 5 裁定呈报用户批准后才动（本轮 chat 内呈报）。
- c-3（Streaming APK 4.3.5 适用范围）挂起不阻塞；bbs Beta 汇总页 SPA 抓取不到，已记录通道限制。
- decision 箱证据汇总信已投（msg-20260906-1630 前后）；用户批准结果待回。

## 追加：2026-09-06 用户批准 + F1 实施完成

- **用户批准**（chat ask_user_question）：F1 授权代改 ✅；F2 现在做 ✅。
- **F1 落盘**：备份 `C:\Users\PC\AppData\Local\openvr\openvrpaths.vrpath.bak-20260906` → 删除悬空行 `D:/AGame/Pico_Neo_3/Streaming Assistant/driver` → `external_drivers` 仅剩 PICO Connect 条目（ConvertFrom-Json 校验 OK，条目数=1）→ 写回（UTF-8 无 BOM）→ 重读复核通过。回退方式：用备份文件覆盖即可。
- **桥探活（规则 28）**：`project.get_status` → mode=live、editorConnected=true、UE 5.6、VRSanguoYanWuchang，编辑器在运行。
- **当前状态**：等待用户重启 PICO Connect 并重连串流（使 F1 生效 + SteamVR 干净重载）→ 我方启动 PIE（L_SkeletonTest）→ 用户执行 F2（SteamVR 管理控制器绑定检查）→ S1 式零触实验复测 → 我方收日志判定。
- **注意**：PIE 须在 XR 会话建立后启动（避免"死会话轮"），顺序不可颠倒。

## 追加：2026-09-06 晚 F2 执行 + S1 复测 + 根因收窄（本轮核心二）

### 执行时序
1. **F2 前置**：用户重连串流（PICO Connect settings.json 19:05 更新）；桥探活 live；关卡 L_SkeletonTest 确认；PIE 启动成功（pawn=BP_VRCharacter_C_0、五 IMC 全应用、XR 会话 FOCUSED）；Verbose 开启（LogEnhancedInput + LogPICOOpenXRInput，桥确认生效）。
2. **用户质疑与新增证据**：用户对"查 SteamVR"提出合理质疑（平常 PICO 直连不经 SteamVR 运行时）并实测 **SteamVR 内所有按键正常、可模拟** → **手柄硬件→PICO 驱动→SteamVR 输入链路排除嫌疑**。已向用户解释：查 SteamVR 依据 = 官方帮助中心指路 + vrserver 日志 Remapping 警告；该测试价值 = 排除一整层，未改任何绑定。
3. **PIE 内按键复测（S1 式）**：用户在 VR 预览按多次 → **IA_Grab 0 条、[TryGrip诊断] 0 条、EnhancedInput 动作事件 0 条、PATH_UNSUPPORTED 0 条**（最后者 = CR-20260905-001 battery 门控修复仍有效，无回归）。
4. **日志深查**（主日志 1588 行，编辑器 11:05 本地启动）：PICO 输入插件存活（`Skipping vibration action creation - parametric haptics extension not enabled` 门控日志），但**全程无任何 interaction profile 绑定记录**；PICO Connect 桌面端日志为 sqlite（log_sdk_v2.db），无明文 profile 线索。
5. 位姿疑点（挂观察项）：本轮读左右 GripMotionControllerComponent 世界位姿均贴地 (0,-121,0) 且左右相同（与 C-0 轮双手独立非零位姿不同）——待用户 sight-check（手柄是否跟手）回报，不阻塞输入判定。

### 判定
- **S1-c 在 F1 清理后复现**；SteamVR 驱动/绑定层已排除；根因收窄至 **PicoStreamingXR 1.1.46 OpenXR 运行时的输入路由层**（候选机制：设备上报 interaction profile 与键名 profile 不匹配 / 运行时接受绑定建议但不路由按键值——待 F3 对照区分）。
- 方案 2（IMC 双绑定）维持暂停：若 F3 证实 PICO 运行时缺陷，双绑定无解（问题不在键名层）。

### F3 呈报（待用户批准）
切 OpenXR ActiveRuntime → SteamVR 对照试验，完整步骤 + 回切清单已呈报（chat ask_user_question）。**用户裁定：不切注册表，改走轻量排查。**

## 追加：2026-09-06 深夜 轻量排查重大转折——S1 复测实验环境无效 + 映射链架构验证通过

### 轻量排查过程（全部只读）
1. **插件启用链**：.uproject OpenXR enabled ✅（OpenXRInput 为 OpenXR 插件内模块，5.6 无独立插件目录）；VRE（VRExpansionPlugin 目录内默认启用，.uproject 里 Enabled:false 条目是另一个同名旧插件 OpenXRExpansionPlugin，非 VRE，虚惊）。
2. **映射链源码验证（三层）**：
   - PICO 插件键表（PICO_Controller.cpp L44-64）：PICONeo3_Left/Right 全键（Grip_Click + Grip_Axis 双键）✅
   - PICO 插件 override 引擎接口 `GetInteractionProfiles`（PICO_Controller.h L91/.cpp L467-521）：注册 5 个 profile，**neo3 无扩展门控**（L479-486，xrStringToPath 成功即注册），profile 字符串 = `/interaction_profiles/bytedance/pico_neo3_controller`（bytedance 命名空间）
   - 引擎 OpenXRInput.cpp（OpenXRHMD 模块内，`Engine/Plugins/Runtime/OpenXR/Source/OpenXRInput/Private/`）L428-434：`Plugin->GetInteractionProfiles(...)` → `Profiles.Add(KeyPrefixes[i], FInteractionProfile(...))` 收集链 ✅
3. **映射链架构结论：完好**（引擎 OpenXR enabled → PICO GetInteractionProfiles → bytedance profile 注册 → 建议绑定）。
4. **日志扩展列表实锤（重磅）**：`LogHMD: OpenXR runtime supported extensions:` 列表含 **`XR_BD_controller_interaction`** → 运行时认识 bytedance 命名空间 profile，候选机制「profile 命名空间不匹配」被削弱（但设备上报仍待验证）。
5. battery 门控日志确认：`XR_EXT_interaction_profile_battery_state_display extension is not enabled - will skip battery action binding` → CR-20260905-001 无回归。

### **重大转折：本轮 S1 复测实验环境无效（执行会话失误自曝）**
- 日志时间线：编辑器 11:05 本地启动 → **XR 会话 11:05:56 FOCUSED → 11:11:40 EXITING** → **此后 8 小时无任何新 XR 会话建立记录**。
- 我 17:50 start PIE 后**无新 XR 会话链**；search_log 搜到的"FOCUSED"是上午旧记录（未核对时间戳，失误）。位姿读数贴地且左右相同 = 无追踪输入，与之自洽。
- **结论：用户 17:54 前后按键时编辑器进程的 XR 输入链路未运行 → 零事件是环境因素，不能作为"PICO 运行时输入缺陷"的复测证据**。与 09-05 死会话轮（C-0 裁定：必验手柄可见）同类教训，本次重蹈。
- S1-c 原判定（09-05，实验有效）仍然成立，但本轮复测无效。

### 待办（干净环境复测，已呈用户）
1. 问用户：头显里此刻显示什么画面（游戏场景/桌面/黑屏）——判定 PIE 期间用户视角。
2. 重启编辑器（stop_editor + start_editor）→ 用户确认串流连接 → PIE → **核对日志新 XR 会话链（时间戳）** + 位姿非零 → 再让用户按键 → 三层日志判定。
3. 若重启后 PIE 仍无新 XR 会话 → 编辑器 XR 栈问题再议（届时 F3 切运行时或升级路径重新上桌）。

## 追加：2026-09-07 时区纠正——复测实验有效，死会话判断作废（用户质疑成立）

- **执行会话失误自纠**：UE 日志时间戳为 UTC。昨晚（09-06）读到的"11:05 启动/11:11 EXITING"实为本地 19:05/19:11——19:05:56 XR 会话 FOCUSED 是用户重连串流后 PIE 的正常会话建立，19:11:40 EXITING 是我 19:11 停 PIE 的正常关闭。凭空推断"8 小时死会话"错误，**上一段"复测实验环境无效"的转折判断作废**。
- **用户证词佐证**：用户确认按键时头显内可见游戏场景（渲染交换链工作 = 会话必然活跃）。
- **修正后判定：09-06 晚实验有效**（XR FOCUSED 全程 + 用户见场景 + 按键多次 + 三层日志零事件）= **S1-c 有效复现**。
- **当前日志深查**（VRSanguoYanWuchang.log 2247 行，09-06 19:05 启动进程持续写至 09-07 15:36）：
  - PIE 窗口（19:05:56-19:11:40）内 LogPICOOpenXRInput 仅 1 条（会话启动 haptic 门控跳过），按键期间**完全沉默**；引擎 LogOpenXRInput 类别零输出；无绑定失败、无 action 触发记录。
  - `LogHMD: Initialized OpenXR on PicoStreamingXR (Standalone) runtime version 1.1.46`；XR_BD_controller_interaction 在运行时支持列表（两次打印）；XR_BD_ultra/ XR_EXT_battery 不可用（门控正常）。
- **根因候选收敛（新增主假设）**：**Neo3 手柄硬件与 PICO4 同款，运行时可能把手柄上报为 `/interaction_profiles/bytedance/pico4_controller`**（插件已注册 PICO4 键前缀），而 IMC 仅用 PICONeo3_* 键（挂 pico_neo3 profile）→ action 挂在无设备 profile → 位姿正常（走 xrLocateViews 不走 action）、按键全无、多键（ValveIndex/OculusTouch/Vive/MixedReality）也全不匹配 → 所有症状自洽。
- **验证路线（呈用户）**：A) 零改动：PICO Connect 端/日志查设备上报 profile；B) 项目改动（需 CR）：IMC 临时加 PICO4_Right_Trigger_Click + PICO4_Right_Grip_Axis 映射同一 IA——立刻有值即实锤，无值则排除、回到 F3 切运行时对照。
- 备注：方案 2（IMC 双绑定）当初 paused 的理由（S1-c 下无意义）需重审——若设备报 pico4 profile，加 PICO4 键即有解。

## 追加：2026-09-07 晚——验证模式澄清 + PIE VR Preview 实测（输入全灭终判）

### 操作模式澄清（用户关键指正）

- **用户日常验证全部在"VR 预览"（编辑器世界）**：能看场景/转头/摇杆移动均为编辑器导航功能，非游戏输入。"能移动能转"的历史观察与游戏输入系统无关。
- **普通 PIE（非 VR 预览）下**：PIE 渲染到桌面窗口，头显只投屏整个桌面；手柄对游戏相当于鼠标。**此前所有"PIE 按键零事件"实验的实验条件不完整**——TASK.md TC 用例写的 "VR Preview" 指的是 Play 的 VR 预览模式（PIE 立体渲染到头显），此前被误读为两个词。
- 执行会话曾替用户启动 PIE 引发不满——**PIE 由用户自己掌控，不擅自启动**。

### PIE + VR Preview 实测（2026-09-07 22:28-22:33，真实验证环境）

- 用户勾选 Play 下拉 "VR Preview" 后进入：**渲染 ✓**（`Resizing VR buffer to 4416x2208` 立体渲染实锤）、**XR FOCUSED ✓**（22:28:27 与 PIE 同秒建立）、**追踪 ✓**（用户确认看到双手手柄模型且跟随）。
- **输入全灭（终判证据）**：
  1. 用户摇杆操作后 pawn 位置零变化（0.0012, -121.13, 98.15 与生成点一致）——摇杆无值活体证明；
  2. 扳机/侧键零事件（IA_Shoot/IA_Grab/TryGrip 零记录）；
  3. LogEnhancedInput + LogOpenXRInput **VeryVerbose 级**在按键窗口零输出（VeryVerbose 落盘已验证：14:31:52 LogHAL 确认行在案）。
- **根因收敛（当前最强判定）**：PicoStreamingXR 1.1.46 串流运行时对 UE PIE 会话的 **action 输入系统不路由**（渲染与追踪走 xrLocateViews 正常，唯独 xrSyncActions/action 值全空）。UE 官方论坛同症状帖佐证（Neo3+UE5.2 输入映射无效、零回复、无公开解法，2024-02）：https://forums.unrealengine.com/t/pico-neo3-in-unreal-5-2-input-mappings/1711739
- pico4 profile 假设排除（CR-20260907-001 试验完成，PICO4 键映射无效果）；NTFS 只读保护正确阻止 IMC 落盘（内存态试验自然回滚）。
- 桥技术债新增：`editor.save_dirty` handler 崩溃编辑器（EditorHandlers.cpp:1221，ue-mcp 1.0.87）；PICO 官方文档站为纯 SPA+反爬无法抓取正文（通道限制）。

### 待用户决策的下一步选项

- A) **PICO 真机 standalone 打包验证**：真机运行时非 PicoStreamingXR，官方插件设计目标环境；但 TC-01~06 证据类型为 PIE 截图/日志，真机证据需决策侧认可（证据类型变更=任务范围调整，需批准）。
- B) **继续修串流输入**：深挖引擎 action attach/sync 路径（execute_python 读 XR 状态或引擎源码分析），耗时不确定。
- C) **PICO Connect Beta 版实验**：c 项遗留（Beta 通道运行时未知），用户侧安装 15 分钟实验。

## 追加：2026-09-07 深夜——网络深查生态全景（用户指正"再查一查"后执行）

### PICO UE 串流生态全景（多源交叉验证）

1. **PICO 两代 UE 插件**（cnblogs gccbuaa 教程 04，2025-10-30，https://www.cnblogs.com/gccbuaa/p/19177427）：
   - PicoXR（旧一代，PXR 函数前缀）：仅支持一体机 Android 打包，不支持 PC exe 串流；2025-09-03 已更新 UE5.6。
   - PicoOpenXR（本项目所用）：支持 exe+apk，PC 串流用此；文章记录 v1.5.0，本项目实装 **v1.6.1**（EngineVersion 5.6.0，比文章更新，版本无问题）。
2. **三种串流方式对比**（作者 Pico4UE 企业版实测）：
   - OpenXR 串流（PicoStreamingXR 运行时 = 本项目当前方式）：作者实测**稳定、手势/追踪正常、可正常开发**。
   - SteamVR 串流：可用但"更容易出现连接中断的小问题"。
   - PDC+LivePreview 串流：显卡满载、DX11 限制，作者不推荐。
3. **PICO Live Preview（官方 UE 开发预览专用插件）**：官方版本最高适配 UE5.3（2025-05 起适配到 5.5？文中"旧版本 v1.0.2 只适配到 UE5.3，今年 5 月才升级适配到 UE5.5"）；社区修改版 MonsterGuo/UE5.4_Pico_Live_Preview 最高 UE5.4。**本项目 UE5.6 无官方 Live Preview 支持**。
4. **PDC 与 PICO 互联互斥**（51cto 转载官方说明）："使用 PDC 工具和 PICO 互联软件会导致 PDC 工具运行异常或串流服务异常……使用 PDC 前确保 PC 端和头戴端都已退出 PICO 互联"。
5. **成功组合 vs 本项目组合差异**：
   - 作者成功：Pico4UE 企业版（新设备）+ 企业串流 v2.0 + PicoOpenXR + OpenXR 串流模式。
   - 本项目：PICO Neo3（2021 老设备）+ PICO 互联 10.6.6（消费者版串流）+ PicoOpenXR 1.6.1 + UE5.6。
   - 差异变量：设备代际（Neo3 老）、串流应用（互联 vs 企业串流）。
6. **中文社区对本问题零讨论**：Bing/DuckDuckGo 多轮检索（PICO 互联+UE5+串流+手柄按键 无反应/教程）均无直接相关帖——**全网未找到"PICO 互联 + UE5 PIE 手柄按键成功"案例**；UE 官方论坛唯一同症状帖（Neo3+UE5.2）零回复。
7. PICO 官方文档站（developer-cn/global.picoxr.com）为纯 SPA+反爬，正文无法抓取（通道限制，两引擎均验证）。
8. CSDN 相关文章（qq_41032419《UE5.2+Neo3 串流与预览控制》）：明确"不建议使用 PDC 工具串流"（缺 UE5.2 LivePreview 插件 + 蓝图 DrawDebug 节点兼容性问题导致画面强制暂停）；该文推荐 SteamVR 路线。
9. CSDN 另一文提及"PICO 互联 PC 端可选择 SteamVR 串流/OpenXR 串流"两种模式——本项目当前即互联的 OpenXR 模式（PicoStreamingXR 运行时）；互联的 SteamVR 模式 = 用户实测过"SteamVR 界面按键正常"的那条链路（vrserver.txt 的 pico_controller 驱动记录）。

### 收敛判定（更新）

- PicoOpenXR 插件版本无问题（1.6.1 为 UE5.6 最新）；OpenXR 串流路线本身可行（作者成功案例）——**失败聚焦在"Neo3 + PICO 互联"组合的 OpenXR action 输入路由**（渲染/追踪正常、action 全空，VeryVerbose 零输出）。
- Neo3 为互联生态的老设备，其 OpenXR 串流的 action 输入支持存疑（互联设计目标为消费级 PCVR 游戏串流，多走 SteamVR 模式）。

### 重排后的决策选项（呈用户）

- A. **临时切 SteamVR OpenXR 运行时跑 PIE 验收**（可逆，注册表 1 值；用户已实测互联 SteamVR 模式驱动层按键正常 → PIE 输入大概率通；TC 证据可出；互联无需卸载）。
- B. **真机 standalone 打包验证**（最终交付形态；半天；TC 证据类型需决策侧认可）。
- C. **PICO 互联降级实验**（10.6.6 → 10.5.10，用户侧操作；成功率未知）。
- D. 维持现状，登记技术债，任务 blocked。

## 追加：2026-09-07 深夜 II——SteamVR 运行时切换实验（进行中，待电脑重启）

### 用户决策与操作

- 用户批准方案 A（临时切 SteamVR 运行时跑 PIE 验收）："行吧，那切 steamvr 试试吧"。
- 用户在 SteamVR 设置 → OpenXR 完成"设为当前 OpenXR 运行时"：`ActiveRuntime = D:\AGame\Steam\steamapps\common\SteamVR\steamxr_win64.json`（原值已备份：`D:\App\PicoConnected\PICO Connect\openvr_driver\resources\ps_xrt\picostreaming-openxr.json`，验收后恢复）。

### 实验受阻证据链

1. 编辑器启动卡 0% → `OpenXRHMDModule::InitInstance` 枚举 SteamVR 扩展成功 → **`Failed to create an OpenXR instance, XR_ERROR_RUNTIME_FAILURE`**（两次：15:05:23 / 15:19:04，各挂近 2 分钟）→ VR Preview 置灰（无 XRSystem）。
2. vrserver 日志：`Unable to read pid from pipe SteamVR_Namespace … CSharedResourceNamespaceServer shutting down`；vrstartup 报 **`VR_Init error: Shared IPC Namespace Unavailable (310)`**。
3. **UE 的 vrclient 日志从未生成**（vrclient_Steam.txt 等存在，无 UnrealEditor 的）——连接在 vrclient 层失败，未达 vrserver。
4. **SteamVR 官方工具 `vrcmd --pollinit` 同样挂起**（20 秒超时）——与 UE 同症状，确认 SteamVR IPC 服务层损坏，非 UE 侧问题。
5. vrserver 被 pico 驱动 "controller update pose without activate" 警告每秒多条刷屏（驱动重试循环）；用户确认头显里 SteamVR 场景可见双手柄图标（手柄数据链路本身通）。
6. 软重启 SteamVR 两轮未恢复 IPC；判定为当天反复切换运行时 + 多次强杀导致的共享 IPC 命名空间残留损坏，**需重启电脑**。

### 电脑重启后的既定流程

1. 用户：启动 PICO 互联（PC 端+头显连接）→ 启动 SteamVR → 状态窗全绿（头显+双手柄图标点亮）。
2. 执行会话：`vrcmd --pollinit` 健康探针（返回=好，挂起=坏）→ 启动编辑器（验证=日志无 `Failed to create an OpenXR instance`）→ 加载 L_SkeletonTest。
3. 用户：Play 下拉 → VR Preview → Play → 扳机 2 秒/侧键 2 秒/摇杆。
4. 执行会话：抓日志判定（IA/TryGrip 事件 + LogEnhancedInput VeryVerbose + pawn 位移）。
5. 通过：记录 TC 证据，询问是否切回 PICO 运行时；仍失败：恢复 PICO 运行时（回切 `picostreaming-openxr.json`），回到决策 B/C/D。

### 本轮新登记技术事实

- SteamVR（D:\AGame\Steam；vrstartup 2.16.7；buildid 23791826）OpenXR 入口 `bin\vrclient_x64.dll`；vrclient 连接需 Steam 客户端环境（steam 进程在跑 ✓）。
- 新版 SteamVR 无"OpenXR 兼容性测试"按钮；`vrcmd --pollinit` 可作 IPC 健康探针。
- pico SteamVR 驱动手柄休眠时刷 "without activate" 警告（每秒多条），疑似拖死 vrserver 服务线程——重启后留意其与 IPC 健康的关联。
- pwsh 5.1 `Get-Content` 默认 GBK 读 UTF-8 中文文件显示乱码（假象）；DSH read 工具正常。本记录经 read 工具核验无损。

## 追加：2026-09-08 下午——SteamVR 路径四连修复 + 根因闭合 + 解锁征询（当前阻塞）

### SteamVR 路径四连修复（全部有日志证据，均用户批准）

1. **IPC 命名空间损坏**：xrCreateInstance 三连失败（RUNTIME_FAILURE×2 + VALIDATION_FAILURE）。定位：SteamVR 共享 IPC 命名空间坏（`vrcmd --pollinit` 同症状挂起实锤；vrserver 日志 `Closing pipe SteamVR_Namespace ... broken`）。软重启两轮无效，**重启电脑后干净启动恢复**（namespace 管道 6ms 连通）。
2. **PICO 插件 required 扩展**（用户批准源码修复）：PICO_Controller.cpp 把 XR_BD_controller_interaction 列为 required → SteamVR 无此扩展即拒。改 required→optional（PICO 键表本属可选增强，真机无影响）。编译生效（UBT 命令行 19.9s Result Succeeded）。
3. **OpenXR API 1.1 vs SteamVR 1.0**（用户批准配置修改）：loader debug 一锤定音 `xrCreateInstance called with invalid API version 1.1. Max supported version is 1.0`；且 SteamVR 返回 VALIDATION_FAILURE（非 API_VERSION_UNSUPPORTED），**绕过了 UE 的 1.1→1.0 回退逻辑**（OpenXRHMDModule.cpp L905 只认后者）。DefaultGame.ini `[/Script/OpenXRHMD.OpenXRHMDSettings]` 加 `bIsOpenXR1_1Enabled=False / bIsOpenXR1_0Enabled=True`（引擎从 GGameIni 读；曾误写 DefaultEngine.ini 已撤销）。PICO 运行时兼容 1.0，真机无需回退。
4. **XR_EXT_UUID 依赖缺失**：`xrclient_UnrealEditor.txt`（首次生成的 vrclient 日志）精确报 `attempted to enable XR_EXT_RENDER_MODEL without enabling required XR_EXT_UUID`。PICO_HMD.cpp GetOptionalExtensions 补 XR_EXT_UUID 行。编译 17.9s 成功。

### 里程碑与根因闭合

- **xrCreateInstance succeeded with XR_API_VERSION_1_0**（日志双行）；VR Preview 不再置灰；PIE 渲染/追踪正常。
- SteamVR 绑定链全通：绑定 json 加载成功（oculus_touch profile）+ remapping 成功（非 S1 时代的空映射）+ **绑定测试器按钮点亮**（驱动层输入数据正常）。
- **真根因（引擎源码实锤）**：BP_VRCharacter BeginPlay 孤立无连线（read_graph_summary 15 节点核实），从未 AddMappingContext；UE5.6 OpenXRInput.cpp L902 仅 sync 已激活 IMC 的 action set → **两运行时下相同零输入现象的最简一致解释**——瓶颈在项目侧，非串流层。

### 修复执行与卡点

- IMC_Weapon_Left/Right 需补 IA_Grab→Grip 映射（IA_Grab_Right/Left_Pressed/Released 资产已存在于 /Game/VRTemplate/Input/Actions/）；BP_VRCharacter BeginPlay 需加 AddMappingContext 节点链（GetPlayerController→GetEnhancedInputLocalPlayerSubsystem→AddMappingContext×2）。
- **第一次尝试教训**：set_property 只改内存未落盘；随后 add_node 触发 BP_VRCharacter 保存 → NTFS 只读 `Cannot remove ... read only` → **appError 致命崩溃**，改动全丢。另：我误加的 PICONeo3_Right_Grip_Click key 不在 OpenXR path 映射表（XR_ERROR_PATH_UNSUPPORTED ensure），重做时撤掉，只留 OculusTouch 行。
- **当前卡点**：编辑器弹「无法从版本控制成功检出 IMC_Weapon_Right」= NTFS 只读治理保护。用户指示走治理流程。
- **已投递决策信箱**：msg-20260908-183359-exec-m01-t001-001（完整修复汇报 + 解锁授权请求，含 Unlock-TraeAssets.ps1 方案：解锁 Content/VRTemplate/Input + BP_VRCharacter.uasset，完成后 -Restore 重锁）。**值班等回复中**。

### 恢复上下文（下次会话速览）

- SteamVR 运行时现役（ActiveRuntime=steamxr_win64.json），xrCreateInstance 已通；编辑器/桥正常（若桥卡 initializing 先查编辑器弹窗）。
- 待办链：决策回复 → 解锁 → IMC set_property（OculusTouch 行版）+ save_all_dirty 立即落盘 → BP_VRCharacter 加节点（连接 BeginPlay→GetSubsystem→AddMappingContext(IMC_Weapon_Right,0)→(IMC_Weapon_Left,0)）→ VR Preview 按键测试（决定性）→ TC-01~06 证据 → 回补 CR-20260905-001 ③④ → -Restore 重锁 → 回切 PICO 运行时。
- VeryVerbose（LogEnhancedInput/LogOpenXRInput）已开但编辑器重启后需重开。
