# 会话记录 session-20260830-001

- 日期：2026-08-30

- 模式：执行模式（VR 武器抓取与 VRE 集成，M01-T001 后续；VRE 角色+GameMode 搭建 + MCP 掉线排查）

- 关联任务：M01-T001；关联资产：BP\_VRCharacter / BP\_VRGameMode / L\_SkeletonTest

## 执行进度

1. **排查 MCP 反复掉线根因**（用户要求"动手前先修 MCP"）：

   - 用命令行直连 `http://127.0.0.1:8080/mcp` 做 `initialize`，返回 200、407 个工具、session id 正常。

   - 结论：UE 侧 ue-bridge 桥接服务始终存活健康（端口被 UnrealEditor 进程监听），**掉线根因在 TraeWork
     客户端侧对** **`mcp_ue-bridge`** **的连接/注册状态管理不稳定**（报 `MCP server '' not found`，server 名为空
     而非连不上 8080）。非项目缺陷，无需改项目。

   - 处置：告知用户属客户端问题，遇掉线在 TraeWork → 设置 → MCP → 点刷新即可恢复。
2. **项目级 MCP 配置清理**：发现项目根 `.mcp.json` 存在但指向死端口 8000（旧 UE5.8 时期遗留），是多余
   且无用的配置。经用户确认后**删除** `D:\AWork\Unreal\Project\VRSanguoYanWuchang\.mcp.json`。
3. **诊断 VR 预览手柄失灵原因**（用户核心诉求：我们自己的游戏在 VR 预览能正常用手柄）：

   - 全局默认 GameMode=`/Game/VRTemplate/Blueprints/VRGameMode_C`（官方 VR 模板）；`L_SkeletonTest` 的
     WorldSettings `default_game_mode=None` → 回退全局默认官方模板。

   - 该关卡生成的是官方 `VRPawn_C` + 默认 `PlayerController`，而用户放的剑是 VRE 的
     `GrippableStaticMeshActor`。官方模板 Pawn 不处理 VRE 抓取与手部输入 → 手柄"只能看场景、能转视角、
     不能移动/操作"（头显位姿由 OpenXR 直接驱动故能转视角；手柄输入需角色接管，官方 VRPawn 不认 VRE 剑）。

   - 确认 VRE 能力其实可用：继承了 AVRCharacter 的类能加载，`GrippableStaticMeshActor`/`VRCharacter`/
     `MotionControllerComponent`/`VRPlayerController` 原生类均在。
4. **自建 VRE 角色 + GameMode**（用户确认"直接改成项目默认 GameMode"）：

   - 新建 `BP_VRCharacter`（`/Game/VRSanguo/VR/BP_VRCharacter`），父类=VRE `AVRCharacter`
     （`/Script/VRExpansionPlugin.VRCharacter`）。AVRBaseCharacter 构造函数自带 VR 相机 + 左右
     `GripMotionControllerComponent`（抓握）+ VR 移动组件，派生命名蓝图即可获得完整 VR 抓取能力。

   - 新建 `BP_VRGameMode`（`/Game/VRSanguo/VR/BP_VRGameMode`），父类=`AGameModeBase`；
     设 `DefaultPawnClass=Bp_VRCharacter`、`PlayerControllerClass=AVRPlayerController`
     （`/Script/VRExpansionPlugin.VRPlayerController`）。

   - 修改 `Config/DefaultEngine.ini`：`GlobalDefaultGameMode` 由官方 `VRGameMode` 改为
     `/Game/VRSanguo/VR/BP_VRGameMode.BP_VRGameMode_C`。

   - 已保存并验证：两个蓝图存档无编译错误（日志仅 SavePackage/AssetCheck 正常流程），GameMode 属性读回正确。
5. **桌面 PIE 验证（桌面层，非真机）**：加载 `L_SkeletonTest` 后启动 viewport PIE；因 MCP 查询运行时
   世界的方式不可靠（`get_worlds()` 返回 0、get-state 返回占位文本），未取到运行时 Pawn 快照。PIE 已停止。
6. **编辑器重启后复核（本轮）**：确认重启后一切保留——

   - `Config/DefaultEngine.ini` 的 `GlobalDefaultGameMode` 仍指向 `/Game/VRSanguo/VR/BP_VRGameMode.BP_VRGameMode_C`；

   - `BP_VRCharacter` / `BP_VRGameMode` 蓝图均可正常加载（`BlueprintGeneratedClass` 已生成）；

   - `BP_VRGameMode_C` CDO 读回：`DefaultPawnClass=BP_VRCharacter_C`、`PlayerControllerClass=VRPlayerController`
     （`/Script/VRExpansionPlugin.VRPlayerController`）——项目默认 VRE 玩家配置完好。
7. **修复光点位移问题（本轮）**：用户反馈"光点不在剑把手上"。诊断发现两个标记球
   `StaticMeshActor_4`/`StaticMeshActor_5`（标签 `GripPulse_VRGripP__0`/`GripPulse_VRGripS__0`）仍停在剑中心
   `(0,-51,54)`。直接 `set_actor_location`/`set_transform` 对这些 VRE 标记球无效（其组件为
   `OptionalRepStaticMeshComponent`）。改用 `edit-level-actor` 的 `attach_actor` 把它们**附着到剑网格的
   握持 socket**：4 号→socket `VRGripP__0`，5 号→socket `VRGripS__0`，`keep_world_transform=false`。
   读回组件世界坐标确认：4 号=`(0,-60.5,100.5)`、5 号=`(0,-73,106.5)`，与剑网格两个握持 socket 世界坐标完全一致，
   且 parent 均为 `GrippableStaticMeshActor_0`。光点现已贴合刀柄握持点并随剑移动。
8. **修复握持光点巨大化（本轮）**：用户反馈"光点巨大把剑吞了"。诊断发现 `attach_actor` 附着到 socket 时因
   `keep_world_transform=false`，把主握持球 `StaticMeshActor_4` 的缩放重置为 `1.0`（单位大小），而副手球
   `StaticMeshActor_5` 保留 `0.05`。用 `set_actor_scale3d` 把 4 号球缩放改回 `0.05`，读回确认两颗球缩放均为
   `0.05`、位置仍精确落在握持 socket。已保存关卡。

   - 已保存关卡。

## 2026-09-01 蓝图接线与 PIE 实测（manager 派工 msg-20260831-102000-mgr-004）

### 执行进度（本轮）

1. **诊断并解除 UBT Rebuild CLR 异常（0xE0434352）**：UBT 在 makefile 阶段崩溃。经查 Windows 事件日志，
   根因是 `git core.quotepath` 开启（默认 true），`git status` 把含中文文件名（`环首刀.uasset`）转义为
   `"Content/.../\347\216\257..."`，导致 UE5.6 `GitSourceFileWorkingSet` 解析该 fragment 时抛
   `ArgumentException: contains invalid directory separators`，进程崩溃。执行 `git config core.quotepath false`
   后 `Build.bat VRSanguoYanWuchangEditor Win64 Development` 成功（exit 0，Result: Succeeded，DLL 22:17:31 更新）。
2. **确认** **`VRSanguoGripLibrary`** **反射可见**：重启编辑器后用新桥 `reflect_class`（className 不加 U 前缀）
   成功返回 `VRSanguoGripLibrary`（parentClass=BlueprintFunctionLibrary），`TryGrip(UGripMotionControllerComponent*,
   float, float, FName)→bool` / `ReleaseGrip(UGripMotionControllerComponent*)→bool` 完整反射，Category="VRGrip"。
3. **解除 add\_node 图写入崩溃**：`add_node` 在 BP\_VRCharacter 上触发编辑器崩溃。查 UE 日志确认根因：
   蓝图资产 `BP_VRCharacter.uasset` 文件只读（Content 资产批量只读），add\_node 编译后自动保存失败 →
   `SaveAssetPackage` 严重错误 → 编辑器退出。临时解除只读后 add\_node 正常。
4. **蓝图接线（左右手各 6 节点 = 12 节点 + 8 条连线）**：

   - 新增 3 个 EI 事件（IA\_Grab\_Right\_Released / Left\_Pressed / Left\_Released，已有 Right\_Pressed）+ 4 个
     Get 变量（Right/LeftMotionController）+ 2×TryGrip + 2×ReleaseGrip。

   - 连线（4 exec + 4 data）：EI\_X.Pressed.Started→TryGrip.execute；EI\_X.Released.Completed→ReleaseGrip.execute；
     Get X MotionController.XMotionController→TryGrip/ReleaseGrip.MotionController。

   - socket：右手 TryGrip.OptionalSocketName=VRGripP\_\_0；左手 TryGrip.OptionalSocketName=VRGripS\_\_0。
5. **compile + asset.save + validate**：compile 成功、save 成功、validate 通过（errorCount=0, warningCount=0, valid=true）。
6. **读回核对（read\_graph\_summary + read\_node\_property + reload\_package）**：8 条连线边与写入完全一致；
   socket 落盘正确（reload\_package 从磁盘重载后读回 VRGripP\_\_0 / VRGripS\_\_0）。
7. **桌面 PIE 实测**：`play_in_editor start` 成功（isPlaying=true）；pawn=BP\_VRCharacter\_C\_0 正确生成
   （DefaultPawn 配置正确）；`get_applied_imcs` 确认 IMC\_Default 已应用（4 个 IA\_Grab\_\* 动作映射就绪）；
   `capture_screenshot` 保存 PIE\_wiring\_test.png。
8. **收尾**：停止 PIE，恢复 `BP_VRCharacter.uasset` 只读（readonly False→True）。

### 关键决策

- ① 不修复 MCP 客户端本身（属 TraeWork 侧管理问题，非项目可修）；遇掉线刷新即可。

- ② 删除 `.mcp.json`（死端口/旧遗留），不做替换——用户明确"直接删了"。

- ③ 采用"自建 VRE 角色+GameMode 并设为项目默认"方案，让所有关卡默认用 VRE 玩家，手柄移动/抓剑由 VRE 接管。

- ④ 手部模型（骨骼/手套/手势）为独立视觉资产任务，**本次只做"手柄能移动+能抓剑"的功能验证**，手部模型
  留待后续任务推进，不混入本次。

- ⑤ **manager 决策（2026-08-30 消息 mg-20260830-222500，inbox msg-20260830-222500-manager-001）**：

  - 抓握路线选 **A（最简可用）**：在 BP\_VRCharacter 补抓握逻辑，Grip 键→检测→GripObject/DropObject。
    Route B（按 IGrabbable 接口封装）属 M01 后续任务（T002/T003），本次不做。

  - 抓握键用 **Grip（握把键）**，非扳机（避免与五武器弓箭拉弦的 Trigger 冲突；PICO 物理 Grip 在握把位置）。

  - 完成后：按 TC-01\~06 逐条验证；登记技术债 **TD-013** 到 `registers/11-tech-debt-register.md`（内容：
    "M01-T001 使用 VRE 内置 GripObject 做功能验证，未按 systems/02 IGrabbable 接口封装；M01 后续任务或 M02
    时按接口标准重构"）；向 inboxes/manager/ 回 reply 信（状态/证据路径/ref）。

## 调研结论（本轮，交叉验证 3 源）

- VRE **不内置抓握输入绑定**：`AVRBaseCharacter` 只创建左右 `GripMotionControllerComponent`，没有
  `SetupInputComponent`/MappingContext 绑定（源码 `VRBaseCharacter.cpp` 仅 113/127 行创建组件）。
  `GripMotionControllerComponent` 源码亦无输入绑定。抓握必须由角色蓝图把手柄输入接到 `GripObject`/`DropObject`。

- VRE 官方最佳实践：Grip 键按下→手柄**球体检测**（Sphere Trace，半径匹配手掌，失败回退 Overlap）→最近可抓取
  `GrippableActor`→调 `GripMotionController.GripObject`（可用 `OptionalSnapToSocketName` 或 `bIsSlotGrip=true`
  自动匹配名称含 "Grip" 的 socket）；松开→`DropObject`。参考 [$TRAE\_REF](https://vreue4.com/documentation?section=grip-motion-controller)。

- **技术偏差**：manager 批复内"GripObject 自带 sphere overlap，不用额外写检测逻辑"与源码不符——`GripObject`
  必须显式传入 `ObjectToGrip`（头文件 985 行），**不会自动扫描附近物体**。故仍需在蓝图里写球体检测找物体。

- 输入层已就绪：`Config/DefaultInput.ini` 全局 DefaultMappingContexts 含 `IMC_Default`，已把手柄抓握按下/释放
  映射到现成动作 `IA_Grab_Left_Pressed`/`IA_Grab_Left_Released`/`IA_Grab_Right_Pressed`/`IA_Grab_Right_Released`
  （读回 IMC\_Default 确认 8 条 mapping 含这四个动作）。**无需新建 IMC/IA**，只需在 BP\_VRCharacter 绑定这 4 个动作。

- 蓝图引用手柄：`AVRBaseCharacter` 暴露 `LeftMotionController`/`RightMotionController`
  （`BlueprintReadOnly` 的 `UGripMotionControllerComponent`），可直接在蓝图取"Left/Right Grip Motion Controller"。

- 合规确认：`standards/03-blueprint-cpp-boundary-standard.md` 允许蓝图调用 C++ `BlueprintCallable` 规则函数
  （VRE 的 GripObject/DropObject 正是如此暴露），属"表现层调规则层"，非绕过。

- 蓝图编辑工具能力：`edit-blueprint-graph` 支持 `K2Node_EnhancedInputAction`（EI 事件节点）、`add_call_function`
  （SphereOverlapActors 等）——可在 BP\_VRCharacter 事件图搭建。但完整链路节点多、pin 名需精确，编译风险可控性需评估。

## 抓握蓝图方案（已定，实施依据）

在 `BP_VRCharacter`（父类 AVRCharacter，自带 Left/Right Grip Motion Controller）事件图搭建，左右手各一套：

**右手（主手）抓取：**

- 事件 `EnhancedInputAction IA_Grab_Right_Pressed`（Started）
  → 从 `Right Grip Motion Controller` 位置做 Sphere Overlap（半径 10cm，距离 30cm）找最近 `GrippableActor`
  → 命中则调 `Right Grip Motion Controller.GripObject(ObjectToGrip, WorldOffset, bIsSlotGrip=true, OptionalSnapToSocketName="VRGripP__0")`

- 事件 `IA_Grab_Right_Released`（Completed）→ `Right Grip Motion Controller.DropObject(bSimulate=true)`

**左手（副手）抓取：**

- 事件 `IA_Grab_Left_Pressed`（Started）→ 左手同样球体检测 → `Left Grip Motion Controller.GripObject`（socket `VRGripS__0`）

- 事件 `IA_Grab_Left_Released`（Completed）→ `Left Grip Motion Controller.DropObject(bSimulate=true)`

技术要点（源自源码核实）：

- `GripObject` 需显式传 `ObjectToGrip`，**不自动扫描**（头文件 985 行）。

- socket 匹配：bIsSlotGrip=true + `OptionalSnapToSocketName` 显式指定（左右手分别 P/S）。

- 输入动作现成：`IA_Grab_Right_Pressed/Released`、`IA_Grab_Left_Pressed/Released`（IMC\_Default 已映射，无需新建 IMC）。

- 球体检测：`add_call_function` 调 `KismetSystemLibrary.SphereOverlapActors`（或组件 `SphereTrace`）。

## 变更文件

| 文件                                          | 变更                                                                           |
| ------------------------------------------- | ---------------------------------------------------------------------------- |
| `.mcp.json`                                 | 删除（死端口 8000 的旧遗留配置）                                                          |
| `Config/DefaultEngine.ini`                  | `GlobalDefaultGameMode` 改为 `/Game/VRSanguo/VR/BP_VRGameMode.BP_VRGameMode_C` |
| `Content/VRSanguo/VR/BP_VRCharacter.uasset` | 新增（父类 AVRCharacter/VRE）                                                      |
| `Content/VRSanguo/VR/BP_VRGameMode.uasset`  | 新增（父类 AGameModeBase；默认 Pawn=Bp\_VRCharacter，PC=VRPlayerController）           |

## 待办 / 阻塞

- **真机验证（PICO）**：VRE 角色生成后，需在 PICO 头显进 VR 预览实测——手柄能否移动、能否抓起环首刀。
  本次仅完成桌面层搭建与无编译错误确认，桌面 PIE ≠ 真机通过（规则 6/11）。

- ~~光点标记（StaticMeshActor\_4/5）位置与剑中心重合~~ → **已解决（本轮）**：已把两颗标记球
  `attach_actor` 附着到剑网格握持 socket `VRGripP__0`/`VRGripS__0`，组件世界坐标与 socket 完全一致
  （4 号 `(0,-60.5,100.5)`、5 号 `(0,-73,106.5)`）。已保存关卡。

- 手部模型：后续独立任务。

***

## 追加：2026-08-31 路线 C（C++ 封装）执行记录

### manager 决策升级（msg-20260830-231000-manager-002）

- 抓握路线由 **A（最简 MVP，已实施受阻）** 升级为 **C（C++ 封装）**。理由：C++ 管规则、蓝图管表现（对齐 `standards/03`）；把易错点收进编译器强类型检查，绕开"ue-bridge 读不回蓝图 pin"的工具边界；该封装是未来 `systems/02` IGrabbable 接口桥雏形。

- 明确禁止重启 TraeWork 刷 MCP（会杀所有会话）；继续用文件等价方式收发；hub\_await 本会话做不了，接受"回信后由用户转达一次查信箱"。

### 实施成果（已完成并编译验证）

1. 新建 `Public/Combat/VRSanguoGripLibrary.h` + `Private/Combat/VRSanguoGripLibrary.cpp`：

   - `TryGrip(UGripMotionControllerComponent*, float SphereRadius=12, float ForwardDistance=30, FName socket=NAME_None)`：球形扫掠（手柄世界位置→前方 30cm）→ 取最近的可抓取物（实现 `IVRGripInterface` 且 `DenyGripping=false`）→ 调 `GripObject`（socket 吸附）。

   - `ReleaseGrip(UGripMotionControllerComponent*)`：`GetGrippedObjects()` → 逐个 `DropObject`。

   - 均为 `BlueprintCallable`，左右手通用。
2. `Build.cs`：`PublicDependencyModuleNames` 加 `"VRExpansionPlugin"`。
3. 编译验证：

   - 首次全量 Rebuild 因编辑器运行锁 DLL 失败（`-Clean` 无法删除被加载模块）。

   - 经 ue-bridge `build-and-relaunch`（关闭编辑器→构建→重启），但该工具只关闭了编辑器、构建未完成；随后手动启动编辑器，发现 8080 未监听（ue-bridge 未启动）。

   - 关闭编辑器后直接命令行全量 Rebuild：`VRSanguoGripLibrary.cpp` 报 1 处编译错误（`DenyGripping` 非 const 成员调用，`C2662`），已修复（`const_cast`）。

   - 重跑 Rebuild：`Result: Succeeded`（`[1/4] Compile VRSanguoGripLibrary.cpp` → Link DLL）。`UnrealEditor-VRSanguoYanWuchang.dll` 更新为 2026-08-30 23:25:48，420352 B。

   - **注意**：ue-bridge 的 `build-and-relaunch`（dry\_run 通过，实际执行只关闭了编辑器）不可靠；真正的 Rebuild 需关闭编辑器后用命令行 UBT 直跑。
4. TD-013 已更新登记（`registers/11-tech-debt-register.md`）：注明 `VRSanguoGripLibrary` 是 IGrabbable 桥接层的雏形。

### 阻塞：蓝图接线后置（用户已确认）

- **根因**：ue-bridge 的 Python 执行器（`execute_python`）在处理蓝图对象时触发 `McpToolRegistry::ExecuteTool()` 崩溃（`UEBridgeMCP.dll`，TD-010 已知不稳定），导致编辑器整体退出（两次，PID 25412→11716→15868 均崩）。

- 现象：`execute_python` 写 marker 能成功（Python 基本可用），但涉及蓝图对象的脚本（读 `simple_construction_script`/`get_all_graphs` 等）会崩编辑器。

- 处置：放弃经 ue-bridge Python 读/改蓝图；蓝图接线（4 个 EI 事件→调 TryGrip/ReleaseGrip）后置，待解决 ue-bridge 稳定或改桌面手动/图编辑工具。

- **2026-08-31 已与用户确认"蓝图后置，先交付当前成果"。**

### 待办（后置）

1. BP\_VRCharacter 蓝图接线（4 个 EI 事件 → `TryGrip`/`ReleaseGrip`，每手 ≤4 节点）。
2. 桌面 PIE 抓/放实测（依赖 1）。
3. 用户 PICO 真机验证 TC-01\~TC-06（桌面 PIE ≠ 真机）。

***

## 追加：2026-09-01 光点复发修复 + 新桥打通

### 用户反馈

- **"光点又位移了"** —— 握持光点标记球 `StaticMeshActor_4/5`（`GripPulse_VRGripP__0`/`GripPulse_VRGripS__0`）再次不贴合剑柄。

### 关键 context：新桥变更（规则 27 重大更新）

- 旧 `UEBridgeMCP`(HTTP 8080) 已退役（插件目录改 `.disabled`）。

- 新桥 = **db-lyon/ue-mcp**（WebSocket JSON-RPC，编辑器端口 9877），由 node stdio 客户端启动：
  `node D:/App/trae/ue-mcp/1.0.87/node_modules/ue-mcp/dist/index.js <uproject>`。

- 本会话 MCP 列表无 `mcp_ue-bridge`，故通过命令行脚本 `uemcp_batch.js` 对接（MCP stdio 握手：initialize→notifications/initialized→tools/call）。

- 打通确认：`project(get_status)` 返回 `editorConnected:true`，工具 `ue-mcp 0.6.4`（23 tools）。

### 根因（诊断链）

1. `level(get_outliner)`：两光点与剑均在 `(0,-51,54)` —— 光点退回剑根，未贴合手柄 socket。
2. `asset(list_sockets)` 查剑网格 `环首刀`：**socketCount = 0** —— `VRGripP__0`/`VRGripS__0` 丢失（与 2026-08-29 崩溃后 socket 未落盘同类）。
3. 结论：socket 未落盘 → 光点 `attachParent` 虽为剑 actor，但 socket 附着失效 → 退回根。

### 修复动作（已验证 + 落盘）

1. `add_socket` 补写两个握持 socket：P=`(9,0,46)`、S=`(18,0,51)`（`onConflict=update`），确认 `socketCount:2`。
2. `attach_actor` 把光点 `SnapToTarget` 附着到对应 socket：4→`VRGripP__0`、5→`VRGripS__0`。

   - `describe_object` 复核：4 号组件 `AttachParent=剑 StaticMeshComponent0`、`AttachSocketName=VRGripP__0`、`RelativeScale3D=0.05` ✓（本次 SnapToTarget 未重置缩放，区别于 08-30 需手动改回 0.05 的情况）。
3. **保存**：`asset(save)` + `level(save)` 均成功（socket + 关卡）。

### 关键坑：Content 资产只读属性

- `环首刀.uasset`、`L_SkeletonTest.umap` 均 `IsReadOnly=True` → UE `SaveAsset`/`SaveDirtyPackages` 全部失败（`save_all_dirty` 返回 `savedAll:false`）。

- `Content\VRSanguo` 下 49 个 uasset/umap 全为只读（仅 2 个非只读）——目录被批量设过只读保护。

- 处置：临时清除 2 个目标文件只读 → 保存 → 复核落盘 → 恢复只读（保住保护姿态）。

- 落盘验证：文件时间戳更新为 `2026/9/1 0:09`；`reload_package` 强制从磁盘重载后 `list_sockets` 仍为 2 —— **socket 已真实落盘**。

### 证据

- 时间戳：`环首刀.uasset` 2026/9/1 0:09:05；`L_SkeletonTest.umap` 2026/9/1 0:09:06（保存后确认）。

- socket：`socketCount:2`（P/S 各 1，坐标正确），磁盘重载复核通过。

- 光点附着：4→P、5→S，`AttachSocketName` 命中，缩放 0.05。

### 待确认（用户侧）

- 建议用户回到编辑器视图查看光点是否贴合剑柄。若仍偏，需复查剑 actor 的 mesh 引用是否为 `环首刀`（附带 socket 的网格）。

***

## 追加：2026-09-01 manager 派工 M01-T001 蓝图接线（工具阻塞已解除）——执行记录

### 派工与归档

- 读 manager 派工信 `msg-20260831-102000-mgr-004`：M02-PREP-002 已验收（db-lyon/ue-mcp v1.0.87 上线），工具阻塞解除，现派工完成 M01-T001 最后一步——BP\_VRCharacter 蓝图接线（左右手各 6 节点，每步 read\_graph\_summary 读回核对，完成后 PIE 实测 + 回执）。

- 归档：派工信与旧信均移入 `AgentHub/inboxes/executor/archive/`，inbox 清空。

### 光点问题复核（顺带，已确认）

- 用户反馈"光点还在中心"。重启动编辑器后 `get_component_details` 读到：主手球 `GripPulse_VRGripP__0` 世界坐标 `(0,-60,100)`、副手球 `(0,-69,105)`，**均在剑柄区域**（非剑根 `(0,-51,54)`），socket 仍为 2。

- 结论：光点已正确贴合 socket。用户关闭编辑器前看到的"中心"是**保存前的旧页面**；重新加载后状态正确。此项无需二次修复。

### 蓝图现状

- `BP_VRCharacter` 父类 `VRCharacter`；组件含 `Left/Right Grip Motion Controller`（GripMotionControllerComponent）。

- `EventGraph` 已有 4 节点：`事件开始运行`/`事件Actor开始重叠`/`事件Tick`（均禁用）+ 1 个 `EnhancedInputAction IA_Grab_Right_Pressed`（此前尝试遗留，未接线）。

- 无自定义变量。

### 阻塞：C++ VRSanguoGripLibrary 运行时不可见（接线前置条件）

- `reflection(reflect_class UVRSanguoGripLibrary)` → **"Class not found"**；`blueprint(search_node_types TryGrip)` → 0 结果。

- 但同模块类 `VRGameFlowComponent`/`VRSanguoBootstrapActor` 可正常 reflect（说明项目模块已加载，reflection 工具正常，唯新库类未注册）。

- 磁盘证据：`Binaries/Win64/UnrealEditor-VRSanguoYanWuchang.dll`（2026-08-31 21:49）**含** **`VRSanguoGripLibrary`/`TryGrip`** **字符串**；源码 `.h/.cpp` 完整；`Build.cs` 含 `VRExpansionPlugin`；`project(read_module)` 确认模块含该头/源。

- 结论：类符号在 DLL 中，但**运行时 UHT 反射未注册**——需 Rebuild 让 UHT 重新生成 `.generated.h` 并重编译。

### 阻塞：Rebuild 受 UBT CLR 异常阻断

- 尝试 `Build.bat VRSanguoYanWuchangEditor Win64 Development`（及 dotnet 直调），**4 次均失败**：在 UBT makefile 创建阶段触发未处理异常，exit code `-532462766`（0xE0434352，CLR 异常），无编译输出，**DLL 未更新**（仍 08-31 21:49）。

- 排查：UBT 主日志 `Log.txt` 停在 "Creating makefile ... / Skipping UE5Rules.dll" 后静默终止，无 error 详情。

- 已处理：清除 `Intermediate/Build/BuildRules/VRSanguoYanWuchangModuleRules.{dll,pdb,Manifest.json}`（旧桥 `UEBridgeMCP` 退役后，manifest 曾报 "Removed source file ... UEBridgeMCPControlRig.Build.cs"，清理后不再出现该行），但 Rebuild 仍异常。

- 状态：**blocked**——蓝图接线无法开始（依赖 VRSanguoGripLibrary 在运行时可见），Rebuild 阻断原因需进一步诊断（疑为 UBT 环境/缓存异常，未盲目继续破坏性重试）。

### 待办/后续

1. 诊断并解决 UBT CLR 异常（0xE0434352）→ 让 `VRSanguoGripLibrary` 反射注册 → `reflect_class` 能查到后，再执行蓝图接线（左右手各 6 节点）。
2. 蓝图接线 + 读回核对 + Compile/Save（每只手一轮）。
3. 桌面 PIE 抓/放实测（标注层级=编辑器/PIE）。
4. 回执 manager（msg-20260831-102000-mgr-004）+ `hub_await(role="executor")`。

***

## 追加：2026-09-03 站立坠落修复 + Manny XR 手部网格——执行记录

### 任务脉络（延续 manager 路线 A 裁决）

- 用户此前反馈"进去可以站着了，但是手柄没有"。本记录完成两件事：① 修复 PIE 无限坠落；② 为 `BP_VRCharacter` 配置手部渲染网格使手柄可见。

### ① 无限坠落修复（深度诊断 → 已修复）

- 现象：`BP_VRCharacter0` 在 PIE 中 Z 一路降到 -186615，velocity=-4000，grounded=false，相机跟随坠落 → 桌面/VR 预览黑屏。

- A/B 对照：同关卡换官方 `VRPawn`（无移动组件、不依赖落地检测）正常；`BP_VRCharacter`（有 `VRCharacterMovementComponent`）坠落 → 问题锁定在角色/移动组件。

- 源码定位：`VRBaseCharacterMovementComponent.cpp` `ComputeFloorDist`（648,702 行）仅沿 `GetGravityDirection()` 向下扫地板。

- **根因**：`PlayerStart` 位于 Z=0（地面高度），角色生成时胶囊中心在地面、底部在 -96（`CapsuleHalfHeight=96`），VRE 向下的落地搜索永远找不到已低于地面的地板 → 永久 `MOVE_Falling`。

- 重力排除：`globalGravityZ` 0 与 -980 均能站立，重力非根因（保留 0）。

- **修复**：`L_SkeletonTest` 的 `PlayerStart` 从 (0,-90,0) 抬到 (0,-90,100)。PIE 实测角色稳定站立（grounded=true、velocity=0、Z=98.15、71s 稳定，二次复测确认），已保存关卡。

### ② 手部网格配置（用户授权 Manny XR 身体+手）

- 诊断：`BP_VRCharacter` 有 `Left/Right Grip Motion Controller`（GripMotionControllerComponent，只做抓握逻辑**不渲染手**），但**缺手部渲染网格组件** → 手柄不可见。

- 参考模板：官方 `B_MannequinsXR`（父=SkeletalMeshComponent，`SKM_MannyXR_left/right` + `ABP_MannequinsXR`），mesh 资产 `SKM_MannyXR_left/right`、骨架 `SK_MannequinsXR`。

- 改动：为 `BP_VRCharacter` 新增两个 SkeletalMeshComponent：

  - `HandMeshLeft`（父=Left Grip Motion Controller，`SKM_MannyXR_left` + `ABP_MannequinsXR_C`）

  - `HandMeshRight`（父=Right Grip Motion Controller，`SKM_MannyXR_right` + `ABP_MannequinsXR_C`）

- 验证：编译成功、`validate`（errorCount=0, valid=true）、保存成功。桌面 PIE 运行态复核：两组件运行时挂载到左右 Grip，网格/材质（MI\_Manny\_02）/骨架（SK\_MannequinsXR）均正确、bVisible=true。

- 工具处理：`BP_VRCharacter.uasset` 批量只读，初次 add\_component 触发保存失败；临时解除只读 → 写入 → 保存 → 恢复只读。期间编辑器崩溃一次，用户重启后复核蓝图已完整落盘（本次会话经真实运行核验配置完好，未重复操作）。

### 验证层级标注（规则 6/11）

- 站立修复与手部挂载均为**桌面 PIE** 结构验证；桌面无 XR 跟踪，截图看不到手，**不能作为可视化证据**。

- VR 预览 / PICO 真机待用户验证（TC-01\~TC-06）。

### Y 向落地偏移（已诊断，待用户决策）

- 角色落地后整体 Y 偏移 \~-31（PlayerStart Y=-90 → 实际 Y=-121.13），102s 稳定 velocity=0 → 判定为**有限固定偏移**（非持续滑动）。

- 已列 3 个处理方向待用户择（A: 先 VR 预览验证；B: 关虚拟底座 `bUseVirtualBase`；C: 补偿 PlayerStart）。

### 治理登记

- 已同步 `execution/reports/tasks/M01-T001.md`（2026-09-03 补充记录）、`CHANGELOG.md`（顶部新增 2026-09-03 条目）、`STATUS.json`（M01-T001 updatedAt→2026-09-03 + note 追加）。

***

## 追加：2026-09-04 手部对齐修复（PICO 官方核对 + 官方 VRPawn 补偿值）——执行记录

### 背景

- 用户反馈两问题：① 手部模型歪了，与手柄朝向不一致；② 抓握按键一点用也没有。

- 用户明确要求：手部对齐检索 PICO 官网指导争取一次性搞定；抓握连线研究 VRE 官方实现，不要闭门造车。

### PICO 官方核对（手部对齐方向）

- 检索 PICO 官方《搭建 XR 场景》确认核心原则：**手柄模型枢轴不一定在模型中心，添加模型后需调整其在父级 MotionController 下的相对位置，使模型处于父组件原点且朝向父组件正方向** [$TRAE\_REF](https://developer-cn.picoxr.com/document/unreal/create-an-xr-scene/)。

- 本项目手网格为 `SKM_MannyXR` 骨骼网格；精确补偿值以**官方** **`VRPawn`** **蓝图同套资产的手网格相对变换**为准（比官方文档的 StaticMesh 手柄模型更贴合本资产）。

### 修复实施（已确认根因并修改）

- 根因：`BP_VRCharacter` 的 `HandMeshLeft/Right` 挂到左右 Grip 控制器，但**相对位置/旋转全为 0** → 手网格的本地坐标系未补偿，与手柄朝向不一致。

- 改动：为 `HandMeshLeft` 设置相对位置 `(-2.98126, -3.5, 4.561753)`、相对旋转 `(pitch=-25, yaw=-179.999999, roll=89.999998)`；为 `HandMeshRight` 设置相对位置 `(-2.98126, 3.5, 4.561753)`、相对旋转 `(pitch=25, yaw=0, roll=89.999999)`。与官方 VRPawn 完全一致。

- 验证：编译成功（readback 确认属性已写入蓝图，`validate` errorCount=0, valid=true）；`save_dirty` 落盘（文件 82166→82618 B，时间戳 2026-09-04 20:29:02）；**桌面 PIE 运行态复核**：两组件运行时 `relativeLocation/relativeRotation` 均带补偿值、挂载到左右 Grip、网格/材质/骨架/bVisible 正确。

- 工具处理：`BP_VRCharacter.uasset` 批量只读 → 临时解除只读 → 写入 → 保存 → **恢复只读**（ReadOnly=True 已确认）。

- 编辑器进程此前已退出（UE\_NOT\_RUNNING），用桥 `start_editor` 重新启动后重连执行。

### 抓握连线研究结论（VRE 官方实现，未改动）

- 读 VRE 源码 `GripMotionControllerComponent.h/.cpp`：官方核心抓握是组件自带的 `GripObject`/`GripObjectByInterface`/`DropObject`/`DropObjectByInterface`（`BlueprintCallable`，含 socket 吸附），检测与吸附分离 [$TRAE\_REF](https://github.com/db-lyon)。

- 自研 `VRSanguoGripLibrary` 底层调用正是官方 `GripObject`/`DropObject`/`GetGrippedObjects`，**非闭门造车**，方向正确无需推翻。

- 事件图 `Get RightMotionController`/`LeftMotionController` 引用的是 `VRBaseCharacter` 原生暴露成员（`TObjectPtr<UGripMotionControllerComponent>`，行 520/523），变量有效。

- `DefaultInput.ini` 已设 `DefaultInputComponentClass=/Script/EnhancedInput.EnhancedInputComponent`；`IMC_Default` 运行时被应用（5 上下文在列），`PICONeo3_*_Grip_Axis` → `IA_Grab_*` 映射生效 → **输入链路通**。

- 待办（下一轮）：核对剑网格 `GrippableStaticMeshActor_0` 碰撞 profile 是否命中 `TryGrip` 的 `ECC_Visibility` 球体扫掠；若不命中则改检测通道或调大半径/距离，否则"按键无效"才能最终定论。

### 验证层级标注（规则 6/11）

- 手部对齐为**桌面 PIE** 结构与运行态验证；桌面无 XR 跟踪，截图看不到手，**不能作为可视化证据**。

- 手部朝向是否与手柄一致的**最终确认需用户 PICO 真机 VR 预览**（TC-01\~TC-06）。

### 治理登记

- 已同步本会话记录；下一步需同步 `execution/reports/tasks/M01-T001.md`、`CHANGELOG.md`、`STATUS.json`（M01-T001 note 追加 2026-09-04 手部对齐）。

***

## 追加：2026-09-04 MotionSource 参考系修正——执行记录

### 背景

- 手部对齐补偿值写入后，用户反馈"感觉角度还是差了一点点"（手部朝向仍未完全与手柄一致）。

### 根因（MotionSource 参考系不匹配）

- 对比官方 `VRPawn` 蓝图：其左右手网格挂载的 MotionController 的 `MotionSource` 为 **`LeftGrip`/`RightGrip`**（握把系）。

- 而 `BP_VRCharacter` 的 `Left/Right Grip Motion Controller` 默认 `MotionSource` 取 **`Left`/`Right`**（掌心系）。

- 参考系不同（握把系 vs 掌心系）→ 同一套补偿值下手部朝向仍差一点。

### 修复实施

- 将 `Left Grip Motion Controller` 的 `MotionSource` 由 `Left` 改为 `LeftGrip`。

- 将 `Right Grip Motion Controller` 的 `MotionSource` 由 `Right` 改为 `RightGrip`。

- 工具处理：`BP_VRCharacter.uasset` 批量只读 → 临时解除只读 → 写入 → 保存 → **恢复只读**（ReadOnly=True 已确认）。

### 验证（桌面 PIE 运行态）

- 编译成功；`validate`（errorCount=0, valid=true）。

- `save_dirty` 落盘：文件 82618→82740 B，时间戳 2026-09-04 21:07:42。

- **桌面 PIE 运行态复核**（`editor.get_runtime_values`）：`Left Grip Motion Controller.MotionSource=LeftGrip`、`Right Grip Motion Controller.MotionSource=RightGrip`；`HandMeshLeft` relativeLocation `(-2.98126,-3.5,4.561753)`、relativeRotation `(-25,-179.999999,89.999998)`；`HandMeshRight` relativeLocation `(-2.98126,3.5,4.561753)`、relativeRotation `(25,0,89.999999)` —— 与官方 VRPawn 完全一致，补偿值正确挂载。

### 验证层级标注（规则 6/11）

- MotionSource 修正为**桌面 PIE** 运行态验证（组件属性读回）；桌面无 XR 跟踪，截图看不到手。

- 手部朝向是否与手柄一致的**最终确认需用户 PICO 真机 VR 预览**（TC-01\~TC-06）。

### 治理登记

- 已同步本会话记录、`execution/reports/tasks/M01-T001.md`（2026-09-04 MotionSource 修正）、`CHANGELOG.md`（顶部新增 2026-09-04 MotionSource 条目）、`STATUS.json`（M01-T001 note 追加 2026-09-04 MotionSource）。

***

## 追加：2026-09-04 按键无效定论（全链路核查，未改动）——执行记录

### 背景

- 延续待办："按键无效"需核对剑网格碰撞是否命中 `TryGrip` 的 `ECC_Visibility` 球体扫掠。用户要求继续排查。

### 结论：配置链路无断点，碰撞非根因

- 面向检测/抓握链路逐环节核查，均通过：

| 环节           | 核查点                                                                                                                        | 结果     |
| ------------ | -------------------------------------------------------------------------------------------------------------------------- | ------ |
| 抓握接口         | `AGrippableStaticMeshActor` 实现 `IVRGripInterface`（`GrippableStaticMeshActor.h:47`）                                         | ✅ 通过   |
| 禁止抓取         | `DenyGripping` 返回 `VRGripInterfaceSettings.bDenyGripping`，运行态结构无该字段（默认 false）                                              | ✅ 未禁止  |
| 碰撞命中         | 剑 `StaticMeshComponent0` profile=`BlockAll`（QueryAndPhysics），Visibility=Block                                              | ✅ 可命中  |
| 检测参数         | 蓝图 `TryGrip`：`SphereRadius=12`/`ForwardDistance=30`/socket=VRGripP\_\_0（右）/VRGripS\_\_0（左）                                 | ✅ 已填   |
| 输入触发         | `IMC_Default` 运行态已应用（5 上下文）；`IA_Grab_*_Pressed` ValueType=Axis1D + `InputTriggerPressed`，Released 用 `InputTriggerReleased` | ✅ 配对正确 |
| 蓝图绑定         | 4 个 EI 事件节点绑定 `IA_Grab_*` 与 IMC 映射一致                                                                                       | ✅ 一致   |
| VRE 冲突       | `AVRBaseCharacter` 无 `SetupPlayerInputComponent`，VRE 不接管输入                                                                 | ✅ 无冲突  |
| MotionSource | `GripMotionControllerComponent.cpp:7421` 内部即以 `LeftGrip/RightGrip` 为 GripSource；PICO OpenXR 官方支持                           | ✅ 一致   |

### 关键新发现

- **项目为 PICO OpenXR 而非 SDK**：`DefaultEngine.ini` 含 `[/Script/PICOOpenXRRuntimeSettings]`，故 IMC 键名 `PICONeo3_*_Grip_Axis`（OpenXR 体系）正确。PICO SDK 版文档写的 `PICO Touch (L/R) Grip Axis` 属另一体系 [$TRAE\_REF](https://developer-cn.picoxr.com/document/unreal/input-mappings/)，不适用于本项目。

### 定论

- 桌面 PIE 无 XR 跟踪（手柄相对位置 `(0,0,0)`、前向固定 `(0,1,0)`），**"按键无效"无法在桌面复现，属真机特有问题**；碰撞/接口/输入/蓝图配置均已排除。

- 待办调整：真机若仍按键无效，走 PIE 运行态调 `VRSanguoGripLibrary.TryGrip`（传真实手柄）看日志分支，区分"输入未触发"与"检测未命中/吸附失败"；或在 `TryGrip` 补 `UE_LOG` 输出命中距离与 socket 名提高定位效率。

### 验证层级标注（规则 6/11）
- 本核查为桌面 PIE + VRE 源码 + PICO 官方文档交叉；真机按键是否恢复仍需用户 PICO 真机 VR 预览（TC-01~TC-06）。

---

## 追加：2026-09-04 TryGrip 诊断日志实施——执行记录

### 背景
- 用户反馈"里面按键操作不了剑"。此前配置链路已排查排除，根因锁定真机运行时。最终决定：在 `TryGrip` 补诊断日志，真机按一次即可定位所属分支。

### 实施（用户已确认）
- 在 `VRSanguoGripLibrary::TryGrip`（`Source/VRSanguoYanWuchang/Private/Combat/VRSanguoGripLibrary.cpp`）内将原 `Verbose` 分支全部提升为 `Log`（分类 `LogVRSanguoWeapon`），并新增 `[TryGrip诊断]` 前缀日志：
  - 每次按键固定输出：起点/前向/终点/半径/前距/socket。
  - 扫掠是否命中、命中数。
  - 每个命中 actor 是否可抓取（未实现接口或 DenyGripping 跳过）、可抓者距离。
  - 最终选定最近可抓取物。
  - `GripObject` 成功/失败（已有 Log/Warning）。

### 配套确认（排除 socket 雷区）
- 剑网格 `环首刀` 握持 socket 完好：`VRGripP__0` @(9,0,46)、`VRGripS__0` @(18,0,51)，socketCount=2（`asset.list_sockets`）。

### 编译验证（真实日志）
- 关闭编辑器 → 命令行 UBT：`Result: Succeeded`（exit 0），`VRSanguoGripLibrary.cpp` 被剔除 unity file 独立编译（确认改动生效），DLL 更新。
- 重开编辑器（桥 start_editor）→ `list_function_libraries` 确认 `VRSanguoGripLibrary.TryGrip` 反射注册。

### 验证层级标注（规则 6/11）
- 诊断日志已编译进 DLL（桌面可捕获 `[TryGrip诊断]`）。
- 真机按一次 Grip 需用户回传 `[TryGrip诊断]` 日志片段，据此区分「输入未触发 / 检测未命中 / 吸附失败」三根因分支。

### 治理登记
- 已同步本会话记录、`execution/reports/tasks/M01-T001.md`（2026-09-04 诊断日志实施）、`CHANGELOG.md`（顶部新增 2026-09-04 诊断日志条目）、`STATUS.json`（M01-T001 note 追加）。

## 追加：2026-09-05 按键无效根因诊断（仅只读，未改码）——执行记录

- **XR 会话检测**：头显（PICO Neo3 串流）连上后，XR 会话达 FOCUSED，双手柄均有真实追踪位姿；`GrippableStaticMeshActor` 剑在世界中。确认「手柄追踪正常、按键不触发」。
- **诊断日志**：`[TryGrip诊断]` 0 条、`Grab` 0 条 → 按键未触发 `TryGrip`（抓握逻辑未被调用）。
- **决定性报错**：`Ensure (Result)>=0 [OpenXRInput.cpp:556]` + `OpenXR call failed with result XR_ERROR_PATH_UNSUPPORTED`，失败点 `xrSuggestInteractionProfileBindings`（向运行时建议交互 profile 绑定被拒）。
- **运行时**：`PicoStreamingXR (Standalone) v1.1.46`（Saved/Logs 831 行）。知识库 `EngineReference.md:78` 记载「PC VR Preview 走 SteamVR OpenXR 运行时（PICO 串流助手）」，与当前运行时不一致——运行时漂移疑点。
- **IMC 键绑定**（asset.list_input_mappings）：`IMC_Default` 抓握 `IA_Grab_*_Pressed/Released` 仅绑 `PICONeo3_*_Grip_Axis`；`IMC_Weapon_Right` 的 `IA_Shoot_Right` 采用多键（ValveIndex/OculusTouch/Vive/MixedReality/PICONeo3）跨平台回退。
- **OpenXR Input 设置**：`DefaultInput.ini` 仅 `EnableDefaultMappingContexts=True` + 5 个默认 IMC，**无** `[/Script/OpenXRInput.*]` 交互 profile / PlayerMappableInputConfig 段（TD-007 尾巴，H2 属实）。
- **根因疑点**（两个证据指向）：
  - 主：PICO 插件 `PICO_Controller.cpp` `OnCreateSession`(L352) 无条件创建 battery action、`GetSuggestedBindings`(L519-535) 无条件为每个 PICO profile 加 `/user/hand/left|right/input/battery/value`；而 PicoStreamingXR 缺 `XR_EXT_interaction_profile_battery_state_display`（日志 818 行）→ pico 系列 profile 绑定被整体拒绝 → PICONeo3 键（grab/move/turn）全链路无绑定。
  - 次：`bEnableHandInteractionEXT=True`（DefaultEngine.ini:191）+ `IMC_Hands`，若运行时缺 `XR_EXT_hand_interaction` 同样报 PATH_UNSUPPORTED（WebKit 判此类报错常为“无害”）。
- **工具缺口**：ue-mcp 无「枚举某 actor 全部子组件」原生命令，用了 `execute_python`（PIE world 访问有坑）。已发信决策模型；决策裁定 v1.0.87 已有 `level.get_component_tree`（world=pie + includeProperties 可读 MotionSource），**不升级、不自研**。
- **状态**：已回执决策模型（msg-20260905-120348-exec-m01-t001-001，证据 + 请求终裁 A/B 根因与 F1 双绑定/修复电池门控方向）。**修复（改 IMC/Config）待决策模型终裁 + 用户确认，未动。** 验证层级：PC 串流 PIE ≠ 真机 standalone。

## 追加：2026-09-05 CR-20260905-001 实施（PICO 插件 battery 门控 + 关手部 EXT）

- **终裁与批准**：决策模型 msg-20260905-125928-decision-001（urgent）——根因实锤疑点 A（battery 无条件绑定 × PicoStreamingXR 缺 XR_EXT_interaction_profile_battery_state_display × 建议绑定原子性 → 整个 pico_neo3 profile 被拒）；疑点 B（hand EXT）为次生噪音顺手关闭。用户选定方案 1+3。变更单 `.trae/execution/CR-20260905-001-pico-battery-gating.md`（已读）。
- **认领续期**：STATUS.json M01-T001 claimedBy=session-20260830-001、updatedAt=2026-09-05、note 追加「2026-09-05 范围扩展 CR-20260905-001 实施中（PICO 插件 battery 门控 + 关手部 EXT）」，Python json 原子改写（JSON_OK 校验通过）。
- **实施范围**：`PICOOpenXRInput/Private/PICO_Controller.cpp`（battery 门控，最小集）+ `Config/DefaultEngine.ini` L191 `bEnableHandInteractionEXT` True→False；`/Game/VRTemplate/Input/*` 零改动。
- **验证层级**：PC 串流 PIE 复测 + 用户 PICO 真机 standalone 复测（分开标注，规则 13）。
- **实施回执**：已回执决策模型 msg-20260905-134723-exec-m01-t001-001（状态/证据路径/登记 ref 三要素），终裁派工信 msg-20260905-125928-decision-001 已归档（hub_check archive）。**③④（抓真剑实测）待用户重连头显串流后按握把键补证；真机 standalone 复测留用户。**
- **疑似回归上报**：用户反馈「改动前 VR 预览双手柄+手部模型正常，改动（方案1+3 实施+编译+编辑器重启）后双手柄不显示」。经排查：控制器位姿 0、桥接 PIE 无 FOCUSED；battery 门控仅作用绑定层、`bEnableHandInteractionEXT=False` 为功能空操作；判断与控制器追踪链路不相交，但用户归因于本次改动。已上报决策模型 msg-20260905-153452-exec-m01-t001-001（urgent，请裁定回退/隔离测试/串流侧排障）。决策模型回信 msg-20260905-135027-decision-001 已归档（抽查四项属实、门禁①②⑤通过、令进值班等③④）。

## 追加：2026-09-06 C-0 结案 + S1 零触实验 + B 路线查证（本轮核心）

### 一、C-0 结案：疑似回归排除，改动无罪
- 决策模型 msg-20260905-164029-decision-001 裁定：重连串流后 XR 会话 FOCUSED、手柄位姿非零 → 此前「双手柄不显示」是**死会话轮**（无 XR 会话）环境问题，与 CR-20260905-001 改动无关。CR 维持实施完成；V1→V2 误写已澄清（V2 = 无条件创建 battery action + 仅绑定门控，录为最终实现）。
- 方案 3（bEnableHandInteractionEXT=False）功能空操作，不回退。

### 二、抓握仍失败的根因深挖
- 用户重连后手柄可见可移动，但「抓握剑仍失败 + 手指做不出握拳（整手仅平移）」。
- 静态链路全部就绪：`IMC_Default` 挂载、`IA_Grab_Right_Pressed`→`PICONeo3_Right_Grip_Axis` 绑定存在、抓取目标 `GrippableStaticMeshActor_0` 可抓、`GripMotionController` 位姿非零。
- 手部模型为 UE MannequinsXR（`SKM_MannyXR`）挂 `ABP_MannequinsXR`，靠 `IA_Hand_Grasp_*`/`IA_Hand_IndexCurl_*` 等输入动作驱动手指；这些动作与抓握共用 **grip 键**。
- 手部动画蓝图机制：`IA_Hand_Grasp_Right`→grip 轴→驱动手指骨骼；抓握 `IA_Grab_Right_Pressed`→grip 轴→TryGrip。**两者共享 grip 键 action 绑定**。

### 三、S1 零触实验终判：S1-c 坐实（运行时不传任何按钮值）
- 决策模型要求 S1：按住右 TRIGGER≈2s + GRIP≈2s，判读 S1-a/b/c。
- 证据链（日志行号均已核）：
  1. 五张 IMC **全部挂载**（get_applied_imcs count=5：Default/Hands/Weapon_L/Weapon_R/Menu）→ 排除证据洞2（TRIGGER 分支无效）。
  2. `Log LogEnhancedInput Verbose` L1584-1586 确认落盘 `verbosity has been raised to Verbose` → 排除「类别不产日志」。
  3. 用户按键后 `EnhancedInput/IA_Shoot/IA_Grab/IA_Hand/Triggered/Completed/Started/ActionValue` 检索 **零条** → 按钮值未注入。
- **结论：仅位姿追踪正常（FOCUSED/GripMotionController 位姿非零），grip/trigger 的 action 值在 PicoStreamingXR 串流下完全未进入增强输入。** 与「手柄能移动、抓握/握拳均不生效」一致。S1-c 成立，B 路线授权。

### 四、B 路线查证（a/c 项）
- **PICO Connect 版本**：桌面端 exe=**10.6.6**，`picostreaming-openxr.dll`=1.1.46.0（与日志运行时 1.1.46 一致）；头显端同 10.6.6（指挥台确认）。
- **SteamVR 已装**：`D:\AGame\Steam\steamapps\common\SteamVR\`（steamxr_win64.json 存在，目录完整）。
- **OpenXR ActiveRuntime**：`HKLM\SOFTWARE\Khronos\OpenXR\1` → `D:\App\PicoConnected\PICO Connect\openvr_driver\resources\ps_xrt\picostreaming-openxr.json`；AvailableRuntimes 另含 SteamVR 的 steamxr_win64.json。
- **关键发现**：PICO Connect `openvr_driver` 目录**自带完整 SteamVR 驱动**（driver.vrdrivermanifest + driver_pico.dll + ps_steamvr_driver_sdk.dll），且 `resources/input` 下有**专供 PICO Neo3 的控制器 profile/绑定/渲染模型**（pico_neo3_controller_profile.json、pico_neo3_{left,right}_controller.json 等）。
- **推断**：PICO Neo3 串流输入 = 经 SteamVR 驱动的 bindings（pico_neo3 profile）映射注入。S1-c 根因很可能落在 **SteamVR 侧对 pico_neo3 控制器的按键绑定是否被识别/加载**，非项目代码。
- **官方文档**（fetch pico-connect-guide.bytedance.com/docs/help_center/3bwaisms）：手柄按键异常官方路径 = 进入 SteamVR → 控制器 → 管理控制器绑定 → 自定义。10.0 版本无自定义调节（邮箱反馈）。
- **Web 佐证**：10.1.5 新增 SteamVR 控制器调节功能（community.picoxr.com/sea/post/7379605261790724102）；PICO 4/Neo3 在 SteamVR 中手柄不可用为常见兼容问题。

### 五、工具缺口与崩溃记录（重要）
- **TD-010 命中**：`blueprint.add_node` 在 `BP_VRCharacter` 上两次触发 `UE_MCP_Bridge` game-thread executor 崩溃（GameThreadExecutor.cpp:106），编辑器退出（日志 13:29/13:32 `StaticShutdownAfterError`）。决策模型已登记 TD-010：**BP 节点编辑类桥操作暂停使用**，探针路线放弃，改用落盘 Verbose 日志判法。
- **execute_python 反射读取增强输入子系统多次受阻**（`get_local_player`/`player`/`get_game_instance` 均不在反射面、GameInstance 无 local player 方法、VRPlayerController 隐藏 player 属性）。已多次触发 workaround 反馈提醒。**建议向 ue-mcp 提交「读取运行时 Input Action 值」原生工具缺口反馈**（idealTool=增强输入子系统读 action 值）。

### 六、交接 / 当前状态（2026-09-06）
- **任务现状**：M01-T001 完整抓取链路已定位到「串流运行时按钮值不注入」，S1-c 坐实，B 路线授权。
- **已上报**：msg-20260906-134418-exec-m01-t001-001（S1-c 终判）、msg-20260906-135052-exec-m01-t001-001（a/c 项结果）、msg-20260906-135138-exec-m01-t001-001（c 项补充）、msg-20260906-135702-exec-m01-t001-001（a 项关键发现：PICO Connect 自带 SteamVR 驱动 + Neo3 绑定）。
- **已归档**：msg-20260905-164029-decision-001、msg-20260905-212327-decision-001、msg-20260906-135014-decision-001、msg-20260906-135627-decision-001。
- **挂起待办**：等待决策模型对下一步指示——a) 转向「SteamVR 内控制器绑定检查」（用户侧 UI 操作，零代码）；b) d 项切运行时对照试验（若需，出完整步骤清单+用户同意）；c) Streaming APK ≥v4.3.5 对 Neo3 消费版适用范围（待确认，不阻塞）。**方案 2 双绑定持续暂停**（S1-c 下无意义）。
- **值班位置**：当前在 `hub_await(role="exec-m01-t001")` 等回信。换会话后新执行模型应从 `inboxes/exec-m01-t001/` 继续 `hub_await`。
- **CR-20260905-001 门禁**：①②⑤ 已过；③④（抓真剑实测）因手柄不显示/按钮值不注入暂无法补，待 S1-c 解决后回补。真机 standalone 复测仍留给用户。



## 2026-09-10 深夜里程碑（握持校准收敛 + 全量落盘）

- **方向校准闭环**：GripDetail 日志实证 RelTrans = socket 精确逆变换（无隐藏覆盖）；真因是手柄坐标系假设错误（PICO 手柄轴接近竖直，手 X≈世界上）。socket 终值：VRGripP__0=(9,2.5,48) pitch-160、VRGripS__0=(18,-2.5,53) pitch-160。用户实测确认"刀方向不错"（刀身朝上举），剩拇指重叠/位置微调（部分待手指弯曲功能消化）。
- **双手并行路径查明**：GripObject 对已握对象走 GripActor L1689 bAllowMultipleGrips 静默拒绝路径；"already gripped" 警告实为同手连按的防重复保护（正确行为）。双手待明早实测。
- **全量落盘**：环首刀.uasset (23:48:26, 27384KB)、L_SkeletonTest.umap (23:48:36) 磁盘实证；save_all_dirty 后 dirtyCount=0。
- **UBT 完整编译成功**：Result: Succeeded (10 actions, 27s)，GripObject 参数(AttachmentGrip+bIsSlotGrip)+V2/GripDetail 日志进二进制，live coding 依赖清除。
- **信箱**：决策侧 10 封全读归档；回执 msg-20260910-235545（L34 归属=session-20260830-001 同批 TryGrip 初建；变更清单；冻结令知悉；V2 日志对"socket 不决定姿态"提示的实证修正；111152 选型信原文重投）。
- **明早顺序**：重开编辑器+桥探活 → 语义回读验证（socket/双握开关/GripPulse×2）→ 用户手感验收（拇指重叠微调+双手持刀实测）→ 收敛确认 → TC-01~06 取证（VR Preview 层级标注）→ CR-20260905-001 ③④ 回补 → 任务报告。重建冻结令维持至手感验收。
