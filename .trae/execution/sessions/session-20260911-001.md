# session-20260911-001 · 决策会话（指挥台）

- **会话身份**：决策模型 / 指挥台（captain）。本会话**不认领任何 MNN-TNNN 任务**（按角色边界，指挥台只裁定方案批准、范围变更、TC 设计、任务包设计、登记册维护、规则立法）。
- **时间窗**：2026-09-10 约 21:00 → 2026-09-11 约 01:00（含一次跨零点收口）。
- **授权依据**：用户 2026-09-10 深夜明示「你加油干我先睡了，我希望明天看到一个干净的项目目录，且你已经时刻了解本项目治理体系和目标了，并已经深度优化本项目治理体系、规则、skill 等，能够胜任我给你派活前的一切前置准备工作」→ 属根级 `AGENTS.md` 规则 20(c)「用户本次明确批准的变更」。**所有 `.trae/` 受控正文改动逐项列入 `execution/CR-20260911-001-governance-consistency-repair.md`，标注「待追认」。**

## 一、执行进度

1. **盘点与复核收口**（承接前序审计队）：接收 t1–t3 盘点、t5 复核、t9 方案、t11/t14/t16 三轮审核（末轮 `pass`）、t12 交接清单、t13/t15 两轮修复。
2. **治理正文一致性修正**（本会话主要产出）：产品范围冲突类 6 处、失效规划名类 5 处、TD-012 结案回填 8 处、推送与门禁口径类 12 处、结构性去重与路径合规类 4 处、门禁加固后新暴露的既存缺陷 2 处。全部逐项登记于 CR-20260911-001 §3.1–§3.6。
3. **门禁加固**（委派实现，指挥台验收）：`dashboard/check-integrity.py` 由 25 项 → **30 项**；severity 三态、稳定 `id`、per-check `try/except`、数据源不可读判 fail、五件套结构、STATUS 镜像、五方状态一致性、会话记录、`integrity.yaml` 一致性。
4. **两次提交与推送**：`cb7ab79`（治理修正 29 文件 + 门禁加固）、`d01c5ae7`（修复 CI 判红 + 治理正文同步 + 两份审计产物）。**两层核验全绿。**
5. **新登记**：TD-012 → `resolved`；**TD-017**、**TD-018** 新增（均 `deferred`）。

## 二、关键决策（本会话作出）

| # | 决策 | 依据 |
|---|---|---|
| D1 | 门禁加固**不得改名**原 25 项检查的名称与类目 | 看板 `dashboard/server.py` 按中文名取值，改名会静默失联 |
| D2 | 参考图 PNG（18 个 / ≈43 MB）**登记为 `deferred`，不记为 `open` 违规** | `open` 恰为 3、已达阈值上限；且属用户已备案例外 |
| D3 | **不改 `.gitattributes`**、**否决 `git lfs migrate` + 强推** | 改写历史风险远大于收益；阈值已固化进 `standards/10` |
| D4 | 白名单**通配项是权限范围**，不判存在性；**具体路径保留存在性判定** | 构建产物目录在干净检出里必然不存在；具体路径判定正是抓到真缺陷的检查 |
| D5 | **门禁绿必须在两套环境下成立**（本机 + 项目外干净浅克隆） | CI 判红事故的根因是验收标准不完整，责任在指挥台 |
| D6 | `M01-T007` 状态格用 `待生成` 而非散文 | 归一化词表是**优先级子串扫描**；且本表下方说明已自定「T007 属『已规划、任务包待生成』一组」 |
| D7 | `AssetConvention.md` 前缀表**单向合并进 `standards/02`** | 两表**互不为子集**，属分裂非重复；取标准为权威（规则 19） |
| D8 | **不重建 `E:\AWorkBackup`** | 其清空系用户裁定的执行结果（t9 P7），非异常灭失 |

## 三、与用户/其他会话的沟通

- **AgentHub**：收 `inboxes/decision/` 1 封（`exec-m01-t001` 三项欠项结清 + 今晚里程碑回执）；向 `exec-m01-t001` 回信 1 封（含技术结论更正、冻结令、口径更正）；向 `channels/registry-changes` **发广播 1 封**（rule 27，逐条列明共享文件新口径）。
- **采纳的技术更正**：`exec-m01-t001` 以实测推翻「socket 不决定握持姿态」——真因是**手柄坐标系假设错误**（PICO 手柄自然握持时手柄轴接近竖直）。**已登记为待更新 `TechnicalDecisions`，解冻后落盘。**
- **对 t11 的裁定**：接受其按契约字面的 `needs_revision`；判定 t10 验收条 #8（编辑器 pid 存活）**判据形态失当**，实质已满足；**拒绝**执行方第三次改写措辞。

## 四、阻塞与待办（**均需用户裁定，本会话未自行处置**）

1. **AFS `SecurityToken` 明文凭据**（**安全项，优先**）：`Config/DefaultEngine.ini:168`，真实 32 位十六进制值；`git log -S` 定位首次入库 = `98b2eb7`（2026-08-09 仓库初始化），**非本次引入**；`Plugins/` 与 git 树均无 AndroidFileServer、`DefaultGame.ini` 的 `IniSectionDenylist` 含对应节 ⇒ **当前不可利用的沉睡风险，一旦启用 AFS 即为有效凭据**。**已随推送进入远程。** 处置方案与四个选项（轮换 + 移出版本控制 / 仅轮换 / 关闭 AFS / **明确否决**改写历史+强推）见 `execution/CR-20260911-002-android-file-server-token.md`（**未实施，待裁定**）。**注意：门禁的「无密钥令牌泄漏」检查存在但模式覆盖不足，未命中该值 ⇒「门禁绿」不得被读作「无凭据泄漏」。**
2. **`open` 技术债已达阈值上限 3**（TD-005 / TD-006 / TD-013），**无余量**：新增债务一律 `deferred`。
3. **CR-20260911-001 全表待追认**（13 项遗留 + §3 全部修改项）。
4. **P1**：`.agent-teams/` 下 4 个**已跟踪**旧团队存档文件（`.gitignore` 忽略目录**不会**取消跟踪），`git rm --cached` 待用户逐项批准。
5. **`Saved/` 禁止清理**：`Saved/Autosaves/Game/VRSanguo/Dev/L_Prototype_1v1_v3_Auto6.umap`（450,808 B）是**老校场 v3 的唯一副本**；`Saved/Evidence/M01-T005`（43 文件）与 `M02-PREP-002`（4 文件）为**唯一副本、不在任何备份**。
6. **`D:\AWork\TraeAdmin\VRSanguoYanWuchang\` 本身无第二副本**：顶层 10 脚本（12 个 A 类文件全唯一）+ `tmp\` 2,613 文件（含事故取证与两套复现克隆），**既不在仓库也不在 E: 备份**。
7. 冻结项：**重建冻结仍有效**（含「120 m 场地口径待用户裁定」）；**重启窗口冻结前提已消失**（编辑器已于 2026-09-10 23:53:03 正常关闭）；`Docs/Scene/` 冻结仍有效。

## 五、恢复上下文

- **当前终态**：`HEAD = origin/master = ls-remote = d01c5ae71f8ff6f22a2b030d6b625385c139bbf5`；CI 两层全绿；工作区仅剩 `Docs/Scene/` 5 个 `??`（用户裁定排除项，`check-ignore --no-index` exit=1 ⇒ **不受忽略保护，任何 `git add -A` 都会带它进库**）。
- **唯一副本警示**：`E:\AWorkBackup\VRSanguoYanWuchang\` **已被清空** ⇒ **仓库 + origin 是唯一副本**，且**代理不可用时不可达**（`ls-remote`/push 需 `-c http.proxy=http://127.0.0.1:7897`）。**恢复演练必须把代理写成前置条件。**
- **门禁运行方式**：本机 Python **不在 PATH 上**，须绝对路径 + `PYTHONIOENCODING='utf-8'`，命令见 `governance/SessionCommands.md` §校验。**宣称绿之前必须在项目外干净浅克隆里复跑一次**（同节「本地绿 ≠ CI 绿」条）。
- **编辑器状态**：**已关闭**（23:53:03），无任何 UE 进程；`Content/Config/Source/Plugins` 与 HEAD 零差异；C++ 已由 UBT 完整编译（源码 22:31:52 → DLL 23:53:49）。另一会话 `session-20260830-001` 计划**明早重开编辑器并探活桥**。

---

## 六、2026-09-11 日间/夜间续（用户裁定回填，承 §四 §五）

> 上下文：编辑器已由执行会话按计划于 **08:30:16** 重开（用户 22:xx 关闭），期间 `L_SkeletonTest` 握持验收与 `L_Prototype_1v1_v5` v5.8d 迭代并行进行。本节只记**指挥台在本窗口内作出的处置与收到的裁定**。

### 6.1 用户裁定（逐条原文 + 处置）

| # | 用户原话 | 处置 |
|---|---|---|
| R1 | 「afs密钥不换了吧…你移到外面」＋「TraeAdmin 沿用吧那就，拿这个当后续大部分项目外的目录？」 | `CR-20260911-002` **已实施**：值移至 `D:\AWork\TraeAdmin\VRSanguoYanWuchang\keys\`，`Config/DefaultEngine.ini` 原行改空值 + 警示注释，**回读「明文残留 = False」**。**不轮换 ⇒ 该值仍有效且仍在公开历史中**（已在 CR §7 明确留档，不得淡化） |
| R2 | 「演武场现在120*120我是满意的，但尺寸还可能调整，不是最终决策」 | 唯一权威表述落于 `CR-20260911-001` §9.3：**现行 120×120（确认满意）· 尺寸待定，非最终**；三处旧值（TASK.md 20m×20m、LevelDesignSpec 70×70、session-20260808-001 70×70）回写待执行会话清单 |
| R3 | 「m01 t007可以撤销，编号后面留着给别的任务吧」 | `07-task-register.md` + `M01-CombatSlice.md` 状态格 → **`archived`**；编号保留给别的任务、**不重分配**；TD-011 真机帧率复验**须另立新编号** |
| R4 | 「Docs/Scene/ 5 文件是演武场的介绍文件…上git吧」 | 本轮随提交入库。**与 t2 审计判断冲突**（审计判「不应入库」：单源生成物 + 2.85 MB 非 LFS PNG）→ **以用户裁定为准**，审计意见作历史意见保留、不追改 |
| R5 | 「.agent-teams/ 4 个已跟踪文件按你说得来」 | 本轮 `git rm --cached`（只动索引） |
| R6 | 「saved/如果确认是历史脏数据就不要了，那个v3很烂的做的」 | 删 `Autosaves`/`Crashes`/`Logs`/`Temp`/`ShaderDebugInfo`/`MaterialStats`/`UBT`/`UE_MCP_Bridge`/`SourceControl` + 28 个 `*.tmp` ≈ **600 MB**。**保留 `Evidence`（唯一副本）与 `Shaders`（非脏数据）** |
| R7 | 「把写错的改成对的 这批都对」 | `CR-20260911-001` §3.2–§3.6 共 31 项**追认**（含 §3.1 A 类 6 项产品范围口径，用户未单独表态，按「恢复与已批准《产品总纲》一致」执行并在此记明） |
| R8 | 「E: 恢复演练我觉得没必要」 | **取消**，不执行。据此**「仓库 + origin 是唯一副本」的风险被用户明示接受**；本会话不再提议恢复演练 |
| R9 | 「TraeAdmin 沿用…不改了」 | AGENTS.md 的「NTFS 管理脚本仅项目外 `D:/AWork/TraeAdmin/VRSanguoYanWuchang/`」条**保持原文，无治理改动**；`keys\` 为其新增子目录 |
| R10 | 「目前灰盒竞技场哪个关卡是最新的你可以发信问执行模型」 | 已向 `inboxes/exec-m01-t001`、`inboxes/executor` 发 question（2026-09-11 12:51）。**回执已到**：最新 = `L_Prototype_1v1_v5.umap`（v5.8d，388 props 对账，`Docs/Scene` 五件同代）；`L_SkeletonTest` 是 M01-T001 的握持测试关、非竞技场 |

### 6.2 指挥台在本窗口的两处仪器失误（均自查发现、均无副作用）

1. **函数名撞 PowerShell 内置别名**：删除脚本把函数命名为 `Del`，而 `del` 是 `Remove-Item` 的**内置别名**，**别名解析优先级高于函数** ⇒ 首轮删除脚本整体抛 `PositionalParameterNotFound`，**只有不走该函数的 `.tmp` 段真正执行**。若当时不回读现场目录，就会据脚本输出误报「已删 600 MB」。**教训**：删除类脚本必须**回读现场**判定结果，不得以脚本自报为准。
2. **未察觉编辑器已被重开**：执行首轮 `Saved/` 删除时，另一会话已于 **08:30:16** 重开编辑器（证据：`Saved\Logs\VRSanguoYanWuchang.log` 被进程占用 → 独占打开失败）。发现后**当即停手**，`Shaders`/`Cooked`/`StagedBuilds`/`Config`/`Temp`/`Evidence`/`Screenshots` **一个未动**。**教训**：**任何目录级删除前必须先查进程占用**，「上次检查时它不在跑」不构成当前前提。

### 6.3 本节新增的未核实项

- `rebuild_v5.py` 的**单一事实源位置**未核实：`07-task-register.md:18` 记 `.trae/tools/scene/rebuild_v5.py`，而项目外 `D:\AWork\TraeAdmin\VRSanguoYanWuchang\rebuild_v5.py` 亦存在且当日被改写（50,614 → 57,059 B）。**若确为两份，即构成平行正文缺陷**（违规则 19）。已登入 `CR-20260911-001` §9.5。
- `Saved/Evidence/`（47 文件 / 43.8 MB）**未删**，仍为唯一副本、仍不在任何备份 —— 「仓库 + origin 是唯一副本」这一表述**对 `Saved/Evidence` 不成立**（它连仓库都不在），该风险**独立于 R8 的裁定**，用户尚未就此单独表态。

### 6.4 提交台账与闭环（2026-09-12 凌晨，承 6.2 的两处失误）

| # | SHA | 内容 | `治理校验 CI 门禁` |
|---|---|---|---|
| 1 | `4b89763` | 治理 13 条（密钥移出仓库 / M01-T007 撤销 / 演武场口径收口 / CR-001 追认回填 / `Docs/Scene` 入库 / `.agent-teams` 移出索引） | #36 **failure** |
| 2 | `d9fac2a` | 执行会话中间态资产快照 5 条（v5.8d 地图 + `BP_VRCharacter` + `环首刀` + 根 `STATUS.json` note + `session-20260908-002`；含 3 LFS ≈28.9 MB） | #37 **failure** |
| 3 | `a4b21d9` | 门禁项自指判红修复 + TD-018 判据收紧 + CHANGELOG 据实补记 | #38 **success** |

- **两次红灯的根因 = 门禁项自指**：`CHANGELOG 不滞后于提交` 以 **HEAD 自身提交日期**为输入，而提交一/二的 committer 时间为 `2026-09-12 00:00:33` / `00:00:56`（**跨本地零点**）⇒ **提交动作本身把门禁判红**，且提交前那一跑必然绿。**这是「本地绿 ≠ CI 绿」的自指分支。** 裁定见 `CR-20260911-001` §10：**判据不放宽、不加特例、门禁脚本未改**（封版三元组逐位相同），改以「跨零点提交前先确认 CHANGELOG 顶部日期 ≥ 当日」作为提交前检查项，写入 `governance/SessionCommands.md`。
- **命令层失误归属（指挥台）**：授权提交时正好是午夜后 33 秒，**未先核对日期**，该预检项本应由指挥台把关。
- **终态**：`HEAD` = `origin/master` = `ls-remote` = `a4b21d9aa04c2df5a19ac6856d68f65022cd20a4`；`porcelain -uall` = **0 条**；两个临时克隆（`tmp\ci-repro`、`tmp\ci-repro-3c`）已删并回读确认；`git lfs fsck` OK。
- **`L_Prototype_1v1_v5.umap` 守卫值**：旧 `673,326 B / 63AFECDF…` **作废** → 现行 **`730,370 B` / `SHA256 836E68C5C5140FB5BFB89D6F1DE276DF8F75BB631BAA5266ACD4D976A570239FB9` / `2026-09-11 23:27:31`**。
