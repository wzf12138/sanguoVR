# M00-T007 判据工具「核对执行器」

- 任务编号：M00-T007
- 里程碑：M00
- 优先级：P0（当日同族立法 18 例，为本项目最高频失效源）
- 前置任务：无（独立任务；挂钩阶段依赖本工具验收通过）
- 预估耗时：~4h（实现 ~3h + 自跑 13 条 TC ~1h）

## 背景与目标

2026-09-12 单日产生「**判据自身不可信**」同族立法 **18 例**，同日实测失误 **10 起**（执行侧 6、决策侧 5，含共同失误 1）。
根因**不是**「执行者不知道规矩」，而是「**判据每次都靠人手临时写脚本**」。
本任务把该族规矩**机械化**为一件工具，使 18 例在机制上不可能发生。

**唯一权威规格正文**：`.trae/execution/CR-20260912-001-judgement-tool.md`（用户 2026-09-12 批准）。
**本文件只做任务化拆解，不复制规格**（`AGENTS.md` 规则 19：一个主题只有一个权威正文）。

## 交付物

| # | 产物 | 路径 |
|---|---|---|
| 1 | 工具本体 | `.trae/tools/verify-claims.py` |
| 2 | 工具索引（新增一行） | `.trae/tools/README.md` |
| 3 | 实现与 TC 证据报告 | `.trae/execution/reports/tasks/M00-T007.md` |

**⚠ 明确不在本任务范围（由决策侧在工具验收通过后亲自执行）**：
- **唯一 Skill（锁定级）挂钩** —— 用户已专项批准修改，但**不进本任务白名单**；
- **`AGENTS.md` 挂钩**（受控文件）；
- **门禁接入**（`dashboard/check-integrity.py` 处于冻结，须另行批准）。

## 验收判据

**入线唯一判据 = 规格 §7 的 TC-01 ~ TC-13 全部通过**（**全绿方可挂钩；任一不绿 ⇒ 挂钩不做**）。
外加三条硬约束：
1. 门禁 `EXIT=0`，且**必须在本机项目根与项目外干净浅克隆两处都成立**（只在开发机跑绿 = 无效声明）；
2. 工具**只读** —— 被检查文件在工具跑前跑后**逐字节 SHA256 相同**；
3. **不得引入 Python 标准库与 `pyyaml` 之外的依赖**。

## 停止条件

- 规格存在自相矛盾或无法实现之处 ⇒ **停止并回报决策侧**（**不得自行改规格**）；
- 出现「无论如何都无法同时通过」的 TC 组合 ⇒ **停止并回报**（**不得修改 TC 使其通过**）；
- 需要修改白名单外任何文件（含锁定文件） ⇒ **停止并回报**。

## 回退

删除 `.trae/tools/verify-claims.py`，还原 `.trae/tools/README.md` 中新增的一行。
**不涉及任何工程资产，回退无副作用。**
