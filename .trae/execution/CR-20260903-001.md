# 集中式变更请求

## 1. 基本信息

- 请求编号：`CR-20260903-001`

- 状态：`已撤销`（用户 2026-09-03 决定暂不动总纲；联机方向暂以 M01-T001 决策记录与 CHANGELOG 为据，本 CR 草稿保留供将来复用）

- 请求人：用户（产品负责人，于 M01-T001 角色地基决策中提出联机方向）+ manager 代拟

- 请求日期：2026-09-03

- 审批人：用户

- 审批日期：

- 关联任务/里程碑：全局（影响 M02+ 架构决策口径；不改变任何一期里程碑）

- 关联决策：M01-T001 角色地基改判（决策信 msg-20260902-234354 / 初裁 msg-20260902-234814 / 改判 msg-20260902-235501）

## 2. 变更摘要

- 当前基线：`GameMasterPlan.md` 产品定义仅有「联网后置，不属于一期依赖或首发承诺」一句，未定义联网形态、长期方向及对架构决策的约束力。

- 拟议变更：在「产品定义」节之后新增「远期愿景（非一期承诺）」一节，明确：长期方向为在线竞技场 PvP 对战；一期范围/里程碑/完成定义不变；自批准起，涉及角色承载结构、战斗判定权威端、状态同步、会话管理的架构决策须评估并记录联机演进空间；联机立项时单列里程碑另走规划。

- 变更原因：联机是产品终点北极星。不落盘则后续所有 AI 决策（如 M01-T001 角色地基）无据可依——本次改判已是实例：决策模型初裁未计联机权重，用户纠正后才保住 VRE 复制型地基。

- 不变范围：一期全部范围、里程碑、完成定义、测试与性能目标；不引入任何联网开发工作或依赖。

- 预期收益：所有后续 AI 会话获得统一架构北极星，避免「现在省事、将来重建」的隐性技术债显性化、可登记、可偿还。

## 3. 影响分析

- 产品范围与玩家体验：一期体验零变化；仅增加远期方向声明。

- 架构、接口与数据：未来架构决策口径变化（需评估联机演进空间并记录），无即时接口/数据变化。

- 代码、配置、资产与插件：无。

- 测试、性能、VR 舒适度与设备：无直接影响；联机相关测试/性能单列于未来联机里程碑。

- 进度、成本与依赖：一期进度零影响。

- 治理正文、索引、登记册和 Skill：`GameMasterPlan.md` 新增一节（最小差异插入）；`index.md` 如列出总纲章节则同步；`CHANGELOG.md` 记录；无登记册条目新增（联机立项时才建）。

## 4. 执行边界

- 允许修改的绝对路径：`.trae/knowledge/GameMasterPlan.md`、`.trae/index.md`（仅当其引用总纲章节）、`.trae/CHANGELOG.md`、本 CR 文件状态字段。

- 禁止路径：其他一切工程与治理文件。

- 前置条件：用户批准本 CR。

- 强制停止条件：发现需要触碰批准范围外文件；批准后实施中发现措辞需实质性变更（回报用户）。

- 是否需要管理员权限/外部凭据/联网：否。

- 是否包含删除、迁移、批量重命名或格式升级：否（纯新增一节）。

## 5. 风险与回滚

- 已知风险：愿景被后续会话误读为一期承诺或排期 → 拟议措辞明确「非一期承诺、无排期、联机立项另走规划」；与产品定义「联网后置」句互为呼应不冲突。

- 风险负责人：manager（决策模型）。

- 回滚锚点：实施前 git 提交（当前工作区提交后）。

- 回滚步骤：`git revert` 该提交或按 diff 删除新增节。

- 回滚验证：回读 GameMasterPlan.md 确认结构恢复；check-integrity 通过。

## 6. 验证计划

- 静态检查：check-integrity.py 通过（治理一致性）。

- 自动化测试：不适用（纯知识层变更）。

- UE 编译/编辑器验证：不适用。

- PICO 真机验证：不适用。

- 文档逐份回读：GameMasterPlan.md（新节渲染、上下文衔接）、index.md（导航）、CHANGELOG.md。

- 唯一 Skill 检查：不适用（未创建 Skill）。

- 证据保存位置：本 CR 文件 §8 实施记录 + git 提交。

## 7. 审批

- 审批结论：`待审批`

- 审批意见：

- 批准的实施范围：

- 附加约束：

## 8. 实施与关闭

- 实施任务编号：

- 实施结果：

- 偏差：

- 验证结果：

- 更新的权威正文：

- 更新的索引/登记册/变更记录：

- 遗留风险与后续任务：

- 关闭人：

- 关闭日期：

***

## 附录：拟插入正文（批准后原样写入 GameMasterPlan.md「产品定义」节之后）

```markdown
## 远期愿景（非一期承诺）

一期单机内容完成并验证核心战斗品质后，产品的长期方向是扩展为在线竞技场对战：玩家以真人对真人的 VR 冷兵器格斗为核心卖点，在一期打磨成熟的战斗、武器、角色与竞技场系统之上接入联网对战。单机一期是这套系统的地基与商业化主体，联网是它的长期形态延伸。

该愿景是架构决策的北极星，不是排期承诺：

- 一期范围、里程碑与完成定义不因此调整；联网开发不出现在一期任何任务、依赖或验收项中。
- 自本愿景确认（2026-09）起，涉及角色承载结构、战斗判定权威端、状态同步与会话管理的架构决策，必须评估并记录其联机演进空间（先例：M01-T001 角色地基决策保留 VRE 复制型角色结构）。
- 联机立项时单列里程碑，覆盖网络同步方案、作弊防护、延迟补偿与竞技公平性设计，届时另走规划与变更流程。
```

