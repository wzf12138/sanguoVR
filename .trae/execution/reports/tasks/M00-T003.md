# M00-T003 UE5.8 工程与工具链基线 报告

- 状态：`approved`（用户验收批准，2026-08-09）
- 执行：执行模型
- 日期：2026-08-09
- 白名单约束：仅修改 `ALLOWLIST.txt` 所列路径；未修改 `.uproject`、`Source/`、`Content/`、`Config/`、`Plugins/` 任何 UE 工程文件。

## 验证结果

1. **工具链检查**：UE5.8 位于 `D:\AWork\Unreal\App\UE_5.8`（注册表确认），`RunUAT.bat`、`Build.bat`、`UnrealBuildTool.dll` 存在；VS2026 位于 `D:\AWork\Visual_Studio_2026\vs2026`，MSVC 14.50.35717/14.51.36231、`vcvars64.bat` 存在；Windows SDK `10.0.22621.0`/`10.0.26100.0` 位于 `C:\Program Files (x86)\Windows Kits\10`。
2. **`.uproject` 核对**：OpenXR、PICOController（Win64/Linux/Android）、OpenXREyeTracker、OpenXRHandTracking、ModelContextProtocol、AllToolsets 六插件均启用；TargetPlatforms 含 Android，符合 M00-Foundation 要求。
3. **清理编译**：`Rebuild.bat VRSanguoYanWuchangEditor Win64 Development` 执行成功（用户本机 PowerShell）。`Result: Succeeded`，总耗时 33.35 秒（UBA 本地执行器 30.29 秒，10 个动作）。源码含 `VRSanguoYanWuchang.cpp`、`VRSanguoBootstrapComponent.cpp`、`MyClass.cpp`，输出二进制 `D:\AWork\Unreal\App\UE_5.8\Engine\Binaries\Win64\UnrealEditor.exe`。（注：2026-08-10 已按 UE 命名规范将 `AVRSanguoBootstrapComponent` 更名为 `AVRSanguoBootstrapActor`，并删除模板残留 `MyClass`；历史编译记录保留原文。）
4. **工具链实际选择**：UBT 选用 MSVC 14.50.35717（VS 14.50.35737）与 Windows SDK 10.0.22621.0（稳定工具集，非 14.51/10.0.26100 预览组合）；UE5.8 已无 `GenerateProjectFiles.bat`，UBT 构建时自动生成 makefile。
5. **编辑器启动验证**：用户确认编辑器进入主界面，无错误弹窗，输出日志无报错；编译前 MCP 会话 `LogLoad` 无 error/fail 条目，`LogTemp` 仅引擎自带 UnifiedErrorTest 测试条目。
6. **MCP 连接器验证**：编译前会话 `list_toolsets` 返回约 40 个工具集，`GetLogCategories`/`GetLogEntries` 调用成功；结果已记录于 `knowledge/TechnicalDecisions.md`。

## 遗留项与风险

- **MCP 桥接重启后未注册工具集**：编辑器重启后 `list_toolsets` 返回空列表（已重试 3 次），可能与 MCP 服务器连接方式有关，需用户在编辑器内确认 MCP 服务器状态或重新连接；不影响本任务已完成的连接器可用性验证。
- **vswhere 未识别 VS2026 实例**：标准位置执行返回空列表，注册信息待落盘；UBT 已能通过其他路径发现 VS2026 实例（编译成功佐证）。
- **Android SDK/NDK/JDK 识别**：未在 UBT 中验证，保持待验证（任务范围外，M00 后续任务处理）。
- **PICO Neo3 真机**：未验证（M00-T004 范围）。

## 证据

- 用户本机 PowerShell 输出：`Rebuild.bat` 完整编译日志（Toolchain Information、10 个动作、`Result: Succeeded`、`Total execution time: 33.35 seconds`）。
- MCP 输出：编译前会话 `list_toolsets` 工具集列表与 `GetLogEntries` 日志条目。
- 写入：`knowledge/EnvironmentSetup.md`、`knowledge/TechnicalDecisions.md`、`registers/09-verification-register.md`（V-003）。

## 恢复条件（遗留项解除）

1. MCP 桥接：用户重新连接或重启 MCP 服务器后，`list_toolsets` 应恢复工具集列表。
2. vswhere：系统重启后注册信息落盘可恢复识别。
