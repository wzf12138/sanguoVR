# M02-PREP-002 编辑器自动化工具链迁移：db-lyon/ue-mcp 替换 UEBridgeMCP 报告

- 状态：`awaiting_review`（迁移完成，待用户审核）
- 执行方：AI（`session-20260831-001`，经 vrsanguo-relay 派工；manager 授权续命 msg-20260831-100234-manager-001）
- 日期：2026-08-31
- 白名单约束：仅修改 `ALLOWLIST.txt` 所列路径；未修改 M01-T001 白名单资产（Content/VRSanguo/VR/**、BP_VRCharacter 等）、未改治理锁定文件。

## 结论摘要

- **迁移完成，TC-01/02/03 全部通过**。编辑器自动化工具链已从 UEBridgeMCP（GPL-3.0，蓝图图写入崩溃，TD-010）切换至 **db-lyon/ue-mcp v1.0.87**（MIT，桥端口 9877），AI 可稳定执行蓝图图编辑（写入+读回验证闭环）。TD-010 已偿还（GPL-3.0 传染风险随退役消除）。
- 过程：v1.3.0（npm 最新）在 UE5.6 系统性编译失败（61 错误）→ blocked；manager 授权续命 → 2a 版本横向探测（静态探针，不浪费编译）→ v1.0.87 + 1 行 5.6 版本门控补丁 → 编译通过 → 全链路验证。

## 版本选型依据（2a 探测记录）

| 候选 | 13 个 5.6 缺失符号命中 | 判定 |
|---|---|---|
| v1.3.0 | 全命中（61 编译错误实证） | 首轮失败（blocked） |
| v1.3.1-beta.2 | 5.8-only API 已门控，但 5.7 引入的 API 仍无门控（`Misc/StringOutputDevice.h`、`EGetObjectsFlags`×4、`GetNaniteSettings`×8、MetaSound `Connect*`、Landscape `GetPhysicalMaterial/SetHardness`；`WITH_FAB_PLUGIN` 在 5.6 有模块但头文件布局不同） | 静态判定仍失败，跳过编译 |
| v1.2.4 / v1.1.44 | 18 命中 | 跳过 |
| **v1.0.87**（选定） | **仅 2 命中（`FPoseSearchDatabaseAnimationAsset`）且已有 `#if UE_MCP_HAS_POSESEARCH_DATABASE_ASSET_API` 门控；5.6 实存 `FPoseSearchDatabaseAnimationAssetBase`（Plugins/Animation/PoseSearch）** | 编译实证 |
| v1.0.0 / v0.7.19 | 同 v1.0.87 | 备选未用 |

注：db-lyon 官方 README/release note 宣称 5.4-5.8 兼容，但 release note（v1.3.1-beta.2）自述"5.4-5.6 remain gated but unbuilt"——与本项目编译实证一致：声明不可作为通过依据（符合 TASK.md 纪律）。

## 测试用例结果

| 编号 | 描述 | 结果 | 证据（`Saved/Evidence/M02-PREP-002/`） |
|---|---|---|---|
| TC-01 | 部署 + UBT 编译 Result: Succeeded | **PASS** | `UBT-Rebuild-1.0.87-first-attempt.log`（全量 Rebuild，仅 1 错误）+ `UBT-Build-1.0.87-fixed-ResultSucceeded.log`（1 行补丁后 `[1/4] Compile AssetHandlers_Import.cpp` → `[3/4] Link UnrealEditor-UE_MCP_Bridge.dll` → `Result: Succeeded`，DLL 4313088B） |
| TC-02 | 测试副本蓝图 add_node→connect_pins→compile→query 读回一致 | **PASS** | `TC02-create-addnode.json`（两节点创建成功，返回 nodeId 与引脚）+ `TC02-connect-compile-readback.json`（connect success；compile success；**read_graph_summary 的 execEdges 与写入完全一致**：McpTestEvent.then → 打印字符串.execute）+ `TC02-bp-editor-window.png`（编辑器中测试蓝图打开截图） |
| TC-03 | UEBridgeMCP 退役后重启无缺失模块 | **PASS** | `Saved/Logs/VRSanguoYanWuchang.log`（重启于 22:46，Startup complete 9.15s，零缺失模块记录；2 条 `LogAutomationTest: Error: Condition failed` 经退役前 backup 日志比对为既有现象） |
| 冒烟 | 打开 L_SkeletonTest | **PASS** | `M02P2_SMOKE_LES=True`、`M02P2_SMOKE_CURRENT_LEVEL=L_SkeletonTest`（Python 经桥执行） |

验证层级标注：全部为**编辑器级验证**（非 PIE、非 PICO 真机）。

## 执行过程

1. **前置**：Node v22.16.0/npx 10.9.4 可用；编辑器关闭；UE5.6=`D:\AWork\Unreal\App\UE_5.6`。
2. **认领**：根+任务包 STATUS 认领（session-20260831-001），会话记录同步建立。
3. **首轮（v1.3.0，blocked）**：非交互 `deploy-cli.js` 部署成功 → 全量 Rebuild `Result: Failed (OtherCompilationError)` 61 错误（缺失头文件 + 5.6 已移除/更名 API，跨 15+ Handler）→ 按停止条件 blocked，回退 `.uproject`、清理失效插件目录。证据 `m02-build.log`（临时区）。
4. **续命重试（manager 授权）**：
   - 2a 静态探针：GitHub releases API 核实（v1.3.1-beta.2 引擎门控范围）→ 横向 npm 安装 6 版本 grep 13 符号（见上表）。
   - v1.0.87 部署（`deploy-cli.js`，`.uproject` 启用 PythonScriptPlugin+UE_MCP_Bridge）→ 全量 Rebuild 1 错误 → 1 行补丁（`#if WITH_EDITORONLY_DATA && (MAJOR>5 || MINOR>=7)` 包住 3 参 `SetSourceString`）→ 增量 Build Succeeded。
5. **TC-02**：MCP stdio 驱动（`mcp-driver.mjs`：initialize → tools/call）→ create/add_node×2/connect_pins/compile/read_graph_summary/save → 读回一致 → 关闭蓝图编辑器标签 → `asset.delete` 测试副本（磁盘无残留）。
6. **TC-03**：Python `quit_editor()` 优雅退出 → `Plugins/UEBridgeMCP` 改名 `UEBridgeMCP.disabled` + `.uproject` 移除条目 → 重启编辑器 → 无缺失模块 → 冒烟加载 L_SkeletonTest。**（2026-09-03 审核修订：改名后的 .disabled 目录未纳入版本控制、在后续清理中丢失；经用户裁决按"彻底删除"定案，回退路径=重新克隆上游+重打补丁）**
7. **治理同步**：见下节。

## 变更文件

| 文件 | 变更 |
|---|---|
| `Plugins/UE_MCP_Bridge/` | 新增（db-lyon/ue-mcp v1.0.87 桥插件源码 + 1 行 5.6 门控补丁；编译产物 Binaries） |
| `Plugins/UEBridgeMCP`（gitlink 移除） | 退役删除（2026-09-03 审核修订：原"改名 .disabled 保留审计"与磁盘不符，按用户裁决定案为彻底删除） |
| `VRSanguoYanWuchang.uproject` | 移除 UEBridgeMCP 条目；新增 PythonScriptPlugin + UE_MCP_Bridge |
| `.trae/execution/active/STATUS.json`、任务包 STATUS.json | awaiting_review + 完成记录 |
| `.trae/registers/11-tech-debt-register.md` | TD-010 → resolved（偿还记录 + 维护注） |
| `.trae/registers/07-task-register.md` | M02-PREP-002 行更新 |
| `.trae/CHANGELOG.md` | 完成条目 |
| `.trae/integrity.yaml`、`.trae/manifest.yaml` | active 任务状态镜像同步 |
| 本报告 + 报告索引 + 会话记录 `execution/sessions/session-20260831-001.md` | 更新 |

## 维护注与遗留

1. **本地补丁维护**：`Plugins/UE_MCP_Bridge/Source/UE_MCP_Bridge/Private/Handlers/AssetHandlers_Import.cpp` 含 1 行 5.6 门控补丁；未来执行 `ue-mcp deploy`/`ue-mcp update` 会以 npm 包源覆盖插件目录（deployer 镜像式同步），需重打补丁或向上游提交 PR。
2. **MCP 客户端注册**：当前会话通过临时 Node 驱动脚本直连（stdio）。若需 TraeWork 常驻接入新桥，按 `mcp-client-config` 注册 `node <包路径>/dist/index.js <uproject>`；桥端口 9877（旧 ue-bridge 为 8080，已随退役失效）。包持久化位置已固化 `D:\App\trae\ue-mcp\1.0.87\`（2026-08-31，node 经 8.3 短路径调用，见根级 `AGENTS.md` 规则 28）。
3. **M01-T001 解锁**：蓝图接线（4 个 EI 事件 → TryGrip/ReleaseGrip）的工具阻塞已解除——新桥具备写+读回闭环（本次 TC-02 实证），且避开源驱动引脚节点即可（本项目用普通函数调用节点）。待 M01-T001 会话推进。
4. 编辑器当前保持运行（冒烟后未关闭），处于 VRTemplateMap→L_SkeletonTest 状态，供用户复核。

## 关联

- 任务包：`.trae/execution/active/M02-PREP-002/`（TASK.md 重试计划节）
- 调研依据：RESEARCH-UEMCP（`inboxes/manager/archive/msg-20260831-092156-res-001.md`）
- manager 授权：`inboxes/executor/archive/msg-20260831-100234-manager-001.md`
- 技术债：TD-010（resolved）；TD-013（M01-T001 相关，open）
- 证据目录：`Saved/Evidence/M02-PREP-002/`（Saved/ 不入 git，本机留档）
