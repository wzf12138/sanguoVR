# t12 输入登记 · t5 复核更正（needs_revision 吸收件）

- 性质：**t12（交接清单）的输入材料 / 更正登记**，不是 t12 本体，也不是 t9 的修订。
- 作者：AgentTeams `vrsanguo-audit-backup` / 成员 planner
- 写盘时点：2026-09-10（本机本地时间）
- 来源：指挥台转达的 **t5 独立复核结论（verdict = needs_revision）** 共 12 条；本文件对其中 **可复核项做了本方独立复测**，并**逐条标注来源**。
- 纪律：**转述 ≠ 本方复现**，全部结论**按来源分层登记**。下表「本方复测」列凡写「未复测」者，本文件**只登记指挥台/verifier 的结论，不宣称已由本方验证**；凡写「本方独立复测」者附命令与实测值，可复算。
- 影响范围：t12 交接清单必须吸收本文件；t9 已终态，**按指挥台指示不改 t9**（但 §3 列出因而不一致的 3 个单元格，供裁定）。

---

## 1. 十二条更正逐条落地

| # | t5 更正内容 | 本方复测 | 对本方产物的影响 |
|---|---|---|---|
| 1 | **验收层口径**：`SessionCommands.md:120` 要求两 workflow 全绿才算「推送完成」→ **基线若带红项推送，交付状态必须记为「未完成 / 开口」**；t11 与 t12 都不得写「完成」。指挥台先前的「`exit≠0` 不阻止推送」**只在「备份是否到手」层成立，验收层不成立** | 未复测（口径裁定） | t9 §4.7 只写了「绝不写通过」，**未写交付状态记「未完成/开口」** → 该口径由 **t12 承载**，并在 t12 明确分两层：**备份层（对象是否到手）** vs **验收层（CI 是否全绿）** |
| 2 | **M01-T005 升级为用户待裁定项**，且**卡住收口**。选项：**A** 不动（→ 交付留开口）；**B** 只把非法值回退为版本库已有值 `in_progress`、保留完成说明文字（→ 体检全绿、一次收口）。**指挥台放行前不做任何修改** | 未复测（裁定） | t9 §9 D1/D2 与 §4.7 已登记「本次不修改、不清零」✓ **一致**；差异点：t9 未写「A/B 选项」与「卡住收口」→ t12 补齐并**按裁定结果写** |
| 3 | **t2 §8 降级**：其「本机无 Python → 25 项未执行」前提**不成立**（本机有 3.10.11 + pyyaml）；**不得引用 t2 静态复算作为实证** | 本方已独立复核解释器：`D:\App\trae\Traedata\TRAE SOLO CN\ModularData\ai-agent\vm\tools\python\python.exe` → `exists=True`、`--version` = **Python 3.10.11** | t12 事实口径 = 「**指挥台实跑 + verifier 独立复现**」；**t2 §8 的密钥/路径扫描只能作为「静态复算」引用，不得当实证** |
| 4 | **F-2 · E: 现场数**：t3 §5「两处」与自身 §6.1 第三处矛盾 → 核实为**三处 + 一处外围**：`E:\AWorkBackup\VRSanguoYanWuchang`(1929 文件/631.0 MB)、`E:\AWork\Temp\VRSanguoRef`(1.77 GB)、`E:\AWork\Temp\VRExpansionPlugin`(2.95 GB) + 外围 `E:\AWork\VRExpansionPlugin`(自带 `.git`)。**「E: 上无有效完整备份、本次推送是项目首个完整备份」仍成立**，但**事实源模型须按三处重述** | 未复测（沿用 t1/t3 实测） | t9 §6 已按「三处现场（E:两片 + TraeAdmin）」写，且已含 `E:\AWork\VRExpansionPlugin` ✓ **方向一致**；t12 的事实源模型按「**三处 + 一处外围**」重述 |
| 5 | **F-3 · 受管图片**：正确为 **48 个 / 47.45 MB**（png 39 / 46.64 + jpg 8 / 0.85 + webp 1 / 0.01），**≥1 MB = 18 个 / 44.19 MB**。t1 摘要「25 个/49 MB」与其正文矛盾（**正文对**）；t3「39 个 PNG/JPG 46.6 MB」**漏计 jpg 与 webp** → 下游用错会**低估 LFS 缺口** | **本方独立复测（一致）**：`git ls-files *.png *.jpg *.webp` + 工作区尺寸 = **48 个 / 49,760,737 B = 47.46 MB**（png 39 / 48,906,799 = 46.64 MB；jpg 8 / 845,346 = 0.81 MB；webp 1 / 8,592 = 0.01 MB）；**≥1 MB：18 个 / 46,337,015 B = 44.19 MB**（与指挥台完全一致；总 MB 的 0.01 差为四舍五入） | t9 §5.2 与 D31 只写了 `Art/References/` 的局部口径（33 图片 / 47.35 MiB），**未写仓库级 48 个** → 由 t12 承载正确的仓库级数字 |
| 6 | **F-4 · 重复计数**：示例工程副本实为 **3 个（其中 1 个嵌套在另一个之内）+ 1 个 zip**，毛额 **2,947.3 MB** 中**含 1,020.6 MB 重复计数** → 可回收空间**必须扣除**，否则虚高 | **本方独立复测（一致）**：`E:\AWork\Temp\VRExpansionPlugin\` = `Extracted\` 842 文件 / **1,070,222,448 B（1020.6 MB）** + `VRExpPluginExample-5.6\` 842 文件 / **1,070,222,448 B（1020.6 MB，与前者字节数相同）** + `VRExpPluginExample-5.6.zip` **950,016,925 B（906 MB）** = 毛额 **3,090,461,821 B = 2,947.3 MB**；嵌套副本实测存在（`Extracted\VRExpPluginExample-5.6-Locked\` 与 `VRExpPluginExample-5.6\VRExpPluginExample-5.6-Locked\`）→ **净可回收（留 1 份目录、去掉第二份目录 + zip）= 2,020,239,373 B = 1,926.7 MB** | **t9 §6.2 写的「回收 ≈2.95 GB」是虚高**（见 §3）→ t12 必须写 **1,926.7 MB** |
| 7 | **F-6**：`CR-20260824-001` **被 5 处引用却无对应文件**（孤儿引用，建议并入 A 类）；另 t2 称「`M00-T004` claimedBy 格式异类」**措辞不符事实**（实测为**空字符串**） | 未复测 | t9 §2.5 只列了「6 个缺失会话记录」，**未列 CR 孤儿引用** → t12 补入 A 类；`M00-T004` 措辞更正由 t12 承载 |
| 8 | **F-8**：`openbrf.zip` 实为 **11.34 MB**（t3 记 39 MB，**高估 3.4 倍**） | **本方独立复测（一致）**：`E:\AWork\Tools\OpenBRF\openbrf.zip` = **11,890,974 B = 11.34 MB** | **t9 §6.2 与 P9 写的「39.0 MB」是错的**（见 §3）→ t12 写 11.34 MB |
| 9 | **F-9 · 证据纪律**：报告在交付后被改写（t1 43,075 → 48,571 → **56,275 B**；t9 63,100 → 71,947 B）→ **所有下游引用必须绑定「文件名 + mtime」**，不得只写章节号 | **本方复测并扩展**：见 §4 绑定表；并**新发现同类第三条** —— `t2-governance-sessions.md` 由 **42,283 B（本方案读取时）→ 48,957 B（22:52:23）**，同样是交付后改写 | t12 引用任何报告一律用「**文件名 + 字节数 + mtime**」，并在 t12 内附绑定表 |
| 10 | **F-10**：治理正文存在未提交改动（`ExecutionModel.md` **+1,079 B**、`task-template-level.md` **+1,730 B**、`task-template.md` **+1,796 B**），会随基线一并固化。**来源注明：两份 `task-template` 的 §0.5「用户确认门禁」是指挥台本人此前所加** → **不得写成「盘点期间被他人改动」** | **本方独立复测（方向一致）**：`git diff --numstat` = `task-template-level.md` **+19 行 / -0**、`task-template.md` **+19 行 / -0**、`ExecutionModel.md` **+9 行 / -0**（指挥台给的是字节数，本文件给行数，二者互不矛盾） | **t12 必须写来源归属**（指挥台本人在其基线提交前所加）；三份文件**均已在本方案 §4.2 A 的 26 条已跟踪修改内** ✓ → **不影响提交集合** |
| 11 | 红线确认：`.agent-teams/`、`.dsh-uploads/`、`Docs/` 三处 `check-ignore` **全部 NO HIT** → **必须显式路径提交** | 本方独立复核（同一结论）：三者均出现在 `git status --porcelain -uall` 的 `??` 行中（未被忽略） | t9 §0.2 / §4.3 / §4.4 已按此写 ✓ **一致**，无需改动 |
| 12 | **推送仍在 HOLD**，等用户 **6 项**裁定；**t12 交接清单须含这 6 项最终结果** | — | t9 §10 已写「5 项」→ **数字更新为 6 项**（t9 写的是 5，属写入时点的差异，见 §3） |

---

## 2. 对 t10 可执行清单的影响：**路径集合不变，仍为 43**

t5 更正全部落在**分析数字**上，**不改动提交集合**。以本文件写盘时刻重新对账（`git status --porcelain -uall` 实测）：

| 项 | t9 记录时点 | 本文件写盘时点 | 差异原因 |
|---|---|---|---|
| 已跟踪修改 ` M` | 26 | **26** | 无变化 |
| 记录删除 ` D` | 2 | **2** | 无变化 |
| 未跟踪 `??` | 31 | **34** | +2 = 审计目录新增 `t5-verification.md`、`verifier-memo-1.md`；+1 = 新增队友信箱 `scout-governance.jsonl` |
| **合计脏项** | 59 | **62** | — |
| 其中 E 类（排除） | 9 | **10** | `.agent-teams/` 由 6 → **7**（6 个 inbox jsonl + `team.json`），`.dsh-uploads/` 3 |
| 其中 `Docs/`（本次排除） | 5 | **5** | — |
| 其中待包含的未跟踪文件 | 19 | **21** | +2（audit 目录内） |
| **暂存路径数** | **43** | **43** | **审计目录按「1 条目录路径」计数 → 内部新增文件不改变路径数** |
| 其中未跟踪**路径** | 14 | **14** | 5（`.trae/execution` 顶层）+ **1**（`reports/audit/` 目录）+ 4（sessions）+ 4（`Content/VRSanguo/Dev`） |

> **复核结论**：`26 + 2 + 14 + 1 = 43` 在 t5 复核后**仍然成立** —— 这正是指挥台 Q2 裁定的意义：**集合由路径清单定义，计数只作对账**。t10 提交前须重新跑一次并**逐条比对路径**，不要拿 43 或 62 当断言。

---

## 3. t9 三个错误数字 —— **已按裁定 (i) 就地勘误（本项已闭合）**

按指挥台指示未改 t9。但以下 3 处**已证实为错**的数字会**随基线一并被提交进仓库**，故必须让用户/指挥台看到：

| t9 位置 | 现写 | 正确 | 性质 |
|---|---|---|---|
| §4.5 规则23 自查 · 历史遗留图片 | 「39 个 PNG/JPG 46.6 MB」 | **48 个 / 47.46 MB**（png 39 + jpg 8 + webp 1）；≥1 MB 18 个 / 44.19 MB | 引用 t3 的**漏计口径**（漏 jpg 与 webp）→ 会**低估 LFS 缺口** |
| §6.2 + §3 P9 · `openbrf.zip` | 「39.0 MB」 | **11.34 MB** | 引用 t3 的**高估 3.4 倍** |
| §6.2 + §3 P8 · VRExpansionPlugin 去重收益 | 「回收 **≈2.95 GB**」 | **1,926.7 MB ≈ 1.88 GB**（毛额 2,947.3 MB 含 1,020.6 MB 重复计数） | **可回收空间虚高**，直接影响用户对收益的判断 |

**注**：另有 2 处**非错误、仅时点差异**：t9 写「5 项用户裁定」→ 现为 **6 项**；t9 写脏项 59 → 现为 62（t9 已自带「计数只作对账、t10 重钉」的免责说明，故不算错误）。

**裁定结果（2026-09-10，指挥台）**：**选 (i) 就地勘误** —— 让已知为假的数字进版本库与 AGENTS.md 禁止虚构（规则 1-6）相悖，代价高于改单元格；**并撤销此前「t9 已终态、不用改 t9」的指令**。

**实际执行**：**不止 3 个单元格 —— 同一事实共出现 10 处，已全部改正**（避免文档内部两套口径）：§2.6（3 行）、§3 P8/P9/**P15**、§4.5、§6.2（2 行）、§9 D12；另在 t9 修订表新增 **v6 行**（写明新旧值 + 依据 = planner 独立复测 F-3/F-4/F-8）。**t9 不再由 t12 出更正表**（同一事实不出两套口径）。本期 2 处**时点差异**（5 项→6 项、脏项 59→63）**不算错误、按裁定不改**。

> **勘误自查方法（可复算）**：对旧值做**全串扫描**而非定点替换 —— 只改指挥台点名的 3 处会漏掉其余 7 处（其中 §3 P15 写作「39 个**已有** PNG/JPG」，定点模式匹配不到）。

> **t12 落点**：§1 的 12 条更正里，F-3 / F-4 / F-8 三项**已在 t9 就地闭合**，t12 只需**引用 v6 勘误结论**，不重复列表。

---

## 4. 证据绑定表（F-9 纪律：文件名 + 字节数 + mtime）

写盘时刻实测（`.trae/execution/reports/audit/`）：

> **⚠ 绑定值 = 写入时刻快照，不是恒值。** 本审计目录内的报告**在交付后仍被反复改写**（实测已发生 5 次以上）→ **下游引用前必须重新实测**，不得直接抄用下表数字。

| 文件 | 字节数（快照） | mtime（快照） | 改写轨迹 / 备注 |
|---|---|---|---|
| `AUDIT-repo.md`（t1） | **57,304** | 2026-09-10 22:56:24 | 43,075 → 48,571 → 56,275 → **57,304**（**已改写 4 次**） |
| `t2-governance-sessions.md`（t2） | **48,957** | 2026-09-10 22:52:23 | 本方案读取时为 **42,283 B** → 交付后改写（F-9 同类） |
| `t3-asset-and-artifact-inventory.md`（t3） | 31,896 | 2026-09-10 22:39:37 | 至本快照时未见改写 |
| `t5-verification.md`（t5） | **61,954** | 2026-09-10 22:56:23 | 快照前为 **43,230 B** → **交付后又改写**（t5 自身也在漂移） |
| `verifier-memo-1.md` | 9,797 | 2026-09-10 22:49:28 | — |
| `t9-cleanup-and-retention-plan.md`（本方案） | **86,424** | 2026-09-10 22:56:07 | v1 62,720 → v2 73,570 → v3 78,219 → v4 81,286 → **v5 86,424** |
| `t12-inputs-t5-corrections.md`（本文件） | 以实测为准（**本行无法自指**） | 同上 | 写入后仍有增补 |

> **下游纪律（指挥台精确化版，写入 t12）**：引用必须绑定 **文件名 + mtime + 字节数**；**引用前必须重新读取当前 mtime**；**对仍处于编辑中的文件，必须标注「引用时点」，并声明「字节数不是同一性凭据」**（并发编辑下字节数不是稳定量 —— 实证：t1 43,075→48,571→56,275→57,304→59,406 B；t2 42,283→48,957 B；t5 43,230→61,954→71,277 B；t9 v1→v6）。**只写章节号 = 引用失效。**

---

## 5. 交付状态口径（第 1 条，写入 t12 的表述）

分两层，**不得混用**：

| 层 | 判据 | 若带红项 |
|---|---|---|
| **备份层** —— 备份是否到手 | 对象是否已推送到 origin（`git ls-remote` SHA 一致 + LFS 无待上传） | 对象到手即「**备份已建立**」；`exit≠0` 不改变这一层 |
| **验收层** —— 交付是否完成 | `SessionCommands.md:113-120`：以本次 `head_sha` 匹配的「治理校验 CI 门禁」「看板部署」**两个 workflow 均 completed/success** | **必须记为「未完成 / 开口」**；t11 与 t12 **均不得写「完成」**；若 Actions 不可达则写「**已推送，CI 未核验**」，同样**不得写「完成」** |

---

## 6. t12 结构硬要求（指挥台指定）
> **⚠ 顺序已按用户裁定重排（推送降级为最后一步）：最终骨架以 §12.3 为准**，本节各条为逐项硬要求，不构成顺序。

**交接清单的第一节 = 用户 6 项裁定的最终结果**（不得后置、不得只写摘要）。

**t12 其余必备章节**（由指挥台多轮裁定累积确定）：
1. **用户 6 项裁定的最终结果**（第一节）；其中 **M01-T005 的 A/B 选择**单独说明其下游效应（A → 交付留开口；B → 体检转绿、一次收口）；
2. **事实源模型**（一句话模型 + 由模型推出的边界 + **E: 现场按「三处 + 一处外围」重述** + 「E: 上无有效完整备份，本次推送是项目首个完整备份」）；
3. **交付状态双层口径**（备份层 / 验收层，见本文件 §5）；
4. **三条 CR 候选（已排序）单列为「留给用户的治理待办」一节** —— 见本文件 §10；本轮**只登记、不执行**；
5. **证据绑定表**（文件名 + 字节数 + mtime）与「按来源分层复核」要求；
6. **本文件 §1 的 12 条更正**的最终落点 —— **F-3 / F-4 / F-8 已在 t9 就地勘误闭合**（见 §3），其余按 §1 各自落点写；
7. **「冻结解除条件」一节**（**新增强制项**）：**t10 一经开始提交，`audit/` 目录必须停止写入** —— 理由三个后果：① 提交后工作区不干净（触发 t9 §4.7 停止条件）；② 仓库内报告版本与引用它的 t12 对不上；③ 违反证据纪律。**例外：确需补写时，等 t10 完成并报告后由指挥台裁定是否补第二次提交。**
8. **「未落盘声明」一节**：写明**截至交接时点仍未落盘 / 未提交的内容**（含 t10/t11/t12 各自未完成的部分），并复述同一条 `audit/` 冻结约束。
9. **F12 / F13 两条**（见 §11）：F12 的**校验判据**（E 类零命中，不是计数）与 F13 的**两个连带后果**必须各自显式成文。

## 7. 指挥台第三轮裁定（2026-09-10，t12 需吸收）

| 项 | 裁定 | t12 落点 |
|---|---|---|
| (a) **流程事** | **就地接受 v1→v5 的同日修订，不开修复任务**；但须**显式登记**「t9 任务记录（终态 output）= v1 内容，与文件现状不一致，已知且已接受」，并补**版本绑定（字节数 + mtime）** | 已写入 t9 文件头「状态说明」；t12 引用 t9 时**以文件为准**，不得以任务 output 为准 |
| (b) **D32 / P20** | **裁定：不适用、本轮不补建会话记录** —— 本轮 t1–t12 是 **DSH AgentTeams 任务，不是 `.trae` 任务包认领**（无 `MNN-TNNN`、无根 STATUS.json `claimedBy`），**不触发规则26**。但「AgentTeams 任务体系 vs `.trae` 治理体系」的边界**在治理正文中未定义** → 与 D26 **合并为一条 CR 候选** | t12 **单列该 CR 候选**，建议标题：**「明确『只读审计 / 外部编排任务』在 `.trae` 治理中的边界与豁免」**；素材含「规则26 声称 `check-integrity.py` 会判定会话记录缺失，**实测 25 项中无此检查项**」 |
| (c) **体量口径收口** | 全仓图片 **48 个 / 47.46 MiB** 与 `Art/References/` **33 个 / 47.35 MiB** 是**两个范围**，必须分列并各标 MiB/MB | 已写入 t9 §5.2 / D31 / §11.2 / §12；t12 引用时**必须带范围标签** |
| (d) **t5 后续** | t5 判 **failed（needs_revision）→ t10 被挡住**；verifier 将在用户就 M01-T005 裁定后做**第 2 轮复核**（范围：**5 处副本一致性 + CHANGELOG 同步 + 体检是否转绿**）；t5 通过后 t10 才解锁；**在此之前 t10 不派工、git-operator 处于 HOLD**。t12 仍待 **t11** 之后派发 | t12 必须登记 **t5 一拍两轮**的事实与二审范围；交付状态按 §5 双层写 |

## 8. 待补的外部输入（阻塞项）

1. **6 项用户裁定的最终结果**（含 M01-T005 的 A/B 选择、`E:\AWorkBackup` 631MB 残留是否清除等）—— 指挥台提供；
2. **t11 的产物**（t12 的依赖项）；
3. ~~§3 的三选一裁定（t9 那 3 个错误单元格）~~ → **已解除**（裁定 (i)，已就地勘误，见 §3）。

---

## 9. t12 第一节骨架 · 用户裁定清单（**6 项**，照指挥台表格结构）

> **状态**：截至本文件写盘时点，**6 项均未到手最终裁定** → t12 逐项写「**待指挥台提供（截至交接时点未到手）**」，**不得写「未裁定」**（两者含义不同）。
> **传递方式**：用户答复到手后**由指挥台直接补齐 t12 该节**（或作为一条消息交 planner 写入）。

| # | 事项 | 当前状态 | 选项 / 指挥台建议 | 是否阻塞推送 | **最终裁定（待补）** |
|---|---|---|---|---|---|
| 1 | **推送时机** | ⏳ 待用户裁定（**时点已变更：见 §12 —— 推送降级为最后一步，在用户确认本地梳理完毕之后；与 t5 何时通过无关**） | 推荐：现在推（E: 上无有效备份，本次是项目首个完整备份） | ✅ **阻塞** | — |
| 2 | **场景生成脚本落点**（`rebuild_v3/v4/v5.py` 在项目外、**无任何备份**） | ⏳ 待用户裁定 | A 维持项目外 ／ B 放进 `.trae/` 下 ／ **C 项目内新建 `Tools/`（推荐）**；C 需修订 AGENTS.md 文件边界 → **走 CR** | ❌ 不阻塞，**另立任务** | — |
| 3 | **参考图处置**（全仓受管图片 **48 个 / 47.45 MB**；其中 `Art/References/` **33 张 / 49,646,936 B**） | ⏳ 待用户裁定 | (a) 现状保留 ／ (b) 全局 `*.png` LFS 规则（**R6 已裁定本次不做**） ／ **(c) 移出 Git、归档到 E: + 登记册建索引（推荐）** | ❌ 不阻塞，**另立任务** | — |
| 4 | **`Docs/Scene/` 是否入库** | ✅ **指挥台已裁定：排除，且已执行** | 无待选项；暂存清单 **43 条 / `Docs` 条目 0** | ❌ 已闭合 | 记为「**已裁定 · 已执行 · 待用户追认**」 |
| 5 | **M01-T005 状态口径冲突**（根 `completed` 非法，且与自身 note、登记册相冲突） | ⏳ 待用户裁定 | A 完全不动（→ 交付记「**未完成 / 开口**」，缺现红项进历史）／ **B 只把非法值回退为版本库已有值 `in_progress`（推荐；不判定任务是否完成，保留完成说明文字）** ／ C 承认完成并把 5 处副本全部同步为合法值 `approved` + 登记册改口径 + CHANGELOG | ✅ **阻塞**（**t5 的 blocker 即此项**） | — |
| 6 | **E 盘 631MB 残骸**（`E:\AWorkBackup\VRSanguoYanWuchang\`，退役插件；属 2026-09-03「彻底删除、不保留回退」裁定的执行残留） | ⏳ 待用户裁定 | 推荐：**清除**（同时消除 `Backup` 之名造成的**假安全感**） | ❌ 不阻塞，**另立任务** | — |

**两条必须写准的要点**
1. **真正阻塞推送的只有第 1 项与第 5 项**；第 2、3、6 项**一律另立任务、不进本次基线**；第 4 项已闭合（记为「已裁定 · 已执行 · 待用户追认」）。
2. 第 3 项数字以**本方案复测**为准：全仓 **49,760,737 B = 47.46 MiB**（与指挥台的 `47.45 MB` 属同一口径的取整差异）；`Art/References/` 图片 **49,646,936 B = 47.35 MiB**。**引用时必须带范围标签**（见 §7 (c)）。

---

## 10. CR 候选清单（**三条，已排序**：按用户可决策性）

> 三条均**只登记、不执行**；在 t12 交接清单中作为「**留给用户的治理待办**」**单列一节**。

### CR-1 · 明确「只读审计 / 外部编排任务」在 `.trae` 治理中的边界与豁免（= **D26 + D32 合并**）
- **根因**：治理正文没有定义「AgentTeams（外部编排）任务」与「`.trae` 任务包认领」的边界。
- **三个表现**：① 规则19（不建平行知识库 / 不重复副本）vs 可再生成产物的入库；② 规则26（会话记录）vs 本轮 AgentTeams 任务 —— **本轮无 `MNN-TNNN` 编号、无根 STATUS.json `claimedBy`，故不构成 `.trae` 认领、不触发规则26**；③ **规则文本与实现不一致** —— 规则26 声称「会话记录缺失 = `check-integrity.py` 将判定失败」，**实测其 25 项中无会话记录检查项**。
- **状态**：只登记（本轮不裁定）。

### CR-2 · 根 `STATUS.json` 与任务包副本之间**没有任何一致性检查**（**新增**）
- **来源**：scout-governance 在收口 B-2 时发现；**指挥台判定为独立治理缺口**。
- **已核事实**：`check_five_piece_set` **只验副本是否存在**；`check_task_register_consistency` **只比「根 vs 登记册」**；**两者都不比对「根 vs 任务副本」**。
- **后果**：**K1（M01-T005 一处权威漂移到 4 份副本之外）与 B-2（M00-T004 副本未随权威清空）这类漂移，在自动化上永远不可能被发现** —— 只能靠人工盘点，且**已实际发生至少两次**。
- **建议修复方向（供用户裁定，不由团队执行）**：① 增加一条「**根 vs 各任务副本的字段级一致性**」检查；② 修复漂移时「**副本回灌权威**」与「**补检查**」**同做**，否则修完还会再漂。
- **状态**：只登记。

### CR-3 · `SessionCommands.md:90` 的 `git add -A` 与「显式路径暂存」的正文级冲突（= D19 留痕项 / P19 / D30）
- **事实**：`SessionCommands.md:90` 写 `git add -A`；而本次基线**必须显式路径暂存** —— `.agent-teams/`、`.dsh-uploads/`、`Docs/` 三处 `git check-ignore` **全部 NO HIT**，`git add -A` 会一次性带入 E 类内容（含 57 处本机绝对路径与用户手机截图）。
- **建议**：正文改为「**优先显式路径**」，或在正文中写明例外条件。
- **状态**：只登记；本轮**不改治理正文**，偏离理由与实测证据（含 t1 的 `.gitignore` 反否定陷阱：`!.trae/**`/`!Config/**`/`!Source/**` 使 `*.log`/`*.tmp` 在三个目录内失效）已留痕在 t9 §4.6。

---

## 11. verifier attempt 2 追加项（**F12 / F13**）—— t12 必须承载

| 编号 | 级别 | 事实（可直接引用） | t12 落点 |
|---|---|---|---|
| **F12** | **high** | **期望值「43 条」不成立**：`reports/audit/` 是 **1 条目录路径**，`git add` 会**递归展开**。**完整算式（planner 独立只读复算）**：43 条路径 = **42 显式 + 1 目录**；42 显式中 **41 条有改动**（26 M + 2 D + 13 未跟踪）+ **1 条干净**（`.gitignore`，已跟踪且未修改）；目录展开 = audit 内文件数（**现 7 个**）→ **当前预测 add 行数 = 48**。verifier 实测 **47 = 41 + 6**（当时 audit 6 文件）→ **差额完全解释，不是矛盾**。**这是计数错、不是集合错**（`git add --dry-run` 零 E 类泄漏：`.agent-teams` / `.dsh-uploads` / `Docs` / `Saved` / `Intermediate` / `Binaries` 全 **0** 命中）。**校验阈值已由「计数值」改为「不得出现任何 E 类路径」** | t12 的校验判据与「冻结解除条件」；**严禁写成「暂存条数 = 43 才继续」** |
| **F12-附** | — | **`.gitignore` 那一行今天根本不产生 add 行**（当前未修改）；只有 §5 增补获批准并写入后才成为第 **49** 行 → **该计数还挂在一个尚未裁定的决定上** | 引用 43/47/48 时必须带此说明 |
| **F13** | medium | **`Docs/` 排除的两个连带后果（指挥台指定必须写进交接清单）**：① 演武场**场景设计**（`Docs/Scene/` 5 文件）**不在备份内**；② 生成它的 `rebuild_v5.py`（项目外 / 50,614 B）**也不在备份内** → **「设计」与「能重建设计的东西」双双在备份之外**（缺口 1 与缺口 2 的同源表现）；③ 该 `scene_topdown.png`（**2,853,240 B = 2.85 MB > 1 MB**）**将来若入库即触发规则 23**（大型非 LFS 二进制） | t12 **显式写明三小点** |

> **▍冻结令（指挥台，即刻生效）**：**本轮写入后 `audit/` 目录冻结**，直到 t10 完成并报告；期间任何人（含 planner）不得写入。违反的三后果：① 提交后工作区不干净；② 仓库内报告版本与 t12 引用对不上；③ 违反证据纪律。**确需补写时，报指挥台裁定是否补第二次提交。**

---

## 12. t12 结构（按用户新顺序**重排** —— 指挥台裁定 2）

**用户原话（顺序纠正）**：
> 「**我不是让你核对完就推，而是要核对完，看哪些清理，哪些改，本地都梳理清楚了，改完了再推**」

**正确顺序**：**盘点 → 清理/修改清单 → 用户确认 → 本地实际清理与修改 → 用户确认改完 → 才推送**

### 12.1 三点必改（指挥台指定）
1. **推送（t10）降级为最后一步**：时点 = **用户确认本地梳理完毕之后**，**与 t5 何时通过无关**（t5 只影响「交付状态」怎么写，不再构成推送的前置叙事）。
2. **新增一节「本地清理与修改执行清单」**：直接承接 t9 §3 的 **P1–P19**（**不经改写，只做索引 + 勾选位**），并标注三类：**需关闭编辑器**、**用户批准后由谁执行**、**属另立任务不进本次**。索引见 §12.2。
3. **明确写出**：**本地清理会产生删除，而删除记录本身也是要提交的内容** → **t10 的暂存集合必然重算，不得沿用 t9 §4.3 的 43 条路径**。凡 t12 中引用「43 条」的段落，一律改为「**现场重算**」。

### 12.2 P1–P19 索引（承接 t9 §3，**不改写**；勾选位留给 t12）

| P# | 动作（索引自 t9 §3 原文） | 执行者 | 前置条件 | 需关闭编辑器 | 归类 |
|---|---|---|---|---|---|
| P1 | `git rm --cached` 4 个 `.agent-teams` 已跟踪文件 | git-operator（用户批准后） | **用户明确批准**（磁盘文件不动、历史不动） | 否 | **独立动作，不进基线** |
| P2 | 删 4 个空任务目录 | 项目内执行者 | 用户批准 | 否 | 本地梳理 |
| P3 | `.trae/index.md` 补 1 行 `M02-PREP-002` 链接 | 项目内执行者 | 用户批准（治理正文） | 否 | 本地梳理 · 补漏 |
| P4 | 新增 `.trae/execution/sessions/MISSING.md` | 项目内执行者 | 用户批准（治理正文） | 否 | 本地梳理 · 补漏 |
| P5 | 删项目根 `gcm-diagnose.log`（8,258 B） | 项目内执行者 | 用户批准 | 否 | 本地梳理 |
| P6 | 清理 5 个可再生目录（**12.29 GB**） | 项目内执行者 | 用户批准 | **是** | 本地梳理 · 大项 |
| **P6a** | 清理 `Saved/`（1.12 GB） | 项目内执行者 | **必须先做完 D28 裁定** | **是** | **高风险 · 前置未满足禁止执行** |
| P7 | 删 `E:\AWorkBackup\VRSanguoYanWuchang\`（631.0 MB） | **用户本人** | 用户批准 | 否 | 项目外 · **团队不得动手** |
| P8 | `E:\AWork\Temp\VRExpansionPlugin\` 去重（回收 **1,926.7 MB**） | **用户本人** | 用户批准 | 否 | 项目外 · **团队不得动手** |
| P9 | 删 `openbrf.zip`（**11.34 MB**） | **用户本人** | 用户批准 | 否 | 项目外 · **团队不得动手** |
| P10 | 删 2 张一次性抓图 + `VRSanguoRef\OBJ\` 空目录 | **用户本人** | 用户批准 | 否 | 项目外 · **团队不得动手** |
| P11 | 清理 TraeAdmin 11 个一次性脚本 + `tmp\` 输出 | **用户本人** | 用户批准；**保留事故证据原件 + 全部 `rebuild_*.py`** | 否 | 项目外 · **团队不得动手** |
| P12 | `Saved/Evidence/` 9 张验收证据是否转正进 Git | 用户裁定 → 执行者 | 用户裁定 | **是**（若移动） | **本轮不得移动任何证据文件** |
| P13 | `L_Prototype_1v1_v4.umap` 去留 | 用户裁定 | 用户裁定 | 否 | 本地梳理 |
| P14 | `.gitattributes` 增 PNG LFS 规则 | — | — | 否 | **本次不做**（范围变更） |
| P15 | 全仓 48 图片历史迁移（`git lfs migrate`） | — | — | 否 | **另立任务**（需重写历史 + force push） |
| P16 | 改 `ExecutionModel.md:124`（临时目录自相矛盾） | 项目内执行者 | 用户批准 / 走变更申请 | 否 | 本地梳理 · 治理正文 |
| P17 | **R5 备选 (c)**：参考图移出 Git、归档 E: | 用户批准 → 执行者 | 用户批准 | 否 | **本轮不做**（内容搬迁） |
| P18 | `Docs/Scene/` 最终归属 | 用户裁定 | 用户裁定 | 否 | 待裁（连带 F13） |
| P19 | CR：`SessionCommands.md:90` `git add -A` 改「优先显式路径」 | — | — | 否 | **只登记 CR，不执行** |
| ~~P20~~ | 已裁定**不适用**（不补建会话记录） | — | — | — | **不执行** |

> **勾选位设计**（t12 用）：每行留三列 —— **① 用户是否批准 ② 是否已执行 ③ 证据（命令 + 输出摘要 + 时点）**。t9 §3 的原文（影响说明 / 可逆性 / 风险）**原样保留为引用**，t12 不复制成第二份权威。

### 12.3 t12 骨架最终顺序（照此排）

1. **用户 6 项裁定最终结果**（§9 表；第 3 项带范围标签）
2. **事实源模型**（三处 + 一处外围；「E: 上无有效完整备份 → 本次是项目首个完整备份」）
3. **本地清理与修改执行清单**（承 P1–P19 + 勾选位）← **新增/上移**
4. **「改完」的判据与用户确认点**（本地梳理完成的定义）
5. **推送（t10）** ← **降级为最后一步**：含**暂存集合现场重算**声明、**E 类零命中**判据、白名单**恰好 2 条**的停止条件
6. **交付状态双层口径**（备份层 / 验收层，见 §5）
7. **三条 CR 候选**（§10，留给用户的治理待办）
8. **证据绑定表** + 按来源分层复核（§4）
9. **「冻结解除条件」与「未落盘声明」**（§6 第 7/8 条）

> **顺序变更对冻结期的连带影响**：推送后移 ⇒ `audit/` 的冻结期**自动延长**到「**推送完成（t10 完成并报告）**」；**本地清理阶段同样不得写入 `audit/`**（除非指挥台裁定允许补写）。

---

## 13. P11 排除清单更正 + 事故证据链登记（指挥台并入批；**t9 已冻结，本节为权威版本**）

> **落盘判定**：指挥台要求把本节两条并入 v6；但 **v6 已于 23:08:20 落盘**，按指挥台规则「**v6 已落盘则不再改 t9，只写登记件**」→ **本节即该两条的正式载体**。
> t9 §3 的 P11 行仍写着「保留事故证据原件」（**t9 冻结、不可改**）→ **以本节为权威**（与 §12.3 对 t9 §10 的处理方式相同）。
>
> **▍效力关系（指挥台指定，逐字成文）**：「**本清单在 P11 项上取代 t9 §3 的同名条目**」
> → 即：t9 §3 P11 行中的「保留事故证据原件」**不再作为排除范围的判据**；**排除范围以 §13.1（点名 10 项）与 §13.2（待确认的 4 项）的逐项点名为准**。

### 13.1 P11 排除清单（事故证据链，**不得删除**）—— **最终 = 14 项**（指挥台点名 10 项 + 采纳 planner 两处缺口 4 项，**2026-09-10 裁定**）

> **▍计数澄清（planner 实测）**：指挥台函头写「**11 个文件**」，但**点名清单实为 10 项**（下方逐项实测**全部存在**）；而 `tmp\` 目录**现有 11 个文件** —— 第 11 个见 **§13.2 缺口 A**。
> → **「11」是目录文件数，不是名单条数**；两者不可混同，**执行 P11 时以逐项点名为准，不得以计数执行**（与 F12 同一条教训：**计数不是同一性凭据**）。
> → **▍最终条数（指挥台裁定）**：**14 项** = 点名 10 项 + §13.2 缺口 A 的 1 项 + 缺口 B 的 3 项。指挥台已采纳两条缺口，并采纳 planner 给出的理由（raw 是 `scene_topdown.png` 的原始输入；顶层 3 脚本与 `tmp\` 产物交错对应）。
>
> **▍交叉引用（指挥台状态通报）**：指挥台通报 **`TraeAdmin` 清理（P11）已取消**（原因：该目录大部分是事故证据链）。→ 本清单的**首要作用随之从「防止误删」转为「登记：该证据链无备份、随时可能被清理」**（§13.3）。**两者不冲突：清单仍须保留** —— 若日后有人重新清理该目录，它是唯一的排除依据。

**位置**：`D:\AWork\TraeAdmin\VRSanguoYanWuchang\tmp\`（实测 10 项**全部存在**）

| # | 文件 | 字节 | mtime |
|---|---|---|---|
| 1 | `STATUS.json.broken-20260910` | 22,331 | 09-10 18:15:36 |
| 2 | `STATUS.repaired.json` | 25,113 | 09-10 18:49:53 |
| 3 | `repair-report.txt` | 879 | 09-10 18:49:53 |
| 4 | `scan.js` | 824 | 09-10 18:42:15 |
| 5 | `scan2.js` | 923 | 09-10 18:42:21 |
| 6 | `scan3.js` | 848 | 09-10 18:42:26 |
| 7 | `scan4.js` | 944 | 09-10 18:42:32 |
| 8 | `verify1.txt` | 1,057 | 09-10 18:49:16 |
| 9 | `verify2.txt` | 1,002 | 09-10 18:50:03 |
| 10 | `verify3.txt` | 1,462 | 09-10 18:50:12 |

理由（指挥台原文）：**「保留证据原件」这句在执行时极易被理解成「只留 broken 那一个」**，而链条的价值在于**全过程可核** —— `repair-report.txt` 记破损点=3861 与源长/产物长；`verify2.txt` 做了破损件与修复件的逐串比对（证明**修复只做截断点闭合、没有重写历史正文**）；`verify3.txt` 记 HEAD 版为 `in_progress` 而修复后为 `completed`。

### 13.2 planner 实测发现的两处**名单缺口** —— **已由指挥台裁定采纳（2026-09-10）**

**缺口 A：`tmp\` 实为 11 个文件，指挥台函头写「11 个」但清单只点名 10 个。**

| 文件 | 字节 | mtime | 判定 |
|---|---|---|---|
| `scene_topdown_raw.png` | **3,090,912**（1920×1920） | 09-10 19:14:48 | **不是事故证据，但是场景设计图的直接上游** |

证据（`rebuild_v5.py` 原文 + 只读实测，**全部可复核**）：
- `rebuild_v5.py:662` `TMP_DIR = r'D:\AWork\TraeAdmin\VRSanguoYanWuchang\tmp'`；`:674` `unreal.RenderingLibrary.export_render_target(world, rt, TMP_DIR, 'scene_topdown_raw.png')` → **该文件是脚本自己的输出**；
- `:651-652`「文档夹清理（旧代次文件移除：svg 老图与 raw 中间产物）」`for _stale in ('plan.svg', 'scene_topdown_raw.png')`；`:661`「raw 中间产物移出文档夹（TraeAdmin\tmp），Docs\Scene 不再留 raw」→ **它是被特意从 Docs 移出的中间产物**；
- `:660`「配方 = yaw=-90 捕获（南上西左）→ `flip_topdown.ps1` 垂直翻转 → `scene_topdown.png`」→ **它就是 `Docs/Scene/scene_topdown.png` 的原始输入**；
- 实测对照：raw **3,090,912 B @19:14:48** → docs **2,853,240 B @19:15:19**（**相隔 31 秒**，同为 1920×1920，**字节不同** —— 与「翻转 + 重编码」流程一致；`flip_topdown.ps1` 存在，935 B，17:52）。
- **风险**：删掉它后想再得到那张设计图，**必须重跑编辑器捕获**（编辑器 + v5 关卡 + `rebuild_v5.py`），而这条链的每一环都**已在备份之外**（F13）。

**缺口 B：链条的「脚本半截」在项目外顶层目录，不在任何排除名单里。**

P11 原文要清「`…\VRSanguoYanWuchang\` 的 11 个一次性脚本」（**实测：该目录顶层现有 10 个文件**），而**事故修复链的脚本**就在其中，时间戳与 `tmp\` 的输出**交错对应**：

| 文件（顶层） | 字节 | mtime | 对应产物 |
|---|---|---|---|
| `repair_status_json5.js` | 11,402 | 09-10 18:49:49 | → `STATUS.repaired.json` / `repair-report.txt`（18:49:53） |
| `verify2.js` | 2,310 | 09-10 18:49:59 | → `tmp\verify2.txt`（18:50:03） |
| `verify3.js` | 1,673 | 09-10 18:50:09 | → `tmp\verify3.txt`（18:50:12） |

→ 指挥台的排除清单**只覆盖 `tmp\` 内的 10 项**，**未覆盖这三个脚本**；而 P11 的描述**正是要清顶层脚本**。「全过程可核」需要**两半都在**（工具 + 产物），否则 `repair-report.txt` 与 `verify*.txt` **无法被重放核对**。

> **▍结论（**已由指挥台裁定采纳**，已生效）：**P11 排除清单 = 10（指挥台点名）+ 1（`scene_topdown_raw.png`）+ 3（顶层 `repair_status_json5.js` / `verify2.js` / `verify3.js`）= 14 个文件**。
> **指挥台原话要点**：缺口 A 的理由「**比我的强**」—— raw 是 `Docs/Scene/scene_topdown.png` 的**原始输入**，删了它**想再要那张图必须重跑编辑器捕获**，而**该链每一环都已在备份之外**；缺口 B「**全过程可核需要工具与产物两半都在**」。
> **planner 做到的区分**（指挥台确认）：把它登记为「**planner 建议、待确认**」而非冒充指挥台裁定 —— 本行现已按裁定转为**已生效**。

### 13.3 新增登记（指挥台指定 → **列入用户裁定项**）

> **事故证据链位于项目外临时目录 `D:\AWork\TraeAdmin\VRSanguoYanWuchang\tmp\`（以及顶层同名目录的 3 个脚本），不在 Git、不在 E: 备份范围、随时可能被清理。** 是否给它一个正式的家（`.trae/` 下证据区 / E: 归档）→ **列入用户裁定项；本轮不动、不搬。**

- 与 §12.2 的 P11 行、以及**缺口 1 / 缺口 2** 同源：**凡不在 git、又不在 E: 备份里的东西，都没有任何副本**（F13）。
- 该 `D:\AWork\TraeAdmin\VRSanguoYanWuchang\` 目录**整体没有任何备份**，其中同时承载：三代 `rebuild_*.py`（**v5 是活跃关卡的唯一事实源**）+ `flip_topdown.ps1`（场景图翻转）+ `Set-TraeGovernanceAcl.ps1` / `Unlock-TraeAssets.ps1` / `Unlock-GripPolish.ps1`（治理配套工具）。

### 13.4 t12 引用纪律追加（指挥台指定）

> verifier 已订正其自述：「**2000 字符**」「**截断**」二词在当前根 `STATUS.json` 的 note 里**实测 ABSENT** → 该数字**目前无原件支撑**（来自会话语境）。
> → **t12 若引用，必须标注「无原件支撑」，不得以事实身份进入下游。**

---

## 14. F15 登记（指挥台指定；**t9 不改，权威载体 = t5**）

**▍先记一条版本事实（防止 t12 引用一个不存在的版本）**：指挥台所指「§4.3 L354 仍逐字为 `期望 43 条（26 M / 2 D / 14 A / 1 A=.gitignore）`」经实测**不成立** —— 该行现为 **L361**（行号因插入内容偏移），与其要求串**逐字节相同**（`-ceq` = **True**，各 **168 字节**）；`期望 43 条` 与 `26 M / 2 D / 14 A` 均 **0 命中**；而 `E 类路径` **8 处**、`不得出现任何` **5 处**、`零 E 类` **2 处**、`集合断言` **1 处**。
→ **v7 未发生**（已满足，无需再改）。

> **▍`t9` 终版 —— t12 定稿措辞（指挥台 2026-09-10 确认；planner 按实际落盘**补全**一处）**：
> **`t9` 终版 = v6 + `2026-09-10 23:12:41` 追加的 §10 执行顺序一行 + `2026-09-10 23:14:11` 的 L360/L662 去数字**（两批均由指挥台授权）；**`t9` 修订表未收录 §10 该行，属已知差异。**
> **绑定 = 96,426 B / 795 行 / mtime `2026-09-10 23:14:11` / SHA256[:16] `7EEBA5D0AA475561`**。
> **▍补全说明（必须写清楚，否则下游按字节比对会失配）**：指挥台函中措辞只写到「v6 + §10 一行」，对应 `96,481 B @23:12:41`；但**其后还有一批经指挥台授权的编辑**（`23:14:11` 去掉 L360/L662 的预测数字）→ **实际进库的是 `96,426 B` 这一版**。`96,309 B @23:08:20`、`96,481 B @23:12:41` 均为**同一文件的更早时点**，按 F-9 纪律各自绑定，不矛盾。

### 14.1 F15 的两处新事实（本方案只读实测，逐项给结论）

| 项 | 内容 | 实测结论 |
|---|---|---|
| 事实 ① | 根 `STATUS.json` 的 M01-T005 `note` **正文自带**「**未闭环遗留（需用户拍板，建议单独立项）**…③ **TD-012 偿还项（PICO 串流、中文标注、PIE stat fps、Lightmass）仍未闭环**」 | **✅ 已验证**（根 `STATUS.json` M01-T005 段内命中该串） |
| 事实 ② | `completed` 在**截断事故现场件中即已存在，非事故修复残留** | **✅ 已验证**（`tmp\STATUS.json.broken-20260910` 内 M01-T005 = `"status": "completed"`，与 `STATUS.repaired.json` 的 claimedBy / startedAt / updatedAt / note 一致；**两件各仅 1 处 `completed`**） |

**▍编号更正（t12 必须照此引用）**：该债务编号是 **`TD-012`**，**不是 `TD-12`**。
实测：t5 内 `TD-012` **6 处**、`TD-12` **0 处**；根 `STATUS.json` M01-T005 段内为 `TD-012` / `TD-016`。→ **一律写 `TD-012`。**

### 14.2 引用口径（指挥台指定：三件并存，缺一不可）

1. **加强证据** —— t5 **§C.11**「加强证据：`completed` 与该条目自身 `note` 的「未闭环遗留」自相矛盾」；
2. **公允记录** —— 上面两处新事实**同时写出**（含「`completed` 非事故残留」这一对 M01-T005 有利的事实）；
3. **反向 Caveat** —— **t9 L40 是摘要行、不是权威载体**；权威载体 = **t5**（原件层：根 `STATUS.json` 与 `tmp\` 事故件）。
→ **t9 L40 不因此改动**（指挥台裁定：那是摘要行）。

### 14.3 另登记：t9 仍含 3 处「计数」表述（**未改，待指挥台裁定**）

| 行 | 现文 | 判定 |
|---|---|---|
| **L297** | 「`reports/audit/` 1 条 → **6 个文件**）→ 实际 add **47 个文件**」 | **已知为假**（audit 现 **7** 文件 → **48**）。但同句末尾已自带「**集合由路径清单定义，计数只作对账**」 |
| **L360** | `git add --dry-run` 注释「**现场复算** add 行数 —— t10 当前预测 = **48**」 | 计数，随时间失效 |
| **L660** | §10 步骤 7「add 行数**现场复算**，当前预测 **48**」 | 同上 |

→ 按指挥台自己的理由（「**留一个已知为假的值进版本库，比多改一行更贵**」），**L297 属同一缺陷类别**。
→ **t9 已定稿，本方案不自行改动**；如需改，**先报指挥台裁定，不得自行判断「这一行很小」**（指挥台新纪律）。

---

## 15. 本地清理对暂存集合的实际影响（指挥台指定写入）

**▍结论句（指挥台指定，逐字）**：
> **12.29 GB 的可再生目录清理本身不产生任何 git 记录**（逐个 `git check-ignore` 实测 True）。只有 **P1（−4）**、**P3（+1 M）**、**P4（+1 A）**、**P12（+9，需反否定或 `-f`）**、**P13（删 v4 则清单 43→42）** 会改变暂存集合。

### 15.1 逐项实测（只读，可复核）

| P# | 目标 | 实测 git 状态 | 对暂存集合的影响 |
|---|---|---|---|
| **P1** | `.agent-teams/` 4 个已跟踪文件 | 已跟踪 | **−4 条**（`git rm --cached`；**磁盘文件与历史不动**） |
| **P3** | `.trae/index.md` | **干净**（`git status` 为空） | 执行则 **+1 条 M** |
| **P4** | `.trae/execution/sessions/MISSING.md` | **不存在**（`Test-Path` = False） | 执行则 **+1 条 A** |
| **P12** | `Saved/Evidence/`（当前代次 9 张 / 10.6 MB） | **被忽略**（`.gitignore:6` `Saved/`） | 若转正则 **+9 条**（**需反否定规则或 `git add -f`**） |
| **P13** | `Content/VRSanguo/Dev/L_Prototype_1v1_v4.umap` | **未跟踪**，但**在现有 43 条清单内** | 删除则清单 **43 → 42**（add 行数同步 −1） |
| **P6 / P6a** | `Intermediate/`、`.vs/`、`Binaries/`、`ArchivedBuilds/`、`DerivedDataCache/`、`Saved/` | **全部被忽略**（逐个 `git check-ignore` 实测 **True**） | **0 条记录** —— **12.29 GB 的清理不在提交里留任何条目** |
| **P5** | 项目根 `gcm-diagnose.log` | 未跟踪且**被忽略** | **0 条** |
| **P7 / P8 / P9 / P10 / P11** | E: 与项目外目录 | **不在 git 内** | **0 条** |

### 15.2 来源与更正（指挥台自行更正）

指挥台此前表述「**删除记录也是要提交的内容**」—— **对可再生目录不成立**（它们被忽略 → 产生 **0 条**记录）。**该表述已由指挥台更正并作废；本方案以 §15.1 实测口径为准。**

**▍但更正不废除原结论**：「**t10 暂存集合必然重算、不得沿用 §4.3 的 43 条路径**」**依然成立** —— 只是变化来自 **P1 / P3 / P4 / P12 / P13**，**不是**来自 12.29 GB 的目录清理（后者是 0 条）。

> **t12 必须照此写准**，否则就是**再造一个错期望值**（与刚清掉的 F12 同类）。

---

## 16. 用户四项**清理**裁定（指挥台通报 · 已执行）+ 范围标签澄清

> **▍口径提醒**：**这四项与「6 项用户裁定」（§1 / §9 那套）是两套，不得合并计数。** 「6 项裁定」截至本登记件时点**仍未到手**。

### 16.1 四项裁定（指挥台通报，措辞照录）

| # | 问题 | 用户裁定 | planner 只读核验 |
|---|---|---|---|
| ① | E 盘 631 MB「假备份」 | **删掉** ✅ 已执行：`E:\AWorkBackup\VRSanguoYanWuchang` **1,929 文件 / 631.0 MB** 已删（删前确认无任何 `.uproject`） | ✅ `Test-Path` = **False**（已不存在）；E: 可用 **794.8 → 797.3 GB**，与删除量同向 |
| ② | E 盘 1.02 GB×2 重复 + 950 MB 压缩包 | **删压缩包、解压目录留一个** ✅ 已执行（zip 完整性已验证：**842 文件 / 1,070,222,448 B**，与解压目录逐字节总量一致） | ✅ 算术核对：`1,070,222,448 B = 1,020.6 MiB = 1.07 GB`，与既有「1.02 GB 重复项」测量**同源一致** |
| ③ | 参考图 95 MB | **留在仓库不动**（**用户明示决定，作为其磁盘规则的一处已知例外**） | ✅ **数值正确**，但**必须带范围标签** —— 见 §16.2 |
| ④ | 8 GB 编译缓存 | **不动** | ✅ 无动作；与 §9 的 P6 系列（可再生目录）**互不影响** |

### 16.2 ★范围标签澄清（防止「差 2 倍」被误读成重复计数）

指挥台通报「参考图 **95 MB**」与 t9 的「全仓 **47.46 MiB**」**两者都对**，差异是**范围**，不是重复计数：

| 范围 | 文件数 | 字节 | 值 |
|---|---:|---:|---|
| **全仓受索引图片**（t9 §2.6 口径） | 48 | 49,760,737 | **47.46 MiB** |
| **`Content/VRSanguo/Art/References` 目录合计**（指挥台通报口径） | 65 | **99,952,138** | **95.32 MiB** |
| ├ 其中 **图片文件**（24 png + 8 jpg + 1 webp） | 33 | 49,646,936 | 47.35 MiB |
| └ 其中 **`.uasset`** | 32 | 50,305,202 | 47.97 MiB |

核验：`49,646,936 + 50,305,202 = 99,952,138` ✓；该目录**全部 65 个文件均受索引（未跟踪 = 0）**；该目录承载全仓受索引图片的 **99.77 %**。

> **▍t12 引用纪律（追加，强制）**：凡引用「参考图体积」，**必须写明范围标签** ——「**全仓受索引图片 47.46 MiB**」与「**`Art/References` 目录 95.32 MiB**」**不可互相替代**；不得只写「95 MB」或只写「47 MB」而不带范围。
> **本次教训**：两个同源数值（47 与 95）**看起来像「2 倍重复计数」**（本项目已发生过一次同形状错误：`E:\AWork\Temp\VRExpansionPlugin\` 毛额 2,947.3 MB 含 1,020.6 MB 重复）——**planner 复测后确认这次不是重复计数，是范围差**。**先复测再下结论，不得凭形状断言。**

### 16.3 ③ 的一个连带事实（登记，不动作）

`Art/References` 内的 **24 个 `.png`（46.53 MiB，多数 > 1 MB）不在任何 LFS 规则覆盖范围内**（`.gitattributes` 只覆盖 `*.uasset` / `*.umap`）→ 与**规则 23（禁止大型非 LFS 二进制）**存在**既有张力**。
- **本次提交新增 = 0 个**（这些文件已跟踪且未修改，不在 §4.3 的 43 条路径内）→ **本推送不引入新的规则 23 问题**。
- 用户已裁定**保留** → 记为**用户明示的已知例外**；**t12 如实登记，不再建议处置**。

---

*本文件为 t12 输入登记件 · planner · 写盘时点 2026-09-10 · 全程只读复核，未执行任何处置动作。*
