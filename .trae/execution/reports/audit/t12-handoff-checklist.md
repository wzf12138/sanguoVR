# t12 · 交接清单 —— 本项目下一次开工的唯一入口

> **写给谁**：项目负责人（你）。读完这一份，你就知道**项目现在什么样、卡在哪、要你拍板什么、接下来能干什么**。
> **时点**：2026-09-11 00:2x（本清单内所有数字都带时点；**引用前请重新实测**，因为有几处仍在变动）。
> **本清单只做一件事**：把前面的盘点、处置与审查结果收口成一份你能直接读的东西。
> **我没做任何处置**：未改治理文件、未动 STATUS.json、未碰编辑器、未在 E: 建任何文件、未删任何东西。

---

## 0. 一句话现状

**代码和素材已经 100% 安全地躺在云端仓库里；现在卡住项目的不是技术，而是一件治理门禁事故 —— 同一份提交在本机判"绿"、在云端判"红"，修复已在本机改到一半、还没提交。**

| 事实 | 实测值（2026-09-11 00:26 复测） |
|---|---|
| 本地 HEAD | `cb7ab790a155b5eb7b9c6ac905df46ff5cbce35b` |
| 云端 master（本地记录 / 云上实时查询） | 同 SHA（`git rev-parse origin/master` 与 `git ls-remote origin` **刚刚实测一致**） |
| 领先 / 落后 | **0 / 0** —— 内容完全同步 |
| 游戏本体（`Content/` `Config/` `Source/` `Plugins/`） | 与云端**零差异**（`git status` 这四个目录 = 0 条） |
| 未上云端的文件 | **仅 10 项（不含本清单自身），全是治理与文档**（4 个改动 + 6 个新文件），**没有一个游戏资产**；**本清单落盘后该数为 11** |
| **卡点** | 云端的"治理一致性校验"对当前提交判 **failure**（run `34501489803`）；修复已在本机进行中，**未提交、未验证** |
| 编辑器 | **已关闭**（2026-09-10 23:53:03），当前无任何 UE 进程；**未发现被丢弃的未保存内容**（见 §5） |

> **产品语言小词典**（首次出现的词，这里一次说清）：
> - **仓库（Git）**：项目的"时光机 + 保险柜"。每次"提交"给整个项目拍一张可回退的快照；"推送"是把快照传到云端。
> - **HEAD**：你本地当前所在的那张快照。
> - **LFS**：因为游戏素材（模型、贴图、关卡）动辄几十 MB，仓库用一个"大文件专用通道"存它们，仓库里只留一行指针。
> - **门禁（CI）**：云端自动跑的体检脚本，每次提交后检查治理文件是否自相矛盾。它判红 = 云端认为这份提交不合格。
> - **基线**：一次性把项目当前状态整体存进仓库的动作（本次已做完）。

---

## 1. 事实源模型：以后东西放在哪

**三句话：**

1. **唯一工作目录** = `D:\AWork\Unreal\Project\VRSanguoYanWuchang` —— 项目只在这里改。
2. **唯一版本管理** = 这个目录里的 Git（`master` 分支）—— 变化只通过"提交"记录。
3. **备份 = 推送到云端** = `https://github.com/wzf12138/sanguoVR.git` —— **没有第二份本地副本，这是有意的。**

**为什么不再保留一份本地备份副本**（这是本次审计的一条核心结论，不是偷懒）：

- **本地副本会立刻过期**：你改一次，副本就旧一次；之后每次都要问"哪份是新的"—— 这正是本项目反复出问题的那类"两个事实源打架"。
- **冗余副本与你的磁盘规则冲突**：规则明确"不保留冗余压缩包/重复副本"。本次清理就直接删掉了一份 631 MB 的"假备份"和一份重复压缩包（见 §3）。
- **真正会丢的东西已经被救回来了**：审计发现，生成校场的脚本（`rebuild_v3/v4/v5.py` + `flip_topdown.ps1`）和 `Docs/Scene/` 文档**从来没进过任何备份** —— 也就是说"丢了脚本 = 丢了校场，而且重建不出来"。本次已把它们**正式收进项目**（`.trae/tools/scene/`，随基线入库）。**这条修好之后，云端仓库才是真正的完整备份。**
- **云端这一份已经被两轮独立审查核对过**：本地 SHA、本地记录的云端 SHA、云上实时查询，三者同一个值；大文件对象 9/9 上传成功；仓库自检（`fsck`）无错误。

> ⚠️ **一处仍需注意**：项目**之外**还有一批文件**不在**这个模型里（见 §2 与 §10 风险①）。

---

## 2. E 盘使用口径（**请重点看这一节**）

**先明确一件事：E 盘是允许使用的目标盘，不是禁区。**
按项目规则，E 盘（机械硬盘）的用途就是：**大文件下载、素材包、低频工具、归档、备份**。红线只有一条 ——

> **AI 不得在 E: 自行新建文件夹。** 要往 E: 放东西，先问你。

**E: 现有内容与归属**（实测：可用 **797.3 GB**）：

| 位置 | 实测内容 | 是什么 / 归属 | 建议（**全部待你批准，我不会自己动**） |
|---|---|---|---|
| `E:\AWorkBackup\` | **已清空：0 文件** | 原有一份 631 MB 的"假备份"（只有插件残件、不含任何 `.uproject`，**不是有效备份**）→ 已按你 2026-09-10 的裁定删除（删前确认无 `.uproject`） | 它现在是**空目录**。若你想要一份"离线冷备份"，**推荐就放这里**（历史备份约定位置、且已存在，不需要新建） |
| `E:\AWork\Temp\VRSanguoRef\` | 429 文件 / 1,766.2 MB | **参考图集归档** —— 素材类，完全符合 E 盘用途 | **保留不动** |
| `E:\AWork\Temp\VRExpansionPlugin\` | 842 文件 / 1,020.6 MB | **插件解压目录**（重复的那份压缩包已按你裁定删除，**留了这一个** ✓） | **保留不动** |
| `E:\AWork\VRExpansionPlugin\` | 164 文件 / 2.7 MB | 同插件的另一处存放 | 保留（体积极小，无收益） |
| `E:\AWork\Tools\` | 5,282 文件 / 334.3 MB | 低频工具（非项目内容） | 保留 |
| `E:\AWork\Temp\` 根下 | `vrsanguo_pie_t0.png` 0.3 MB、`vrsanguo_sword_view5.png` 2.6 MB | 两张**项目相关的截图**放在项目外 | 建议由你决定：留在原地，或某次一并归档 |
| `D:\AWork\TraeAdmin\VRSanguoYanWuchang\`（**不在 E:**） | 顶层 10 个脚本 + `tmp\` **2,613 文件** | 项目外的"管理脚本现场"：三代重建脚本的副本、NTFS 权限脚本，以及**事故取证的文件**（详见 §10 风险①） | **待你批准** —— 见 §10；本轮不动、不搬、不删 |

> **E 盘现在没有任何项目备份（彻底没有）。** 这不影响安全 —— 因为云端仓库已经是完整备份。**是否需要一份离线冷备份，由你决定**；需要的话请指定一个**你已有的**文件夹。

---

## 3. 处置结果汇总：哪些已执行、哪些等你批、哪些仍是待定

### 3.1 ✅ 已执行（均有证据）

| # | 动作 | 结果 / 证据 |
|---|---|---|
| 1 | **项目基线提交** | 提交 `f2550366827a717575332bed6a93c56d42c71f9c`（2026-09-10 23:53:00），**58 个文件**变更（含 `.gitignore`、治理同步、校场关卡 v4/v5、蓝图与 C++ 改动） |
| 2 | **推送到云端** | 原始输出 `eb319c3..f255036  master -> master`，退出码 0；大文件对象 **9/9（29 MB）** 上传成功 |
| 3 | **第二次提交（治理修正）** | 提交 `cb7ab79`（2026-09-11 00:20:16）：治理一致性修正 + 门禁加固，已推送；**本次推送后三方同 SHA** |
| 4 | **删除 `.vs/` 临时目录** | 回收 **3.63 GB**（据指挥台通报）；实测：git 脏项数**不变**、该目录相关条目 **0** —— **产生 0 条提交记录**（它是被忽略的构建缓存） |
| 5 | **删除 E: 上的 631 MB"假备份"** | 实测 `E:\AWorkBackup\VRSanguoYanWuchang` 已**不存在**；E: 可用空间 `794.8 → 797.3 GB` 同向变化 |
| 6 | **删除 E: 上的重复压缩包** | 保留一个解压目录；压缩包完整性经校验（842 文件 / 1,070,222,448 B，与解压目录逐字节总量一致） |
| 7 | **`.gitignore` 增补两行** | `+.agent-teams/`、`+.dsh-uploads/`（AI 工具运行时目录，不属于项目内容） |
| 8 | **校场生成脚本正式入库** | `.trae/tools/scene/` 收下 `rebuild_v5.py`（现行单源）、`rebuild_v4.py`、`rebuild_v3.py`、`flip_topdown.ps1`，并在 `.trae/tools/README.md` 登记字节/哈希/用途 —— **修掉了"脚本从未进备份"这个真实风险** |
| 9 | **M01-T005 口径修正 + TD-012 结案回填** | 2026-09-11 修正：旧的"PICO 串流未解决"记录**已失效**（TD-012 已结案）；当前未闭环项改为中文标注等待办 |
| 10 | **校场玩法裁定落档** | `.trae/execution/arena-gameplay-decisions.md` 已随基线入库（见 §9） |

### 3.2 ⏳ 等你批准（P 系列，逐条见 §4）

主要包括：AFS 凭据处置、门禁修复的收口方式、`M01-T007` 的去留、规则 23 的"多大算大"判据、补提交窗口、4 个旧团队存档文件的移出、项目外证据链的去处。

### 3.3 🔵 D-待定（不影响你下一步开工）

| 编号 | 待定项 | 为什么悬着 |
|---|---|---|
| D28 | `Saved/Autosaves/` 能否清理 | 里面躺着**老校场 v3 的唯一副本**（`L_Prototype_1v1_v3_Auto6.umap` 450,808 B）—— **在 v3 去留定下来之前，不要清 `Saved/`** |
| D22 | Python 解释器绝对路径 | 门禁脚本要用；需要固定一个值写进治理正文 |
| D24 | CHANGELOG 顶部日期 | 若提交日期晚于 CHANGELOG 顶部日期，会触发一致性检查 |
| R6 | 9 张证据图（`Saved/Evidence/`）的正式去处 | 现在既不在仓库、也不在备份 |
| — | AgentHub 信箱 | **13 封未读，其中 2 封"仍生效的指令"**（重建冻结令、编辑器共享进程预警）—— 是否需要回执，由你定 |
| — | 场景生成脚本的注释口径修正 | 7 处文本仍是旧说法（"夺袍彩头"等），**已登记为"下次重建时一并生效"**（文档是脚本产物，手改无效） |

---

## 4. 需你裁定清单（每条 = 选项 + 我的推荐 + 一句话影响）

### 4.1 需要你拍板才能继续（建议优先处理前两条）

**① AFS 凭据留在仓库里 —— 最该先处理的一条**
- **是什么**：`Config/DefaultEngine.ini` 第 168 行有一个**真实可用的安全令牌**（用于移动端联网调试）。它是 **2026-08-09 项目初始化时**就带进来的，**不是这次提交带进来的**。相关插件当前不在项目里，所以现在**用不上这把钥匙**，但"钥匙放在公开仓库里"本身需要处理。
- **选项**：(a) **轮换这把钥匙** + 从版本控制中移出 👈 **推荐**；(b) 只移出不轮换；(c) 暂不处理。
- **一句话影响**：(a) 需要你去对应后台重新生成一次令牌（几分钟），之后旧钥匙作废、仓库里不再有可用凭据；不处理则这把钥匙会一直留在仓库历史里。
- **明确不建议**："改写历史 + 强制覆盖云端" —— 风险远大于收益，已在技术债登记中记明。

**② 门禁修复怎么收口（决定"门禁通过"这句话以后还算不算数）**
- **是什么**：修复已在本机改到一半（门禁脚本 74,566 B → 76,310 B，**本清单写作时它仍在被修改**）。问题是**当前没有一个"最终版本"的权威冻结点** —— 也就是说，任何人现在说"门禁绿了"，都无法证明他跑的是哪一版。
- **选项**：(a) 修完 → **在项目外的干净副本里验证一次** → 提交，并**把版本哈希写进提交说明，声明"自此不再重写"** 👈 **推荐**；(b) 修完就提交，不做声明；(c) 先不提交。
- **一句话影响**：(a) 以后"门禁通过"有据可查；不做则很容易重复本次事故（本机绿、云端红）。

**③ `M01-T007`（真机直连验证）的去留**
- **是什么**：这个任务原本唯一的目的，是绕开"PICO 串流按键失灵"。该问题已在你 2026-09-10 的裁定中解决（"PICO 串流在 M01-T001 已经解决了"）→ **任务的唯一前提消失**。
- **选项**：(a) **关闭该任务** 👈 推荐；(b) 改成"真机帧率采集"继续保留；(c) 原样保留。
- **一句话影响**：不关闭的话，任务清单上会长期挂着一个"前提已不存在"的任务，后续每个接手者都要重新判断一次。

**④ 规则 23 的"多大算大"判据**
- **是什么**：规则要求"禁止提交大型非 LFS 二进制"。实测同一目录下：`.uasset` 是 **132 字节的指针**（真实内容走大文件通道），`.png` 是 **4.6 MB 的真图**（不走通道）—— 两者都落在禁令的灰区，判据不明确。
- **选项**：(a) **按真实字节判定**（只看不走大文件通道的真实内容）👈 推荐；(b) 按扩展名判定；(c) 维持现状。
- **一句话影响**：影响 `Art/References` 下 **18 个 / 44.2 MB** 参考图的合规判定 —— **你已经裁定这些图留在仓库**，这条只是让规则与现实自洽、不再每次都要人工解释一遍。

**⑤ 补提交的窗口（关系到你要的"干净目录"）**
- **是什么**：现在有 **10 项**未上云端（4 个治理文件改动 + 6 个新文件），其中包括正在进行的门禁修复。
- **选项**：(a) **先约定一段"治理文件停写窗口"**，窗口内一次性提交 👈 推荐；(b) 随时提交；(c) 不提交。
- **一句话影响**：不约定窗口的话，你刚提交完，下一分钟又会看到工作区"不干净"（**本次已经真实发生过一次**：补提交尚未完成，文件又变了）。

**⑥ 仓库里还留着 4 个"早期 AI 团队"的存档文件**
- **是什么**：`.gitignore` 本次已把 AI 工具的运行时目录 `.agent-teams/` 排除在版本管理之外，但**有 4 个文件是在这条规则之前就被仓库跟踪的**（属于一个已退役的早期团队：`archive/vrsanguo-survey/` 的 3 个文件 + `retired-members.json`，均实测仍在索引中）。忽略规则**不会自动把已跟踪的文件挪出去**。
- **选项**：(a) **把这 4 个文件移出版本管理**（文件仍留在你磁盘上，只是不再随仓库走）👈 推荐；(b) 保持现状（它们会一直跟着每次提交走）。
- **一句话影响**：(a) 仓库里不再混入工具运行记录，`.gitignore` 那条规则也才真正自洽；**不会删你任何文件**（只影响"是否随仓库同步"）。

### 4.2 只有你能做的确认（不是选项，是一条待确认的玩法细节）

- **观众可否发信号**（挥手 / 指点 / 语音）：见 §9 —— 校场玩法裁定把它明确列为"**确认前不得实现、不得写入任务包**"。

---

## 5. 未落盘声明（**据实改写**）

> 本节原定的说法是"**M01-T001 的改动目前只在编辑器内存中**"。**这句话现在不成立**，我按实测事实改写如下，并说明为什么原说法失效。

**事实（均为实测）：**

1. **编辑器已关闭**：进程 pid 24284 于 **2026-09-10 23:53:03** 正常关闭。日志有完整关闭序列（`Preparing to exit` → `Editor shut down` → `Exiting.`，`Log file closed 09/10/26 23:53:03`）。**当前系统中没有任何 UE 进程在运行。**
2. **UE 自己的"未保存包恢复表"是空的**：`Saved/Autosaves/PackageRestoreData.json`（23:52:59）内容为 `{"RestoreEnabled": false, "Packages": []}` —— 这是 UE 用来记录"哪些包还没保存"的文件。**未观察到任何未保存内容被丢弃。**
3. **关闭前最后一次显式保存 = 23:48:36**（保存的是 `L_SkeletonTest`）；校场关卡 v5 的最后保存 = **21:44:17**（该写入已由四条独立证据链确认为编辑器的"保存地图"，不是脚本重跑）。
4. **游戏工程与云端零差异**：`Content/` `Config/` `Source/` `Plugins/` 的 `git status` = **0 条**。
5. **C++ 改动已编译**：`VRSanguoGripLibrary.cpp` 源码时间 22:31:52，编译产物 `UnrealEditor-VRSanguoYanWuchang.dll` 时间 **23:53:49**、模块索引 23:54:03 —— **产物比源码新**。

**结论（严格措辞）**：**不存在"还需要补存的内存态"。**
> ⚠️ 我**不**给出更强的结论"一定没有丢失任何东西"—— 按本项目的证据纪律，"**未观察到**未保存内容"不等于"**不存在**未保存内容"。上面第 2 条（UE 自己的恢复表为空）是现有最强的证据，所以我把下一步设计成"一次回读核对"，把它彻底钉死。

### 5.1 那么现在要你做的，只剩"回读核对"（推荐但可选，约 5 分钟）

| 步 | 动作 | 谁做 | 为什么 |
|---|---|---|---|
| 1 | **启动编辑器一次**（同时会完成 C++ 完整编译） | **必须你亲自** —— 我不会替你启动编辑器 | 让工程恢复可操作状态；顺便完成编译 |
| 2 | 打开校场关卡 `L_Prototype_1v1_v5`，确认场上要素齐全 | 你，或你指定的下一个 AI 任务 | 关卡是本项目最贵的单一资产 |
| 3 | 打开角色蓝图 `BP_VRCharacter`，确认**左右手各 6 个抓握节点、共 12 个节点**的接线仍在 | 同上（**只读检查，不需要手改**） | 这是 M01-T001 的主要成果 |
| 4 | 确认两把剑的**握持点**（`VRGripP__0` / `VRGripS__0`）仍存在 | 同上 | 这两个点丢失过一次，是历史复发点 |
| 5 | **戴上 PICO 真机，做一次"抓 / 放"按键测试** | **必须你亲自** | 桌面测试 ≠ 真机测试，这条只能真机验 |

> **第 1–4 步若都正常**，`M01-T001` 就可以从"执行中"改为"完成"，**后续任务才有生成条件**（见 §7）。
> **这仍然是 M01-T001 唯一的验收门**：它的状态（执行中/blocked）在三个文件里写法还不一致，见 §8。

---

## 6. 冻结项与解除条件

| 冻结项 | 内容 | 现状（实测） | 解除条件 |
|---|---|---|---|
| **重建冻结**（2026-09-10 21:32 生效） | 在 M01-T001 落盘收敛前，校场侧**不得重跑 `rebuild_v5.py`**、**不得重写 `Docs/Scene`** | ✅ **仍然有效** —— 实测 `rebuild_v5.py`、`Docs/Scene` 五件套 mtime 全部未变 | ① M01-T001 收敛（§5 的核对完成）；**② 且校场 120 m 场地口径本身仍在等你裁定**（见 §7） |
| **重启窗口冻结**（21:42 生效） | 任何人不得擅自关闭 / 重启编辑器 | ⚠️ **前提已消失** —— 编辑器已于 23:53:03 关闭，是**外部会话**的有序关闭，不是本队所为 | 无需“解除”；**下次启动请由你本人发起** |
| **`Docs/Scene/` 冻结** | 不得改写该目录 | ✅ **仍然有效** —— 5 个文件 mtime 仍为 `19:14:48 ×4 + 19:15:19` | 下次重建时一并生效（含 §9 的 7 处文本修正） |
| **审计目录冻结**（本队自我约束） | 盘点报告写作期间只读 | ✅ 已收口 —— 本清单是最后一个写入者 | 本次任务结束即自动解除 |

---

## 7. 后续开发入口

### 7.1 唯一的门

**`M01-T001`（武器抓取与 VR 集成）的验收** —— 即 §5.1 第 5 步：**你戴 PICO 做一次真机抓 / 放测试**。
这道门不开，后面四个任务**连任务包都不能生成**（治理规则：前一任务未验收，后续不得开工）。

### 7.2 开门后可以立即推进的工作包

| 工作包 | 前置条件 | 内容（产品语言） |
|---|---|---|
| **M01-T002 伤害结算** | T001 验收通过 | 让挥砍真的能造成伤害：命中判定、不重复扣血、不误伤（约 3 小时） |
| **M01-T003 挥砍与格挡** | T002 完成 | 连续轨迹采样、武器互格、卡刀自动解开（约 3 小时） |
| **M01-T004 基础 AI 战斗** | T002 完成 | 一个会靠近、攻击、格挡、挨打、恢复的对手（约 4 小时） |
| **M01-T006 1v1 测试流程** | T003 + T004 完成 | 可重复开局 / 结算 / 重置的成套测试（约 2 小时） |
| **校场布局定版（v5.6b）** | **你的裁定** | 你 2026-09-10 明示"还要再改几轮"；玩法裁定**不锁定布局** |
| **治理收口**（§4 + §8） | 你的裁定 | 补提交、AFS 凭据、门禁冻结点 |

### 7.3 建议顺序

**① T001 真机验收 → ② T002 伤害 → ③ T003 与 T004 并行 → ④ T006 测试流程 → ⑤ 校场定版。**
（§4 的治理项可以与 ②③④ 并行处理，但它们**不该继续往后拖** —— 门禁没有权威冻结点、凭据还在仓库里。）

---

## 8. 治理待更新项（**列出，但不代替执行**；逐条标注归属方）

| # | 待更新 | 现状（实测） | 归属方 |
|---|---|---|---|
| 1 | 根 `STATUS.json` ↔ 各任务包副本 | 门禁报 **3 条副本字段滞后**（警告级，不阻断） | 治理会话 |
| 2 | `M01-T001` 状态词 | 根 `STATUS.json` = `in_progress`、任务登记册 = 执行中、但 `M01-CombatSlice.md` 写 `blocked` —— **三处不一致** | 治理会话（§5 核对完成后一并改） |
| 3 | `M01-T005` 状态词 | 根 `STATUS.json` = `in_progress`、登记册 = 执行中、但 `M01-CombatSlice.md` 写 `approved` —— **三处不一致** | 治理会话 |
| 4 | 会话记录 | 本次三个会话（08-30-001 / 09-08-002 等）是否需补齐收口 | 各会话归属方 |
| 5 | **4 个治理文件的未提交改动** | `.trae/CHANGELOG.md`、`.trae/governance/SessionCommands.md`、`.trae/skills/…/SKILL.md`、`dashboard/check-integrity.py` | 指挥台（**正在进行中**，本清单写作时仍在改） |
| 6 | **6 个未跟踪文件**（+ 本清单自身，共 7 个） | `t16-review-round3.md` + 本清单 + `Docs/Scene/` 五件套（**后者是既有裁定排除入库的，不是漏网**） | 指挥台裁定 |
| 7 | **项目外证据链** | `D:\AWork\TraeAdmin\VRSanguoYanWuchang\`：顶层 10 个脚本 + `tmp\` **2,613 文件** | **你裁定**（见 §10 风险①） |
| 8 | AgentHub 信箱 | 13 封未读（含 2 封仍生效指令） | **你裁定**是否需要回执 |

> **说明**：以上动作**一律不由我执行**。我只有本次审计报告目录的写权限，且治理文件、`STATUS.json`、登记册、会话记录都在我的禁区里。

---

## 9. 与校场玩法裁定（`arena-gameplay-decisions.md`）的衔接

**权威落点**：`.trae/execution/arena-gameplay-decisions.md`（已随基线入库）。
**本清单不新增任何玩法结论，且与它不冲突** —— 凡涉及玩法，以该文件为准。

| 场地元素 | 你的裁定（照录该文件） | 状态 |
|---|---|---|
| **靶场** | **只做打靶练习。彩头 / 夺袍玩法先搁置。** 比射胜负规则、锦袍归属、分队比射**均未立项，不得写入任何任务包** | 已定 |
| **演武厅** | **点将 + 观战两个职能都做**（含两翼"将佐观武席"） | 已定 |
| **⚠️ 观众可否发信号**（挥手 / 指点 / 语音） | 该文件原文：「用户此前示意"只能看 + 可挥手"，**但该细节待其明确确认，确认前不得实现、不得写入任务包**」 | **仍在等你确认** —— 本清单不做任何预设 |
| 校场布局 v5.6b | **未定版**（你明示"还要再改几轮"）；玩法裁定**不锁定布局**，改布局时靶场/锦袍杆/演武厅须保持存在与可用 | 等你继续调 |
| 脚本与文档的文本口径 | 7 处旧表述（"夺袍彩头""M03 机制"等）**须在下次重建时一并修正**（文档是脚本产物，**手改无效**） | 已登记，待重建生效 |

---

## 10. 风险与已知例外（诚实登记）

| # | 风险 / 例外 | 说明 |
|---|---|---|
| **例外①** | **参考图留在仓库** | `Content/VRSanguo/Art/References` = **65 个文件 / 95.32 MiB**（其中图片 33 个 / 47.35 MiB、`.uasset` 32 个 / 47.97 MiB）；全仓受索引图片 = 48 个 / **47.46 MiB**。**这是你的明示裁定**，构成磁盘规则的一处已知例外。（注意两个数**不同范围**，引用时必须带范围标签） |
| **例外②** | **AFS 凭据在仓库里** | 见 §4.1 ①；非本次引入，当前不可利用，但需处置 |
| **风险①** | **项目外证据链无备份** | `D:\AWork\TraeAdmin\VRSanguoYanWuchang\`：顶层 10 个脚本 + `tmp\` **2,613 文件**（含一次 2026-09-10 状态文件事故的取证材料、以及**本次门禁复现用的两套干净副本**）。**它既不在仓库、也不在 E: 备份**；其中"一次性排障脚本"按规则本就不该入库，而**取证材料是唯一副本**。→ **建议由你决定它的正式去处（待你批准）；本轮不动、不搬、不删。** |
| **风险②** | **门禁"绿"没有权威冻结点** | 本机绿、云端红已真实发生一次；修复在途（§4.1 ②） |
| **风险③** | **`Saved/Autosaves/` 里有老校场 v3 的唯一副本** | `L_Prototype_1v1_v3_Auto6.umap`（450,808 B）—— **在 v3 去留定下来之前，不要清理 `Saved/`** |
| **风险④** | **规则 23 阈值未定** → 参考图的合规判定悬空 | 见 §4.1 ④ |
| **风险⑤** | **治理写入仍在进行** | 本清单写作时（00:26）门禁脚本仍在被修改 → **本清单描述的"当前状态"是**一个时点快照**，随后续工作会变 |

---

## 11. 本清单自身的证据与限制

- **性质**：纯文档。**未执行任何处置动作**；所有"已执行"项均引用其他成员已核验的结果，并标注来源。
- **数字纪律**：每个数字都带时点；**引用前请重新实测**（尤其门禁脚本、未提交文件数、`Git` SHA）。
- **未独立复核的两项**（据实声明）：
  1. **云端 CI 服务端状态** —— 依 `.trae/CHANGELOG.md` 记载（run `34501489803` 判红）；步骤日志端点需认证（403），本清单未能读到原始日志。
  2. **E: 删除动作的归因** —— 依 `GIT-BASELINE.md` 的时序结论（删除时刻 23:19:56 早于提交 23:53:00），本清单未独立重查。
- **本清单未做**：未改任何治理文件 / `STATUS.json` / 登记册 / 会话记录；未删除或移动任何文件；未关闭或重启编辑器；未跑 `rebuild_*.py`；未改写 `Docs/Scene/`；未在 E: 新建任何文件夹或文件。

### 机读摘要（供下一位 AI 接手者快速接管）

```json
{
  "handoffVersion": "t12-r1",
  "writtenAt": "2026-09-11T00:26:00+08:00",
  "git": {
    "localHead": "cb7ab790a155b5eb7b9c6ac905df46ff5cbce35b",
    "originMaster": "cb7ab790a155b5eb7b9c6ac905df46ff5cbce35b",
    "lsRemote": "cb7ab790a155b5eb7b9c6ac905df46ff5cbce35b",
    "aheadBehind": "0/0",
    "baselineCommit": "f2550366827a717575332bed6a93c56d42c71f9c"
  },
  "blockingIssue": "CI 门禁对 HEAD 判 failure（run 34501489803）；本地修复进行中、未提交未验证",
  "editor": { "pid24284Alive": false, "closedAt": "2026-09-10T23:53:03+08:00", "unsavedPackagesDiscarded": "未观察到（PackageRestoreData.json 为空）" },
  "guards": { "v5UmapSha256": "63AFECDF89DA19C8FC6A316DDF8D0A868FC04ED7215CA7D94F3A470BBE7A9BD6", "v5UmapBytes": 673326, "v5UmapMtime": "2026-09-10T21:44:17+08:00" },
  "uncommitted": { "modified": 4, "untracked": 6, "contentDirsClean": true },
  "frozenNow": ["重建冻结（rebuild_v5.py / Docs/Scene）", "Docs/Scene 冻结"],
  "frozenLifted": ["重启窗口冻结（前提：编辑器已关闭）"],
  "nextGate": "M01-T001 用户 PICO 真机抓/放验收",
  "awaitingUser": ["AFS 凭据处置", "门禁修复收口方式", "M01-T007 去留", "规则23 阈值", "补提交窗口", "观众可否发信号", "项目外证据链去处", "AgentHub 是否回执"]
}
```

---

*本清单由 planner 于 2026-09-11 00:2x 编写 · 全程只读复核 + 仅写入本文件 · 未执行任何处置动作。*
