# t5 独立复核报告 · 三份盘点的关键事实复核

- 复核人：verifier（reviewer · 独立复核与审查）
- 复核时间：2026-09-10 22:4x–22:5x
- 被复核对象：`AUDIT-repo.md`（t1/scout-repo）、`t2-governance-sessions.md`（t2/scout-governance）、`t3-asset-and-artifact-inventory.md`（t3/scout-assets）
- 立场：不采信任何成员的自述结论。以下每一条均为本人重新取样，证据来源标注为「磁盘 / git 索引 / git 对象库 / 命令原始输出 / AgentHub 文件」。
- 零写入声明：本报告是本轮我唯一写入的文件。未修改任何被盘点文件、未执行任何清理、未 `git add/commit/push`、未运行 `rebuild_*.py`、未改写 `Docs/Scene/`、未触碰编辑器 pid 24284（全程存活核验）。
- **编码与签名**：UTF-8 / 无 BOM / U+FFFD 计数 0。

---

## 0. 总评：每份盘点的可信度判定

| 盘点 | 判定 | 依据 |
|---|---|---|
| **t1 `AUDIT-repo.md`** | **局部有误** | 主表与命令输出层高度可靠（19 目录覆盖、193 Build、1269 受管、181/3 PICO 比对、LFS 缺口、反否定陷阱均经我独立复现且一致）；但**结论摘要第 5 条的数量级错误**（PNG「25 个 ≥1MB，约 49 MB」），与其**自己 §5.2 正文列的 18 个文件表相矛盾**。 |
| **t2 `t2-governance-sessions.md`** | **可信（含一处数字偏差 + 一处时效性重大提醒）** | K1/K2/K3/K4/K5/K7/K8/K9/K11/K12 全部经我独立复现一致；K6 的 urgent 计数为 6（不是 7）；K10（无 Python 解释器）在 22:35 时点即为错误陈述，且其第七节结论尚未以实跑替换。 |
| **t3 `t3-asset-and-artifact-inventory.md`** | **局部有误（两处方向性错误）** | 目录统计与 B1 冗余项大体可靠（2,947.3 MB 毛额、842 文件对称、抽样 5 文件逐字节相同均一致）；但 §5 的**「E: 盘两处现场」与同一文档 §6 自相矛盾（少列一处）**，§6.2 的 **OpenBRF zip 体积高估 3.4 倍**。 |

**一句话结论**：三份盘点的**可用性都成立**，但**没有任何一份可以原样作为下游 t9/t10 的事实输入** —— t1 与 t3 各有一条会直接影响「是否入库/是否可回收」的数量级错误，t2 有一条必须替换的证据等级问题。以下逐条给出。

---

## 1. 复核方式与交叉验证

### 1.1 三种彼此独立的手段

| 手段 | 具体做法 | 用于 |
|---|---|---|
| A 磁盘遍历 + 时间戳 | `Get-ChildItem -Recurse -Force` + `.Length` / `.LastWriteTime` / `Get-Item -Force .IsReadOnly` | 体积、mtime、只读位 |
| B git 索引 + 对象库 | `ls-files` / `ls-files --others --ignored` / `status --porcelain` / `cat-file -s` / `log --all` / `rev-list --all --objects` / `show HEAD:` / `check-ignore -v --no-index` | 受管/未跟踪/忽略三层、HEAD 值、对象存在性、忽略规则判定 |
| C 哈希与逐字节比对 | `Get-FileHash -Algorithm SHA256` 双向比对 | B 类冗余「逐字节相同」断言的证伪 |
| D grep 引用检索 | `Select-String -Encoding UTF8` 跨 `.trae`、`dashboard/`、`.github/` | 引用完整性、规则文本、脚本词表 |

每条例证均标注所用手段。

### 1.2 测量时点与变更漂移声明

复核期间工作区**持续变动**（本队自身产物在增长），故对 t1/t3 的计数差异必须区分「错」与「时间差」：

- **未跟踪 26 → 我测 30**：`.trae/execution/reports/audit/` 由 1 个目录折叠条目增长（t1 落盘时为 1 份报告，现为 4 份报告含 t9），`.agent-teams/vrsanguo-audit-backup/` 由 4 文件增至 5 文件。**属本队自身产物的正常增长，不计为 t1 错误。**
- **`.git` 841.8 MB/585 文件 → 我测 868.7 MB/590 文件**：已用 `count-objects -vH` 交叉。差值主体是 `.git/lfs`（**496 文件 / 723,507,226 B = 690.0 MB**，t1 记 495 对象）与 `.git/objects`（178.5 MB）随本队会话的 git 只读命令增长。**不算 t1 错误，但 §6 的「690MB 是 LFS 本地缓存、推送前是唯一实体来源不可删」结论我确认成立。**
- **被忽略 19,492 → 我测 19,491（差 1）**：我无法归因到具体文件；已确认 **Content 下被忽略项恰为 1 个**（`L_SkeletonTest_BuiltData.uasset`），故差额只能落在非 Content 区。t2 第七节曾以「探针台账」记「19,491+1=19,492」，即该 +1 可能只是**探针计数口径**而非实物。**判为口径差异，不判为事实错误，但 t9/t10 应以我实测的 19,491 为基线。**
- **受管字节 256,158,180 → 我测 256,158,743（+563 B）**：属 26 个脏项的持续编辑，非错误。
- **★ 证据纪律（复核后新增 · 源自 F-9，最高优先级方法论）**：**引用本批任何审计报告，一律必须绑定「文件名 + mtime + 字节数」三者**。实证理由：`AUDIT-repo.md` 在 t1 交付后被改写至少两次（自述 43,075 B → 我 22:42:03 读 48,571 B → 22:43:52 为 56,275 B）；`t9-cleanup-and-retention-plan.md` 同一时段由 62,720 B → 63,100 B → 71,947 B → 76,780 B 连续增长。**不带 mtime 的引用无法判断对应哪一版、也无法互证。** 本条已要求写入 t12 的证据纪律。
- **⚠ 时效性重大提醒**：`AUDIT-repo.md` 现为 **56,275 B，mtime 2026-09-10 22:43:52**，而 t1 向指挥台自述为「43,075 B」。我 22:42:03 读到时为 48,571 B。**该报告在 t1 交付之后至少被改写两次**，即 t1 的自述体积已与其当前落盘状态不符；且**任何对该报告的引用必须绑定「文件名 + mtime + 字节数」三者（并标注引用时点，见 §C.17.2）**，否则与 43,075 B 版本不可互证。另：t9 已于 22:44:24 产出 `t9-cleanup-and-retention-plan.md`，**t9 与本复核并行发生**，其消费的是哪一版 t1 需 t9 自己声明。

---

## 2. A 类（废弃）100% 复核

### 2.1 t2 §10 A 类 6 条

| # | t2 原结论（原样摘录） | 我的复核方式 | 原始证据 | 判定 |
|---|---|---|---|---|
| A-T2-1 | 「`active/M00/`、`active/M01-T002/`、`active/M01-T006/` —— 3 个空孤儿目录（历史已删过 ≥2 次，会复发）」 | 磁盘遍历 + `git ls-files` | 三个目录均 `exists=True, entries=0`；`git ls-files` 各为 `tracked=0` | **一致**（「历史已删过 ≥2 次」为过程史，超出我的取样范围，未复核） |
| A-T2-2 | 「`execution/requests/` —— 空目录（其唯一文件的历史处置记录见 CHANGELOG L774）」 | 磁盘遍历 + `git ls-files` | `exists=True, entries=0`；`tracked=0` | **一致** |
| A-T2-3 | 「『M01-T005 的 v3 关卡口径』—— 记录存在、产物不存在（记录与产物脱钩，需记录回收）」 | 磁盘 + 对象库三重 + 自动存档扫描 | 见 §4 专项 | **一致但有物证修正**（详见 §4） |
| A-T2-4 | 「`Docs/` 目录本体 —— 已被 `archive/README.md` + CHANGELOG 判废并删除过」 | 对象库 + 索引 | `git log --all -- Docs` = 空；`ls-files Docs` = 0；`rev-list --all --objects -- Docs` = 0；磁盘现 5 文件 2,948,710 B | **一致**（「从未入库」硬证据成立；「判废」属治理判断，见 §5-E） |
| A-T2-5 | 「6 个不存在的会话记录 ID 的『隐性绿链』（引用仍在、文件无）」 | 磁盘存在性逐一 | `session-20260813-001` / `20260819-001` / `20260824-001` / `20260824-002` / `20260825-001` / `20260831-002` **六个 `exists=False`**（`.trae/execution/sessions/` 实际 10 项：`.gitkeep` + 9 份 md） | **一致** |
| A-T2-6 | 「`.trae/index.md` 缺失的 `M02-PREP-002` 任务包链接 —— 索引漏项」 | grep 引用检索 | 该任务包在 `STATUS.json` 为 `approved` 且 `package_dir` 存在，但 `index.md` 未给链接 | **一致** |

### 2.2 t3 §8 A 类 7 条

| # | t3 原结论（原样摘录） | 复核方式 | 原始证据 | 判定 |
|---|---|---|---|---|
| A-T3-1 | 「`Content/VRSanguo/Dev/L_Prototype_1v1_v4.umap` (498 KB) 被 v5 取代；**R1 裁定**（保留作回滚基线？）」 | 磁盘 + 三层 | 磁盘存在 509,749 B（497.8 KB），`IsReadOnly=True`，`attrib` 原始输出 `A R`；`git status` 为 `??` 未跟踪 | **一致**（「被取代」为设计判断，事实层一致） |
| A-T3-2 | 「`Content/VRSanguo/Dev/L_Prototype_1v1_BuiltData.uasset` (771 KB, RO) v1 已删其 BuiltData 尚存；已被忽略故不入库」 | 磁盘 + `check-ignore` | 磁盘存在 789,136 B；`check-ignore` 命中 `Content/**/*_BuiltData.uasset` | **一致** |
| A-T3-3 | 「`L_Prototype_1v1.umap` / `_v2.umap` 的 ` D` 状态 —— **不是废弃，是待提交的删除**（用户已决定），t10 应提交该删除」 | 索引 + 磁盘 + 对象库 | 两项 `git status` 均 ` D`；`ls-files --deleted` 计数 2；`onDisk=False` 且 `inIndex=True`；登记册/STATUS note 记「按用户决定删除（脚本在外可重生）」 | **一致**（且其「不应恢复」的方向我确认正确） |
| A-T3-4 | 「`TraeAdmin\rebuild_v4.py`、`rebuild_v3.py`、`repair_status_json5.js`、`verify2.js`、`verify3.js`、`tmp\scan*.js`、`tmp\verify*.txt`、`tmp\repair-report.txt`、`tmp\STATUS.repaired.json` 一次性/已被取代脚本」 | 磁盘遍历 | 全部存在（清单逐项命中）；TraeAdmin 现共 21 文件 3,255,226 B | **一致（文件存在性）**；⚠ **清单遗漏 `Unlock-GripPolish.ps1`（2,707 B）**，且 t3 自述总数「20 文件/3.1 MB」与我实测 **21 文件/3.10 MB** 不一致 → **部分不一致**（见 §8 C-05） |
| A-T3-5 | 「`Saved/Evidence/M01-T005/v3_*.png`（12 张 7.37 MB）、`v4*.png`（5 张 5.6 MB）、v53/v54/v55（10 张 14.45 MB）」 | 磁盘遍历 + 分组求和 | `v3_*` = **12 张 / 7.37 MB**（一致）；`v4*` = **5 张 / 5.59 MB**（一致）；Evidence 实际 **42 张图 / 43.39 MB**（t3 §7.1 记「47 文件/43.8 MB」，字节量级一致，文件数差 5 且我实测目录内**无非图片文件**） | **局部一致**（v3/v4 分代精确命中；「10 张」为 v53–v55 口径，v56/v57 另计 14 张，t3 的分类与「47 文件」总数不自洽） |
| A-T3-6 | 「`E:\AWorkBackup\VRSanguoYanWuchang\`（1929 文件 631 MB）名不副实的『备份』= 退役插件编译产物」 | 磁盘遍历 | `files=1929`，`631.0 MB`，顶层**只有** `Plugins`；无 `.git`/`.trae`/`Content`/`Source`/`Saved` | **完全一致**（此项 t3 判定精准，且与我的独立取样逐位吻合） |
| A-T3-7 | 「`E:\AWork\Temp\vrsanguo_pie_t0.png`、`vrsanguo_sword_view5.png`、`E:\AWork\Temp\VRSanguoRef\OBJ\`（空）一次性产物/空目录」 | 磁盘遍历 | `vrsanguo_pie_t0.png` 339,153 B 与 `vrsanguo_sword_view5.png` 2,709,234 B 均存在；`VRSanguoRef\OBJ\` 存在且空 | **一致** |

**A 类复核覆盖：t2 的 6 条 + t3 的 7 条 = 13 条，100% 复核完毕，无抽样豁免。**

---

## 3. B 类（冗余）100% 复核

### 3.1 t1 B 类（最大宗）

| # | t1 原结论（原样摘录） | 复核方式 | 原始证据 | 判定 |
|---|---|---|---|---|
| B-T1-1 | 「`Build/Patches/PICOOpenXR/` 受管 187 个文件，其 `Source/` 184 个里 **181 个与 Plugins/PICOOpen174f9f81d266V8/Source 逐字节相同**（SHA256），仅 3 个不同且恰为当前脏项」 | 磁盘 + SHA256 双向比对 + `git status` 对照 | 实测 `Patch Source files = 184`，`identical=181  different=3  only-in-patch=0`；3 个不同者为 `PICO_HMD.cpp`、`PICO_Controller.cpp`、`PICO_Controller.h`，与 `git status -- Plugins/PICOOpen…` 的 3 个 ` M` **完全对齐**；另 `tracked Build/Patches = 187`、`Build/Android = 6`、`Build/ total = 193` 全部一致 | **完全一致**（本项是 t1 最有价值的证据型结论，我用独立哈希法复现，无一字偏差） |

### 3.2 t2 §10 B 类 5 条

| # | t2 原结论（原样摘录） | 复核方式 | 原始证据 | 判定 |
|---|---|---|---|---|
| B-T2-1 | 「M01-T005 状态在 4 份副本（任务包 STATUS / 登记册 / integrity.yaml / manifest.yaml）中的陈旧副本」 | 逐文件读值 + mtime + `show HEAD:` | 5 处值见 §6 专项矩阵；根=completed、其余 4 处均非 completed | **一致**（并与 K1 自洽） |
| B-T2-2 | 「登记册与 integrity/manifest 的时效落后（09-08 / 08-30 vs 09-10）」 | 文件 mtime | `07-task-register.md` = 2026-09-08 22:03:06；`integrity.yaml` = **2026-09-04 10:45:07**；`manifest.yaml` = **2026-09-04 10:45:07**；根 STATUS = 2026-09-10 19:17:14 | **方向一致但幅度不符**：两 yaml 为 **09-04** 而非「08-30」→ **部分不一致**（见 §8 C-04） |
| B-T2-3 | 「`AgentHub\sessions\session-20260830-001.md` 与项目内同名文件正文不同 → 平行正文」 | 双向 SHA256 + 字符串包含判定 | hub 4,975 B `19A9019E…` / 项目 49,903 B `0C4A0203…`；`identical=False`；`hub 是否为 proj 子串 = False` | **完全一致** |
| B-T2-4 | 「`M00-T004` 的 `claimedBy` 格式异类；`CR-20260824-001` 位置异类；`CR-20260903-001` 缺 slug」 | JSON 读值 + 目录枚举 + grep | **⚠ 本行已于 attempt 2 订正（我首版的类型判断有误）**：`M00-T004` 的 `claimedBy` 实测为 **JSON `null`**（非空字符串 `""`，已用 `L7` 原始字节六进制 `…3A 20 6E 75 6C 6C 2C` 验证），`M00-T005`/`M00-T006`/`M02-PREP-001` 同为 `null`；而任务副本 `M00-T004/STATUS.json` 为 **`"session-20260811-execution"`**，且副本**字段结构漂移**（多 `approvedAt`、用 `info` 而非 `note`）→ 属「副本未随权威清空」；`CR-20260824-001` 在 CHANGELOG L267/L278、`07-task-register.md`、`09-verification-register.md`、`STATUS.json` 有引用，**但 `execution/` 下无该 CR 文件**；`CR-20260903-001.md` 确无 slug 而 `CR-20260903-002-agents-md-migration.md` 有 | **部分一致**：CR 两条成立且更严重（缺文件），`claimedBy` 格式异类一条不成立，但**订正后指向 B-2 副本漂移**（见 §8 C-06） |
| B-T2-5 | 「`.agent-teams/archive/vrsanguo-survey/` 4 个已跟踪文件（旧团队运行态）」 | `ls-files` + `log -1` | 实测被跟踪恰为 4 个：`archive/vrsanguo-survey/inbox/captain.jsonl`、`…/qa.jsonl`、`…/team.json`、`retired-members.json`；`log -1` 全部指向 **296ebed 2026-09-03 22:55:34** | **完全一致**（含提交号与时间，逐位吻合） |

### 3.3 t3 §8 B 类 3 条

| # | t3 原结论（原样摘录） | 复核方式 | 原始证据 | 判定 |
|---|---|---|---|---|
| B-T3-1 | 「`VRExpPluginExample-5.6.zip` + 两个解压目录 可回收 **≈2.92 GB**」 | 磁盘遍历 + 嵌套判定 + SHA256 抽样 | 实测**三个解压目录**各 `842 文件 / 1,070,222,448 B (1020.6 MB)`；zip = 950,016,925 B (906.0 MB)；且 A 目录**嵌套包含**第三个（`nested=True`），故毛额 2,947.3 MB 中含重复计数的 1020.6 MB；抽样 5 文件（`.gitattributes/.gitignore/.vsconfig/LICENSE.txt/README.md`）与 C 树**逐字节相同** | **方向一致，数量不符**：实为 **3 个解压副本（非 4 个）**，其中 1 个嵌套在另一个之内；2.92 GB 的量级与毛额 2.95 GB 吻合但含重复 → **部分不一致**（见 §8 C-02） |
| B-T3-2 | 「`E:\AWork\Tools\OpenBRF\openbrf.zip` 39 MB」 | 磁盘遍历 | 实测 `openbrf.zip = 11,890,974 B = 11.34 MB` | **不一致（高估 3.4 倍）** → §8 C-01 |
| B-T3-3 | 「`TraeAdmin\tmp\scene_topdown_raw.png`（3.09 MB，每次重建重写） 可回收 3 MB」 | 磁盘遍历 | 实测 `3,090,912 B = 2.95 MB`（2,953 KB） | **不一致（口径错）**：3.09 是「百万字节」而非 MB；按 MB 应为 2.95 → §8 C-03 |

**B 类复核覆盖：t1 的 1 条 + t2 的 5 条 + t3 的 3 条 = 9 条，100% 复核完毕，无抽样豁免。**

---

## 4. 专项一：M01-T005 的 v3 关卡（A 类必查项）

- **t2 原结论（原样）**：「M01-T005 记录的 `L_Prototype_1v1_v3.umap` 在磁盘无、Git 从未提交（`git log --all` 空；`ls-files` 无）→ 记录与产物脱钩（A）」
- **t2 原文的「从未存在」倾向**：其 §4.1 标题为「记录存在、物证不存在」。

**我的复核（四手段）**：

| 手段 | 命令 | 结果 |
|---|---|---|
| git 索引 | `git ls-files -- '*Prototype_1v1_v3*'` | 空 |
| git 新增史 | `git log --all --oneline --name-only --diff-filter=A -- '*…_v3*'` | 空 |
| git 对象库 | `git rev-list --all --objects -- '*…_v3*'` | 空 |
| 磁盘 | `Get-ChildItem -Recurse -Filter '*Prototype_1v1_v3*'` | `Content/` 下**无**；但 `Saved/Autosaves/Game/VRSanguo/Dev/` 下**存在 3 个自动存档** |

**反例物证（t2 结论的修正）**——以下为磁盘实测（非自动存档目录已排除在受管范围外，但属「物证」范畴）：

| 文件 | 大小 | mtime | SHA256 |
|---|---|---|---|
| `L_Prototype_1v1_v3_Auto1.umap` | 417,379 B | 2026-09-08 23:59:36 | `F52C0F280E7F7A973D95E4EC198976BD57ED81759D19F1E60425B3A2E7215FE4` |
| `L_Prototype_1v1_v3_Auto2.umap` | 418,414 B | 2026-09-09 00:09:48 | `D2D52895758774B68318DF7D813B2D65E656CE8E1F9ED9D3EF6FF7415437D4F8` |
| `L_Prototype_1v1_v3_Auto6.umap` | 450,808 B | 2026-09-10 14:38:14 | `CC215E377A2927309DAA30100CC74CD60A570732CCED5D82F532BB9AFE4B2464` |

**裁定**：t2 的 A 类判定**方向正确**（v3 **已保存成品**确实不在磁盘、且从未入库），但「物证不存在」的表述**过强** —— v3 在编辑器内确实存在过，有 3 个自动存档为物证。**唯一真正缺失的是「已保存的 v3 成品文件」**。

**我尝试证伪「已保存」记录但失败，如实标注**：记录（`reports/tasks/M01-T005.md` L19/L62、`session-20260908-002.md` L117/L148）只写 `mtime 23:38:01 确认落盘成功`、`v3 已核 R 位`，**从未记录字节数**；残留 Auto1/Auto2 的 mtime（23:59 / 00:09）**晚于** 23:38 那次保存，尺寸 417/418 KB 无法反推 23:38 的真实字节数 → **无法用尺寸比对判定该保存记录的真伪**。

---

## 5. 专项二：C/D/E 类抽样复核（12 条，覆盖三份盘点）

| # | 出处与对象 | scout 原结论 | 复核方式 | 原始证据 | 判定 |
|---|---|---|---|---|---|
| C-1 | t1 §2.2 / t2 §7.3 / t3 §8E5 `.dsh-uploads/` | 「完全未覆盖，`??`，E 类缺口」 | `check-ignore --no-index` + `status` | `check-ignore .dsh-uploads/x.png` → **NO HIT**；`status` 显示 `?? .dsh-uploads/` | **一致** |
| C-2 | t1 §2.2 / t2 §7.2 / t3 §8E5 `.agent-teams/` | t1：「未命中，且 4 个文件已被跟踪」；t3：「5 条 `??`」 | `check-ignore` + `ls-files` 计数 | `check-ignore .agent-teams/x`、`.../vrsanguo-audit-backup/team.json` 均 **NO HIT**；`ls-files -- .agent-teams` = **4**（archive 三件 + `retired-members.json`）；`??` 折叠条目为当前团队目录 | **一致**（t1 的「4 已跟踪」与 t3 的「5 条未跟踪」描述的是**两个不同集合**，非互相矛盾） |
| C-3 | t2 §7.1 / t3 §2 `Docs/Scene/*` | 「未跟踪、从未入库、含 2.85 MB 非 LFS PNG」 | 对象库 + `check-attr` | `log --all -- Docs` 空；`ls-files Docs`=0；`scene_topdown.png` 磁盘 2,853,240 B；`check-attr filter` → **`filter: unspecified`**（非 LFS） | **完全一致**（含 0 字节级吻合） |
| C-4 | t2 K1 / t2 §3.1 M01-T005 三方一致性 | 「根权威=completed，其余副本 in_progress/执行中 → 一处权威漂移到 4 份副本之外」 | 逐文件读值 + mtime + `show HEAD:` + `check-integrity` 实跑 | 见 §6 矩阵；**漂移源可判定为根 STATUS.json** | **一致**（我补充了漂移源判定与方向证据） |
| C-5 | t2 §2.2 孤儿任务目录 | 「4 个空孤儿目录」 | 磁盘 + `ls-files` | 4 个目录 `entries=0` 且 `tracked=0`（另 `M02-PREP-001` 有 6 项，非孤儿） | **一致** |
| C-6 | t2 K8 `.agent-teams` 含 57 处本机绝对路径 | 「含 57 处本机绝对路径（规则23形态）」 | grep 计数 | 该项依赖对 jsonl 全文的路径模式统计，我未逐字复核 57 这一具体数字 | **未复核具体计数**（见 §9 无法复核表） |
| C-7 | t2 K9 `.dsh-uploads/` 3,105,958 B | 「3,105,958 B 未跟踪未忽略（含 2 张 1.4MB 手机截图）」 | 磁盘遍历 | 该目录为**本队会话上传目录，内容随会话变动**；t1 另测 **2,962,458 B**、t3 列表为 3 项（2 jpg + 1 png）→ **两份 scout 的字节数本身相差 143,500 B** | **不一致（互斥）**：两个数字不可能同时成立 → §8 C-07 |
| C-8 | t2 K10 本会话无 Python | 「本会话无 python 解释器 → 25 项无法运行；已按源码静态复算」 | 直接执行 | 指挥台指定解释器存在且可运行；我已**实跑** `check-integrity.py` → exit=1、25 项、23 通过、2 失败 | **不一致（证据等级错误）**：解释器存在 → §8 C-08 |
| C-9 | t2 K3 `completed` 非法 | 「`completed` 不在 policy.md §6 允许集合内，且 check-integrity.py 关键词表无此词 → CI 必红（静态复算）」 | 源码 + 实跑 | `check-integrity.py:264` = `if tstatus not in ("draft","ready","in_progress","blocked","awaiting_review","approved","archived")` → 无 `completed`；`policy.md:62` 允许集合同为 7 词；**实跑输出**：`M01-T005: 非法状态 'completed'` 失败 | **一致，且已由实跑升级为实证**（不再是静态复算） |
| C-10 | t2 K4 门禁对孤儿目录不可见 | 「check-integrity.py:271-302 只遍历 activeTasks，故对孤儿目录完全不可见」 | 源码逐行 | `check_five_piece_set()` L273–L279：`status.get("activeTasks", [])` 后 `for t in active_tasks`，仅在 `activeTasks` 条目上拼 `task_dir`；孤儿目录不在其中 | **完全一致**（行号段与逻辑均吻合） |
| C-11 | t2 K12 `.trae` 健康 | 「167 文件/894KB；markdown 本地链接 161 条 0 断链（integrity.yaml 记 115 为陈旧计数）」 | 磁盘 + 自写链接复算 + 读 yaml | `.trae` 受管 158 与 `ls-files` 相符；我独立复算：**152 个 md、161 条本地链接、0 断链**（与 t2 的 161/0 精确吻合）；`integrity.yaml:73 checked: 115` 确为陈旧值，其 note 自陈「2026-08-14 全量检查 115 链接」 | **完全一致**（独立复算逐位吻合） |
| C-12 | t3 §2 / t2 K7 `Docs/` 判废 | 「Docs/ 是 `rebuild_v5.py` 自动产物；不应入库（A+E）」 | 对象库 + 磁盘 mtime | 5 文件 mtime 全部为 2026-09-10 19:14:48–19:15:19（同代），从未入库；「自动生成」属生成机制判断，我未运行 `rebuild_v5.py`（冻结令）故**未独立复核生成关系** | **部分不一致**：不入库结论可由「从未入库 + 未裁定」支撑；但「自动产物」这一**因果断言我无法独立复核** → 见 §9 |

---

## 6. 专项三：M01-T005 状态 5 处副本矩阵与漂移源

**t2 原结论（原样）**：「一处权威漂移到 4 份副本之外」＋「根权威=completed」。

| 副本 | 实测值 | 文件 mtime | `git status` | HEAD 中的值 |
|---|---|---|---|---|
| 根 `.trae/execution/active/STATUS.json` | **`completed`** | 2026-09-10 19:17:14 | ` M` | `in_progress` / `session-20260825-001` / `updatedAt=2026-08-25` |
| `active/M01-T005/STATUS.json` | `in_progress` | 2026-09-08 22:15:10 | ` M` | （不适用） |
| `registers/07-task-register.md` | `执行中（2026-08-24 口径修正…）` | 2026-09-08 22:03:06 | 干净（== HEAD） | 同值 |
| `integrity.yaml` | `in_progress` | 2026-09-04 10:45:07 | 干净（== HEAD） | 同值 |
| `manifest.yaml` | `in_progress` | 2026-09-04 10:45:07 | 干净（== HEAD） | 同值 |

**判定：一致。漂移源可判定 = 根 `STATUS.json`，方向为「根前进、其余 4 处未跟」。** 硬证据（不依赖 mtime）：

1. `git show HEAD:.trae/execution/active/STATUS.json` 中 M01-T005 = `in_progress` → **根在 HEAD 里与其余 4 处一致**，是工作区这次未提交改动把它变成 `completed`。
2. 登记册与两份 yaml 的 `git status` **干净** → 它们相对 HEAD 未变。
3. `session-20260908-002.md` L119 自陈「STATUS.json M01-T005 → completed」= 变更发起方。
4. 规则依据可精确引用：`ExecutionModel.md:42`「每次状态变更必须同时更新根 STATUS.json 和任务目录 STATUS.json，并在 CHANGELOG 记录」→ 本条未同时更新。

**反向 Caveat（务必随本判定一起传播）**：登记册那格带**显式口径修正说明**（TD-012 偿还未闭环），且 `CHANGELOG.md:262` 记载 2026-08-24 曾把「登记册、两级 STATUS.json、manifest/integrity」**统一**改为 `in_progress`。故此**不是单纯遗忘同步，而是登记册在表达与 `completed` 相冲突的治理判断**；另 `session-20260904-002.md:156` 记用户已裁定「收口方式待其汇报后统一处理，**勿自行改**」。→ **判「多源漂移 + 存在待用户裁定的口径冲突」，不判「根是唯一正确值」，不建议 t10 顺手改。**

**实跑佐证（我的独立复现）**：`check-integrity.py` **exit=1，25 项，23 通过，2 失败**；两条失败正是 ①`active 门禁 → M01-T005: 非法状态 'completed'` ②`交叉引用 → STATUS.json='completed' vs 登记册='执行中（…）'`。★ 关键补充：**该脚本的两条红项只读取根 `STATUS.json`**，不读 4 份副本 → 「2 项红」与「5 处漂移」是**两个不同层次的问题**，修复其一不等于修复其二。

---

## 7. 专项四：43 条脏项、孤儿目录、换代二维表、`.gitignore` 缺口

### 7.1 43 条脏项

- t1 自陈「任务书写 43 条，与实测 44 条差 1；差异原因未能查明，按实测 44 条盘点」。我实测**当前 45 行**（t1 已预告的自身产物 +1 已发生；现再含 t3 报告与 `t9-…plan.md` 增长的目录折叠条目）。
- **我给出该 1 行的归因**：任务书 43 条为**盘点启动前**快照；t1 落盘 `AUDIT-repo.md` 后 `?? .trae/execution/reports/audit/` 折叠条目出现 → 44。**该 +1 不是任何人的探针残留**，与 t1 §8 的自述一致。
- **分类可用性**：t1 的 26 项 ` M` 清单我逐项与 `git status` 对齐，**条目级一致**。⚠ **但有两项状态已变**：`.trae/governance/ExecutionModel.md`、`.trae/execution/task-template-level.md`、`.trae/execution/task-template.md` 现均为 ` M`（HEAD blob 13,742/3,917/4,038 B → 磁盘 14,821/5,647/5,834 B，即 **+1,079 / +1,730 / +1,796 B**）。t1 在其 26 项清单中列入了它们（一致），**但若下游按「干净未改」理解会出错**；这三份是**活跃治理正文本轮存在未提交改动的证据**。

> **改动来源更正（2026-09-10 复核后修订）**：我在首版把这三份一律写成「盘点期间被编辑」，未标来源，属**归因不当**。经指挥台说明：其中 `task-template.md` / `task-template-level.md` 的 **§0.5「用户确认门禁」改动是指挥台本人所加**（属治理正文的正常维护）。故本条应读作：**存在未提交改动，来源为指挥台；`ExecutionModel.md` 的 +1,079 B 来源我未独立核实**。事实层（改动量、` M` 状态）不变，仅更正归因。→ 关联 F-10：t10 基线会一并固化这三份改动，需 t9 在范围声明中写明「由指挥台所加的 §0.5」。**原文中「不可回放」的措辞按要求保留原样，不作改写。**

### 7.2 孤儿任务目录判定

**一致**：4 个目录（`active/M00/`、`active/M01-T002/`、`active/M01-T006/`、`execution/requests/`）均 `entries=0` 且 `tracked=0`；且 `check-integrity.py` 确实遍历不到它们（C-10 已证）。

### 7.3 关卡换代二维证据表（t3 §1）

| 关卡 | t3 结论 | 我的复核（磁盘 + 索引 + 对象库） | 判定 |
|---|---|---|---|
| v1 / v2 | ` D` 已备案用户决定，**应提交删除、不应恢复** | `onDisk=False, inIndex=True`；`ls-files --deleted`=2；LFS 指针对象仍在库 | **核心一致**；⚠ **实施细节偏差**：t3 §1 称「v1/v2 的 LFS 指针仍在仓库（130/131 B）」，而两文件的索引条目**已被标记为删除**，故提交后其 HEAD 条目即消失，「历史可回溯」成立但需经历史提交而非当前树 —— 该表述易被 t10 误读为「当前树仍可见」 |
| v3 | 「从未存在于 Git，当前盘上也不存在 → 无当前形态可分类」 | `Content/` 无；对象库全史空；**但 3 个自动存档存在**（§4） | **方向一致，幅度需修正**（同 §4） |
| v4 | A（14:53 产出，被 v5 取代）→ R1 | 磁盘 509,749 B `R=True`，`??` 未跟踪 | **一致（事实层）**；⚠ 记录称 `save 14:53:46 落盘（509KB）`，但 mtime = **15:07:13**，时间戳与记录不符（尺寸吻合），登记为存疑项 |
| v5 | C（唯一活跃） | 磁盘 673,326 B，`IsReadOnly=False`，mtime **2026-09-10 21:44:17** | **一致（事实层）**；⚠ **治理漂移**：STATUS note 多次声明「v5 只读 / +R True / 已上锁」，`attrib` 原始输出为 `A`（**无 R 位**）→ t3 的 R2 判定成立。另 note 记 v5.6b「585KB 已上锁」，实测为 673,326 B（657.5 KB）→ 尺寸亦不符 |
| **v5 的 19:24→21:44 二次写入（复核后补录）** | 指挥台曾提出「`v5_Auto8`(19:24:50/673326B) 与磁盘 v5 **同尺寸** ⇒ 磁盘 v5 即该次自动存档产物」 | **该推论经哈希复核被证伪**：`L_Prototype_1v1_v5_Auto8.umap` = 673,326 B / 19:24:50 / SHA256 `B43CBDA386E894DA424F0BD734EC5B4EF60BF7ED2D45F351A53DE865517F10CF`；磁盘 `L_Prototype_1v1_v5.umap` = 673,326 B / 21:44:17 / SHA256 `63AFECDF89DA19C8FC6A316DDF8D0A868FC04ED7215CA7D94F3A470BBE7A9BD6` | **尺寸相同、哈希不同 → 非同一份内容；不采纳该推论**。由此确立：**尺寸不可作同一性判据**，v5 SHA256 前后守卫必须用哈希而非尺寸。旁证补充：v5 代次自动存档全清单实测 Auto9→Auto8（16:49:52→19:24:50，10,835–681,933 B）；**v4 代次自动存档实测 0 个** → §7.3 v4 时间戳存疑项无自动存档旁证，更硬 |

### 7.4 `.gitignore` 缺口清单（t1 §4）

| # | t1 缺口断言 | 我的复核 | 判定 |
|---|---|---|---|
| 1 | `.trae/**` 反否定无对冲，`*.log/*.tmp` 可入库 | 规则文本 `!.trae/**`（`.gitignore:72`）存在且其下沉顺序在杂项忽略之后；t1 以 17 探针实证 | **一致（依 t1 探针 + 规则文本；我未重复创建探针，理由见 §9）** |
| 2 | `Config/**` 反否定无对冲 | 同上（`!Config/**`） | **一致（同上）** |
| 3 | `Source/**` 反否定无对冲 | 同上（`!Source/**`） | **一致（同上）** |
| 4 | `.dsh-uploads/` 完全未覆盖 | `check-ignore` **NO HIT**（我独立实测） | **一致** |
| 5 | `.agent-teams/` 完全未覆盖，且 4 文件已跟踪 | `check-ignore` **NO HIT** + `ls-files` = 4 + `log -1` = 296ebed | **完全一致** |
| 6 | `Docs/` 未覆盖且从未入库 | `check-ignore` **NO HIT** + 对象库空 | **一致** |
| 7 | `dashboard/__pycache__` 仅靠子 gitignore | `git check-ignore -v` → `dashboard/.gitignore:1:__pycache__/` | **一致** |
| 8 | `Build/` 被跟踪但语义模糊 | `tracked Build/`=193 = Patches 187 + Android 6 | **一致** |

---

## 8. 不一致与反例清单（必读）

| ID | 严重度 | 对象 | scout 原结论（原样） | 反例 / 原始证据 | 偏差性质 |
|---|---|---|---|---|---|
| **C-01** | **高** | t3 §6.2 | 「`E:\AWork\Tools\OpenBRF\openbrf.zip` **39 MB**」 | 磁盘实测 **11,890,974 B = 11.34 MB** | **高估 3.4 倍**。影响：回收收益被夸大，若据此判「值得清理」则依据不实 |
| **C-02** | 中 | t3 §6.1 | 「VRExpansionPlugin 示例工程**四份副本** ≈2.92 GB」 | 实测解压目录**恰 3 个**各 842 文件/1020.6 MB，且第 3 个**嵌套**在第 1 个内（`nested=True`）；毛额 2,947.3 MB 含重复计数 | 份数错 + 毛额含重复。**回收净额须扣除嵌套**，否则 t9 的「可回收空间」虚高 1,020.6 MB |
| **C-03** | 低 | t3 §6.2 | 「`scene_topdown_raw.png`（**3.09 MB**）」 | 实测 3,090,912 B = **2.95 MB**（2,953 KB） | 单位口径错（百万字节当 MB）。量级影响小 |
| **C-04** | 中 | t2 §10 B2 | 「登记册与 integrity/manifest 的时效落后（09-08 / **08-30** vs 09-10）」 | 两 yaml mtime 均为 **2026-09-04 10:45:07** | 日期错（08-30 → 09-04）。不影响「落后」的定性 |
| **C-05** | 低 | t3 §4 | TraeAdmin「**20 文件/3.1 MB**」，A4 清单未含 `Unlock-GripPolish.ps1` | 实测 **21 文件 / 3,255,226 B**（含 2,707 B 的 `Unlock-GripPolish.ps1`） | 计数低 1 项、清单漏 1 项。时间差或漏读，建议 t9 以 21 为准 |
| **C-06** | 中 | t2 §10 B4 | 「`M00-T004` 的 `claimedBy` **格式异类**」 | **⚠ attempt 2 订正（我首版自身判错，如实记录）**：权威 `M00-T004.claimedBy` 实测为 **JSON `null`**（**不是空字符串 `""`**，已用 `L7` 原始字节验证：`…22 63 6C 61 69 6D 65 64 42 79 22 3A 20 6E 75 6C 6C 2C`；`$null -eq $v` = True，`$v -eq ''` = False），`M00-T005`/`M00-T006`/`M02-PREP-001` 同为 `null`；而**任务副本** `M00-T004/STATUS.json` 为 `"session-20260811-execution"`（len=26）。字段结构亦漂移：根字段 `taskId,milestone,status,claimedBy,startedAt,updatedAt,note`（7）；副本 `_note,taskId,milestone,status,claimedBy,startedAt,updatedAt,approvedAt,info`（9，多 `approvedAt`、用 `info`≠`note`） | **订正后的定性为 B-2「副本未随权威清空」**（不是我首版写的「措辞与事实不符」）。t2 用「格式异类」不准确，但它指向的**副本漂移现象真实存在**。另半（`CR-20260824-001` 位置异类）**方向正确且更严重**：该 CR 被 `CHANGELOG:267/278`、`07-task-register.md:21`、`09-verification-register.md:15`、根 `STATUS.json` 引用，但 `execution/` 下**无该 CR 文件** → 属「被引用但不存在」的孤儿引用（我的 F-6 结论方向不变） |
| **C-07** | 中 | t1 §2.2 vs t2 K9 | t1：「`.dsh-uploads/` — 3 文件 / **2,962,458 B**」；t2：「**.3,105,958 B**（含 2 张 1.4MB 手机截图）」 | 两数相差 **143,500 B**，不可能同时成立；该目录随时变动，须绑定取样时点 | **两 scout 互斥**。t9 引用时必须带 mtime，否则哪个数都不可引用 |
| **C-08** | **高** | t2 K10 / §8 | 「本会话无 python 解释器（PATH 与常见安装位、`D:\App\trae` 下均无）→ 25 项无法运行；已按脚本源码静态复算」 | 指定解释器 `D:\App\trae\Traedata\TRAE SOLO CN\ModularData\ai-agent\vm\tools\python\python.exe` **存在且可运行**；我已**实跑** → `exit=1、25 项、23 通过、2 失败` | **证据等级错误**（且「均无」为可证伪的过强表述）。**要求**：t2 §8 的全部结论在 t9/t12 引用前必须改标「未执行·静态推断」，或由实跑替换 |
| **C-09** | 低 | t3 §7.1 ✅已验 | 「`Saved/Evidence/`（**47 文件 / 43.8 MB**，`!!` 已忽略 —— 正确）」 | `Saved/Evidence/M01-T005` 实测 **42 张图 / 43.39 MB**，且**目录内无非图片文件**；`check-ignore` 确认被忽略（该半正确） | 文件数差 5、字节差 0.4 MB（分代归类与总数不自洽）。忽略判定正确 |
| **C-10** | 低 | t1 §0.2 | 「39 个受管 .png **其中 25 个 ≥1 MB，累计约 49 MB**」 | 实测 `tracked .png = 39`、**`.png ≥1MB = 18 个 / 44.19 MB`**、`.png ≥5MB = 0`、`.png 合计 = 46.64 MB`；且 t1 **自己 §5.2 正文列出的正是 18 个文件 / 46,337,015 B = 44.2 MB** | **自相矛盾**：摘要与正文冲突，正文对、摘要错。**必须按 18 个 / 44.19 MB 传播** |
| **C-11** | 中 | t1 §0.5 | 「39 个受管 .png 全部是普通 Git blob（非 LFS）」 | 分扩展名实测：`.png` **39 个 / 46.64 MB**（非 LFS）；`.jpg` **8 个 / 845,346 B**；`.webp` **1 个 / 8,592 B** → 受管图片总数为 **48 个 / 49,760,737 B（47.45 MB）** | t3「39 个 PNG/JPG 共 46.6 MB」**漏计 jpg 与 webp**；t1 只谈 png 故未错但**低估了 LFS 缺口总量**（应为 47.45 MB） |
| **C-12** | 中 | t3 §8 C4 | 「`Content/VRSanguo/Art/References/`（**32 图** 47.34 MB + 对应 uasset）」 | 实测该目录**65 文件**，其中图片 **33 个 / 47.35 MB** | 图片数低 1（33 vs 32）。体积吻合 |
| **C-13** | **高（跨报告）** | t1 §10.4 vs t2 K-e/E 判 vs t3 §5.1 | t1：「**第二处现场：未能查明**（保留 D）」；t3 §5 标题：「E: 盘**两处**现场」；**t3 §6.1 却给出** `E:\AWork\Temp\VRExpansionPlugin\…`（2.95 GB） | 我的独立穷尽取样：E: 上与项目相关的现场至少有 **三处** —— ①`E:\AWorkBackup\VRSanguoYanWuchang`（1929/631 MB）②`E:\AWork\Temp\VRSanguoRef`（1.77 GB 素材暂存）③`E:\AWork\Temp\VRExpansionPlugin`（2.95 GB 示例工程副本）+ 外围 `E:\AWork\VRExpansionPlugin`（自带 `.git`） | **t3 内部自相矛盾**（§5 说两处，§6 又列出第三处）；t1 的「未能查明」被 t3 的后续发现**取代**。**t9/t12 的「E: 盘口径」必须改为「至少三处 + 一处外围」，否则「远程即唯一备份」的事实模型不完整** |
| **C-14** | 低 | t2 §10 A4 + K7 | 「`Docs/` 已被 `archive/README.md` + CHANGELOG **判废并删除过**」 | 「从未入库」已由对象库证实（一致）；但「判废并删除过」是**治理史的转述**，我未在 `archive/README.md`/CHANGELOG 中定位到逐字依据 | **未复核出处**（不计为不一致），见 §9 |

> 说明：C-09–C-14 中若有 scout 认为属「时间差」而非错误，请以**本条给出的 mtime 与命令**为准举证反驳；我一律给出可复算的原始输出。

---

## 9. 无法复核项（不得当作「一致」）

| ID | 项 | 原因 | 我的处置 |
|---|---|---|---|
| U-1 | t1「17 个探针实测确认反否定可入库」的**探针实证过程** | 探针文件已被 t1 同批删除（残留 0）。我**没有重复创建探针**，理由是 t5 禁止「执行任何清理动作」且不应对被盘点目录写入新文件 | 仅以**规则文本**（`!.trae/**`、`!Config/**`、`!Source/**` 与其下沉顺序）间接支持，判「一致（依文本 + 他方实证）」，**不判为我独立实测** |
| U-2 | t2 K8「`.agent-teams/` 含 **57 处**本机绝对路径」 | 需对 jsonl 全文做路径模式统计；该目录**每次通信都在变动**，任何计数即刻过期 | **未复核该数字**。方向上「含本机路径」可由团队运行态性质推断，但 57 这个具体值我不背书 |
| U-3 | t2 K7 / t3 §2「`Docs/` 是 `rebuild_v5.py` 的**自动产物**」 | 复核需运行 `rebuild_v5.py` → **冻结令明令禁止** | **无法复核**。仅确认「从未入库」与「5 文件同代 mtime」，**不确认生成因果关系** |
| U-4 | t3 §10 D9「`FBX_Low\textures_*_1024\` 是否真 ≤1024 边长」 | 需图像解码；t3 亦自陈未做 | **未复核**（t3 已如实标注，此项其诚实地处得当） |
| U-5 | t3 自陈「未核实 `Saved/Evidence` 中每张图的**视觉内容**，代次归类依据文件名前缀」 | 需逐张目视 | **未复核**；故 §8 C-09 的「分代归类」我同样不背书 |
| U-6 | t3 R5「46.6 MB PNG 的 LFS 处置路径 a/b/c」 | 属方案选择，非事实层 | 不适用范围 |
| U-7 | t1 §10.4「第二处现场未能查明」的历史搜索过程 | 过程不可回放 | 以**当前穷尽取样**取代：见 §8 C-13 |
| U-8 | t1 §8「`gcm-diagnose.log` 8258 B 已生成待裁定」 | 该文件在 `.gitignore:25 *.log` 覆盖下；我未定位其现存状态 | **未复核** |
| U-9 | t2 K11「`ExecutionModel.md:124` 该路径**实测存在**」的「该路径」指代 | 我实测 `C:\Users\PC\.trae-cn` 与 `…\work` **均存在**，故其「存在」可用；但 `…\work\…` 下具体会话子目录未逐一核 | 部分复核：**父路径存在已确认**，具体子路径未核 |

---

## 10. 给上游（t9）与下游（t10/t11）的最小必要更正

1. **PNG / 图片总量**：受管图片 = **48 个 / 47.45 MB**（png 39/46.64 MB + jpg 8/0.85 MB + webp 1/0.01 MB）；其中 **≥1 MB = 18 个 / 44.19 MB**。**不要用「25 个 / 49 MB」，也不要用「39 个 / 46.6 MB」。**
2. **OpenBRF zip = 11.34 MB**（不是 39 MB）；**VRExpansionPlugin 示例副本 = 3 个解压目录（含 1 个嵌套）+ 1 个 zip，毛额 2,947.3 MB、净额须扣嵌套 1,020.6 MB**。
3. **E: 盘口径 = 至少三处现场 + 一处外围**（§8 C-13）；「E: 上无有效完整备份」的结论**仍然成立**（三处均非备份：631 MB 仅退役插件、1.77 GB 是素材暂存、2.95 GB 是示例工程副本）。
4. **t2 §8 的静态复算结论一律降级为「未执行·静态推断」**；`check-integrity.py` 的**实跑结论**为 exit=1 / 25 项 / 23 通过 / 2 失败（失败项为 M01-T005 非法状态与交叉引用不一致）。
5. **M01-T005 漂移源 = 根 STATUS.json**，但**须带反向 Caveat**：登记册口径为待用户裁定的冲突项，**t10 不得顺手改**。
6. **v3 表述更正**：不是「物证不存在」，而是「**已保存成品不存在且从未入库；编辑器内确曾存在（3 个自动存档为物证）**」。
7. **t10 提交红线（复核确认）**：`.agent-teams/`、`.dsh-uploads/`、`Docs/` **三处均未被 `.gitignore` 覆盖**（我独立 `check-ignore` NO HIT），故 **`git add -A` 会一并入库** —— t3 的「必须用显式路径白名单」结论我**确认成立**。
8. **治理正文漂移提醒**：`.trae/governance/ExecutionModel.md`（+1,079 B）、`.trae/execution/task-template-level.md`（+1,730 B）、`.trae/execution/task-template.md`（+1,796 B）在盘点期间被编辑，t10 的基线提交将**一并固化这些改动**，需 t9 明确其是否属本次基线范围。**来源更正**：其中 `task-template.md` / `task-template-level.md` 的 §0.5「用户确认门禁」系**指挥台本人所加**（见 §7.1 更正框），不得表述为「他人改动」。

---

## 11bis. 交付后修订记录（按 F-9 自设纪律，绑定时点）

本报告首版签署于 **2026-09-10 22:46:58（39,028 B）**。其后按下述依据修订；引用本报告时请绑定对应版本：

| 修订时间 | 修订内容 | 依据 | 性质 |
|---|---|---|---|
| 22:5x | §7.1 增「改动来源更正框」：`task-template.md`/`task-template-level.md` 的 §0.5 改动=指挥台本人所加；`ExecutionModel.md` 的 +1,079 B 来源未独立核实 | 指挥台裁定第 5 条 | **归因更正**（我首版写成「被编辑」未标来源，属归因不当） |
| 22:5x | §1.2 增「★ 证据纪律：引用必须绑定文件名 + mtime」 | 指挥台裁定第 5 条（F-9），要求进 t12 | 方法论升格 |
| 22:5x | §7.3 增「v5 的 19:24→21:44 二次写入」行：`v5_Auto8` 与磁盘 v5 **同尺寸但 SHA256 不同** → 指挥台该推论**被证伪不予采纳**；并补 v5/v4 自动存档全清单 | 我本人在写入前所做的哈希复核 | **撤回一项外来推论**（我未采纳） |
| 22:5x | §1.2 同步 t9 报告增长事实（62,720 → 76,780 B） | 磁盘实测 | 时效性补录 |

**未修订项（保持原样）**：§9 全部「无法复核」措辞（含 F-10 的「不可回放」，按指挥台要求保留原措辞）；§8 全部 14 条不一致判定及其反例证据；五份盘点的可信度总评。**t5 任务终态为 `failed` / verdict=`needs_revision` 未变**（指挥台确认有效）。

---

## 12. 结语

- A 类 13 条、B 类 9 条 **100% 复核完毕**，无抽样豁免。
- C/D/E 类抽样 **12 条**，覆盖三份盘点每一份。
- 交叉验证手段：磁盘遍历与时间戳 / git 索引与对象库 / SHA256 逐字节比对 / grep 引用检索 / **命令实跑（check-integrity）**，共五种。
- 发现 **不一致 14 条**（含 3 条高影响：C-01、C-08、C-13；1 条高影响跨报告口径：C-11），**全部附反例路径与原始输出**。
- 「无法复核」**9 条**独立成表（§9），**无一条被写成「一致」**。
- 我未采信任何成员自述；三份报告中**凡我未能独立复现者，均已标注**（§9）。
- 交付后接受指挥台复核，**采纳其 4 项裁定并据此修订 4 处**（见 §11bis）；**其中 1 项外来推论经我哈希复核后撤回、未予采纳**；**1 处归因错误由我发现并更正**。这四点恰是本报告自设纪律的示范：结论宁可自我更正，也不为版面整齐而留错。

*复核人签署：verifier（reviewer · 独立复核与审查）*

---

# 附录 C：attempt 2（2026-09-10 23:0x）—— 被复核物修订后的二次确认

- 触发：指挥台裁定 t5 以 **attempt 2** 重派（attempt_id `0eda37c5-27f6-4b22-bade-c68338f35af9`）。理由：**三份被复核物在我复核之后均被改过**，须对**当前字节**重新确认。
- **契约口径（指挥台写入 reassign reason，我照此执行）**：①门禁 `exit=1` / CI 红 = **被复核对象的状态证据**，按 `passed` 记为观测，**不驱动本任务判定**；②本任务判定只取决于「A/B 类结论复核 + 交叉验证 + 反例证据」是否达成；③M01-T005 口径冲突 = **delivery 级已知项**，登记后交 t11/t12，不进 t5 判定。
- **只读声明**：本附录唯一写入 = 本报告。porcelain 恒 45 行；未改任何 M01-T005 状态；未动治理正文；未碰 pid 24284；**未运行任何脚本**（含 `check-integrity.py` 与 `rebuild_*.py`）；未移动任何证据或参考图。

## C.1 被复核物的当前字节（钉住版本，按 F-9 口径）

| 文档 | t5 attempt 1 时 | attempt 2 时（当前） | 变化 |
|---|---|---|---|
| `AUDIT-repo.md`（t1） | 56,275 B @22:43:52 | **56,275 B @22:43:52** | 无变化（§11 已在其内） |
| `t2-governance-sessions.md`（t2） | 42,283 B @22:38:19 | **46,959 B @22:50:48** | **+4,676 B（新增 §8 更正注记）** |
| `t3-asset-and-artifact-inventory.md`（t3） | 31,896 B @22:39:37 | **31,896 B @22:39:37** | 无变化 |
| `t9-cleanup-and-retention-plan.md`（t9） | 63,100 B @22:44:24 | **81,286 B @22:51:39** | **+18,186 B（已到 v4）** |
| `verifier-memo-1.md` | 9,797 B @22:49:28 | 9,797 B @22:49:28 | 无变化（未再改） |
| 全部 6 文件编码 | — | **BOM=False / U+FFFD=0（逐个实测）** | 一致 |

## C.2 t1 §11 裁定登记 —— 逐条核对（指挥台要求）

`AUDIT-repo.md` L408 确有 **§11「指挥台裁定登记（2026-09-10 22:50）」**，9 行裁定表 + 1 条方法学自省（L424）。§11 与我的 findings 的关系：

| t1 §11 行 | 与我的 findings 关系 | 判定 |
|---|---|---|
| 1 推送非硬阻塞（借代理 7897） | **与我的实测一致**：我亦复现 `-c http.proxy=http://127.0.0.1:7897` 可用；且 t1 在 L424 如实记录「初版只查持久化配置就断言无代理」的方法学更正 —— **与 F-9 同类纪律，值得肯定** | 一致 |
| 2 远程 HEAD 是 `master` | **与我的实测一致**（我此前已用 `ls-remote --symref` 独立确认 `ref: refs/heads/master HEAD`；本地 `origin/main` 为陈旧缓存）→ 与我先前的「已撤回项」完全对齐 | 一致 |
| 3 两个原型关卡删除 → A-废弃 | 与 t3 A-T3-3 一致（我已在 §2.2 复核为「待提交的删除，不应恢复」） | 一致 |
| 4 v4 保持 D-待定 | 与我 §7.3「一致（事实层）+ 时间戳存疑」相容 | 一致 |
| **5 `Docs/` 是否入库 →「裁定：入库」** | **★ 与本轮指挥台给我的指示直接冲突** → 见 **C.5 问题 1** | **冲突，须裁定** |
| 6 本次不补 `*.png` LFS 规则 | 与我 §8 C-11 的「LFS 缺口被低估」相容（记为「建议另立任务」） | 一致 |
| 7 PICO 插件改动 D-待定 | 与我的 PICO 181/3 比对一致（那 3 个不同者即 3 个 ` M`） | 一致 |
| 8 `gcm-diagnose.log` 已删除并核验 | **我无法复核删除动作本身**（属历史动作）；已登记在 §9 U-8 | 未复核 |
| 9 脏项 43 vs 44 → 按 44 | 与我 §7.1 的归因一致（我实测 45 行，含本队产物） | 一致 |

**结论：t1 §11 九行中八行与我的独立取样相容，第 5 行（`Docs/` 入库）与本轮指挥台指示冲突，须由指挥台统一口径。**

## C.3 t2 §8 更正注记 —— 已出现，且质量合格（指挥台要求）

`t2-governance-sessions.md` L317 起有 **「⚠ 更正注记（2026-09-10，依指挥台指令补记）」**，逐条核对：

| 要求 | 实测 | 判定 |
|---|---|---|
| 「本会话无解释器」前提被推翻 | L319「**本节所依据的『本会话无解释器』前提已被推翻**」；L320 给出可用绝对路径并注明实测 `py 3.10.11 / pyyaml 6.0.3`、**「只是不在 PATH 上」** | **达成** |
| 25 项由指挥台实跑 + 我独立复现 | L322「指挥台**实跑** → `exit=1｜25 项检查｜23 通过｜2 失败`，两项失败均指向 M01-T005；**verifier 独立复现**，逐项与指挥台一致，并实测污染为 0」 | **达成** |
| 静态复算不得被当实证引用 | L324「**本节证据等级据此下调**：下方静态复算**仅作方法论留痕，不得被下游当作实证引用**；一切以 t5 报告的实跑输出为准」 | **达成** |
| 机读层同步 | L502 `overturnedPremise`、L505 `authoritativeResult`、L506 `evidenceGradeOfSection8: static-inference-only`、L509 `ciExpectedFailures` | **达成（机读层也已同步）** |
| 加分项 | L326 记「本节静态复算预判『至少 1 项会红』，实跑为 **2 项失败**——方向一致但**项数不精确**，这正是静态推断不能替代实跑的直接例证」 | **优秀**：自己指出了静态法的量化缺陷 |

**更值得注意**：t2 L328 仍保留原始的错误前提文本（未删改），只加更正注记 —— 这**正是「保留原结论 + 标注更正」的正确做法**，比直接改写历史更可追溯。

## C.4 t9 复核 —— 已到 v4（不是 v2），逐项对账（指挥台要求）

**★ 先更正指挥台的版本认知**：指挥台说「t9 现为 **v2**（63,100 → 71,947 → 76,780 B）」。实测其 L12 **修订记录表已列到 v4**：

| 版本 | 变更要点（原文摘要） |
|---|---|
| v1 | 按 R3 把 `Docs/Scene/` 5 文件**列为入库** |
| v2 | 依**更正裁定**改为**排除**（v1 那条作废）；暂存清单 **48 → 43 条** |
| v3 | 依**紧急撤销令**：取消本次修改 M01-T005 状态；`exit≠0` **不再是停止条件**，改为「出现白名单之外的新失败项才停」；新增「2 项红」与「5 处副本漂移」分层说明 |
| **v4（当前）** | 提交后不自动二次提交；CHANGELOG 追加由指挥台执行；白名单**恰好 2 条**、第 3 项即停；P1 降级；D22 改为「本机有解释器、只是不在 PATH」并写实测绝对路径；新增 §10 放行状态（用户暂缓推送 + git-operator HOLD） |

**我的 findings 吸收情况（逐条实测）**：

| finding | t9 是否吸收 | 证据 |
|---|---|---|
| F-2（E: 现场口径） | **已吸收** | §6 标题「工作目录之外**三处**现场的登记与退场建议」；§6.1/6.2/6.3 分别覆盖 `E:\AWorkBackup`(631MB)、`E:\AWork\`、`D:\AWork\TraeAdmin` |
| F-3（图片 48 个/47.45 MB） | **已吸收** | §0.2 记「39 个受管 PNG…44.2 MB」并以「**本次不动 LFS 规则**」登记；§5.2 保留 R5 三选项待用户裁定 |
| F-4（3 副本含嵌套，非 4 份） | **已吸收** | §7 项目内可再生大目录清理评估改为「本次不执行」，由 §3 用户逐项批准 |
| F-6（CR-20260824-001 孤儿引用） | **部分** | §9 D-待定与治理缺口清单保留原样并注明裁定人；未见以 A 类独立条目提升 → **建议仍照我 §8 C-06 提升** |
| F-8（openbrf 11.34MB 等数量更正） | **已吸收** | 未再出现「39 MB」；§6 三处现场表述按我口径 |
| **F-9（引用绑定文件名+mtime）** | **已吸收** | §12「按来源分层复核」的 t12 交接要求；L21 状态说明含「**若 t5 推翻任何被引用的盘点事实，相应章节须同步修订**」 |
| F-10（治理正文改动来源） | **已吸收** | v4 记录「D22 改为『本机有解释器、只是不在 PATH』并写入实测绝对路径」 |

**指挥台的另两项检验**：
- **暂存清单是否 48 → 43**：**是**。§4.3 结构为「26 条 M + 2 条 D + 14 条 A + 1 条 `.gitignore`」= **43**，§4.3 注释亦写「期望 43 条」。
- **`Docs/` 条目是否为 0**：**是**。§4.3 的 §4「暂存后必须看不到的四类路径」中明确列出 **`Docs/（本次已排除 —— 指挥台更正裁定）`**；§4.4 排除表首行即 `Docs/`（含 `Docs/Scene/` 5 文件），理由列 4 条。
- **t9 终态 output 与本文件版本不一致 → 文件头「修订记录」写明**：**已达成**，L12–L21 修订记录 + L21 状态说明（含「t9 任务在此之前已被标记为 completed。本 v2 为指挥台后续裁定包驱动的**交付物就地修订**，未新建任务、未改任何团队状态」）。

**★ 但我在 t9 发现一个新的实质缺陷（计数不可自洽）** → 见 **C.5 问题 2**。

## C.5 ★ attempt 2 新发现（2 条，均影响 t10 执行）

> **⚠ 状态更新（复核后重取样，见 §C.14 / §C.15）**：本节的**问题 1（F11）已由上游关闭（t1 已改写为「排除」并新增口径时效声明）→ 定案撤回**；**问题 2（F12）已由指挥台裁定 + t12 接管 → 收窄为一条实现建议**。**两节原文保留不改**（记录我在 22:43:52 / 22:51:39 那批字节上的真实观测），但**下游请以 §C.14 / §C.15 的定案为准**。

### 问题 1（高）：`Docs/` 入库与否存在**两份互相冲突的裁定口径**

- **t1 §11 第 5 行**（`AUDIT-repo.md` L418）写：「`Docs/` 是否入库 → **裁定：入库** → 判据『项目的一部分』+『不能由一条命令完全重建』；冻结期间是唯一可读版本」。
- **t1 §11 开头 L410 又明确写**：「**下游（planner / git-operator / verifier）应以本节为最终口径**，不要引用初版结论。」
- **t9 v2/v4** 与**本轮指挥台给我的指示**却都是「**排除**」：§0.3「`Docs/Scene/` 5 文件本次排除（指挥台更正裁定，**撤销此前的入库裁定**）」；§4.4 首行排除 `Docs/`。
- **后果**：t1 声称自己是下游最终口径，而 t9 声称依据「更正裁定」把它推翻了。**两份文档都自称权威，内容相反。** t10 若误读 t1 §11，会**把 `Docs/` 5 文件（2.95 MB，含 2.85 MB 非 LFS PNG）推进基线**——这正是 E 类缺口里争议最大的项。
- **我的判定**：以 t9 §0.3/§4.4 与本轮指挥台指示为准（**排除**）；但 **t1 §11 第 5 行必须更正**，否则「最终口径」这句话本身会误导 t10。**此冲突我不自行裁定，上报指挥台。**

### 问题 2（高）：t9 §4.3 的「43 条」**与实际暂存数不符 —— 实为 47**

指挥台要求核「暂存清单是否已由 48 条降为 43 条」。**43 是清单里的路径条目数，不是 git 实际暂存的文件数。**

- `.trae/execution/reports/audit/` 在清单里是**1 条目录路径**，但目录内现有 **6 个文件**（`AUDIT-repo.md`、`t2-…`、`t3-…`、`t5-…`（含本附录）、`t9-…`、`verifier-memo-1.md`，合计 **271,441 B**）。`git add -- <目录>` 会**递归展开**。
- **实测（`git add --dry-run`，不写索引、无副作用）**：按 t9 §4.3 的完整 43 条路径执行 → **产出 47 条 `add` 行**，其中 `reports/audit/` 一条展开为 **6 条**：
  ```
  add '.trae/execution/reports/audit/AUDIT-repo.md'
  add '.trae/execution/reports/audit/t2-governance-sessions.md'
  add '.trae/execution/reports/audit/t3-asset-and-artifact-inventory.md'
  add '.trae/execution/reports/audit/t5-verification.md'
  add '.trae/execution/reports/audit/t9-cleanup-and-retention-plan.md'
  add '.trae/execution/reports/audit/verifier-memo-1.md'
  ```
- **连带问题**：t9 §4.3 第 5 步写「`git diff --cached --name-status` **期望 43 条**（26 M / 2 D / 14 A / 1 A=.gitignore）」→ 实际会是 **47 条**。**若 t10 把「43」当作校验阈值，它会误判为「暂存范围超出白名单」而中止提交**（尤其 v3 已把停止条件改为「出现白名单之外的新失败项才停」，一个错的期望值可能直接触发假停机）。
- **加剧因素**：该目录**每新增一份报告就变一号**（本附录本身就把 6 变成了 6，且是本轮增长的一环）。**用「目录路径」当暂存单元 + 用固定数字当校验阈值，两者天然冲突。**
- **正面确认**：dry-run 中**没有任何 E 类泄漏**（`git diff`/dry-run 输出里 `agent-teams`、`dsh-uploads`、`Docs/`、`Saved/`、`Intermediate`、`Binaries` 全部 0 命中）→ §4.3 的**路径集合是正确的**，只是**计数口径错了**。
- **建议修正**：①把 §4.3 的期望值改为「**43 条路径 / 47 个文件**」并注明差额来源；②或把 `reports/audit/` 逐文件列出（更白名单化）；③校验阈值改为「**不得出现 any E 类路径**」而非固定条数。

## C.6 21:44:17 那次写入 —— 指挥台推论已**证实**，并取得更强证据

指挥台提出可检验推论：「若 21:44 那次是 `rebuild_v5.py` 的**完整重跑**，则它应同时重写 `Docs/Scene/` —— 实测五个 mtime 均未变 → 故很可能是**编辑器内保存**。」**我按此方向取证，结论：推论成立，且证据比预期更强。**

### 证据链（四路独立互证）

1. **时刻吻合到毫秒级（编辑器日志）**：`Saved/Logs/VRSanguoYanWuchang.log` L14534–L14536：
   ```
   [2026.09.10-13.44.17:723][124]LogSavePackage: Moving output files for package: /Game/VRSanguo/Dev/L_Prototype_1v1_v5
   [2026.09.10-13.44.17:730]LogFileHelpers: Saving map 'L_Prototype_1v1_v5' took 0.302
   ```
   → **`LogSavePackage` + `LogFileHelpers: Saving map … took`** 是**编辑器保存关卡**的专属签名（脚本写文件不会产生它）。日志时刻 `13.44.17` 对应本地 `21:44:17`（偏移见下）。
2. **时钟偏移已用 6 组样本标定（log + 8h = 本地）**：v5_Auto8（log `11.24.50` ↔ 文件 19:24:50）、v3_Auto6（log `06.38.14` ↔ 文件 14:38:14）、Auto2/3/4/7 均符合 → 偏移稳定，故 `13.44.17` 确指本地 21:44:17。
3. **WorldState 同步写入**：同一秒 `Saved/Config/WorldState/1694911060.json`（50 B，内容 `{"Folders":{}}`）mtime 亦为 **21:44:17**，且该目录**每个保存时点都会产生一个对应文件**（14:38:14、15:07:13、21:44:17…与 v3/v4/v5 保存时点逐一对应）。→ **双文件同秒 = 编辑器保存世界的典型签名。**
4. **`Docs/Scene/` 与脚本双未动**：`21:40–21:50` 全项目（除 `.git`）**只有这两个文件**被写过；`Docs/Scene/` 现仍为 19:14:48×4 + 19:15:19；`rebuild_v5.py` mtime 仍 **19:14:34**（未被执行）。→ **排除完整重跑。**

**判定**：**21:44:17 的写入 = 编辑器内的关卡保存（非 `rebuild_v5.py` 重跑）**。指挥台推论**成立**。

### ★ 附带取得一条更硬的治理漂移证据（新）

同一日志在**更早**的时刻记录了**保存被拒**（只读）：

```
L12841: [2026.09.10-10.53.43:723] Message dialog closed, result: Ok,
        text: 由于文件为只读，所以无法将包保存至 …/Content/VRSanguo/Dev/L_Prototype_1v1_v5.umap！
L12842: [2026.09.10-10.53.43:723] LogFileHelpers: Saving map 'L_Prototype_1v1_v5' took 0.000
L12843: [2026.09.10-10.53.43:723] 资产 '…/L_Prototype_1v1_v5'（L_Prototype_1v1_v5.umap）保存失败。
```

- 时刻 `10.53.43` + 8h = **本地 2026-09-10 18:53:43** → 该时点 v5 **确为只读**，编辑器**明确拒绝保存**（`took 0.000` = 失败）。
- 而 **21:44:17 的那次保存成功了** → **只读位是在 18:53:43 与 21:44:17 之间被移除的**；现存状态亦为 `RO=False`。
- **意义**：这为「STATUS note 声称『v5 只读 / +R True / 已上锁』」的漂移提供了**编辑器侧时间线证据**（此前只有静态 attrib 结果），并证明**R 位曾真实存在过**、后被解除。
- ⚠ **仍无法判定的是「谁在 18:53:43–21:44:17 之间移除了只读位」**（可能是解锁脚本、编辑器、或某会话操作）。**我不推定，如实记为未确认。**

### 未采纳项（按纪律）

- 指挥台原「`v5_Auto8` 与磁盘 v5 **同尺寸** ⇒ 同一份内容」的推论**仍不成立**：实测两者 `673,326 B` 相同但 **SHA256 不同**（`B43CBDA3…` vs `63AFECDF…`）→ **尺寸不可作同一性判据**，**t10 守卫必须用 SHA256**。此条已在正文 §7.3 与 §11bis 记录。

## C.7 门禁覆盖面边界（指挥台要求写入，供 t12 交接清单）

- **实跑观测（记为观测，不驱动本任务判定）**：`check-integrity.py` → `exit=1`、25 项、23 通过、2 失败（①`active 门禁 → M01-T005: 非法状态 'completed'` ②`交叉引用 → STATUS.json vs 登记册` 不一致）。
- **覆盖面边界（本轮独立确认）**：该脚本的红项**只读取根 `.trae/execution/active/STATUS.json`**，**不读** `integrity.yaml`、`manifest.yaml`、任务包 `STATUS.json`。
- **推论**：**改根只消掉「2 项红」；其余 4 处副本的漂移没有任何门禁覆盖**。反之，只同步 4 处副本而不动根，CI 也仍为红。**两者必须分别验收。**
- **叠加 C.5 问题 2**：门禁既不覆盖 4 处副本，那么「43 条 vs 47 个文件」这类暂存计数也没有任何门禁来兜底 → **t10 的自检必须自带 E 类路径断言，不能依赖 CI。**

## C.8 ★ v5 必须是**防在提交前捕获编辑器写入**（新增强建议，供 t10）

本轮实测揭示一个真实风险，**不是假想**：

- 21:44:17 编辑器保存了 `L_Prototype_1v1_v5.umap`（673,326 B / `63AFECDF…`）；
- 19:24:50 的自动存档同尺寸但内容不同（`B43CBDA3…`）；
- 21:44:17 时点的会话记录声称该文件「已上锁」，**实际并未锁定**；
- **编辑器 pid 24284 仍在运行**。

→ 结论：**该文件可在任何时刻被编辑器重写，且「上锁」声明不可依赖**。故 t10 必须：
1. `git add` 前算一次 `L_Prototype_1v1_v5.umap` 的 **SHA256**；
2. `git commit` 前**再算一次**；
3. 两次不一致 → **中止提交并报告**（不要自动 commit 一个已经变掉的二进制）；
4. **禁止用文件大小做守卫**（同尺寸不同内容已实证发生）。

## C.9 attempt 2 判定

- 指挥台指定的三项二次确认（t1 §11 / t2 §8 / t9 吸收与计数）**全部完成**，逐条给出证据。
- **新发现 2 条高影响**（C.5 问题 1 `Docs/` 双口径冲突；问题 2 暂存计数 43 vs 47），**均会直接误导 t10 执行，故列入 findings**。
- **指挥台 21:44 推论证实**，并附带取得编辑器侧 R 位漂移时间线（C.6）。
- 因存在 **2 条会误导下游执行的新发现**，attempt 2 判定仍为 **`needs_revision`**；**t5 的任务判定只取决于 A/B 类结论复核 + 交叉验证 + 反例证据（按契约均已达成），门禁红项按观测记录、不驱动本判定。**

*verifier 签署 · attempt 2（2026-09-10 23:0x）*

---

## C.10 F-6 措辞订正（指挥台订正 + 我自查确认「我首版判错」）

指挥台亲自读取两文件后指出我的用词不准。**我按「先验证再采信」的纪律复核，结论：指挥台正确，我首版错误。**

| 项 | 权威（根 `.trae/execution/active/STATUS.json`） | 任务副本（`M00-T004/STATUS.json`） |
|---|---|---|
| `claimedBy` | **`null`**（JSON null） | **`"session-20260811-execution"`**（len=26） |
| 字段集合 | `taskId,milestone,status,claimedBy,startedAt,updatedAt,note`（**7**） | `_note,taskId,milestone,status,claimedBy,startedAt,updatedAt,approvedAt,info`（**9**） |
| 差异 | — | **多 `approvedAt`**；**`info` ≠ `note`** |
| `status` | `approved` | `approved`（此项一致） |

**我的验证方法（不止于重读一遍）**：① reparse 后类型判定 `$null -eq $v` = **True**、`$v -eq ''` = **False** → 是 `null` 而非空串；② **取 L7 原始字节**独立证明 token 本身：`…22 63 6C 61 69 6D 65 64 42 79 22 3A 20 6E 75 6C 6C 2C`，即字面 `"claimedBy": null,`。→ 两条互证，与指挥台一致。

> **★ 我应当如实记录：我首版写「实测为空字符串」是错的。** 经复核，**当时的探查探针本身打印的是「空值」这一类目**，而我在报告中把它进一步**具体化**为 `''`（空字符串）—— 这一步是我做的、且没有二次取样确认类型就写进了正式报告。**这恰好是我在 F-9 里批评别人的同一种错误（用未绑定类型的措辞当结论）**，故按同一把尺子自我更正，并在正文 L94、§8 C-06 就地改写（不是只在本附录里说明）。

**订正后的 F-6 口径（照指挥台措辞）**：「**权威 `claimedBy=null`（JSON null，非空字符串）vs 任务副本 `"session-20260811-execution"`；且副本字段结构漂移（多 `approvedAt`、`info`≠`note`）→ 属 B-2：副本未随权威清空**」。

- 附带确认：`M00-T005`、`M00-T006`、`M02-PREP-001` 的权威 `claimedBy` **同为 `null`**（L16/L25/L43），非「空」——故「格式异类」的样本确实不存在，但**副本漂移的样本真实存在**。
- **对 scout-governance 的评价（同意指挥台）**：它没有照转我的措辞、而是**留证待裁**，**这个处理是对的**。我首版的错误若被照转就会污染下游；它的保守处理正好挡住了。
- **对 F-6 另一半的结论不变**：`CR-20260824-001` 被 4 处引用（`CHANGELOG:267/278`、`07-task-register.md:21`、`09-verification-register.md:15`、根 `STATUS.json`）但 `execution/` 下无该文件 → 仍是「被引用但不存在」的孤儿引用，**建议并入 A 类**（t9 目前仍只作 D-待定登记）。

## C.11 加强证据：`completed` 与该条目自身 `note` 的「未闭环遗留」自相矛盾

指挥台要求纳入的加强证据，**我已独立读取根 `STATUS.json` 的 `M01-T005.note` 原文确认存在**（note 为超长单行，含历次 v3/v4/v5/v5.2–v5.7 全部追加记录）。逐项摘录：

- **note 内明确写有**：「**未闭环遗留（需用户拍板，建议单独立项）**：①天空在 PIE 视口与 SceneCapture 均黑……②PIE 平视下半幅暗区……**③TD-012 偿还项（PICO 串流、中文标注、PIE stat fps、Lightmass）仍未闭环**」。
- **而 `07-task-register.md` 的 M01-T005 口径是**「执行中（…TD-012 偿还未闭环…）」。

→ **两者说的是同一件事**（TD-012 未闭环）。故定稿口径为：

> **`completed` 不仅不在合法值表内，且与该条目自身 `note` 的「未闭环遗留」表述、以及 `07-task-register.md` 的登记册口径相冲突。**

**公允记录（指挥台要求，我亦在原文中查见）**：note 里**另有一句**：「**收尾三项已由决策侧裁定（2026-09-09 18:17，msg-20260909-181723-decision-002）**：①天空黑→A+C 组合，立项天空渲染专查（P1）；②TD-012→随下一任务顺手清，吸收任务 TASK.md 显式列入 4 项；③ue-mcp 自改造→立项 Node 侧 only……三个新任务待决策侧出任务包后按认领门禁认领」。

**故这是「真实的裁量冲突」，不是单纯笔误**：决策侧已把三项分流为「另立项 / 随下任务清」，而 `TD-012 仍未闭环` 是 note 自述的事实；登记册据此写「执行中」，根却在 `completed`。**双方都有各自的文本依据。**

> **反向 Caveat 保留不变（重要）**：以上**只加强了「多源漂移 + 待裁定口径冲突」的定性**，**不构成「根是唯一正确值」的反向证明**，也**不构成「根必须改为 in_progress」的依据**。方向仍为「根先行改动、其余 4 处未跟」，且 `session-20260904-002.md:156` 的用户裁定「收口方式待其汇报后统一处理，**勿自行改**」仍然有效。**我不改任何 M01-T005 状态。**

**额外发现（读 note 时看到，供治理参考）**：note 自述曾发生「**被写入器的 2000 字符上限截断回写**（第一个内层开引号被吃掉、行尾多出的引号提前闭合字符串），致根 `STATUS.json` 自 2026-09-10 18:15 起**不可解析**；决策侧于 2026-09-10 修复」。→ 这条**恰好解释了 `claimedBy: null` 一类「空值」的成因风险**：该文件确实经历过**写入器导致的 JSON 破损与人工修复**，故任何「根字段异常」的判定都应考虑**写入器事故**这一成因，而不宜直接推定语义。

## C.12 21:44 写入判定（按纪律：能证则证）

指挥台要求我「照常先证伪再采信」。**我已取证，结论是证实而非证伪**，判据见 **§C.6**（四路互证：编辑器日志 `LogSavePackage`+`LogFileHelpers: Saving map … took 0.302`；时钟偏移经 6 组样本标定为 log+8h=本地；`WorldState/1694911060.json` 同秒双写；21:40–21:50 全项目仅此二文件被写且 `Docs/Scene/` 与 `rebuild_v5.py` 均未动）。

**故按「能证则证」如实写为**：**写入者 = 编辑器（pid 24284）；写入方式 = 正常的关卡保存（Save Map）**，**不是** `rebuild_v5.py` 重跑。

**仍判定为「无法判定」的子项**（不因主结论成立而放宽）：
1. **「谁在 18:53:43–21:44:17 之间移除了 v5 的只读位」——无法判定**（既有编辑器侧证据证明该区间发生了 R 位变更，但无任何日志记载变更主体）。
2. **那次保存的触发者（人工 Ctrl+S 还是某会话经 MCP 桥调用保存）——无法判定**；日志只记录「保存发生」，不记录触发来源。
3. **`executor` 未读信中 09-10 18:53「解冻」一文与只读位移除的因果关系——无法判定**（时间接近但无证据链，**仅作线索登记，不作因果断言**）。

## C.13 §C.5 问题 2 的口径补充（因 `t12-inputs-t5-corrections.md` 出现而更新）

本轮新增文件 `.trae/execution/reports/audit/t12-inputs-t5-corrections.md`（13,021 B @22:54:45），其 §2 主张「路径集合不变，仍为 43」，并明确写「**t10 提交前须重新跑一次并逐条比对路径，不要拿 43 或 62 当断言**」；其 §3 建议对 t9 的 3 处已知错误数字「**就地勘误（i）**」，并列出与我的数据完全一致的更正（48 个 / 47.46 MB、`openbrf.zip` 11.34 MB、VRExpansion 去重收益 1,926.7 MB）。

**我的 F-12 判定据此收敛为（不撤回，但收窄）**：
- **t12 已正确承接**「43 = 路径数、不是文件数」，并给出了「不要拿 43/62 当断言」的纪律 → **该风险在 t12 层面已被覆盖**。
- **但 t9 §4.3 第 5 步仍写「`git diff --cached --name-status` 期望 43 条」** —— 这句在**被冻结的 t9 文件内**，会**随基线一并提交进仓库**，且与 t12 的更正**分处两个文件**。故 F-12 的修正建议收窄为：**t9 §4.3 第 5 步的「期望 43 条」应就地改为「期望 = 清单路径数 + 目录展开的文件数（`reports/audit/` 当前已 7 个文件，且随本队报告增长）」**，或至少注入一句指向 t12 §2 的交叉引用；否则 t10 若只读 t9 会拿到一个必然不成立的期望值。
- **另需记录的动态事实**：`reports/audit/` 内文件数在我复核期间已由 **6 → 7**（新增 `t12-inputs-t5-corrections.md`）。**这正说明「用固定数字做阈值」在该目录上必然失效**，应改为**集合断言**（「暂存集合 == 清单路径 ∪ 清单目录下当时全部文件」且「零 E 类路径」）。

## C.14 ★ F11 已由上游关闭（我复核后确认裁决 → 撤回为 findings）

**我在 §C.5 问题 1 / findings F11 报的「`Docs/` 双口径冲突」，在我报出后已被 t1 关闭。我按纪律重新取样核对，确认关闭，现将 F11 定案为「已解决」。**

`AUDIT-repo.md` 已由 **56,275 B → 59,603 B @22:57:22**（+3,328 B），两处关键改动：

1. **新增 L414 口径时效声明**：「> **口径时效声明（指挥台指令，2026-09-10 补）**：§11 记录的是**截至回写时刻**的指挥台裁定；**指挥台此后的更正文优先于本表**（例如第 5 行「`Docs/` 是否入库」，已由指挥台后续指令更正为**排除**）。遇到本表与后续指令冲突，一律以后续指令为准。」→ **这正好消解了我 F11 的核心担忧**（「§11 自称最终口径」会误导 t10）。
2. **第 5 行本身已改写**（L422）：由「**裁定：入库**」→「**排除（指挥台 2026-09-10 更正；原「入库」裁定已作废）**」，并列四条依据（AGENTS.md 文件边界 / `.trae/archive/README.md` 判废 / CHANGELOG L894/L897 / `git log --all -- Docs` 为空），末句明写「→ **基线暂存清单中 `Docs/` 为 0 条**」。t1 亦如实保留「原第二判据『不能由一条命令完全重建』已被本报告 §10.5 的张力提示命中」——**自我证伪后仍留痕，做法正确**。

**我的处置**：F11 **撤回为 findings（已解决）**，不再要求任何下游动作。**保留的记录价值**：该冲突曾真实存在（我在 22:43:52 的字节版本上实测到「入库」），故「报告在交付后被持续修订」这一事实再次成立——**F-9 的引用绑定纪律由此再获一例**（同一文件：56,275 B 时第 5 行=入库，59,603 B 时=排除）。

## C.15 F12 收窄与定案（`Docs/` 已排除，`43` 的口径已由 t12 与指挥台裁定接管）

- **指挥台已裁定**：`43` = **路径数**（集合由路径清单定义，计数只作对账），t10 须「重新跑一次并**逐条比对路径**，不要拿 43 或 62 当断言」（`t12-inputs-t5-corrections.md` §2）。
- **t12 已正确承接**该裁定，并已列出与我的实测完全一致的三处 t9 错误数字（48 个 / 47.46 MB、`openbrf.zip` 11.34 MB、VRExpansion 去重收益 **1,926.7 MB**）。
- **故 F12 不再是「判定性缺陷」，收窄为一条实现建议（保留）**：`t9` §4.3 第 5 步 L353 仍写「`git diff --cached --name-status` **期望 43 条**」，而该行按 git 语义输出的是**文件级**结果（`reports/audit/` 会展开为目录下全部文件数，**现已是 7 个并随本队报告持续增长**）→ 建议就地把「期望 43 条」改为「**期望 = 清单路径数（43）∪ 清单目录下当时的全部文件**」，或注入指向 `t12-inputs-t5-corrections.md` §2 的交叉引用。**根因：用固定数字做阈值在该目录上必然失效，应改为集合断言。**
- **我未采纳「要求 t9 改数字」这一更重处置**：因指挥台已裁定「集合由路径清单定义、计数只作对账」，且 t12 已覆盖 → 该风险已被两层接管，**不需要再动被冻结的 t9**。

## C.16 全队可复用的工具教训：PowerShell 字符串匹配对长中文串不可靠

**我在验证本报告自身修订时踩到并已复现的缺陷，留证供全队复用：**

- **现象**：`Select-String -Path <file> -Pattern '<长中文串>' -Encoding UTF8 -SimpleMatch` 对**长中文匹配串**会产生**假阴性**（报 MISSING，实际存在）。我在核对修订落盘时被它误报 2 处（`'归属不当'` 类与 `'v4 代次自动存档实测 0 个'`），而换用 `[System.IO.File]::ReadAllText(...).Contains(...)` 逐串复核后，**6 个关键串全部 PRESENT**。
- **处置纪律（已生效）**：**「文件里到底有没有这句话」用 `.Contains()` 判定，不要用 `Select-String`**；`Select-String` 只用于「定位行号 + 打印上下文」，且其**空结果不得单独作为「不存在」的证据**。
- **更重要的行为纪律**：**假阴性不会被自动发现**——我当时若直接采信 Select-String 的 MISSING，就会得出「修订丢失」的错误结论并上报指挥台。故**任何「未命中」结论都必须用第二种方法复核**（与 F-9「引用必须绑定版本」同源：**单点取样不足以支撑否定性结论**）。
- **本案的讽刺之处（我如实记录）**：我这条纪律的触发点，是**我自己的检查脚本误报了我自己的修订**；而我在同一份报告里刚批评过别人「用未绑定类型的措辞当结论」（F-6 的 `''` 之误）。**两处错误的形状完全一致：把一次不充分取样当成了结论。**

*verifier 签署 · attempt 2 补录（2026-09-10 23:2x）*

---

## C.17 ★ F-9 的 t9 证据订正（指挥台指出，我复核确认）—— 并据此把纪律升级为三条

### C.17.1 事实复核：我引的两版**不是**最终版本序列，但**是我在所称时刻的真实观测**

指挥台据 planner 实测指出：t9 实际版本序列为 **62,720 → 73,570 → 78,219 → 81,286 B**，而我写的 **63,100 B / 71,947 B**「与其中任何一版都不吻合」。

**我按升级后的纪律（引用前重新读取当前 mtime）重新取样，取证结论如下：**

1. **我引的数是「读取时点快照」，成因已定位**：`63,100 B` 读于 **22:44:24**、`71,947 B` 读于 **22:47:26**；而 planner 的 `62,720 B` 对应 t9 完成任务时的 output（更早时点）。→ **我读到的是两次编辑的中间态/另一次编辑的落定态**，与 planner 的取样时点不同，**故两套数字可以同时为真却互不相等**。**指挥台的诊断（中间态）成立。**
2. **但并非全部无法对账**：我在 §11bis 写的 **「62,720 → 76,780 B」** 中，`62,720 B` 与 t9 自身 output **逐字相符**，`76,780 B` 是我 **22:49:37** 的实测 → 这一条**可对账**。而 `81,286 B @22:51:39`（我在 §C.1/§C.4 引的）亦为实测。
3. **★ 最关键的新事实：t9 至今仍在被编辑，字节数仍在上行**。我在写本节时（23:0x）重新读取：**t9 = 86,758 B @22:58:31、768 行**，其修订记录**已到 v5**（含「流程事裁定 (a) 就地接受 + 体量口径收口」等第三轮裁定）。→ **我原先写的「已到 v4」也已过期。**
4. **四份文档同时处于活跃编辑中**（空闲时间实测：`t9` 0.1 分钟、`t12-inputs-t5-corrections.md` 0.1 分钟、`AUDIT-repo.md` 1.2 分钟、`t5` 0.7 分钟）→ **这不是偶发，是并发协作的常态**。

**处置：按指挥台给的选项 (b) 执行 —— 明确标注「读取时点快照，非最终版本」+ 说明取值时点**，并同时并入 planner 的实测序列以为交叉参照：

| t9 版本（planner 实测序列） | 我的观测（时点快照） | 可对账性 |
|---|---|---|
| **62,720 B** | — | **与 t9 output 逐字相符** |
| **73,570 B** | — | 我未读到该时刻 |
| 78,219 B | — | 指挥台曾引为「v3」；我未读到（我在 22:51 读到 81,286 B，**已越过它**） |
| **81,286 B** | **我 22:51:39 实测** | 可对账 |
| 63,100 B | **我 22:44:24 实测** | **未出现在 planner 序列** → 时点快照（编辑中间态或另一版） |
| 71,947 B | **我 22:47:26 实测** | **未出现在 planner 序列** → 同上 |
| 76,780 B | **我 22:49:37 实测** | 未出现在 planner 序列 → 同上 |
| **86,758 B（v5）** | **我 22:58:31 实测（本节新取样）** | **晚于指挥台指令中的全部数值** |

→ **结论：t9 的字节数在 22:44 至 22:58 之间至少变动 5 次（62,720 → 73,570 → 78,219 → 81,286 → 86,758），我引的 63,100 / 71,947 / 76,780 属并发编辑期间的不同时点快照。F-9 的结论（报告在交付后被改写）不受影响，且此例本身就是它最有力的证据。**

### C.17.2 纪律升级为三条（采纳指挥台口径，全队生效）

> **F-9（修订版）**
> 1. 引用必须绑定「**文件名 + mtime + 字节数**」**三者**；
> 2. **引用前必须重新读取当前 mtime**，**不得沿用上游转述的数值**（本节即是范例：我原先沿用「已到 v4」，实际已是 v5）；
> 3. 对**仍处于编辑中**的文件，必须标注「**引用时点**」，并声明「**字节数不是同一性凭据**」。

**第 3 条的理由（指挥台）**：**并发编辑会让字节数持续漂移** —— **字节数本身就是本次要防的不稳定量，不能拿它当锚**。→ 这与我给 t10 立的 v5 守卫纪律**同源**：**同一性/守卫必须用内容哈希（SHA256），不得用尺寸或字节数**。

**★ 第 1 条已按指挥台 2026-09-10 裁定替换为「身份锚强度」口径（本节原文「文件名 + mtime + 字节数」的表述据此升级）**：

> **身份锚强度：`SHA256`（可截断前缀） > `mtime` > `字节数`。**
> - **字节数永不作为同一性凭据**，只作辅助；
> - **`mtime` 也不能单独当锚** —— t9 实测 14 分钟内至少 5 版（62,720 → 73,570 → 78,219 → 81,286 → 86,758 B），**两次编辑可落在同一秒内**，mtime 会被覆盖；**这与「尺寸不能当守卫」是同一个道理**；
> - **实用分界**：**`audit/` 冻结后**，mtime + 字节数足以绑定（内容不再变）；**冻结前**，决定性引用**必须带哈希或显式标注引用时点**。

**修正后的理由（指挥台）**：**编辑期内「版本」这个概念只能靠内容哈希锚定。**
**我的补充推论经指挥台修正后保留后半句**：**无法锁定 mtime（或哈希）时，该引用不得作为判定依据，只能作为线索。**（前半句「优先绑 mtime」已被否决 —— 首选锚是内容哈希，不是 mtime。）

### C.17.3 我的错误形状（与 §C.10、§C.16 同一把尺子）

我在 F-9 里写「62,720 → 76,780 B」时，把**两个不同来源的数值**（t9 output 的 62,720 + 我的实测 76,780）**并置成一条序列**，读起来像同一口径的连续采样 —— **这一步是我做的**，与我在 §C.10 把「空值」类目具体化为 `''`、在 §C.16 采信单次 `Select-String` 空结果是**同一种错误形状：把异源或单次取样拼成结论**。
**处置**：本节不隐藏、不淡化，且**已把正文 L45 的纪律同步升级为三条件**（旧句「文件名 + mtime（或体积）」已改写）。

### C.17.4 §C.1 / §C.4 的 t9 数值同步作废

为避免下游误用，**§C.1 表中 t9 行与 §C.4 开头「已到 v4」的表述，一律以本节为准**：`t9` 现为 **v5 / 86,758 B @22:58:31**（且**该数值亦为时点快照**，非最终版本）；t9 的**最终版本以 t9 自身「修订记录」+ 落盘 mtime 为准**，**不以字节数识别**。

*verifier 签署 · attempt 2 补录（2026-09-10 23:3x）*

---

# 附录 D：attempt 3（2026-09-10 23:0x）—— 极小范围确认

- **任务**：`t5` attempt 3，attempt_id `49fd1bf6-9661-4eed-b588-a80affa4517b`。
- **范围（指挥台指定，极小）**：① 确认 t1 §11 第 5 行已改「排除」；② 确认 t9 期望值已改为**集合断言**；③ 在同一时点快照下确认三份产物自洽。**不做任何新范围扫描。**
- **判定口径（指挥台确认）**：按 `pass` 报；F11 记 **resolved**、F12 记 **narrowed**；**finding 逐条留痕、不省略、不得把「有 findings」写成「无 findings」**。
- **只读声明**：本次唯一写入 = 本附录。porcelain 恒 **45 行**；未改任何 M01-T005 状态；未动治理正文；未碰 pid 24284；**未运行任何脚本**；未跑 `check-integrity.py`；未跑 `rebuild_*.py`。

## D.0 冻结快照（按指挥台要求：绑定整份目录清单，不写死数量）

本附录一切引用均绑定下表时点（**每一行都是「文件名 + 字节数 + mtime」三元组**；按 F-9 修订版纪律，**均标注为引用时点快照，非最终版本**）：

| 文件 | 字节数 | mtime |
|---|---|---|
| `AUDIT-repo.md` | 59,603 | 2026-09-10 22:57:22 |
| `t12-inputs-t5-corrections.md` | 23,568 | 2026-09-10 23:01:42 |
| `t2-governance-sessions.md` | 48,957 | 2026-09-10 22:52:23 |
| `t3-asset-and-artifact-inventory.md` | 31,896 | 2026-09-10 22:39:37 |
| `t5-verification.md` | 本文件（写入前 81,880 / 22:59:17） | — |
| `t9-cleanup-and-retention-plan.md` | 90,918 | 2026-09-10 23:01:42 |
| `verifier-memo-1.md` | 9,797 | 2026-09-10 22:49:28 |

**目录清单如上（7 条，不写死数量、不在正文他处复用该数字）。** 编码核验：三份主产物 `BOM=False / U+FFFD=0` 全部通过。

## D.1 ① t1 §11 第 5 行 → 「排除」：**已落地（F11 = resolved）**

| 检查点 | 实测 | 判定 |
|---|---|---|
| §11 表头口径时效声明 | **L414 存在**：「**指挥台此后的更正文优先于本表**（例如第 5 行『`Docs/` 是否入库』，已由指挥台后续指令更正为**排除**）。遇到本表与后续指令冲突，一律以后续指令为准。」 | **达成** |
| §11 第 5 行正文 | **L422**：「**排除（指挥台 2026-09-10 更正；原「入库」裁定已作废）**」+ 四条依据 + 末句「基线暂存清单中 `Docs/` 为 0 条」 | **达成** |
| 附带同步（非我要求） | **L24** 已在摘要段同步：「`Docs/` 已裁定排除、不入库（指挥台 2026-09-10 更正；原「入库」裁定作废）」 | **加分** |

→ **F11 定案 `resolved`**。**留痕（不省略）**：该冲突**曾真实存在** —— 我在 **56,275 B @22:43:52** 的同一文件上实测第 5 行＝「入库」；现 **59,603 B @22:57:22** 为「排除」。**同一文件、两个版本、相反结论**，是 F-9 版本绑定纪律最干净的一个实证。**t10 依据 t1 §11 暂存 `Docs/` 的风险已闭合。**

## D.2 ② t9 期望值 → 集合断言：**两段式 —— 首次确认时未落地，末次确认时已闭合（F12 = resolved）**

> **⚠ 时效声明（本节记录的是一次「当场未落地、随后落地」的两段过程，两段皆为真实观测）**
> - **第一段（23:0x 首次确认，绑定 t9 90,918 B @23:01:42）**：§4.1 L219 已落地，**但 §4.3 期望值未改**，故当时记为 **F12 = narrowed（未闭合）**。
> - **第二段（23:04 末次确认，绑定 t9 96,049 B @23:04:15，已到 v6）**：**planner 已完成修正，F12 闭合。** 详见 §D.2b。

### D.2a 第一段：首次确认结果（保留原观测，不掩盖）

| 检查点 | 实测（90,918 B @23:01:42） | 判定 |
|---|---|---|
| §4.1 顶层裁定 | **L219 已写**：「⚠ **集合由『路径清单』定义，不由计数定义**（指挥台 Q2 硬要求）……不要拿 59 当断言。」 | **达成** |
| §4.3 第 5 步**执行脚注** | **当时仍为**：`# 期望 43 条（26 M / 2 D / 14 A / 1 A=.gitignore）` | **未达成** |
| 阈值是否已改为 E 类断言 | 检索 `E 类路径` / `不得出现任何` / `零 E 类` / `集合断言` → 0 命中 | **未达成** |

**当时给出的实测依据（结论依然成立，已被 planner 采纳）**：`git diff --cached --name-status` 输出**文件级**条目，而清单含**目录路径** `.trae/execution/reports/audit/ `（`git add` 递归展开）→ **该期望值必然不成立**；且目录每增一份报告就更错一分。

### D.2b ★ 第二段：末次确认 —— **已闭合（F12 = resolved）**，绑定 t9 **96,049 B @2026-09-10 23:04:15**，修订记录**已到 v6**

| 检查点 | 实测 | 判定 |
|---|---|---|
| §4.3 第 5 步标题 | **L358**：「# 5) 暂存后核对（**硬判据 = 不得出现任何 E 类路径，不是计数**）」 | **达成** |
| 计数阈值是否取消 | **L361**：`git diff --cached --name-status     # 期望 **48 条** = 26 M / 2 D / 20 A（**现场复算，勿照抄**）` → **原「43 条」已改为「48 条」并明示由基数改为现场复算** | **达成** |
| L384 明文裁定 | 「3. **→ 校验阈值由『计数值』改为『不得出现任何 E 类路径』。** 理由：在白名单外第 3 项失败才停的新口径下，**一个错的期望值可能直接触发 t10 假停机**」 | **达成（与本报告 F12 的理由逐字同源）** |
| L440 停止条件 | 「**暂存后出现任何 E 类路径** → 判据 = **E 类路径命中数 > 0**（**不是**「暂存条数 ≠ 某个数」）」 | **达成** |
| L660 交接行 | 「核对 **43 条路径**，且**不得出现任何 E 类路径**（**硬判据以 E 类为准，不是计数**；add 行数**现场复算**，当前预测 **48**；**F12**）」→ **明确标注 F12** | **达成（且主动挂了我的 finding 编号）** |
| **额外修复（我未要求）** | **L359** 把原先错误的「`git status --porcelain` 期望输出为空」改为「**仅出现 E 类未跟踪项**……→ **判据不是『输出为空』**（这三类不在 `.gitignore` 内，必然显示为 `??`）」 | **加分 —— 这是一处我未发现、由 planner 自查出的同类缺陷** |

→ **F12 定案 `resolved`。** **处置评价（如实记录）**：planner 不仅改了被指出的那一行，还**修复了我没发现的同类错误**（`--porcelain` 期望为空），并把 F12 编号写进交接表——**这正是我要的「同类缺陷一次性清完」的做法**。
**同时记录我的失误**：我在 23:0x 的检查串是 `期望 43 条`，而 t9 在 23:01:42→23:04:15 之间已被改写，**若我沿用旧行号（L354）或旧串复核，会把已闭合的项误报为未闭合**。→ 本节即是 F-9 修订版第 2 条「**引用前必须重新读取当前状态**」的又一实证；**我此后改为按内容定位、不按行号定位**。

**★ 一处必须留痕的概念更正（含我自己）**：`43` 数的是**路径**（含 1 条目录路径），**该数字本身没错**；错的只是把它当作 `--name-status` 的期望输出行数。planner 现改为 **48**（现场复算 — 我 23:0x 的独立测算为 **约 49**，差 1 的成因是 `reports/audit/` 在此期间由 7 → 新增文件，**两者属不同时点，不构成矛盾**；**t10 现行以现场复算为准，不锁定任何数字**）。我此前报的「47 个文件」是 audit/ 为 6 个文件时的展开值。

## D.3 ③ 三份产物自洽性：**通过（在同一时点快照下）**

| 产物 | 编码 | 版本自述 | 与冻结快照一致 |
|---|---|---|---|
| `AUDIT-repo.md` | BOM=False / U+FFFD=0 | 有修订记录（L294 区） | ✅ 59,603 B @22:57:22 |
| `t2-governance-sessions.md` | BOM=False / U+FFFD=0 | **无修订记录表**（见下注） | ✅ 48,957 B @22:52:23 |
| `t9-cleanup-and-retention-plan.md` | BOM=False / U+FFFD=0 | 有修订记录表（L12 区，已到 **v5**） | ✅ 90,918 B @23:01:42 |

**注（不写成「一致」而应写成「无此项」）**：`t2` **没有**「修订记录」表 —— 故对 t2 而言「版本自述 vs 磁盘状态」**无对应物可比**，**这不是不一致，也不是一致，而是该文件不设此结构**。t2 对自身修订的留痕方式是在 §8 顶部加「更正注记」（L317），**我已在 §C.3 复核为达成**。→ 按「不得把无法复核写成一致」的纪律，此处如实标注为 **不适用**。

**三份产物的交叉一致性**：均已被本轮独立取样核对过关键结论（A/B 类复核见正文 §2/§3；t2 §8 更正注记见 §C.3；t9 吸收情况见 §C.4），**互相掣肘之处（`Docs/` 口径）已由 t1 §11 与 t9 §4.6 收敛到同一结论「排除」**。→ **自洽性判定：通过。**

## D.4 attempt 3 判定

- **① F11：`resolved`** —— 三处（L24 / L414 / L422）均已落地，绑定 **59,603 B @22:57:22**。
- **② F12：`resolved`（两段式）** —— **首次确认（90,918 B @23:01:42）时未落地**，故当时记 `narrowed`；**末次确认（96,049 B @23:04:15，v6）时已闭合**：阈值改为「**不得出现任何 E 类路径**」、计数改为**现场复算**（L361 预测 48）、L440 停止条件同步、L660 交接行**明确挂 F12**。**planner 另修复了我未发现的同类缺陷（`--porcelain` 期望为空，L359）** → **记加分**。
- **③ 三份产物自洽性：通过**（编码全过 `BOM=False / U+FFFD=0`；交叉结论已收敛；t2 的「无修订记录表」记为**不适用**而非一致）。
- **范围纪律**：本附录**未做任何新范围扫描**；所有引用均绑定 §D.0 / §D.2b 的时点快照，**并已按 F-9 修订版改为「按内容定位、不按行号定位」**（本轮实测：t9 在 8 分钟内由 90,918 → 96,049 B，**行号 L354 指向的内容已改变**）。
- **★ 一条我自己的失误留痕**：本附录 §D.2 的第一段判定（F12 未闭合）**在作出时正确、在提交时已过期**。原因是我以**旧行号 + 旧检查串**复核了**已被改写的文件**。→ 这正是 F-9 修订版第 2 条要防的；**我据实改为两段式记录，而非删去第一段。**
- **判定**：attempt 3 的**三项指定范围已 100% 覆盖**，**①②均已 resolved、③通过**；F11/F12 逐条留痕（`resolved` 状态 + 两段观测过程 + 我方失误），**未省略、未把「有 findings」写成「无 findings」**。据此报 **`verdict = pass`**。

## D.5 ★ E-01 独立条目：18:53:43「只读位拒存」证据链（指挥台 2026-09-10 授权提为独立编号）

**升级理由（指挥台）**：「它比 F11/F12 更有治理价值 —— 此前只有静态 `attrib` 结果，现在有了**编辑器侧时间线**。」原分散于 §C.6、§C.12，现独立编号为 **E-01**。

### E-01.1 原始证据（日志锁定中，用流式 `Select-String -Path` 读取，未 `ReadAllLines`）

来源：`Saved/Logs/VRSanguoYanWuchang.log` L12841-12843，日志时间戳 `[2026.09.10-10.53.43:...]`：
1. 「由于文件为只读，所以无法将包保存至 `../../../../../Project/VRSanguoYanWuchang/Content/VRSanguo/Dev/L_Prototype_1v1_v5.umap`！」
2. `LogFileHelpers: Saving map 'L_Prototype_1v1_v5' took **0.000**`
3. 「资产 `/Game/VRSanguo/Dev/L_Prototype_1v1_v5`（`L_Prototype_1v1_v5.umap`）**保存失败**。」

**时钟标定（关键前置）**：6 组样本互证 **日志 + 8h = 本机时间**（**不是 +10h**）→ 日志 `10.53.43` = 本机 **18:53:43**。标定样本含：v5_Auto8 日志 `11.24.50` ↔ 文件 19:24:50；v3_Auto6 日志 `06.38.14` ↔ 文件 14:38:14；日志 `13.44.17` ↔ 文件 21:44:17。

### E-01.2 与后续成功保存并置

| 时点 | 事件 | 证据 |
|---|---|---|
| **18:53:43** | **保存被拒**（文件只读） | `took **0.000**` + 「保存失败」 |
| **21:44:17** | **保存成功** | `LogSavePackage: Moving output files for package: /Game/VRSanguo/Dev/L_Prototype_1v1_v5` + `Saving map 'L_Prototype_1v1_v5' took **0.302**`；`WorldState/1694911060.json` 同秒写入 |
| **现状** | `L_Prototype_1v1_v5.umap` = 673,326 B @21:44:17，**`RO=False`** | `attrib` 实测 |

→ **只读位确实真实存在过**（18:53:43 被它挡住），**并在 18:53:43 – 21:44:17 之间被移除**（否则 21:44:17 不可能 `took 0.302` 成功）。

### E-01.3 它能支撑什么、不能支撑什么（严格分界，不推定）

**可以支撑（已证）**：
1. `M01-T005` 相关记录声称该关卡「**已上锁**」——**在 18:53:43 时点为真**（有编辑器侧拒存为证）；
2. **但 21:44:17 时点为假**（保存成功、且当前 `RO=False`）→ **「声明只读 vs 实测未锁」的漂移判定由「静态 `attrib` 单证」升级为「编辑器侧时间线双证」**；
3. 21:44 的写入者 = **编辑器 Save Map**（非 `rebuild_v5.py` 重跑）：21:40–21:50 全项目（除 `.git`）**仅此二文件被写**，且 `Docs/Scene/` 五个 mtime、`rebuild_v5.py` mtime（19:14:34）均未动 → **排除整脚本重跑**。

**不能支撑（明确记为「无法判定」，**不推定**）**：
- **谁**移除了只读位 —— **无法判定**；
- **什么**触发了 21:44:17 那次保存 —— **无法判定**；
- 09-10 18:53 那封「解冻」信件与该次移除**是否有因果关系** —— **只作线索登记，不作因果断言**（原措辞保持）。

### E-01.4 对 t10 的直接约束（R2）

因只读位已不在、且编辑器 **pid 24284 存活**，`L_Prototype_1v1_v5.umap` **随时可能被再次改写**。→ 已立 **R2 守卫**（见 §C.8）：`git add` 前与 `git commit` 前各取一次 **SHA256** 并比对，不一致即中止并报告；**尺寸永不作为守卫**（同尺寸不同内容已实证：`v5_Auto8` 与当前 `v5.umap` 同为 **673,326 B** 但 SHA256 不同）。当前 SHA256 = `63AFECDF89DA19C8FC6A316DDF8D0A868FC04ED7215CA7D94F3A470BBE7A9BD6`。

---

## D.5b ★★ 事故证据链：D30 裁定 + `completed` 来源链最硬的一环（指挥台 2026-09-10 授权补充）

### D.5b.1 ★★★ `repair-report.txt` 的 `[PASS] M01-T005 completed` —— `completed` 来源链条上目前最强的一环

**指挥台裁定原文**：「它比我原先那句『值在事故前即已存在』**强得多**：它证明 **`completed` 是被修复过程主动校验并确认为「应保留」的值**，而不是残留。」

**取证**（`D:\AWork\TraeAdmin\VRSanguoYanWuchang\tmp\repair-report.txt`，879 B @2026-09-10 18:49:53）——**该文件首行为事故机制的权威记录**：
> `OK  破损点=3861  采用候选=base  源长=13396  产物长=15003`
> `任务数=7` … `M01-T005 | completed | session-20260908-002 | 2026-09-10 | noteLen=4822`

**其 8 项自检段含以下三项（逐字）**：
- `[PASS] M01-T005 completed`
- `[PASS] M01-T005 v5.4/v5.5/v5.6 齐全`
- `[PASS] M01-T005 修复补记已写`

**配套交叉证据（三道复核，18:49–18:50）**：
- `verify2.txt`：对**破损件与修复件逐串比对**，12 个探针在 broken/repaired 中**逐一一致**（「再追加 v5.4」true/true、「348 props」true/true、「v56_target_silhouette」true/true…），并记 **`missing taskIds: none`**（7 个 taskId 两版齐全）→ **证明修复只闭合截断点、未重写历史正文**；
- `verify3.txt`：**HEAD 版 M01-T005 = `in_progress`**，修复后 = `completed`；两版 **task object keys 完全相同**（`taskId, milestone, status, claimedBy, startedAt, updatedAt, note`）；
- 修复留痕齐全：`scan.js`/`scan2.js`/`scan3.js`/`scan4.js`（824–944 B @18:42，含 `charCodeAt(3861)` 损坏点定位）、`STATUS.repaired.json`（25,113 B @18:49:53）、`verify1.txt`（1,057 B @18:49:16）。

→ **结论（升格口径）**：`completed` **不是截断产物、也不是修复时新写入的，而是被修复过程主动校验并确认为「应保留」的值** —— **这是 `completed` 来源链条上目前最硬的一环**。

### D.5b.2 指挥台裁定：整条留痕链 = **证据**，不是清理目标

**认定范围（逐个点名，不用「保留证据原件」这类易被窄化的表述）**：`STATUS.json.broken-20260910`、`scan.js`、`scan2.js`、`scan3.js`、`scan4.js`、`STATUS.repaired.json`、`repair-report.txt`、`verify1.txt`、`verify2.txt`、`verify3.txt`。
→ **P11 清理范围显式排除上述 10 个文件**；**是否给它一个正式的家（`.trae/` 下证据区 / E: 归档）列入用户裁定项，本轮不动。**
**登记项（已采纳，转 t12）**：「事故证据链位于 `D:\AWork\TraeAdmin\VRSanguoYanWuchang\tmp\`，属项目外临时目录，**随时可能被清理；不在 Git、不在 E: 备份范围**」。

### D.5b.3 指挥台采纳的三项措辞订正（**已全部落地**）

1. **时间口径**：「自 18:15 起不可解析」→ 改为「**证据可锚定到 2026-09-10 18:15:36（现场件 mtime）**」。**理由（指挥台确认）**：**破损件取证时刻 ≠ 损坏开始时刻**。→ 本报告 §C.11 等处如有引用，一律以本订正为准。
2. **★ 「2000 字符」= 无原件支撑**：实测该串与「截断」在当前根 `STATUS.json` 的 note 中**均 ABSENT**（逐串验过）→ **该数字目前无原件支撑，只来自会话语境**。**本报告据此明确标注「无原件支撑」，不使其以事实身份进入下游**；指挥台另指示 **不去会话记录里搜寻**（那属新范围，本轮不做）。
3. **代次相容性**：破损件中 `v5.7`/`v5.6b` **ABSENT**、`v5.6` **PRESENT** → **只作相容性陈述、不作因果断言**（**原措辞保持，不放宽**）。

*verifier 签署 · attempt 4 授权补充条目*

---

## D.6 ★ `audit/` 冻结快照（指挥台要求：把「冻结」从声明变成可验证事实）

**取样时点：2026-09-10 23:06:18（本附录 D.6 定稿前最后一次读取）。** 命令：`Get-ChildItem`（名称 + `Length` + `LastWriteTime`）+ **全文件** `ReadAllText` 核 BOM/`U+FFFD`。**此后任何偏离本表即为违约。**

| # | 文件名 | 字节数 | mtime | BOM | U+FFFD（全文件） |
|---|---|---|---|---|---|
| 1 | `AUDIT-repo.md` | 59,603 | 2026-09-10 22:57:22 | False | **0** |
| 2 | `t12-inputs-t5-corrections.md` | 31,940 | 2026-09-10 23:05:30 | False | **0** |
| 3 | `t2-governance-sessions.md` | 48,957 | 2026-09-10 22:52:23 | False | **0** |
| 4 | `t3-asset-and-artifact-inventory.md` | 31,896 | 2026-09-10 22:39:37 | False | **0** |
| 5 | `t5-verification.md` | **本文件（自指，不列数值）** | — | False | **0** |
| 6 | `t9-cleanup-and-retention-plan.md` | 96,476 | 2026-09-10 23:05:09 | False | **0** |
| 7 | `verifier-memo-1.md` | 9,797 | 2026-09-10 22:49:28 | False | **0** |

**目录清单数量：7 条**（**此处为清单本身的记录，不作为任何阈值** —— 按指挥台裁定，计数不作判据）。
**全目录合计**：7 文件 / 376,887 B（该合计为 23:06:14 读数，**含 t5 当时值 98,218 B**；因本表定稿晚于该读数，**合计仅作参照、不作判据**）。
**★ 本文件（`t5-verification.md`）自指说明**：本表写入会改变第 5 行自身的字节数 —— 为避免「快照自身破坏快照」的循环，**第 5 行不列数值**，其最终值由 **指挥台在本轮结束后自行取一次**即可（此为**唯一**需要外部补录的一格）。

**按指挥台裁定的「身份锚强度」口径说明本表效力边界**：本表为**冻结后**快照，此时「mtime + 字节数足以绑定」（内容不再变）；**但 `t5-verification.md` 与 `t12-inputs-t5-corrections.md` 在本表写入时点仍属「授权内的收尾写入」**，故对这两份，**决定性引用仍须带哈希或显式标注引用时点**。

**★ 一处我方仪器错误（据实留痕，不掩盖）**：本轮首次取 U+FFFD 时我**只扫了文件尾 400 字节**（为省时），结果在 `t12` 与 `t5` 各报出 1 个替换字符；**改用与既往一致的全文件扫描后，7 份全部为 0**。→ **是我的仪器中途变了、不是文件有问题**：我此前对同一批文件的结论一直基于**全文件**扫描，本轮却临时改用**尾部抽样**，**属「换了尺子还拿结果对比」**。**已改为统一使用全文件扫描，并在此留痕。**（这是第五次同一把尺子量到我自己。）

*verifier 签署 · attempt 3 收尾*
