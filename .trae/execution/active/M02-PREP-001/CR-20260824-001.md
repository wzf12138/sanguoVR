# 集中式变更请求

> 仅用于改变已批准基线。未获批准前不得实施；执行中的缺陷修复若不改变基线，可在原任务内记录。

## 1. 基本信息

- 请求编号：`CR-20260824-001`
- 状态：`approved`
- 请求人：session-20260824-001（执行模型，代用户提交）
- 请求日期：2026-08-24
- 审批人：用户
- 审批日期：2026-08-24（用户指令"审批 M02-PREP-001"，由 session-20260824-002 代为登记）
- 关联任务/里程碑：M02-PREP-001
- 关联决策："不需要"（命名对齐，非路线变化）

## 2. 变更摘要

- 当前基线：`.trae/standards/02-naming-and-path-standard.md` 中武器示例名为 `SK_Weapon_Shield_Round`（圆盾）、`SK_Weapon_Polearm_Glaive`。
- 拟议变更：盾牌示例名改为 `SK_Weapon_Shield_Han`，长柄示例名改为 `SK_Weapon_Polearm_Ji`，并同步该文件内所有引用处。
- 变更原因：M02-PREP-001 Step 4 已定稿形制为"钩镶/长方盾"与"卜字戟"，现标准示例名与定稿形制不符（Round=圆盾、Glaive 为泛指，均非定稿形制），若不改将导致后续资产命名混乱。
- 不变范围：五武器其余三类名称、目录结构规则、前缀规则（SK_Weapon_*）均不变；仅替换两个示例名及其引用。
- 预期收益：资产命名与已定稿形制一致，避免 M02 实施时出现双命名体系。

## 3. 影响分析

- 产品范围与玩家体验：无影响（纯文档命名对齐）。
- 架构、接口与数据：无影响。
- 代码、配置、资产与插件：无既有资产使用旧名（AST 登记册中尚无 Shield/Polearm 正式资产落盘，用户生成的 FBX 在项目外临时区）。
- 测试、性能、VR 舒适度与设备：无影响。
- 进度、成本与依赖：消除 M02 实施时的一处潜在返工点。
- 治理正文、索引、登记册和 Skill：仅改 `02-naming-and-path-standard.md` 一个受控文件；如该文件被 `index.md` 或完整性校验引用哈希，需同步更新校验记录。

## 4. 执行边界

- 允许修改的绝对路径：`d:\AWork\Unreal\Project\VRSanguoYanWuchang\.trae\standards\02-naming-and-path-standard.md`
- 禁止路径：其他 standards/、governance/、knowledge/ 文件；Source/、Config/、Plugins/
- 前置条件：本 CR 获用户批准（状态 draft → approved）
- 强制停止条件：发现该标准文件中两名称被其他受控正文交叉引用且无法在本文件内闭环时，停止并扩大 CR 范围重新审批
- 是否需要管理员权限/外部凭据/联网：否
- 是否包含删除、迁移、批量重命名或格式升级：否（仅文本替换）

## 5. 风险与回滚

- 已知风险：极低——纯示例名替换；若 Git 已跟踪该文件，可随时还原。
- 风险负责人：用户
- 回滚锚点：Git 提交历史中该文件的上一版本
- 回滚步骤：`git checkout <commit> -- .trae/standards/02-naming-and-path-standard.md`
- 回滚验证：回读文件确认旧名恢复

## 6. 验证计划

- 静态检查：全库 Grep 确认旧名仅存在于归档/报告的历史记录中，不再出现在有效标准正文
- 自动化测试：不适用
- UE 编译/编辑器验证：不适用
- PICO 真机验证：不适用
- 文档逐份回读：修改后回读全文一次
- 唯一 Skill 检查：不涉及 SKILL.md 变更
- 证据保存位置：本任务报告"风险与待办"第 2 条 + `.trae/CHANGELOG.md` 条目

## 7. 审批

- 审批结论：approved（附条件通过 M02-PREP-001 时一并批准本 CR 实施）
- 审批意见：用户指令"审批 M02-PREP-001，完成后结合之前发现的问题一起处理"（2026-08-24）；命名对齐作为验收附带条件闭环。
- 批准的实施范围：§2 拟议变更全量——仅 `standards/02-naming-and-path-standard.md` 内 `SK_Weapon_Shield_Round`→`SK_Weapon_Shield_Han`、`SK_Weapon_Polearm_Glaive`→`SK_Weapon_Polearm_Ji` 及文件内引用同步。
- 附加约束：实施后须全库 Grep 复核旧名仅存于历史记录；TASK.md 第 94-95 行为任务包历史规格，不在修改范围。

## 8. 实施与关闭

- 实施任务编号：M02-PREP-001（收尾范围内实施，session-20260824-002）
- 实施结果：已完成。`02-naming-and-path-standard.md` 第 37-38 行两处示例名已按批准范围替换；该文件内无其他引用处需同步（全库 Grep 确认）。
- 偏差：无。
- 验证结果：全库 Grep `Shield_Round|Polearm_Glaive` 仅余 6 处历史记录（CHANGELOG 草案记录、任务报告历史描述、TASK.md 历史规格、本文件基线描述），有效标准正文零残留。
- 更新的权威正文：`.trae/standards/02-naming-and-path-standard.md`
- 更新的索引/登记册/变更记录：`.trae/CHANGELOG.md`（2026-08-24 验收条目）；资产登记册 AST-009 路径修正为同批次附带条件一并完成。
- 遗留风险与后续任务：无。M02 实施期新武器资产直接使用定稿形制名。
- 关闭人：session-20260824-002
- 关闭日期：2026-08-24
