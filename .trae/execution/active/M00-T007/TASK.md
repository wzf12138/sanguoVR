# M00-T007 判据工具「核对执行器」

- 任务编号：M00-T007
- 状态：见 `STATUS.json`
- 里程碑：M00
- 优先级：P0
- 前置任务：无
- 预估耗时：~4h

## 目标

按 `.trae/execution/CR-20260912-001-judgement-tool.md`（**唯一权威规格正文**）实现判据工具 `verify-claims.py`，并通过该规格 §7 的 **TC-01 ~ TC-16**。

**为什么做**：2026-09-12 单日产生同族立法 **22 例**、判据与数字失误 **12 起**（截至当日收尾）；根因是**判据每次都靠人手临时写脚本**。本任务把该族规矩**机械化**。**用户关切「后面可能换人的，我怕到别人那里就忘了」** ⇒ 工具是"换人也躲不开"的载体（挂钩由决策侧在本任务验收通过后执行）。

> **修订（2026-09-12 决策侧）**：TC 总数由 **13** 增至 **16**（新增 §4.3 第 16/17/18 条 ⇒ TC-14/15/16）；§4.3 能力表由 15 条增至 **18 条**。**本文件与规格正文不一致时以规格正文为准。**

## 范围

**只改两个文件**：
1. 新建 `.trae/tools/verify-claims.py`（工具本体）
2. 修改 `.trae/tools/README.md`（新增一行索引）

**另写报告**：`.trae/execution/reports/tasks/M00-T007.md`（含 **16** 条 TC 的原始输出）。

**明确不做**：唯一 Skill（锁定）、`AGENTS.md`、`dashboard/check-integrity.py`（冻结）、任何 `Content/` `Source/` `Config/` `Plugins/` 文件。

## 实施步骤

| Step | 内容 | 预估 |
|------|------|------|
| 1 | 读规格 §4（形态 / 三态 / **18 条内置能力** / 7 条禁止事项 / 输入契约 / 退出码），产出实现清单 | 0.3h |
| 2 | 实现判据文件解析 + **判据合法性校验**（缺 `match` / 缺 `measure` / `id` 重复 / 枚举外取值 ⇒ **退出码 3，且不执行任何判据**） | 0.7h |
| 3 | 实现 `contains` / `count_regex` / `shape` / `sha256` / `exec` 五类判据（含**可满足性前置**、**三口径**、**双算法交叉**） | 1.0h |
| 4 | 实现**三态输出** + **fail-closed 默认** + **退出码 0/1/2/3** + **测量驱动的汇总段**（禁止固定文本） | 0.5h |
| 5 | 实现**报告段结构性解耦**（独立遍历，零 `continue`/`skip`/`return`）+ **独立「报告段非空」断言**（规格第 15 条 / TC-13） | 0.3h |
| 6 | 自跑 TC-01 ~ TC-16，逐条记录原始输出 | 1.2h |
| 7 | 写报告 + 同步 `.trae/tools/README.md` 索引 | 0.2h |

## 停止条件

- 规格自相矛盾或无法实现 ⇒ **停止回报，不得自行改规格**；
- 出现无法同时通过的 TC 组合 ⇒ **停止回报，不得改 TC 使其通过**；
- 需修改白名单外任何文件（含锁定文件） ⇒ **停止回报**。

## 回退

删除 `.trae/tools/verify-claims.py` + 还原 `.trae/tools/README.md` 新增行。**无工程资产牵连。**

## 报告路径

`.trae/execution/reports/tasks/M00-T007.md`
