# M00-T007 必需事实源与输入

> **读取顺序即依赖顺序**。规格是**唯一权威**，本文件只指路，不复制规格内容（`AGENTS.md` 规则 19）。

## 1. 唯一权威规格（**必读全文**）

- `.trae/execution/CR-20260912-001-judgement-tool.md`
  - §1 批准依据（用户原话逐字；含"允许改唯一 Skill"的专项批准）
  - **§4.2 三态输出 + fail-closed（最高优先级）**
  - **§4.3 十五条内置能力**（第 13 条标识符统一规范化 / 第 14 条量具定义显式声明 / **第 15 条报告义务不可被短路**）
  - **§4.4 七条禁止事项**（含第 7 条"禁止用截断文本做归属判定"）
  - §4.5 输入契约（`measure` / `fence_measure` 缺失 ⇒ 拒绝执行该判据）
  - §4.6 输出与退出码（0 全过 / 1 有未通过 / 2 有未取到数且未加 `--allow-unknown` / 3 判据文件不合法）
  - **§7 TC-01 ~ TC-13（验收判据）**

## 2. 该族立法的原始来源（**理解"为什么"，18 例**）

- `.trae/governance/SessionCommands.md` §推送 · 判据补充节
  - 第 3 / 6 / 10 / 11 / 12 / 13 / 14 / 15 / 16 / 17 / 18 例及其立法
  - **可满足性前置**、**三口径**、**格式敏感度声明**、**匹配方式声明**、**字节导出法**、**四项脚本化要求**

## 3. 参考实现（**只读，不得修改**）

- `dashboard/check-integrity.py`
  - **注意：该文件处于冻结，不得修改**。仅作**代码风格与输出约定**的参考（宿主语言同为 Python、门禁输出格式）。
  - 已知覆盖边界（**不要照抄其密钥检查**）：其密钥扫描只匹配厂商格式，无法发现自由文本凭据（见 CR-20260911-002）。
- `.trae/tools/README.md`（索引格式；`rebuild_v3.py` / `rebuild_v4.py` / `rebuild_v5.py` 为既有工具）

## 4. 运行环境（**已实测**）

```
Python 绝对路径（不在 PATH 上，必须用绝对路径）：
  D:\App\trae\Traedata\TRAE SOLO CN\ModularData\ai-agent\vm\tools\python\python.exe
版本：3.10.11；`pyyaml` 已可用（已实测）
调用前必须设：$env:PYTHONIOENCODING='utf-8'（否则输出含特殊字符会抛 UnicodeEncodeError: 'gbk'）
```
**允许依赖**：Python 标准库 + `pyyaml`。**不得引入其他依赖。**

## 5. 边界与纪律

- `AGENTS.md`：规则 18（改前先读）、19（一个主题一个权威正文，只链接不复制）、20（`.trae/` 创建路径限制）、22（未验证项标记）、23（禁止提交密钥与本机路径凭据）。
- **本任务不改任何锁定文件**；挂钩由决策侧在 TC 全绿后亲自执行。
