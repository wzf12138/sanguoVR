# 任务工作流

## 编号

格式为 `MNN-TNNN`，例如 `M00-T001`。一个编号对应一个可验收目标。

## 必填字段

- 状态、优先级、负责人。
- 背景与范围。
- 输入事实源。
- 交付物及路径。
- 实施步骤。
- 验收方法与证据。
- 风险、阻塞与回滚。

## 生命周期

`草案 → 已批准 → 进行中 → 已实现未验证 → 已验证`。

若验证失败，状态回到“进行中”或“阻塞”，不得保持“已验证”。

## 同步要求

任务状态变更时同步 `registers/TaskRegister.md`；产生决策、风险或验证证据时同步相应登记册。
